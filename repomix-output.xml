This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
AGENTS.md
CLAUDE.md
components.json
docs/200万字长篇分析重构方案.md
docs/百万字小说分析全流程.md
docs/系统流程完整介绍.md
docs/notes/JS-Open-Source-Awards-2024-2026.md
docs/pipeline-redesign-discussion.md
docs/pipeline-redesign-plan.md
eslint.config.mjs
next.config.ts
package.json
postcss.config.mjs
public/file.svg
public/globe.svg
public/next.svg
public/vercel.svg
public/window.svg
README.md
src/app/api/chapter/generate/route.ts
src/app/api/chapter/review/route.ts
src/app/api/chapter/revise/route.ts
src/app/api/generation/chapter-plan-detail/route.ts
src/app/api/generation/chapter-plan-framework/route.ts
src/app/api/generation/chapter-set-detail/route.ts
src/app/api/generation/chapter-set-framework/route.ts
src/app/api/generation/outline/route.ts
src/app/api/generation/phase-detail/route.ts
src/app/api/generation/phase-framework/route.ts
src/app/api/generation/volume-detail/route.ts
src/app/api/generation/volume-framework/route.ts
src/app/api/library/delete/route.ts
src/app/api/library/get/route.ts
src/app/api/library/list/route.ts
src/app/api/library/rename/route.ts
src/app/api/library/save-step/route.ts
src/app/api/library/save/route.ts
src/app/api/library/upload/route.ts
src/app/api/novels/delete/route.ts
src/app/api/novels/detail/route.ts
src/app/api/novels/list/route.ts
src/app/api/novels/save/route.ts
src/app/api/project/create/route.ts
src/app/api/project/delete/route.ts
src/app/api/project/list/route.ts
src/app/api/project/load/route.ts
src/app/api/project/save/route.ts
src/app/api/prompts/list/route.ts
src/app/api/prompts/save/route.ts
src/app/api/source/process/ablation-testing/route.ts
src/app/api/source/process/character-dynamics/route.ts
src/app/api/source/process/classify-entities/route.ts
src/app/api/source/process/deep-analysis/route.ts
src/app/api/source/process/dna-compression/route.ts
src/app/api/source/process/event-alignment/route.ts
src/app/api/source/process/event-extraction/route.ts
src/app/api/source/process/experience-annotation/route.ts
src/app/api/source/process/narrative-constraints/route.ts
src/app/api/source/process/plot/route.ts
src/app/api/source/process/reader-experience/route.ts
src/app/api/source/process/samples/route.ts
src/app/api/source/process/slice/route.ts
src/app/api/source/process/style/route.ts
src/app/api/source/process/summary/route.ts
src/app/api/source/process/tension-tracking/route.ts
src/app/error.tsx
src/app/favicon.ico
src/app/globals.css
src/app/layout.tsx
src/app/page.tsx
src/components/chapter/ChapterGenerationView.tsx
src/components/chapter/FeedbackInput.tsx
src/components/chapter/ReviewPanel.tsx
src/components/generation/HierarchyTree.tsx
src/components/generation/LayerCard.tsx
src/components/generation/LayerGenerationView.tsx
src/components/project/SourceRoleSelector.tsx
src/components/project/WritingProjectView.tsx
src/components/prompts/PromptManagementView.tsx
src/components/SettingsDialog.tsx
src/components/Sidebar.tsx
src/components/source/MarkdownViewer.tsx
src/components/source/ProcessStepIndicator.tsx
src/components/source/SourceLibraryView.tsx
src/components/source/SourceNovelCard.tsx
src/components/source/SourceNovelDetailView.tsx
src/components/source/StylePlotLibraryView.tsx
src/components/StreamingText.tsx
src/components/ui/badge.tsx
src/components/ui/button.tsx
src/components/ui/card.tsx
src/components/ui/checkbox.tsx
src/components/ui/dialog.tsx
src/components/ui/dropdown-menu.tsx
src/components/ui/input.tsx
src/components/ui/label.tsx
src/components/ui/scroll-area.tsx
src/components/ui/select.tsx
src/components/ui/separator.tsx
src/components/ui/slider.tsx
src/components/ui/sonner.tsx
src/components/ui/switch.tsx
src/components/ui/tabs.tsx
src/components/ui/textarea.tsx
src/lib/ai/prompts/ablation-testing.ts
src/lib/ai/prompts/chapter-plan-detail.ts
src/lib/ai/prompts/chapter-plan-framework.ts
src/lib/ai/prompts/chapter-review.ts
src/lib/ai/prompts/chapter-revision.ts
src/lib/ai/prompts/chapter-set-detail.ts
src/lib/ai/prompts/chapter-set-framework.ts
src/lib/ai/prompts/chapter-writing.ts
src/lib/ai/prompts/character-dynamics-extraction.ts
src/lib/ai/prompts/deep-analysis.ts
src/lib/ai/prompts/dna-compression.ts
src/lib/ai/prompts/dna-generation.ts
src/lib/ai/prompts/entity-classification.ts
src/lib/ai/prompts/event-extraction.ts
src/lib/ai/prompts/experience-annotation.ts
src/lib/ai/prompts/helpers.ts
src/lib/ai/prompts/index.ts
src/lib/ai/prompts/narrative-constraints-extraction.ts
src/lib/ai/prompts/outline-generation.ts
src/lib/ai/prompts/phase-detail.ts
src/lib/ai/prompts/phase-framework.ts
src/lib/ai/prompts/pipeline-common.ts
src/lib/ai/prompts/plot-extraction.ts
src/lib/ai/prompts/reader-experience-extraction.ts
src/lib/ai/prompts/sample-selection.ts
src/lib/ai/prompts/source-slicing.ts
src/lib/ai/prompts/style-extraction.ts
src/lib/ai/prompts/summary.ts
src/lib/ai/prompts/tension-tracking.ts
src/lib/ai/prompts/volume-detail.ts
src/lib/ai/prompts/volume-framework.ts
src/lib/ai/providers.ts
src/lib/analysis-chunker.ts
src/lib/api/stream-route.ts
src/lib/atomic-write.ts
src/lib/chunker.ts
src/lib/constants.ts
src/lib/file-parser.ts
src/lib/generation/chapter-reviewer.ts
src/lib/generation/context-assembler.ts
src/lib/generation/layer-pipeline.ts
src/lib/hooks/use-streaming-fetch.ts
src/lib/safe-path.ts
src/lib/source-processing/ablation-tester.ts
src/lib/source-processing/basic-pipeline.ts
src/lib/source-processing/character-dynamics-extractor.ts
src/lib/source-processing/dao-pipeline.ts
src/lib/source-processing/dna-compressor.ts
src/lib/source-processing/entity-classifier.ts
src/lib/source-processing/experience-annotator.ts
src/lib/source-processing/narrative-constraints-extractor.ts
src/lib/source-processing/pipeline-types.ts
src/lib/source-processing/plot-extractor.ts
src/lib/source-processing/program-indexer.ts
src/lib/source-processing/reader-experience-extractor.ts
src/lib/source-processing/sample-selector.ts
src/lib/source-processing/smart-slicer.ts
src/lib/source-processing/style-extractor.ts
src/lib/source-processing/tension-tracker.ts
src/lib/source-processing/unified-pipeline.ts
src/lib/source-processing/value-sampler.ts
src/lib/source-processing/windowed-event-extractor.ts
src/lib/store/navigation.ts
src/lib/store/project.ts
src/lib/store/prompts.ts
src/lib/store/settings.ts
src/lib/store/source-library.ts
src/lib/store/source-processing.ts
src/lib/stream-fetcher.ts
src/lib/sync/idb-helpers.ts
src/lib/text-cleaner.ts
src/lib/utils.ts
src/types/index.ts
start.ps1
test-client.html
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="docs/200万字长篇分析重构方案.md">
# 200 万字长篇小说分析 — 重构方案

## 核心原则

**不要压缩文本，要压缩认知。** 中间产物不是摘要文本，而是结构化事件数据库。因果链不丢。

## 新架构：四阶段

```
200万字原文
    │
    ▼
═══════════ 阶段 1：流式索引（程序化，廉价） ═══════════
  ├─ 按章节/段落边界预切分为 ~400 个 Chunk（每 Chunk ~5000字）
  ├─ 对每个 Chunk 做轻量实体提取（已有的 program-indexer.ts）
  ├─ 构建实体字典：角色/地点/组织/道具/概念
  └─ 产出：chunks.json + entity_index.json
    │
    ▼
═══════════ 阶段 2：重叠窗口事件提取（AI，核心步骤） ═══════
  ├─ 5个窗口 × ~45万字（重叠5万字缝合）
  ├─ 每个窗口：AI 提取结构化事件列表
  │   {id, 章节, 角色[], 类型, 导致→[事件ID], 被导致←[事件ID]}
  ├─ 在重叠区匹配相同事件 → 合并去重
  └─ 产出：event_graph.json（5000-10000条事件，含因果边）
    │
    ▼
═══════════ 阶段 3：价值采样 + 道/气分析（AI，并行） ═══════
  ├─ 从事件图谱中自动识别：高潮节点、转折节点、伏笔-回收对
  ├─ 按情绪曲线做价值采样 → 选取 30-50 个核心切片用于深度分析
  ├─ 并行运行：体验流标注 + 消融测试 + 势能追踪
  └─ 产出：experience_curve.json + ablation_results.json + tension_analysis.json
    │
    ▼
═══════════ 阶段 4：DNA V2 蒸馏（程序化） ═══════
  ├─ 输入：事件图谱 + 实体字典 + 体验曲线 + 消融结果 + 势能分析
  ├─ buildNovelDNA()  → 六层 DNA（道/气/骨/肉/引擎/风险）
  └─ 产出：novel_dna_v2.json（< 3万字）
```

## 各阶段详解

### 阶段 1：流式索引

**目标**：不调 AI。用程序化手段把 200 万字变成可检索的结构。

**做的事**：
1. `fs.createReadStream` 流式读取，按章节标题正则（"第X章"）或固定 5000 字切分
2. 复用已有的 `program-indexer.ts`（中文 n-gram 实体提取）对每个 Chunk 提取实体
3. 构建全局实体字典：合并同名实体、检测别名（"韩立"/"韩道友"/"厉飞雨"）
4. 产出约 400 个 Chunk + 一份实体字典（几千条实体）

**API 调用**：0（全部程序化）

---

### 阶段 2：重叠窗口事件提取

**目标**：这是替代当前 Step 0-3 的核心步骤。不再产出"文风报告""情节报告"等文本摘要，而是产出结构化事件。

**5 个窗口**：

| 窗口 | 覆盖范围 | 大小 |
|------|---------|------|
| W1 | Chunk 001-090 | ~45万字 |
| W2 | Chunk 070-160 | ~45万字（重叠 W1 的 070-090） |
| W3 | Chunk 140-230 | ~45万字 |
| W4 | Chunk 210-300 | ~45万字 |
| W5 | Chunk 280-400 | ~45万字 |

**每个窗口内做的事**：
1. 将该窗口的所有 Chunk 原文 + 实体字典装入上下文
2. AI 提取结构化事件：

```json
{
  "id": "E0142",
  "chapter": 32,
  "type": "拜师",
  "participants": ["韩立", "墨居仁"],
  "location": "七玄门",
  "description": "墨居仁正式收韩立为徒，传授长春功",
  "causes": ["E0101"],      // 导致此事件的前置事件
  "effects": ["E0170","E0260"],  // 此事件导致的后置事件
  "tension_change": 2,      // 紧张度变化 -5~+5
  "emotion": "希望",
  "foreshadowing_of": null, // 如果是伏笔，指向回收事件
  "foreshadowing_from": "E0095" // 如果是回收，指向埋设事件
}
```

3. 5 个窗口**并行**执行（`Promise.all`）
4. 在重叠区（如 W1 的 070-090 与 W2 的 070-090），用**实体+章节号+类型**三重匹配识别同一事件，合并去重

**为什么重叠 5 万字？** 防止一个事件被切成两半。重叠区的同一个事件在两个窗口都会被提取，合并时取信息更完整的版本。

**API 调用**：5 次（5 个窗口并行）

---

### 阶段 3：价值采样 + 道/气分析

**目标**：从事件图谱中选出最值得深度分析的切片，运行已有的道/气模块。

**采样策略**（不平均，按价值选）：

| 类别 | 选取条件 | 数量 |
|------|---------|------|
| 高潮点 | tension_change ≥ 4 或因果边密集 | 10-15 个 |
| 转折点 | 事件 type 含"背叛/揭露/死亡/觉醒" | 5-8 个 |
| 伏笔-回收对 | foreshadowing_of 非空 | 5-10 对 |
| 呼吸段落 | tension_change ≈ 0 且在两个高潮之间 | 5-8 个 |
| 用户锚点 | 用户标注的"最打动我的片段" | 3-5 个 |

总计约 **30-50 个核心切片**，而非均匀的 200 个。

**并行运行**（复用现有模块）：
- 体验流标注 → 仅对采样的 30-50 个切片
- 消融测试 → 同上
- 势能追踪 → 基于事件图谱（不需原文）

**API 调用**：9-16 次（比原来减少 80%，因为只分析精选切片）

---

### 阶段 4：DNA V2 蒸馏

**目标**：从事件图谱 + 实体字典 + 体验曲线 + 消融结果中程序化构建六层 DNA。

**输入不再是文本摘要，而是结构化数据**：

| DNA 层 | 数据来源 |
|--------|---------|
| 道（主情绪场） | 事件图谱的情绪分布 + 体验曲线高点 + 用户确认 |
| 气（维持方法） | 事件图谱中 tension_change≈0 的事件模式 |
| 骨（必须结构） | 因果链中入度+出度最高的事件节点 |
| 肉（增强体验） | 消融测试 muscle 分类 |
| 填充（节奏） | 消融测试 filler_a/b + 事件密度分布 |
| 引擎（势能） | 事件图谱中 tension_change 的周期模式 |
| 风险 | 长程因果链（跨>100章）的比例 |

**产出大小**：< 3 万字（因为所有输入都是结构化数据，不是文本报告）

---

## 与原方案对比

| 维度 | 原方案 | 新方案 |
|------|--------|--------|
| 中间产物 | 5 份 markdown 报告（累计 2-8 万字） | 1 个事件图谱 JSON（结构化） |
| 因果链 | 丢失（报告是自然语言，无法追踪因果） | 保留（事件节点有 causes/effects 边） |
| 跨卷伏笔 | 无法识别（报告只说"存在伏笔"） | 可追踪（foreshadowing_of/foreshadowing_from 字段） |
| 阶段 A API 调用 | 22-70 次 | 5 次（窗口事件提取） |
| 阶段 B API 调用 | 9-16 次 | 9-16 次（但只分析精选切片） |
| DNA 质量 | "报告总结的总结" | "事件图谱的结构分析" |
| 200万字总耗时（估算） | 31-86 次 × 10s = 5-14 分钟 | 5 次 × 30s + 10 次 × 10s = 4 分钟 |

## 实施步骤

### Step 1：实现事件提取 prompt（新文件）
- `src/lib/ai/prompts/event-extraction.ts`
- AI 输入：Chunk 原文 + 实体字典 → 输出：结构化事件列表 JSON
- 关键要求：必须标注 causes（前置事件ID）和 effects（后置事件ID）

### Step 2：实现重叠窗口编排器（新文件）
- `src/lib/source-processing/windowed-event-extractor.ts`
- 5 窗口并行 → 重叠区事件合并
- 事件 ID 去重策略：实体+章节+类型匹配

### Step 3：实现价值采样器（新文件）
- `src/lib/source-processing/value-sampler.ts`
- 从事件图谱中按 tension_change / 类型 / 伏笔关系选取 30-50 个核心切片

### Step 4：更新 dao-pipeline.ts
- 替换输入：事件图谱替代文风/情节/角色报告
- `buildNovelDNA()` 改为从结构化事件数据中推断

### Step 5：更新 context-assembler.ts
- 生成时注入事件图谱中的因果链上下文（而非文风报告全文）

### 保留不改的
- 体验流标注器（experience-annotator.ts）
- 消融测试器（ablation-tester.ts）
- 势能追踪器（tension-tracker.ts）
- 5 层生成管线
- 6 维审查系统
</file>

<file path="docs/百万字小说分析全流程.md">
# 百万字小说进入 Ainovr — 完整流程

> 目标读者：AI 系统（用于理解全链路）
> 更新日期：2026-06-10
> 前提：用户已完成 API 配置（apiKey / model / baseURL 存入浏览器 localStorage）

---

## 流程总览

一本 100 万字的网文进入 Ainovr 后，经历 **三阶段、约 30-90 次 AI 调用**，最终输出一份六层 DNA 和一个技术样本库。

```
上传 (.txt)
  │
  ▼
═══════════ 阶段 A：基础分析管线（8 步串行） ═══════════
  Step 0  智能切片        → SemanticSlice[] (~50-200 个切片)
  Step 1  文风提取        → styleProfile (markdown, ~3000-8000 字)
  Step 2  叙事动力学/情节 → plotReport (markdown, ~3000-6000 字)
  Step 3  角色动力学      → characterDynamics (markdown, ~2000-4000 字)
  Step 4  读者体验   ╮
  Step 5  叙事约束   ╯  并行 → readerExperience + narrativeConstraints
  Step 6  样本选取        → RepresentativeSample[] (3-5 个)
  Step 7  DNA 压缩(V1)    → novelDna (YAML, 旧版扁平结构)
  │
  ▼
═══════════ 阶段 B：道/气深度分析（DAG 并行） ═══════════
  Phase B1  体验流标注     → 多 reader persona 并行投票
  Phase B2  消融测试 ╮
            势能追踪 ╯ 并行 → 四分类 + 势能周期 + 节奏画像
  Phase B3  DNA V2 构建    → 六层 DNA (程序化，无需 AI)
  Phase B4  技术样本库     → 8 类 × 3 样本 (程序化)
  │
  ▼
═══════════ 阶段 B'：事件图谱（200 万字长篇专用） ═══════════
  Stage 1  文本分块        → ~450K 字符/块 + 实体字典 (程序化)
  Stage 2  滑动窗口提取    → NovelEvent[] (多窗口并行 AI)
  Stage 3  图谱合并        → EventGraph + 因果链索引 (程序化)
  Stage 4  价值采样        → 30-50 高价值切片 (程序化)
  Stage 5  DNA 蒸馏        → 六层 DNA from 事件图谱 (程序化)
  │
  ▼
═══════════ 阶段 C：写作生成（5 层层次化） ═══════════
  Layer 1  全书大纲       → ~200 字
  Layer 2  阶段规划       → 4-6 个 Phase × ~500 字
  Layer 3  分卷           → N 个 Volume × ~300 字
  Layer 4  章节集合       → M 个 ChapterSet × ~200 字
  Layer 5  每章计划       → K 个 ChapterPlan × ~100 字
  │
  ▼
  章节正文生成 → 5 维审查 → 修正循环（最多 3 轮）
```

---

## 阶段 A：基础分析管线

> **编排器**：`src/lib/source-processing/basic-pipeline.ts` — `runBasicPipeline()`
> **所有步骤串行执行**，前一步的输出作为后一步的输入

### Step 0 — 智能切片

**输入**：`raw.txt`（100 万字清洗后文本）

**清洗规则**：
- 去除非文本内容（乱码、控制字符）
- 统一换行符
- 保留段落结构

**过程**：
1. 系统检查文本长度。100 万字远超 6500 字阈值，触发 AI 切片。
2. 将全文发送至 `/api/source/process/slice`，maxTokens=8192。
3. AI 将小说按语义边界切分为片段，每个片段标注：
   - 语义标签（如 `["战斗", "修炼", "转折"]`）
   - 情节弧线归属
   - 情绪基调
   - 叙事功能（setup / escalation / climax / cooldown / resolution）
   - 紧张度（0-10）
   - 依赖关系（长流程伏笔、跨章节故事线的关联）
4. 若 AI 输出无法解析，使用 `fallbackSlice()` 按固定长度硬切分。

**语义边界判断**：AI 根据内容转折（场景变换、时间跳跃、视角切换、情绪转变）确定切分点。当单个语义段落超长时，AI 会进一步按次级边界拆分。

**产出**：`slices.json` — 约 50-200 个 `SemanticSlice` 对象。
**持久化**：每切片完成后立即 `POST /api/library/save-step` 写入磁盘。

**100 万字场景**：文本太长，单次 AI 调用无法处理。当前行为是将全文传入（可能截断）。未来方案：先做程序化预切分再 AI 精修（事件图谱管线已实现此方案）。

---

### Step 1 — 文风提取

**输入**：`SemanticSlice[]`（所有切片内容拼接为 markdown）

**多批次处理**：
1. `computeStyleBatches()` 计算是否需要分批。
   - 公式：`maxChars = max(10000, floor(maxContextTokens * 1.5) - 8000)`
   - 100 万字情况下几乎必定分批（可能 5-20 批）
2. 第一批：发送切片内容 → AI 生成初始文风档案。
3. 后续批次：发送"上一批报告 + 新切片块"→ AI 增量精炼。
4. 每批调用 `/api/source/process/style`，maxTokens=8192。

**产出**：`style_profile.md` — 9 维度文风分析：
- 基础六维：句式、修辞、词汇、叙事视角、对话风格、描写手法
- 增强三维：章节首尾技术、叙述/场景比例、紧张语言标记
- 每个规律附带：支持证据 + 反例 + 适用率

---

### Step 2 — 叙事动力学（情节提取）

**输入**：`SemanticSlice[]` + `styleProfile`

**过程**：
1. 依赖 Step 1 的产出（`styleProfile` 作为上下文传入 prompt）。
2. 同样支持多批次。
3. 调用 `/api/source/process/plot`。

**产出**：`plot_report.md` — 情节结构分析：
- 大情节框架（主线节点映射）
- 小情节模式库（重复出现的叙事模式）
- 伏笔手法
- 节奏规律

---

### Step 3 — 角色动力学

**输入**：`SemanticSlice[]` + `plotReport`

**过程**：依赖 Step 2 的叙事动力学报告。多批次支持。

**产出**：`character_dynamics.md` — 角色关系拓扑 + 角色功能分类。

---

### Step 4 + Step 5 — 读者体验 ‖ 叙事约束（并行）

Step 4 和 Step 5 通过 `Promise.all` 并行执行，因为二者都依赖 Step 2+3 但不互相依赖。

**Step 4 输入**：`SemanticSlice[]` + `styleProfile` + `plotReport` + `characterDynamics`

**产出**：`reader_experience.md` — 读者心理分析：
- 心理缺失模型（小说满足了什么心理需求）
- 高潮势能逆向追踪（势能类型：Cognitive / Emotional / Mixed）
- 综合翻页驱动力

**Step 5 输入**：`SemanticSlice[]` + `plotReport` + `characterDynamics`

**产出**：`narrative_constraints.md` — 叙事约束：
- 冷却机制
- 风险模式
- 禁忌事项

---

### Step 6 — 样本选取

**输入**：切片摘要（仅标题+标签+情绪，不含全文）+ 前序报告

**产出**：`samples.json` — 3-5 个代表性切片。

> **注意**：此步骤在阶段 B 被扩展为 `技术样本库`（8 类 × 3 样本 = 24 个样本）。

---

### Step 7 — DNA 压缩 (V1)

**输入**：全部 5 份分析报告（不含切片内容）

**产出**：`novel_dna.yaml` — 旧版扁平结构 DNA（保留兼容）。

> **注意**：新版六层 DNA 在阶段 B 由 `buildNovelDNA()` 程序化构建。

---

## 阶段 B：道/气深度分析

> **编排器**：`src/lib/source-processing/dao-pipeline.ts` — `runDaoPipeline()`
> **触发**：用户在 UI 点击「深度分析」按钮
> **DAG 并行执行**：步骤间按依赖关系编排，无依赖的步骤并行

### Phase B1 — 体验流标注

**目标**：记录"读者阅读时的主观体验"，而非分析"作者写了什么"。

**过程**：
1. 系统内置 3 个读者 persona：
   - `casual`（随便看看的普通读者）
   - `immersive`（追求沉浸感的读者）
   - `fast_paced`（追求快节奏爽感的读者）
2. **3 个 persona 并行**发起 AI 调用（`Promise.allSettled`）。
3. 每个 persona 对每个切片输出四维分数：
   - `immersion`（沉浸感 1-10）
   - `emotionalIntensity`（情绪强度 1-10）
   - `anticipation`（期待感 1-10）
   - `perceivedPace`（节奏感知 fast/medium/slow）
4. `aggregateExperienceCurves()` 聚合多 persona 结果：
   - 计算每个切片的平均分数
   - 计算标准差（衡量主观性——标准差大 = 读者分歧大）
   - 标记"体验高点"（多 persona 共识 immersion ≥ 7 且标准差 ≤ 2）

**产出**：
- `ExperienceAnnotation[]`（原始标注，每个 persona 一份）
- `ExperienceCurve[]`（聚合后的体验曲线）

**API**：`POST /api/source/process/experience-annotation`

**100 万字场景**：50-200 个切片，3 个 persona × 可能的 2-3 批 = 6-9 次 AI 调用。

---

### Phase B2 — 消融测试 + 势能追踪（并行）

#### 消融测试

**目标**：对每个切片执行"消融测试"——删掉这段，会失去什么？

**四分类**：
| 分类 | 含义 | 删除后果 |
|------|------|---------|
| `bone`（骨架） | 必须存在 | 剧情崩 |
| `muscle`（肌肉） | 增强体验 | 体验下降但剧情不崩 |
| `filler_a`（体验填充） | 维持"气" | 阅读节奏被打乱 |
| `filler_b`（机械填充） | 纯凑字数 | 什么都不变 |
| `uncertain`（不确定） | **默认分类** | AI 拿不准时归类于此 |

**设计原则**：默认输出 `uncertain`，宁可承认不确定也不强行归类。无明确证据则不推断深意。

**API**：`POST /api/source/process/ablation-testing`

#### 势能追踪

**目标**：追踪每个高潮的"积累→释放"周期。

**核心公式**：`爽感 = 势能积累 × 释放效率`

**追踪内容**：
- 高潮类型（情感/认知/权力/身份/关系）
- 积累起点（sliceIndex）
- 积累时长（跨越多少切片）
- 中途强化点
- 回报倍率（low / medium / high / extreme）

**节奏画像**（全局统计）：
- 推进/蓄力/释放/呼吸/存在/校准 场景比例

**API**：`POST /api/source/process/tension-tracking`

---

### Phase B3 — DNA V2 构建（程序化，无需 AI）

**输入**：前序所有产出（切片 + 5 份报告 + 体验曲线 + 消融结果 + 势能分析）

**过程**：`buildNovelDNA()` 程序化构建六层结构：

```
┌─────────────────────────────────────────┐
│ 道 (dao)                                │
│ ├─ 主情绪场（从体验曲线高点推断）         │
│ ├─ 备选解读                             │
│ ├─ 读者为何停留                         │
│ ├─ AI 置信度                            │
│ └─ 用户确认标记                         │
├─────────────────────────────────────────┤
│ 气 (qi)                                 │
│ ├─ 维持方法列表（从消融+体验推断）        │
│ ├─ 呼吸周期描述（从势能追踪）            │
│ ├─ 节奏画像                             │
│ └─ 破坏因素                             │
├─────────────────────────────────────────┤
│ 结构 (structure)                        │
│ ├─ 骨（bone 消融结果）                  │
│ ├─ 肉（muscle 消融结果）                │
│ ├─ 体验填充（filler_a）                 │
│ └─ 机械填充（filler_b）                 │
├─────────────────────────────────────────┤
│ 引擎 (engines)                          │
│ ├─ 势能模式列表                         │
│ ├─ 角色决策模型                         │
│ └─ 信息控制方式                         │
├─────────────────────────────────────────┤
│ 风险 (failure_modes)                    │
│ ├─ 机械填充过多 → 直接模仿会注水         │
│ ├─ 势能积累周期过长 → 不适合快节奏平台    │
│ └─ 主情绪场为 AI 推断，未经人工确认       │
├─────────────────────────────────────────┤
│ 风格引擎 (style_engine)                 │
│ └─ 引用原始文风报告                      │
└─────────────────────────────────────────┘
```

**"道"的推断逻辑**：
- 找到体验高点切片
- 统计这些切片的情绪基调
- 结合平均期待感分数推断
- 置信度 = 体验高点数量 > 3 则 0.7，否则 0.4
- **不分析"作者想表达什么"**，从"读者在哪些段落体验最好"反推

---

### Phase B4 — 技术样本库（程序化）

**目标**：替代旧版 3-5 个代表性样本，按叙事技术分类采集。

**分类采集**（每类选 3 个）：

| 类别 | 筛选条件 |
|------|---------|
| **hooks**（开头钩子） | `narrativeFunction === 'setup'` 或 `index ≤ 3` |
| **climaxes**（高潮） | `tensionLevel ≥ 8` 或 `narrativeFunction === 'climax'` |
| **buildups**（蓄力） | `tensionLevel 5-8` 且 `narrativeFunction === 'escalation'` |
| **transitions**（转场） | `narrativeFunction === 'cooldown'` 或 `setup` |
| **relationship**（关系推进） | 标签含 `['关系', '情感', '互动']` |
| **worldRevelations**（世界观揭露） | 标签含 `['世界观', '设定', '揭示']` |
| **breathPassages**（呼吸段落） | 消融结果 `filler_a` 或低紧张度非骨架段落 |
| **foreshadowings**（伏笔） | 标签含 `['伏笔', '铺垫', '暗示']` 或有依赖关系 |

---

## 阶段 B'：事件图谱（200 万字长篇专用）

> **设计原则**：压缩认知，不压缩文本
> **核心文件**：`src/lib/source-processing/windowed-event-extractor.ts`
> **适用场景**：200 万字以上长篇小说，基础管线上下文窗口不足时

### 为什么需要事件图谱

基础管线（阶段 A+B）处理 100 万字以内小说效果良好，但面对 200 万字以上的长篇存在瓶颈：
1. **切片太多**：50-200 个切片的多批次处理在极端长度下效率低下
2. **因果链断裂**：文本摘要丢失跨百章的伏笔-回收关系
3. **认知压缩不够**：markdown 报告无法精确表达复杂叙事结构

事件图谱方案将小说转化为**结构化事件数据库**，保留因果链、伏笔链接和张力曲线。

### Stage 1 — 文本分块 + 实体索引（程序化）

**过程**：
1. 将全文按 ~450,000 字符切分为文本块（`TextChunk`）
2. 程序化提取实体字典（中文 n-gram 2-4 字）
3. 每个文本块记录章节范围和提取的实体

**无 AI 调用**，纯程序化处理。

### Stage 2 — 滑动窗口事件提取（AI，多窗口并行）

**核心策略**：`computeWindows()` 计算重叠窗口：
- 每个窗口 ~450K 字符
- 相邻窗口 10% 重叠（防止事件被切分到窗口边界）
- 所有窗口**并行**调用 AI（`Promise.all`）

**AI 调用**：
- `POST /api/source/process/event-extraction`
- maxTokens = 16384
- 每窗口独立提取 `NovelEvent[]`

**事件提取规则**：
- 只有**改变故事状态**的才构成事件（对话/描写/心理活动不是事件）
- 12 种固定分类：combat/conflict、betrayal、breakthrough、decision、death、reunion、revelation、sacrifice、alliance、power_shift、loss、daily/transition
- 每个事件标注因果链（`causes[]` / `effects[]`）和伏笔关系（`foreshadowingOf[]` / `foreshadowingFrom[]`）

### Stage 3 — 图谱合并（程序化）

**过程**：
1. `mergeOverlappingEvents()` — 去重重叠区事件（同章节 ±2、同类型、共享参与者）
2. `buildEventGraph()` — 构建三级索引 + 伏笔配对：
   - `byChapter`：章节 → 事件 ID
   - `byParticipant`：角色 → 事件 ID
   - `byType`：类型 → 事件 ID
   - `foreshadowingPairs`：{ setup, payoff, distance }

**无 AI 调用**。

### Stage 4 — 价值采样（程序化）

**`sampleByValue()`** 从事件图谱中选 30-50 个高价值切片用于深度分析：

| 策略 | 比例 | 筛选条件 |
|------|------|---------|
| 高潮 | 30% | 高 tensionChange 或 combat/breakthrough/death 类型 |
| 转折点 | 20% | betrayal/decision/reunion 类型 |
| 伏笔对 | 25% | 距离 > 5 章的 setup-payoff 配对 |
| 呼吸段 | 15% | 低紧张度 daily/transition 事件 |
| 用户锚点 | 5 个 | 用户标记的重要段落 |

### Stage 5 — DNA 蒸馏（程序化）

**`buildNovelDNAFromEventGraph()`** — 与 `buildNovelDNA()` 不同的 DNA 构建路径：

- **道**：从事件情绪分布和类型比例推断主情绪场
- **气**：从张力变化模式和事件类型比例推断维持方法
- **结构**：高度数事件 = 骨，高张力变化 = 肉，低张力日常 = 填充
- **引擎**：势能模式 + 角色决策模型 + 信息控制方式
- **风险**：自动检测长程伏笔（>30 章）、填充比例过高

**关键差异**：`buildNovelDNA()` 依赖文本报告（markdown），`buildNovelDNAFromEventGraph()` 依赖结构化事件数据。后者无需 AI 调用。

---

## 阶段 C：写作生成

### 上下文组装

`assembleLayerContext()` 根据目标层级组装不同上下文：

| 层级 | plotGuide 取什么 | 道/气上下文 | 节奏处方 |
|------|-----------------|------------|---------|
| L1 大纲 | 大情节框架 | 精简版（300 字） | 精简版 |
| L2 阶段 | 节奏+伏笔规则 | 完整版 | 精简版 |
| L3 分卷 | 小情节模式库 | 完整版 | — |
| L4 章节集合 | 小情节模式库 | 完整版 | — |
| L5 每章计划 | 小情节模式库 | 完整版 | **完整版** |

### 5 层生成（先框架后填充）

每层先生成框架（如"有哪些阶段"），再逐个填充细节。道/气上下文和节奏处方已注入所有层的 prompt。

### 章节正文生成

**生成 prompt 注入内容**：
- 文风档案（完整）
- 道/气上下文（主情绪场 + 维持方法）
- 节奏处方（势能模式 + 骨肉填充比例 + 避坑指南）
- 层级上下文（大纲→阶段→卷→集合→计划）
- 前情提要（前一章末尾 1200 字 + 状态增量摘要）

### 5 维审查

| 维度 | 检查内容 |
|------|---------|
| `style_consistency` | 文风是否一致 |
| `plot_coherence` | 情节逻辑是否通顺 |
| `pacing` | 节奏是否符合指令 |
| `pattern_execution` | 小情节模式是否执行 |
| `foreshadow_execution` | 伏笔是否落实 |

全部 ≥ 7 分通过，任一 < 7 分进入修正循环（最多 3 轮）。

---

## API 调用次数估算（100 万字）

| 阶段 | 步骤 | 估计调用次数 | 说明 |
|------|------|-------------|------|
| A | Step 0 切片 | 1-3 | 可能需要分批 |
| A | Step 1 文风 | 5-20 | 多批次 |
| A | Step 2 情节 | 5-15 | 多批次 |
| A | Step 3 角色 | 3-10 | 多批次 |
| A | Step 4 读者 | 3-10 | 与 Step 5 并行 |
| A | Step 5 约束 | 3-10 | 与 Step 4 并行 |
| A | Step 6 样本 | 1 | 单批次 |
| A | Step 7 DNA V1 | 1 | 单批次 |
| **A 小计** | | **22-70** | |
| B | 体验流标注 | 6-9 | 3 persona × 2-3 批 |
| B | 消融测试 | 2-5 | 与势能追踪并行 |
| B | 势能追踪 | 1-2 | 与消融测试并行 |
| **B 小计** | | **9-16** | |
| **A+B 总计** | | **31-86 次 AI 调用** | |
| B' | 事件提取 | 3-5 | 窗口并行（200 万字时） |
| B' | 价值采样 | 0 | 程序化 |
| B' | DNA 蒸馏 | 0 | 程序化 |
| **B' 小计** | | **3-5** | |
| C | 5 层生成 | 视项目规模 | 大纲 1 + 阶段 1+N + 卷 1+M + 集 1+K + 计划 1+J |
| C | 章节生成 | 每章 1-4 | 生成 + 0-3 轮修正 |

---

## 持久化文件结构（分析完成后）

```
data/source_library/{novel_id}/
├── meta.json                    # 状态标记
├── raw.txt                      # 原始文本
├── slices.json                  # Step 0: SemanticSlice[]
├── style_profile.md             # Step 1: 文风报告
├── plot_report.md               # Step 2: 情节报告
├── character_dynamics.md        # Step 3: 角色报告
├── reader_experience.md         # Step 4: 读者体验报告
├── narrative_constraints.md     # Step 5: 叙事约束报告
├── samples.json                 # Step 6: 旧版样本
├── novel_dna.yaml               # Step 7: 旧版 DNA
├── experience_curve.json        # Phase B1: 体验曲线
├── ablation_results.json        # Phase B2: 消融结果
├── tension_analysis.json        # Phase B2: 势能分析
├── novel_dna_v2.json            # Phase B3: 六层 DNA V2
├── technique_samples.json       # Phase B4: 技术样本库
├── event_graph.json             # Stage B': 事件图谱
└── text_chunks.json             # Stage B': 文本块索引
```
</file>

<file path="docs/系统流程完整介绍.md">
# Ainovr — 系统流程完整介绍

> **目标读者**：AI 系统（用于多 Agent 协作分析）
> **生成日期**：2026-06-10
> **代码版本**：Next.js 16.2.6 · React 19 · Zustand 5 · Tailwind CSS v4

---

## 一、项目概览

**Ainovr** 是一个 AI 辅助小说风格模仿创作工具。核心流程分为三大阶段：

```
┌─────────────────────────────────────────────────────────────────┐
│  阶段 A：基础分析管线（8 步串行 · 客户端编排 · 服务端流式 AI）    │
│  上传小说 → 语义切片 → 文风/情节/人物/体验/约束 → DNA 压缩      │
├─────────────────────────────────────────────────────────────────┤
│  阶段 B：道/气深度分析（DAG 并行）                               │
│  体验流标注 → 消融测试/势能追踪 → DNA V2 构建 → 技术样本库       │
├─────────────────────────────────────────────────────────────────┤
│  阶段 B'：事件图谱（200 万字长篇专用）                           │
│  滑动窗口事件提取 → 因果链合并 → 价值采样 → DNA 蒸馏             │
├─────────────────────────────────────────────────────────────────┤
│  阶段 C：写作生成管线（5 层层次化 · 先框架后细节 · 审查修正循环）  │
│  大纲 → 阶段 → 分卷 → 章节集 → 每章计划 → 正文生成 → 审查 → 修正 │
└─────────────────────────────────────────────────────────────────┘
```

**技术栈关键特征**：
- 单页应用，无 URL 路由（通过 Zustand `activeView` 切换视图）
- 无数据库，纯文件系统存储（`data/` 目录下 JSON/TXT/MD/YAML）
- 自研 SSE 流式客户端（原生 fetch + ReadableStream），不依赖 AI SDK
- AI 配置（apiKey/model/baseURL）存于浏览器 localStorage，每次请求从客户端传至服务端
- 26+ 个 prompt 模板，用户可自定义覆盖任意 prompt
- 服务端不存储 AI 凭证，API Route 均为无状态流式代理

---

## 二、顶层架构

### 2.1 目录结构

```
src/
├── app/
│   ├── page.tsx                     # 唯一页面入口，视图路由
│   ├── layout.tsx                   # 根布局
│   └── api/                         # 44 个 Route Handler
│       ├── library/                 # 素材库 CRUD（7 个）
│       ├── novels/                  # 旧版小说 CRUD（4 个）
│       ├── project/                 # 写作项目 CRUD（5 个）
│       ├── prompts/                 # 提示词管理（2 个）
│       ├── source/process/          # 素材处理管线（13 个）
│       ├── generation/              # 层级生成（9 个）
│       └── chapter/                 # 章节生成+审查（3 个）
├── components/
│   ├── Sidebar.tsx                  # 侧边栏导航
│   ├── SettingsDialog.tsx           # AI 设置弹窗
│   ├── StreamingText.tsx            # 通用流式文本显示
│   ├── source/                      # 素材库视图组件
│   │   ├── SourceLibraryView.tsx    # 素材列表 + 上传
│   │   ├── SourceNovelCard.tsx      # 素材卡片
│   │   ├── SourceNovelDetailView.tsx# 素材详情 + 一键分析
│   │   ├── MarkdownViewer.tsx       # Markdown 渲染
│   │   ├── ProcessStepIndicator.tsx # 处理步骤指示器
│   │   └── StylePlotLibraryView.tsx # 风格/情节库视图
│   ├── project/                     # 项目视图组件
│   │   ├── WritingProjectView.tsx   # 项目主页（含标签页）
│   │   └── SourceRoleSelector.tsx   # 源小说角色选择
│   ├── generation/                  # 层级生成组件
│   │   ├── LayerGenerationView.tsx  # 5 层规划界面
│   │   ├── LayerCard.tsx            # 单层卡片
│   │   └── HierarchyTree.tsx        # 层次树可视化
│   ├── chapter/                     # 章节生成组件
│   │   ├── ChapterGenerationView.tsx# 章节生成+审查界面
│   │   ├── ReviewPanel.tsx          # 5 维审查面板
│   │   └── FeedbackInput.tsx        # 人工反馈输入
│   └── prompts/                     # 提示词管理视图
│       └── PromptManagementView.tsx
├── lib/
│   ├── ai/
│   │   ├── providers.ts             # 自研 SSE 流式客户端（核心）
│   │   └── prompts/                 # 26+ prompt 构建器
│   │       ├── index.ts             # 统一导出
│   │       ├── helpers.ts           # Prompt 辅助函数
│   │       ├── source-slicing.ts    # 切片 prompt
│   │       ├── style-extraction.ts  # 文风提取 prompt
│   │       ├── plot-extraction.ts   # 情节提取 prompt
│   │       ├── character-dynamics-extraction.ts
│   │       ├── reader-experience-extraction.ts
│   │       ├── narrative-constraints-extraction.ts
│   │       ├── dna-compression.ts   # DNA 压缩 prompt
│   │       ├── sample-selection.ts  # 样本选择 prompt
│   │       ├── entity-classification.ts
│   │       ├── event-extraction.ts  # 事件图谱提取 prompt
│   │       ├── experience-annotation.ts # 体验流标注 prompt
│   │       ├── ablation-testing.ts  # 消融测试 prompt
│   │       ├── tension-tracking.ts  # 势能追踪 prompt
│   │       ├── outline-generation.ts
│   │       ├── phase-framework.ts / phase-detail.ts
│   │       ├── volume-framework.ts / volume-detail.ts
│   │       ├── chapter-set-framework.ts / chapter-set-detail.ts
│   │       ├── chapter-plan-framework.ts / chapter-plan-detail.ts
│   │       ├── chapter-writing.ts
│   │       ├── chapter-review.ts
│   │       └── chapter-revision.ts
│   ├── generation/
│   │   ├── layer-pipeline.ts        # 5 层生成管线编排器
│   │   ├── context-assembler.ts     # 上下文组装器
│   │   └── chapter-reviewer.ts      # 5 维审查解析器
│   ├── source-processing/           # 素材分析提取模块（17 个）
│   │   ├── basic-pipeline.ts        # 基础 8 步管线编排器
│   │   ├── dao-pipeline.ts          # 道/气深度分析管线 + DNA V2 构建
│   │   ├── smart-slicer.ts          # Step 0: 智能切片
│   │   ├── style-extractor.ts       # Step 1: 文风提取
│   │   ├── plot-extractor.ts        # Step 2: 叙事动力学
│   │   ├── character-dynamics-extractor.ts  # Step 3: 角色动力学
│   │   ├── reader-experience-extractor.ts   # Step 4: 读者体验
│   │   ├── narrative-constraints-extractor.ts # Step 5: 叙事约束
│   │   ├── sample-selector.ts       # Step 6: 样本选择
│   │   ├── dna-compressor.ts        # Step 7: DNA 压缩
│   │   ├── program-indexer.ts       # 程序化实体索引（n-gram）
│   │   ├── entity-classifier.ts     # AI 实体分类
│   │   ├── experience-annotator.ts  # 体验流标注（多 persona）
│   │   ├── ablation-tester.ts       # 消融测试（四分类）
│   │   ├── tension-tracker.ts       # 势能追踪（积累→释放）
│   │   ├── windowed-event-extractor.ts # 滑动窗口事件提取（长篇专用）
│   │   └── value-sampler.ts         # 价值采样（基于事件图谱）
│   ├── store/                       # Zustand 状态管理（6 个 Store）
│   │   ├── settings.ts              # AI 配置（localStorage 持久化）
│   │   ├── source-library.ts        # 素材库列表 + 当前选中
│   │   ├── source-processing.ts     # 管线编排 + 流式状态
│   │   ├── project.ts               # 写作项目 + 视图导航
│   │   ├── prompts.ts               # 自定义 prompt 覆盖
│   │   └── navigation.ts            # 视图导航状态
│   ├── safe-path.ts                 # 路径遍历防护
│   ├── constants.ts                 # 默认模型/URL/协议白名单
│   └── analysis-chunker.ts          # 多批次分块工具
└── types/
    └── index.ts                     # 全部 TypeScript 类型定义
```

### 2.2 数据存储

```
data/
├── source_library/{nanoid}/         # 每个源小说一个目录
│   ├── meta.json                    # 元数据 + 处理状态标记
│   ├── raw.txt                      # 清洗后的原始文本
│   ├── slices.json                  # SemanticSlice[]
│   ├── style_profile.md             # 文风分析报告
│   ├── plot_report.md               # 叙事动力学报告
│   ├── character_dynamics.md        # 角色动力学报告
│   ├── reader_experience.md         # 读者体验报告
│   ├── narrative_constraints.md     # 叙事约束报告
│   ├── samples.json                 # RepresentativeSample[]
│   ├── novel_dna.yaml               # 压缩 DNA V1（旧版兼容）
│   ├── experience_curve.json        # 体验曲线（道/气分析）
│   ├── ablation_results.json        # 消融结果（道/气分析）
│   ├── tension_analysis.json        # 势能分析（道/气分析）
│   ├── novel_dna_v2.json            # 六层 DNA V2
│   ├── technique_samples.json       # 技术样本库
│   ├── event_graph.json             # 事件图谱（长篇专用）
│   └── text_chunks.json             # 文本块索引（长篇专用）
├── projects/{nanoid}/
│   └── project.json                 # 完整 WritingProject（含所有层级 + 章节）
├── novels/{nanoid}/                 # [旧版兼容]
│   ├── meta.json / full.txt / chunks.json
│   └── chunks/
└── prompts.json                     # 自定义 prompt 覆盖（单文件）
```

### 2.3 6 个 Zustand Store

| Store | 持久化 | 核心职责 |
|-------|--------|---------|
| `useSettingsStore` | localStorage (v7) | apiKey, model, baseURL, maxContextTokens |
| `useSourceLibraryStore` | 无（从服务端加载） | sourceNovels[], activeSourceId |
| `useSourceProcessingStore` | 无（瞬时） | 管线编排：streamContents, isStreaming, progress, currentStep |
| `useProjectStore` | 无（从服务端加载） | projects[], activeProjectId, activeView, 层级 setters |
| `usePromptStore` | 无（从服务端加载） | defaultPrompts, customPrompts, 覆盖逻辑 |
| `useNavigationStore` | 无（瞬时） | 视图导航辅助状态 |

### 2.4 视图导航

通过 `useProjectStore.activeView` 切换，当前活跃的 4 个视图：

```
'source-library'     → SourceLibraryView     # 素材库管理
'source-detail'      → SourceNovelDetailView # 单小说详情 + 一键分析
'writing-project'    → WritingProjectView    # 项目主页（含 3 个标签页）
'prompt-management'  → PromptManagementView  # 提示词编辑器
```

WritingProjectView 内部有三个标签页，通过组件内 state 切换：
1. **项目设置** — 项目信息、源小说链接、危险区
2. **层级规划** — 嵌入 LayerGenerationView（5 层生成）
3. **章节生成** — 嵌入 ChapterGenerationView（正文+审查）

---

## 三、AI 集成层

### 3.1 核心流式客户端（`src/lib/ai/providers.ts`）

只有一个核心函数 `chatCompletionStream()`：

```typescript
function chatCompletionStream(options, params): ReadableStream<Uint8Array>
```

**流程**：
1. `validateBaseURL()` — SSRF 防护：检查协议白名单，拒绝 localhost/127.0.0.1/内网地址
2. 构造 OpenAI-compatible 请求体 `{ model, messages, max_tokens, stream: true }`
3. 发送 `POST {baseURL}/chat/completions`，Bearer 认证
4. 读取 `response.body` ReadableStream
5. 解析 SSE 格式：`data: {"choices":[{"delta":{"content":"..."}}]}`
6. 提取 `delta.content` 逐块 enqueue 到返回的 ReadableStream

**关键特征**：
- 不使用 `ai` / `@ai-sdk/openai` 包（它们在 dependencies 中但运行时未使用）
- 原生 `fetch` + `ReadableStream` API
- 通过 OpenAI-compatible 协议兼容任何 API（DeepSeek、GLM 等）
- 默认 maxTokens: 4096（处理管线步骤用 8192，实体分类用 2048，DNA 压缩用 4096）

### 3.2 AI 配置流

```
localStorage (useSettingsStore)
    ↓ (每次请求时读取)
客户端组件 / 管线编排器
    ↓ (作为 JSON body 传参)
Next.js API Route Handler
    ↓ (调用 chatCompletionStream)
OpenAI-compatible API
    ↓ (SSE 流式响应逐块返回)
客户端 ReadableStream → 累积展示
```

**凭证不落盘**：服务端不存储 apiKey，每次请求都从客户端传入。

### 3.3 Prompt 系统

26+ 个 prompt 构建器，每个导出：
- `build*Messages()` — 动态构建 `{ systemPrompt, userMessage }`
- `DEFAULT_SYSTEM_PROMPT` — 默认 system prompt 常量

**Prompt 分类**：
- 素材处理（15 个构建器）：切片、实体分类、文风提取+补充、情节提取+补充、角色动力学+补充、读者体验+补充、叙事约束+补充、DNA 压缩、样本选择、体验流标注、消融测试、势能追踪、事件图谱提取
- 层级生成（8 个构建器）：大纲、阶段框架+详细、分卷框架+详细、章节集合框架+详细、每章计划框架+详细
- 章节生成（3 个构建器）：章节写作、审查、修正

**自定义覆盖**：用户通过 PromptManagementView 修改任意 prompt → POST `/api/prompts/save` → 写入 `data/prompts.json`。`usePromptStore.getPrompt(key)` 优先返回自定义版本。

---

## 四、素材分析管线

### 4.1 基础分析管线（8 步串行）

> **编排位置**：`src/lib/source-processing/basic-pipeline.ts` 的 `runBasicPipeline()` 方法
> **状态机**：`raw → slicing → extracting → character_dynamics → deep_analyzing → selecting → compressing → ready`（任何步骤失败进入 `error`）

### 执行流程图

```
用户点击"一键分析"
    │
    ▼
Step 0: 智能切片 (smart-slicer.ts)
    │ POST /api/source/process/slice
    │ Input: raw.txt
    │ Output: SemanticSlice[]
    │ save: slices.json
    │ 状态: 'slicing'
    ▼
Step 1: 文风提取 (style-extractor.ts)
    │ POST /api/source/process/style
    │ Input: SemanticSlice[] + 文风分析 prompt
    │ Output: styleProfile (markdown)
    │ save: style_profile.md
    │ 状态: 'extracting'
    │ 多批次：支持长文本拆分
    ▼
Step 2: 叙事动力学/情节 (plot-extractor.ts)
    │ POST /api/source/process/plot
    │ Input: SemanticSlice[] + styleProfile
    │ Output: plotReport (markdown)
    │ save: plot_report.md
    │ 依赖 Step 1 的输出
    ▼
Step 3: 角色动力学 (character-dynamics-extractor.ts)
    │ POST /api/source/process/character-dynamics
    │ Input: SemanticSlice[] + plotReport
    │ Output: characterDynamics (markdown)
    │ save: character_dynamics.md
    │ 依赖 Step 2 的输出
    ▼
Step 4: 读者体验 (reader-experience-extractor.ts)
    │ POST /api/source/process/reader-experience
    │ Input: SemanticSlice[] + styleProfile + plotReport + characterDynamics
    │ Output: readerExperience (markdown)
    │ save: reader_experience.md
    │ 依赖 Step 1+2+3
    ▼
Step 5: 叙事约束 (narrative-constraints-extractor.ts)
    │ POST /api/source/process/narrative-constraints
    │ Input: SemanticSlice[] + plotReport + characterDynamics
    │ Output: narrativeConstraints (markdown)
    │ save: narrative_constraints.md
    │ 依赖 Step 2+3
    ▼
Step 6: 样本选择 (sample-selector.ts)
    │ POST /api/source/process/samples
    │ Input: 切片摘要 + styleProfile + plotReport
    │ Output: RepresentativeSample[] (3-5 个)
    │ save: samples.json
    ▼
Step 7: DNA 压缩 (dna-compressor.ts)
    │ POST /api/source/process/dna-compression
    │ Input: 全部分析报告（不含切片内容）
    │ Output: novelDna (YAML)
    │ save: novel_dna.yaml
    │ ⚠️ 此步失败不阻塞管线
    ▼
完成 (status: 'ready')
```

### 关键实现细节

**多批次处理**（Steps 1-5 支持）：
- `computeMaxCharsPerBatch(maxContextTokens)` 计算单批最大字符数：`max(10000, floor(maxContextTokens * 1.5) - 8000)`
- `splitIntoBatches()` 按段落/换行/句号边界拆分
- 第一批：`build*Messages()` 生成初始报告
- 后续批次：`build*SupplementMessages(previousReport, newChunk)` 增量精炼

**短文本跳过**：
- Step 0：文本 < 6500 字符时跳过 AI，`createSingleSlice()` 直接创建一个切片

**增量持久化**：
- 每步完成后立即 `POST /api/library/save-step`，更新 `meta.json` + 写入对应文件
- 刷新页面后可从已有步骤继续（通过 meta.json 的状态标记判断）

**Step -1（实体索引，未集成到 8 步管线中）**：
- `program-indexer.ts`：纯程序化的中文 n-gram 实体提取（2-4 字）
- `entity-classifier.ts`：AI 分类实体类型（角色/地点/组织/神器/概念）

### 4.2 道/气深度分析管线（DAG 并行）

> **编排位置**：`src/lib/source-processing/dao-pipeline.ts` 的 `runDaoPipeline()`
> **触发**：用户在 UI 点击「深度分析」按钮

```
基础管线完成后
    │
    ▼
Phase B1: 体验流标注 (experience-annotator.ts)
    │ 3 个 reader persona 并行投票
    │ 输出: ExperienceCurve[]
    ▼
Phase B2: 消融测试 + 势能追踪（并行）
    │ ├── ablation-tester.ts → 四分类 (bone/muscle/filler_a/filler_b)
    │ └── tension-tracker.ts → 势能周期 + 节奏画像
    ▼
Phase B3: DNA V2 构建（程序化，无需 AI）
    │ buildNovelDNA() → 六层结构（道/气/结构/引擎/风险/风格引擎）
    ▼
Phase B4: 技术样本库（程序化）
    │ 8 类 × 3 样本（hooks/climaxes/buildups/transitions/relationship/worldRevelations/breathPassages/foreshadowings）
    ▼
完成
```

### 4.3 事件图谱管线（200 万字长篇专用）

> **编排位置**：`src/lib/source-processing/windowed-event-extractor.ts`
> **设计原则**：压缩认知，不压缩文本

```
上传长篇小说（200 万字+）
    │
    ▼
Stage 1: 文本分块 + 实体索引（程序化，无 AI）
    │ 将全文分为 ~450K 字符的文本块
    │ 提取实体字典
    ▼
Stage 2: 滑动窗口事件提取（AI，多窗口并行）
    │ computeWindows() → 计算重叠窗口
    │ Promise.all → 每窗口独立 AI 调用
    │ POST /api/source/process/event-extraction
    │ 输出: NovelEvent[]（含因果链、伏笔标注）
    ▼
Stage 3: 图谱合并（程序化）
    │ mergeOverlappingEvents() → 去重重叠区事件
    │ buildEventGraph() → 构建索引 + 伏笔配对
    │ 输出: EventGraph（byChapter/byParticipant/byType 索引）
    ▼
Stage 4: 价值采样（value-sampler.ts）
    │ sampleByValue() → 基于事件图谱选 30-50 个高价值切片
    │ 策略: 高潮 30% + 转折 20% + 伏笔对 25% + 呼吸 15% + 用户锚点 5
    ▼
Stage 5: DNA 蒸馏（程序化）
    │ buildNovelDNAFromEventGraph() → 从事件图谱直接构建六层 DNA
    │ 不依赖文本报告，纯结构化数据驱动
    ▼
完成
```

**核心数据类型**：

```typescript
// 事件
interface NovelEvent {
  id: string; chapter: string; chunkIndex: number;
  type: string;           // 12 种固定分类（combat/betrayal/breakthrough/...）
  participants: string[];
  location: string;
  description: string;
  causes: string[];       // 前向因果链
  effects: string[];      // 后向因果链
  tensionChange: number;
  emotion: string;
  foreshadowingOf: string[];   // 指向的未来事件
  foreshadowingFrom: string[]; // 来自的铺垫事件
  confidence: number;
}

// 事件图谱
interface EventGraph {
  events: NovelEvent[];
  byChapter: Record<string, string[]>;      // 章节 → 事件 ID
  byParticipant: Record<string, string[]>;  // 角色 → 事件 ID
  byType: Record<string, string[]>;         // 类型 → 事件 ID
  foreshadowingPairs: {                     // 伏笔配对
    setup: string; payoff: string;
    distance: number;
  }[];
}
```

---

## 五、写作生成管线（5 层层次化）

> **编排位置**：`src/lib/generation/layer-pipeline.ts` + `LayerGenerationView.tsx`
> **核心模式**：每层先"框架"后"填充"（framework → detail）

### 5 层结构

```
Layer 1: 全书大纲     (~200 字)                          ← 单次调用
    │
    ▼
Layer 2: 阶段规划     (4-6 个 Phase, 每份 ~500 字)       ← 框架 + N×详细
    │
    ▼
Layer 3: 分卷         (每 Phase 1-2 卷, 每卷 ~300 字)   ← 框架 + N×详细
    │
    ▼
Layer 4: 章节集合     (每卷 3-5 个 Set, 每个 ~200 字)   ← 框架 + N×详细
    │
    ▼
Layer 5: 每章计划     (~100 字/章)                       ← 框架 + N×详细
    │
    ▼
章节正文生成 → 5 维审查 → 修正循环（最多 3 轮）
```

### 上下文组装器（`context-assembler.ts`）

`assembleLayerContext(layer, sourceNovels, project)` 为每层组装不同上下文：

| 层级 | styleGuide | plotGuide（提取部分） | hierarchyContext | chapterTask |
|------|-----------|----------------------|------------------|-------------|
| L1 | 全部文风源 | 大情节框架（## 一、） | — | — |
| L2 | 全部文风源 | 节奏+伏笔规则（## 三/四、） | 大纲 | — |
| L3 | 全部文风源 | 小情节模式库（## 二、） | 大纲 + 阶段 | — |
| L4 | 全部文风源 | 小情节模式库 | 大纲 + 阶段 + 分卷 | — |
| L5 | 全部文风源 | 小情节模式库 | 大纲 + 阶段 + 分卷 + 集合 | 本章计划 |

**源小说角色**（通过 `SourceRole` 控制）：
- `style`：仅贡献 styleGuide
- `plot`：仅贡献 plotGuide
- `style_and_plot`：两者都贡献

### 每层对应的 API 路由

| 层级 | 框架 | 详细 | Prompt 构建器 |
|------|------|------|-------------|
| L1 大纲 | `/api/generation/outline` | — | `buildOutlineGenerationMessages` |
| L2 阶段 | `/api/generation/phase-framework` | `/api/generation/phase-detail` | `buildPhaseFrameworkMessages` / `buildPhaseDetailMessages` |
| L3 分卷 | `/api/generation/volume-framework` | `/api/generation/volume-detail` | `buildVolumeFrameworkMessages` / `buildVolumeDetailMessages` |
| L4 集合 | `/api/generation/chapter-set-framework` | `/api/generation/chapter-set-detail` | `buildChapterSetFrameworkMessages` / `buildChapterSetDetailMessages` |
| L5 计划 | `/api/generation/chapter-plan-framework` | `/api/generation/chapter-plan-detail` | `buildChapterPlanFrameworkMessages` / `buildChapterPlanDetailMessages` |

### 层次化数据结构

```typescript
Phase { id, index, title, content, volumeCount }
  └→ Volume { id, phaseId, index, title, content, chapterSetCount }
       └→ ChapterSet { id, volumeId, index, title, content, chapterCount }
            └→ ChapterPlan { id, chapterSetId, index, title, content, plotKeywords[], patternStructure }
```

---

## 六、章节生成与审查循环

> **编排位置**：`src/lib/generation/chapter-reviewer.ts` + `ChapterGenerationView.tsx`

### 三段式循环

```
从 ChapterPlan 开始
    │
    ▼
┌─────────────────────────────────────────────────┐
│ 1. 生成                                          │
│    POST /api/chapter/generate                    │
│    输入：章节计划 + 文风档案 + 层级上下文 + 前情   │
│    输出：GeneratedChapter                        │
│    状态：pending_review                          │
├─────────────────────────────────────────────────┤
│ 2. 审查                                          │
│    POST /api/chapter/review                      │
│    5 维度各评分 1-10                              │
│    • style_consistency    文风一致性              │
│    • plot_coherence       情节逻辑               │
│    • pacing               节奏                   │
│    • pattern_execution    小情节模式执行度        │
│    • foreshadow_execution 伏笔/铺垫执行度          │
│    全部 ≥ 7 分 → passed                          │
│    任一 < 7 分 → needs_revision                  │
├─────────────────────────────────────────────────┤
│ 3. 修正（仅 needs_revision 时）                   │
│    POST /api/chapter/revise                      │
│    输入：原文 + 审查问题 + 人工反馈(可选)          │
│    revisionCount++                               │
│    最多 3 轮 → 回到步骤 2                         │
└─────────────────────────────────────────────────┘
```

### 前情提要

最近 5 章内容的前 300 字摘要作为 `previousState` 传入，保持叙事连续性。

---

## 七、完整数据流总览

```
┌──────────────────────────────────────────────────────────────────┐
│                         用户交互层                                │
│                                                                  │
│  [上传 .txt]  [一键分析]  [创建项目]  [选择源小说角色]            │
│  [逐层生成]   [审查+修正] [自定义 Prompt]  [AI 设置]              │
└──────────────┬───────────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────────┐
│                     Zustand Store 层（6 个）                      │
│                                                                  │
│  useSettingsStore ←→ localStorage (apiKey/model/baseURL)         │
│  useSourceLibraryStore ←→ /api/library/*                        │
│  useSourceProcessingStore → 编排管线 (启动/流式/错误/保存)        │
│  useProjectStore ←→ /api/project/* + activeView 路由             │
│  usePromptStore ←→ /api/prompts/*                                │
│  useNavigationStore → 视图导航辅助                                │
└──────────────┬───────────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────────┐
│                      API Route 层（44 个 Handler）                │
│                                                                  │
│  ┌─ CRUD 路由 ─────────────────────────────────────┐             │
│  │ /api/library/*   素材库增删改查（7 个）           │             │
│  │ /api/project/*   项目增删改查（5 个）             │             │
│  │ /api/prompts/*   提示词管理（2 个）               │             │
│  │ /api/novels/*    旧版兼容（4 个）                 │             │
│  └─────────────────────────────────────────────────┘             │
│  ┌─ AI 流式代理 ───────────────────────────────────┐             │
│  │ /api/source/process/*  13 个处理步骤              │             │
│  │ /api/generation/*      9 个层级生成步骤           │             │
│  │ /api/chapter/*         3 个章节步骤               │             │
│  └─────────────────────────────────────────────────┘             │
│  所有 AI 路由的通用模式：                                         │
│  parse JSON body → 提取 {systemPrompt, userMessage, apiKey,      │
│  model, baseURL} → chatCompletionStream() → text/plain 流式响应  │
└──────────────┬───────────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────────┐
│                      AI 调用层                                    │
│                                                                  │
│  chatCompletionStream(options, params)                           │
│  ├── validateBaseURL()  SSRF 防护                                │
│  ├── POST {baseURL}/chat/completions  (OpenAI-compatible)        │
│  ├── SSE 流解析: data: {"choices":[{"delta":{"content":"..."}}]} │
│  └── 返回 ReadableStream<Uint8Array>                             │
│                                                                  │
│  Prompt 构建: build*Messages() 函数（26+）                        │
│  ├── 输入: 切片内容 / 上一阶段报告 / 层级上下文                   │
│  ├── 输出: { systemPrompt, userMessage }                         │
│  └── 可被用户自定义覆盖                                           │
└──────────────┬───────────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────────┐
│                    文件系统持久化层                               │
│                                                                  │
│  data/                                                           │
│  ├── source_library/{id}/   ← 素材分析全部输出                   │
│  ├── projects/{id}/         ← 写作项目全部输出                   │
│  └── prompts.json           ← 自定义 prompt 覆盖                 │
│                                                                  │
│  所有写路径通过 safeJoin(baseDir, id) 防路径遍历                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 八、当前状态与待办

### 当前架构状态

| 方面 | 状态 |
|------|------|
| 素材库上传/管理 | ✅ 完成 |
| 基础 8 步分析管线 | ✅ 完成（`basic-pipeline.ts`） |
| 道/气深度分析管线 | ✅ 完成（`dao-pipeline.ts`） |
| 事件图谱（长篇专用） | ⚠️ 组件级完成，顶层编排未接入 |
| 5 层生成管线 | ✅ 完成 |
| 章节生成+审查+修正 | ✅ 完成 |
| 多源小说角色（style/plot/style_and_plot） | ✅ 完成 |
| 自定义 Prompt | ✅ 完成 |
| AI 配置 localStorage | ✅ 完成 |
| 流式展示 | ✅ 完成 |
| 视图路由（单页） | ✅ 完成 |
| 测试 | ❌ 无测试框架/测试用例 |
| Lint | ⚠️ ESLint 配置存在但未强制执行 |

### 事件图谱待完成项

| 待办 | 说明 |
|------|------|
| 顶层编排接入 | `runBasicPipeline()` / `runDaoPipeline()` 未调用事件图谱函数 |
| 长篇/短篇自动切换 | 无逻辑判断何时走事件图谱路径 vs 基础管线 |
| 端到端集成测试 | 所有组件已实现但未串联运行 |
</file>

<file path="components.json">
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "base-nova",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "iconLibrary": "lucide",
  "rtl": false,
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "menuColor": "default",
  "menuAccent": "subtle",
  "registries": {}
}
</file>

<file path="docs/notes/JS-Open-Source-Awards-2024-2026.md">
# JS Open Source Awards 完整对比分析

## 📅 2025 年（第6届）获奖者

来源：https://osawards.com/javascript/2025

| 奖项 | 🏆 获奖者 | 说明 |
|------|-----------|------|
| Breakthrough of the Year | [Rolldown](https://rolldown.rs) | Rust 写的 JS 打包器，Vite 的未来引擎 |
| The Most Exciting Use of Technology | [TanStack Form](https://github.com/TanStack/form) | 无头、高性能、类型安全的表单状态管理，支持 React/Vue/Angular/Solid/Lit |
| Powered by AI | [Micro-agent](https://github.com/micro-agent) | AI 驱动的编程辅助 |
| Productivity Booster | [Zod](https://github.com/colinhacks/zod) | TypeScript 优先的 schema 验证库 |
| Community of the Year | [meet.js Wrocław](https://meetjs.pl) | 波兰弗罗茨瓦夫的 JS/TS 开发者社区 |

## 2024 年获奖者（对比）

| 奖项 | 🏆 获奖者 |
|------|-----------|
| Breakthrough of the Year | [Rspack](https://rspack.dev) |
| The Most Exciting Use of Technology | [Effect-TS](https://effect.website) |
| Powered by AI | [LangChain.js](https://github.com/langchain-ai/langchainjs) |
| Productivity Booster | [Biome](https://biomejs.dev) |

---

## 📅 2026 年（第7届）提名

来源：[osawards.com/javascript](https://osawards.com/javascript/)

### 1️⃣ Innovation Peak of the Year（年度创新之巅）🆕 新奖项

| 提名项目 | 说明 |
|----------|------|
| [Open Claw](https://github.com/openclaw/openclaw) | 个人 AI 助手，运行在你自己的设备上，支持语音和 Canvas |
| [Open Code](https://github.com/anomalyco/opencode) | 开源 AI 编程 Agent |

### 2️⃣ Breakthrough of the Year（年度突破）

| 提名项目 | 说明 |
|----------|------|
| [Valdi](https://github.com/Snapchat/Valdi) | Snapchat 出品的跨平台 UI 框架，原生性能+开发效率 |
| [Ripple](https://github.com/Ripple-TS/ripple) | 优雅的 TypeScript UI 框架，无虚拟 DOM |
| [TanStack Start](https://tanstack.com/start) | 基于 TanStack Router 的全栈 React 框架，支持 SSR/Streaming |

### 3️⃣ The Most Exciting Use of Technology（最激动人心的技术应用）

| 提名项目 | 说明 |
|----------|------|
| [TanStack DB](https://github.com/TanStack/db) | 响应式客户端数据存储，支持 live queries 和乐观更新 |
| [ForesightJS](https://github.com/spaansba/ForesightJS) | 基于鼠标轨迹预测用户意图的库 |
| [TypeGPU](https://github.com/software-mansion/TypeGPU) | WebGPU 工具包，用 TypeScript 写 shader |
| [Blazediff](https://github.com/teimurjan/blazediff) | 高性能视觉回归测试图片对比工具 |

### 4️⃣ Productivity Booster（生产力提升）

| 提名项目 | 说明 |
|----------|------|
| [es-toolkit](https://github.com/toss/es-toolkit) | 现代 JS 工具库，小体积高性能（lodash 替代品） |
| [Biome](https://github.com/biomejs/biome) | 快速的 linter + formatter（再次提名） |
| [Codemod](https://github.com/codemod/codemod) | 编译器感知的代码分析与转换工具 |
| [Rstest](https://github.com/web-infra-dev/rstest) | 基于 Rspack 的测试框架，Jest 替代品 |
| [Sparo](https://github.com/tiktok/sparo) | TikTok 出品，让大型 monorepo 的 Git 操作保持快速 |

### 5️⃣ AI Project of the Year（年度 AI 项目）

| 提名项目 | 说明 |
|----------|------|
| [Midscene](https://github.com/web-infra-dev/midscene) | 视觉驱动的 UI 自动化框架，支持 Web/Android/iOS |
| [Infographic](https://github.com/antvis/Infographic) | 蚂蚁 AntV 声明式信息图可视化引擎 |
| [TanStack AI](https://github.com/TanStack/ai) | 跨提供商的 AI SDK，无供应商锁定 |

### 6️⃣ Fun Side Project of the Year（年度趣味项目）

| 提名项目 | 说明 |
|----------|------|
| [RSC Explorer](https://github.com/gaearon/rscexplorer) | Dan Abramov 出品，逐步检查 RSC 数据流 |
| [Popcorn](https://github.com/software-mansion/popcorn) | 在浏览器中用 WASM 运行 Elixir 代码 |
| [TailwindSQL](https://github.com/mmarinovic/tailwindsql) | 用 Tailwind 风格的 className 写 SQL 查询 😂 |
| [Pretext](https://github.com/chenglou/pretext) | 多语言文本测量与排版库 |

### 7️⃣ React Highlights（React 亮点）🆕 新奖项

| 提名项目 | 说明 |
|----------|------|
| [React Bits](https://github.com/DavidHDev/react-bits) | 创意 UI 组件库 |
| [React Native AI](https://github.com/callstackincubator/ai) | React Native 端侧 AI 原语 |
| [React Doctor](https://github.com/millionco/react-doctor) | 检测 AI 写的坏 React 代码 |
| [React Grab](https://github.com/aidenybai/react-grab) | 复制任意 UI 元素给 AI Agent |

### 8️⃣ The Most Impactful Contribution to the Community（社区贡献）🆕 新奖项

- Seattle JS
- React Prague
- ZurichJS
- ReactJS Bangalore
- Toronto JS
- JS Monthly London

---

## 📊 年度趋势对比

| 趋势 | 2024→2025 | 2025→2026 |
|------|-----------|-----------|
| 打包工具 | Rspack 获奖后 Rolldown 接力 | 不再是主角，转向测试框架（Rstest） |
| AI | LangChain.js → Micro-agent | Midscene、TanStack AI、Open Claw/Code，AI 全面渗透 |
| TanStack 系 | Form 获奖 | DB、Start、AI 三线提名，成为最大赢家候选 |
| 新方向 | — | Rust 引擎从打包扩展到测试（Rstest）和 Git（Sparo） |
| 新奖项 | — | Innovation Peak、React Highlights、社区贡献，分类更细 |
| 中国项目 | — | es-toolkit（Toss/韩国）、Infographic（蚂蚁/AntV）亮相 |

---

## 🎯 对 Ainovr（墨韵仿写）的适用分析

### 直接能用上的

- **es-toolkit** — lodash 替代品，体积小、性能高，原生 TypeScript。Zustand 管理复杂状态中 debounce、throttle、chunk 等工具函数会频繁用到
- **Biome** — ESLint + Prettier 的 Rust 替代品，一个工具搞定 lint + format，速度快 10-100 倍
- **Rstest** — Jest 的 drop-in 替代，30+ 个 API Route Handler 和 9 个素材提取模块都适合写单元测试
- **React Doctor** — 检测 AI 写的坏 React 代码，检查渲染性能和反模式

### 可以考虑的

- **TanStack AI** — 跨提供商 AI SDK，但项目已自研 SSE 流式客户端，暂不需要
- **React Bits** — 创意 UI 组件库，跟 shadcn/ui 互补，可提升写作用户体验
- **Blazediff** — 视觉回归测试，配合 Vitest/Jest 使用

---

Sources:
- [OS Awards 官网](https://osawards.com/javascript/)
- [OS Awards 2025 获奖者](https://osawards.com/javascript/2025)
- [OS Awards 2024 获奖者](https://osawards.com/javascript/2024)
- [GitNation/OSAwards.com GitHub 源码](https://github.com/GitNation/OSAwards.com)
</file>

<file path="docs/pipeline-redesign-discussion.md">
# 分析管线重构讨论记录

## 概述

本文档记录了 Ainovr 分析管线重构的完整讨论过程——从三份 AI 审查报告的交叉分析，到两轮 grill-me 深度追问，最终收敛为"双层切片 + 全书记忆图谱"的统一管线架构。

---

## 第一阶段：三份 AI 审查报告的交叉分析

### 报告来源

- **报告 1（英文）**：架构洞察最深，指出客户端编排是最大瓶颈
- **报告 2（中文）**：测试用例和代码示例最扎实，直接可落地的样板代码
- **报告 3（中文）**：与前两者高度重叠

### 三者共识（7 项必做）

1. 引入 Zod 做运行时校验，替代 regex 解析 LLM 输出
2. 提取魔法数字为常量文件
3. 修复空 catch 块，至少加日志
4. 补充单元测试，优先覆盖 `text-cleaner`、`safe-path`、`program-indexer`
5. 拆分大文件（`LayerGenerationView`、`SourceNovelDetailView`、pipeline 文件）
6. Repository 模式抽象文件系统访问
7. API route 统一错误处理，结构化错误响应

### 取舍原则

- **以报告 1 定方向，报告 2 填细节**
- 报告 1 的"客户端编排迁移到后端"暂缓——本地部署场景下客户端编排可接受
- 报告 3 的 monorepo/OpenTelemetry 暂不采纳——当前单仓规模不需要

### 用户特别约束

- **不设硬编码字数阈值**（原方案建议 200 万字分流）
- **仅考虑本地部署**（不需要后端 job 队列、对象存储、多用户支持）

---

## 第二阶段：管线现状梳理

### 当前三条管线

| 管线 | 入口函数 | 接入状态 |
|------|---------|---------|
| 基础管线（8 步串行） | `runBasicPipeline` | ✅ UI 已接入 |
| 道/气管线（4 阶段） | `runDaoPipeline` | ⚠️ Store 有 action，UI 无按钮 |
| 事件图谱管线 | `runWindowedEventExtraction` | ❌ 完整实现但无调用方 |

### 用户对现状的评论

> "事件管线我觉得也应该在切片那个部分，方便后面进行更好的分析。这样就是带有事件、历史的分析。"

> "道/气管线也是分析管线的一部分，并入分析管线。"

> "这些（事件图谱后续的 DNA 构建）和上面一部分就没必要了吧，整个管线应并入基础管线。"

---

## 第三阶段：Grill-me 深度追问

### 第一轮：架构方向（12 项决策）

| # | 问题 | 用户回答 |
|---|------|---------|
| 1 | 三条管线的关系 | **合并为一条，一键到底** |
| 2 | 事件提取 vs 切片顺序 | **事件提取在前**，切片 AI 可参考事件信息 |
| 3 | 实体识别方式 | 事件提取 AI 自行识别 |
| 4 | 道/气如何嵌入 | 拆分嵌入各步骤之间 |
| 5 | 体验标注位置 | 放在文风之前，情绪信息喂给后续 |
| 6 | DNA 产出 | 合并为一步，单一输出 |
| 7 | 样本选取 | AI 选，但能看到事件数据 |
| 8 | 消融 vs 势能 | 并行 |
| 9 | 技法样本库 | 合并到 DNA 步骤 |
| 10 | DNA 构建方式 | 纯代码填充 + AI 定性归纳 |
| 11 | 事件提取窗口切分 | 按字符数直接截窗口，不依赖章节 |
| 12 | 用户配置 | 零配置，一键到底 |

### 用户关键追问

**Q：按叙事节拍切分——如何界定？为什么是 5000 字？**

A（代码核实）：Prompt 告诉 AI "以情节完整性为最高优先级，不在高潮中间切断，以章节边界为自然切割点"。5000 是默认值，允许 ±30% 浮动。2000~30000 字均可配置。

**Q：自动分批为什么不在切片步骤？**

A：切片步骤的输入是全文，输出是多个切片。分批出现在 Step 1-5，因为此时需要把所有切片拼接发 AI，拼接后可能超上下文。

**Q：候选实体如何确认界定？能包括所有吗？**

A：已有 `entity-classifier.ts` 用 AI 分类候选实体（character/location/organization/artifact/concept），只是未接入管线自动流程。最终决定由事件提取 AI 自行识别。

### 用户推动架构转向

> "切片是为了解决模型一次性无法输入长文的问题。你的问题（200 万字时 AI 读不下全文，切片怎么办）如何理解？"

→ 用户指出我搞反了因果关系。切片本身就是解决长文的方案。真正的问题是：第一遍切片如何在不读全文的情况下做？

> "第一次切片可以是小切片，第二次是大段分析的大切片，带着第一次切片分析后的全书记忆图谱，分析大段原文切片，进入后续分析流程。"

→ 用户提出**双层切片架构**

---

## 第四阶段：双层切片架构细化

### 用户的核心设计

```
第一遍（小切片 ~几章/几千字）：建记忆
  逐切片 AI 提取事件 → 合并为全书记忆图谱

第二遍（大切片 ~按模型上下文）：深度分析
  每个大切片 + 全书记忆图谱 → AI 深度分析 → 汇总 → DNA
```

### 第二轮 Grill-me 追问

**Q：大事件跨窗口、小事件挤一起——怎么让 AI 正确处理事件粒度？**

> "统一粒度，靠因果关系链连接。"

→ 不区分"大事件"和"小事件"，每个事件按统一标准提取。跨切片的大事件通过因果链（causes/effects）连接。

**Q：切片 AI 看不到跨切片因果——causes/effects 怎么填？**

> "切片内因果 + 链接时跨切片。"

→ 小切片 AI 只标注切片内能确认的因果。跨切片因果交给 Step 3 链接 AI 统一建立。

**Q：每个大切片分析产出什么？**

> "每个大切片都分析所有维度，然后再总结。会不会有可能文章作品前后文风不一致的问题。事件很多时候都没有结局，有的是坑、伏笔，后面作者都忘记了。给我你的建议。"

→ 采纳建议：
1. **文风不一致**：不强制统一，如实记录演化轨迹。每个大切片产出局部观察，汇总阶段做演化分析。
2. **未完成事件/伏笔**：显式标注 open/dangling 状态，不假设一定有结局。

**Q：分析目的是什么？**

> "不是分析师视角，而是逆向工程分析。分析的目的和结果要指导实践，也就是文章的生成。不需要一些套话。如果分析不出来，就不写。"

→ 确立**机制型分析**原则：
- ❌ "文风冷峻克制"（描述型套话）
- ✅ "前 80% 段落句长 5-12 字，对话不加心理描写，高潮段落句长缩至 3-5 字"（可复现的操作）

**Q：667 个独立 AI 调用怎么保证一致性？**

> "统一 schema 约束。"

→ 统一 JSON schema 约束输出格式。事件类型用枚举，人物名从原文精确引用。

**Q：消融测试还需要吗？**

> "也不是纯机制。有的文本可能是作者水字数，不要过度分析。作者和文本也会有错误。消融的意思是在事件上分析，有没有推进剧情、或者舒缓节奏、营造氛围的功效。这个是不是水，是一个很主观的想法。"

→ 消融测试改为**事件功能标注**（推进剧情/舒缓节奏/营造氛围/角色塑造/伏笔/过渡），不做"水字数"硬性判定——而是标注功能后由用户判断。

**Q：汇总 AI 怎么处理矛盾和幻觉？**

未解决——用户在最后提出这个担忧后暂停讨论，要求先整理文档。

---

## 最终架构：统一分析管线（7 步）

```
                     rawText
                        │
        ┌───────────────┴───────────────┐
        │   第一遍：构建全书记忆图谱      │
        │                               │
        │  Step 1: 小切片（程序化）       │
        │  Step 2: 逐片事件提取（AI并行） │
        │  Step 3: 事件链接（一次AI）     │
        │       → 全局 EventGraph        │
        └───────────────┬───────────────┘
                        │
        ┌───────────────┴───────────────┐
        │   第二遍：深度分析              │
        │                               │
        │  Step 4: 大切片（程序化）       │
        │  Step 5: 逐片深度分析（AI并行） │
        │  Step 6: 汇总+事件功能标注      │
        │       → 全局报告               │
        └───────────────┬───────────────┘
                        │
                  Step 7: DNA 压缩
                (代码填充 + AI 定性)
```

### Step 1：小切片（程序化，无 AI）
- 输入：rawText
- 按灵活尺寸切分（5-10 章或几千字/片）
- 纯代码分割

### Step 2：逐切片事件提取（AI，并行）
- 每个小切片 AI 提取结构化事件和实体
- 统一 JSON schema 约束输出格式
- 实体由 AI 自行识别
- 只标注切片内因果，不猜测跨切片关系
- 失败重试 2 次，仍失败则跳过

### Step 3：事件链接（AI，一次调用）
- 输入：所有 per-slice 事件片段
- AI 构建全局事件图谱：因果链、伏笔配对、事件分组、人物归一化
- 只输出**连接关系**（增量），不重新输出事件本体
- 代码机械合并 Step 2 事件 + Step 3 连接 = 全局 EventGraph

### Step 4：大切片（程序化，无 AI）
- 按模型 maxContextTokens 自适应窗口
- 纯代码分割

### Step 5：逐大切片深度分析（AI，并行）
- 输入：大切片原文 + 全书记忆图谱（压缩摘要格式）
- 机制型逆向工程分析，输出结构化 JSON
- 所有维度一步输出：文风机制、叙事机制、角色机制、情绪机制、体验标注（含多 persona 视角）、约束
- **核心原则**：指导生成实践，不写套话，分析不出来就留 null
- 每个机制结论指向"怎么做"而非"是什么"

### Step 6：汇总 + 事件功能标注（AI，一次调用）
- 输入：所有大切片分析 JSON + 全局 EventGraph + 原文抽样
- 合成全局报告（文风演化轨迹、叙事结构、角色弧线、情绪曲线、约束清单）
- 标注每个事件的叙事功能（推进/舒缓/氛围/角色/伏笔/过渡）
- 标记 open/dangling 事件和未回收伏笔
- **待解决**：如何处理多个大切片分析之间的矛盾和幻觉

### Step 7：DNA 压缩（代码填充 + AI 定性）
- 纯代码：从 EventGraph + 汇总数据计算道/气/结构/引擎的量化部分
- AI 定性：提炼风格引擎、技法库、风险标注
- 一份 DNA 包含全部，机制型输出
- 覆盖旧 `novelDna` / `novelDnaV2` / `techniqueSamples`

---

## 全部 20 项设计决策

| # | 决策 | 结论 |
|---|------|------|
| 1 | 管线架构 | 一条统一管线，双层切片 |
| 2 | 小切片尺寸 | 灵活（5-10 章或几千字），不固定 |
| 3 | 大切片尺寸 | 按模型 maxContextTokens 自适应 |
| 4 | 事件提取方式 | 逐小切片 AI 提取，并行 |
| 5 | 实体识别 | 事件提取 AI 自行识别 |
| 6 | 事件链接 | 一次 AI 全量链接，只输出连接关系 |
| 7 | 事件功能标注 | 汇总阶段统一标注 |
| 8 | 体验标注 | 合并到大切片深度分析 |
| 9 | 分析风格 | 机制型逆向工程，不写套话，分析不出就留 null |
| 10 | 文风不一致 | 如实记录演化轨迹，不强制统一 |
| 11 | 未完成事件/伏笔 | 显式标注 open/dangling |
| 12 | 水字数判断 | 主观，由事件功能标签体现，不做硬性判定 |
| 13 | 样本选取 | 去掉 |
| 14 | 消融测试 | 改为事件功能标注 |
| 15 | 势能追踪 | 吸收到汇总 |
| 16 | DNA 产物 | 一份全包（基因+技法+风险），机制型 |
| 17 | DNA 构建方式 | 纯代码填充结构 + AI 定性归纳 |
| 18 | 并行容错 | 重试 2 次后跳过 |
| 19 | 用户配置 | 零配置，一键到底 |
| 20 | 部署 | 仅本地 |

---

## 待解决事项

1. **汇总 AI 的矛盾和幻觉处理**：Step 6 汇总 AI 收到多份大切片分析，可能包含互相矛盾的判断和 AI 幻觉。用户提出需要验证机制，讨论在"汇总 = 验证，不是合并"方案处中断。

2. **大切片分析 JSON schema 的具体字段**：方向已确定（机制型、全维度），具体字段定义待定。

3. **事件功能标注的具体分类**：推进剧情/舒缓节奏/营造氛围/角色塑造/伏笔/过渡——待确认是否完整。

4. **pipeline-types.ts 的完整类型定义**：小切片事件、链接输出、大切片分析 JSON、汇总报告的类型定义。

---

## 代码变更规划

### 新建
- `src/lib/source-processing/unified-pipeline.ts` — 统一管线编排器
- `src/lib/source-processing/pipeline-types.ts` — 管线类型定义

### 修改
- `src/lib/store/source-processing.ts` — 重构为统一管线状态
- `src/components/source/SourceNovelDetailView.tsx` — 一键分析 + 统一进度
- `src/lib/source-processing/windowed-event-extractor.ts` — 改为逐切片事件提取
- `src/lib/ai/prompts/event-extraction.ts` — prompt 适配

### 废弃/合并
- `dna-compressor.ts` → 合并到 Step 7
- `value-sampler.ts` → 废弃
- `program-indexer.ts` → 废弃
- `entity-classifier.ts` → 废弃
- `ablation-tester.ts` → 改为事件功能标注
- `tension-tracker.ts` → 吸收到汇总
- `experience-annotator.ts` → 合并到 Step 5
- `dao-pipeline.ts` → 拆分嵌入统一管线
- `basic-pipeline.ts` → 被统一管线替代

### 保留
- `smart-slicer.ts` — 可能复用输出格式
- 5 层生成管线 — 不变（NovelDNA 接口不变）
- API routes — 新增 1-2 个，旧的逐步废弃

---

## 讨论时间线

- **2026-06-11**：三份 AI 审查报告交叉分析
- **2026-06-11**：用户提出不设字数阈值、仅本地部署
- **2026-06-11**：第一轮 grill-me（12 项架构决策）
- **2026-06-11**：用户推动双层切片架构转向
- **2026-06-11**：第二轮 grill-me（事件粒度、因果链接、机制型分析、矛盾处理）
- **2026-06-11**：方案文件定稿，本文档整理
</file>

<file path="docs/pipeline-redesign-plan.md">
# 统一分析管线重构方案

## 背景

当前有三条独立管线，只有基础管线接入 UI。用户提出将三者合并为一条统一管线，采用"双层切片 + 全书记忆图谱"架构。

## 架构概览

```
                     rawText
                        │
        ┌───────────────┴───────────────┐
        │   第一遍：构建全书记忆图谱      │
        │                               │
        │  小切片 → 逐片事件提取(并行)    │
        │        → 事件链接(一次AI)       │
        │        → 全局 EventGraph        │
        └───────────────┬───────────────┘
                        │
        ┌───────────────┴───────────────┐
        │   第二遍：深度分析              │
        │                               │
        │  大切片 → 逐片深度分析(并行)     │
        │        → 汇总+事件功能标注(一次AI)│
        │        → 全局报告               │
        └───────────────┬───────────────┘
                        │
                     DNA 压缩
                (代码填充 + AI 定性)
```

## 7 步详细流程

### Step 1：小切片（程序化，无 AI）

- 输入：rawText
- 按灵活尺寸切分（5-10 章或几千字/片，不固定数值）
- 纯代码分割，不调 AI
- 输出：小切片列表

### Step 2：逐切片事件提取（AI，并行）

- 输入：每个小切片的原文
- AI 提取该切片内的结构化事件和实体
- 实体由 AI 自行识别，不设独立实体识别步骤
- 所有切片并行调用，失败重试 2 次，仍失败则跳过（该切片事件留空）
- 输出：per-slice 事件片段列表（JSON）

### Step 3：事件链接 → 全书记忆图谱（AI，一次调用）

- 输入：所有 per-slice 事件片段
- AI 构建全局事件图谱：因果链、伏笔配对、事件分组
- 一次 AI 调用完成（小切片事件摘要是紧凑 JSON，总量可控）
- 输出：全局 EventGraph（全书记忆图谱）

### Step 4：大切片（程序化，无 AI）

- 输入：rawText
- 按模型上下文窗口切分（基于 settings 里的 maxContextTokens）
- 纯代码分割
- 输出：大切片列表

### Step 5：逐大切片深度分析（AI，并行）

- 输入：每个大切片原文 + 全局 EventGraph
- AI 做机制型逆向工程分析，输出结构化 JSON
- **核心原则**：分析目的是指导生成实践，不是文学评论。分析不出来就留 null，不写套话
- 体验标注（多 persona 读者视角）合并到此步骤
- 输出维度：
  - 文风机制（句式规律、对话密度、描写策略——可复现的操作）
  - 叙事机制（章节结构、节奏控制、视角切换——怎么做）
  - 角色机制（弧线阶段、行为模式、关系变化——可模仿）
  - 情绪机制（情绪走向、积累-释放模式）
  - 读者体验（沉浸度、节奏感——多 persona 视角）
  - 叙事约束（世界观规则、角色限制、情节约束）
- 输出格式：结构化 JSON

### Step 6：汇总 + 事件功能标注（AI，一次调用）

- 输入：所有大切片分析 JSON + 全局 EventGraph
- AI 一次性完成：
  1. 合成全局报告（文风演化轨迹、叙事结构、角色弧线、情绪曲线、约束清单）
  2. 标注每个事件的叙事功能（推进剧情/舒缓节奏/营造氛围/角色塑造/伏笔/过渡）
  3. 识别未完成事件（open）和未回收伏笔（dangling）
  4. 检测文风不一致/演化（不强制统一，如实记录变化）
- 输出：汇总报告 + 事件功能标签

### Step 7：DNA 压缩（代码填充 + AI 定性）

- 纯代码填充：从 EventGraph + 汇总数据计算道/气/结构/引擎的量化部分
- AI 定性：提炼风格引擎、技法库、风险标注
- 一份 DNA 包含全部（基因 + 技法 + 风险）
- 机制型输出，所有结论指向"生成时怎么做"
- 输出：NovelDNA（唯一输出，覆盖旧 novelDna / novelDnaV2 / techniqueSamples）

## 设计决策汇总

| # | 决策 | 结论 |
|---|------|------|
| 1 | 管线架构 | 一条统一管线，双层切片 |
| 2 | 小切片尺寸 | 灵活（5-10 章或几千字），不固定 |
| 3 | 大切片尺寸 | 按模型 maxContextTokens 自适应 |
| 4 | 事件提取方式 | 逐小切片 AI 提取，并行 |
| 5 | 实体识别 | 事件提取 AI 自行识别 |
| 6 | 事件链接 | 一次 AI 全量链接 |
| 7 | 事件功能标注 | 汇总阶段统一标注 |
| 8 | 体验标注 | 合并到大切片深度分析 |
| 9 | 分析风格 | 机制型逆向工程，不写套话，分析不出就留 null |
| 10 | 文风不一致 | 如实记录演化轨迹，不强制统一 |
| 11 | 未完成事件/伏笔 | 显式标注 open/dangling |
| 12 | 水字数判断 | 主观，由事件功能标签体现，不做硬性判定 |
| 13 | 样本选取 | 去掉 |
| 14 | 消融测试 | 改为事件功能标注（推进/舒缓/氛围/角色/伏笔/过渡） |
| 15 | 势能追踪 | 吸收到汇总（EventGraph tensionChange + 大切片 emotionalArc） |
| 16 | DNA 产物 | 一份全包（基因+技法+风险），机制型 |
| 17 | DNA 构建方式 | 纯代码填充结构 + AI 定性归纳 |
| 18 | 并行容错 | 重试 2 次后跳过 |
| 19 | 用户配置 | 零配置，一键到底 |
| 20 | 部署 | 仅本地 |

## 涉及的代码变更

### 新建文件
- `src/lib/source-processing/unified-pipeline.ts` — 统一管线编排器（7 步）
- `src/lib/source-processing/pipeline-types.ts` — 管线类型定义（小切片/大切片 JSON schema）

### 修改文件
- `src/lib/store/source-processing.ts` — 重构为统一管线状态管理
- `src/components/source/SourceNovelDetailView.tsx` — 简化 UI（一键 + 统一进度）
- `src/lib/source-processing/windowed-event-extractor.ts` — 改为逐切片事件提取（去掉窗口机制，去掉 TextChunk 依赖）
- `src/lib/ai/prompts/event-extraction.ts` — prompt 适配逐切片提取

### 废弃/合并
- `dna-compressor.ts` → 合并到 DNA 步骤（Step 7）
- `value-sampler.ts` → 废弃（去掉样本选取）
- `program-indexer.ts` → 废弃（事件提取 AI 自行识别实体）
- `entity-classifier.ts` → 废弃
- `ablation-tester.ts` → 改为事件功能标注（Step 6 内）
- `tension-tracker.ts` → 吸收到汇总
- `experience-annotator.ts` → 合并到大切片分析（Step 5）
- `dao-pipeline.ts` → 各阶段拆分嵌入统一管线
- `basic-pipeline.ts` → 被统一管线替代

### 保留不动
- `smart-slicer.ts` → 大切片步骤可能复用其输出格式
- `style-extractor.ts` 等 → 逻辑吸收到大切片分析 prompt，文件可后续清理
- 5 层生成管线 → 不变（读取 NovelDNA 接口不变）
- 所有 API routes → 可能需要新增 1-2 个，旧的逐步废弃

## 验证方案

1. **10 万字小说**：小切片 ~33 个 → 并行事件提取 33 次 → 1 次链接 → 1 个大切片 → 1 次深度分析 → 1 次汇总 → DNA
2. **200 万字小说**：小切片 ~667 个 → 并行事件提取 667 次 → 1 次链接 → ~3 个大切片 → 3 次并行深度分析 → 1 次汇总 → DNA
3. **无章节短篇**：小切片按字数切，流程不变
4. **容错验证**：模拟 API 失败，验证重试+跳过不阻塞管线
5. **生成管线兼容**：验证新 DNA 格式能正常驱动 5 层生成
</file>

<file path="eslint.config.mjs">
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
</file>

<file path="postcss.config.mjs">
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
</file>

<file path="public/file.svg">
<svg fill="none" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M14.5 13.5V5.41a1 1 0 0 0-.3-.7L9.8.29A1 1 0 0 0 9.08 0H1.5v13.5A2.5 2.5 0 0 0 4 16h8a2.5 2.5 0 0 0 2.5-2.5m-1.5 0v-7H8v-5H3v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1M9.5 5V2.12L12.38 5zM5.13 5h-.62v1.25h2.12V5zm-.62 3h7.12v1.25H4.5zm.62 3h-.62v1.25h7.12V11z" clip-rule="evenodd" fill="#666" fill-rule="evenodd"/></svg>
</file>

<file path="public/globe.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><g clip-path="url(#a)"><path fill-rule="evenodd" clip-rule="evenodd" d="M10.27 14.1a6.5 6.5 0 0 0 3.67-3.45q-1.24.21-2.7.34-.31 1.83-.97 3.1M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16m.48-1.52a7 7 0 0 1-.96 0H7.5a4 4 0 0 1-.84-1.32q-.38-.89-.63-2.08a40 40 0 0 0 3.92 0q-.25 1.2-.63 2.08a4 4 0 0 1-.84 1.31zm2.94-4.76q1.66-.15 2.95-.43a7 7 0 0 0 0-2.58q-1.3-.27-2.95-.43a18 18 0 0 1 0 3.44m-1.27-3.54a17 17 0 0 1 0 3.64 39 39 0 0 1-4.3 0 17 17 0 0 1 0-3.64 39 39 0 0 1 4.3 0m1.1-1.17q1.45.13 2.69.34a6.5 6.5 0 0 0-3.67-3.44q.65 1.26.98 3.1M8.48 1.5l.01.02q.41.37.84 1.31.38.89.63 2.08a40 40 0 0 0-3.92 0q.25-1.2.63-2.08a4 4 0 0 1 .85-1.32 7 7 0 0 1 .96 0m-2.75.4a6.5 6.5 0 0 0-3.67 3.44 29 29 0 0 1 2.7-.34q.31-1.83.97-3.1M4.58 6.28q-1.66.16-2.95.43a7 7 0 0 0 0 2.58q1.3.27 2.95.43a18 18 0 0 1 0-3.44m.17 4.71q-1.45-.12-2.69-.34a6.5 6.5 0 0 0 3.67 3.44q-.65-1.27-.98-3.1" fill="#666"/></g><defs><clipPath id="a"><path fill="#fff" d="M0 0h16v16H0z"/></clipPath></defs></svg>
</file>

<file path="public/next.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 394 80"><path fill="#000" d="M262 0h68.5v12.7h-27.2v66.6h-13.6V12.7H262V0ZM149 0v12.7H94v20.4h44.3v12.6H94v21h55v12.6H80.5V0h68.7zm34.3 0h-17.8l63.8 79.4h17.9l-32-39.7 32-39.6h-17.9l-23 28.6-23-28.6zm18.3 56.7-9-11-27.1 33.7h17.8l18.3-22.7z"/><path fill="#000" d="M81 79.3 17 0H0v79.3h13.6V17l50.2 62.3H81Zm252.6-.4c-1 0-1.8-.4-2.5-1s-1.1-1.6-1.1-2.6.3-1.8 1-2.5 1.6-1 2.6-1 1.8.3 2.5 1a3.4 3.4 0 0 1 .6 4.3 3.7 3.7 0 0 1-3 1.8zm23.2-33.5h6v23.3c0 2.1-.4 4-1.3 5.5a9.1 9.1 0 0 1-3.8 3.5c-1.6.8-3.5 1.3-5.7 1.3-2 0-3.7-.4-5.3-1s-2.8-1.8-3.7-3.2c-.9-1.3-1.4-3-1.4-5h6c.1.8.3 1.6.7 2.2s1 1.2 1.6 1.5c.7.4 1.5.5 2.4.5 1 0 1.8-.2 2.4-.6a4 4 0 0 0 1.6-1.8c.3-.8.5-1.8.5-3V45.5zm30.9 9.1a4.4 4.4 0 0 0-2-3.3 7.5 7.5 0 0 0-4.3-1.1c-1.3 0-2.4.2-3.3.5-.9.4-1.6 1-2 1.6a3.5 3.5 0 0 0-.3 4c.3.5.7.9 1.3 1.2l1.8 1 2 .5 3.2.8c1.3.3 2.5.7 3.7 1.2a13 13 0 0 1 3.2 1.8 8.1 8.1 0 0 1 3 6.5c0 2-.5 3.7-1.5 5.1a10 10 0 0 1-4.4 3.5c-1.8.8-4.1 1.2-6.8 1.2-2.6 0-4.9-.4-6.8-1.2-2-.8-3.4-2-4.5-3.5a10 10 0 0 1-1.7-5.6h6a5 5 0 0 0 3.5 4.6c1 .4 2.2.6 3.4.6 1.3 0 2.5-.2 3.5-.6 1-.4 1.8-1 2.4-1.7a4 4 0 0 0 .8-2.4c0-.9-.2-1.6-.7-2.2a11 11 0 0 0-2.1-1.4l-3.2-1-3.8-1c-2.8-.7-5-1.7-6.6-3.2a7.2 7.2 0 0 1-2.4-5.7 8 8 0 0 1 1.7-5 10 10 0 0 1 4.3-3.5c2-.8 4-1.2 6.4-1.2 2.3 0 4.4.4 6.2 1.2 1.8.8 3.2 2 4.3 3.4 1 1.4 1.5 3 1.5 5h-5.8z"/></svg>
</file>

<file path="public/vercel.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1155 1000"><path d="m577.3 0 577.4 1000H0z" fill="#fff"/></svg>
</file>

<file path="public/window.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill-rule="evenodd" clip-rule="evenodd" d="M1.5 2.5h13v10a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1zM0 1h16v11.5a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 0 12.5zm3.75 4.5a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5M7 4.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0m1.75.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5" fill="#666"/></svg>
</file>

<file path="src/app/api/library/delete/route.ts">
// ============================================
// POST /api/library/delete — 删除源小说
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

export async function POST(req: NextRequest) {
  try {
    const { id } = await req.json();
    if (!id) {
      return NextResponse.json({ error: '缺少 id' }, { status: 400 });
    }

    const novelDir = safeJoin(LIBRARY_DIR, id);
    await fs.rm(novelDir, { recursive: true, force: true });

    return NextResponse.json({ success: true });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '删除失败';
    console.error('Delete library error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/library/list/route.ts">
// ============================================
// GET /api/library/list — 列出素材库中所有源小说
// ============================================

import { NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

export async function GET() {
  try {
    await fs.mkdir(LIBRARY_DIR, { recursive: true });
    const entries = await fs.readdir(LIBRARY_DIR, { withFileTypes: true });

    const novels = [];
    for (const entry of entries) {
      if (!entry.isDirectory()) continue;
      const metaPath = path.join(LIBRARY_DIR, entry.name, 'meta.json');
      try {
        const raw = await fs.readFile(metaPath, 'utf-8');
        novels.push(JSON.parse(raw));
      } catch {
        // 跳过没有 meta.json 的目录
      }
    }

    return NextResponse.json({ novels });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '读取素材库失败';
    console.error('List library error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/library/rename/route.ts">
// ============================================
// POST /api/library/rename — 重命名源小说
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

export async function POST(req: NextRequest) {
  try {
    const { id, title } = await req.json();
    if (!id || !title || typeof title !== 'string') {
      return NextResponse.json({ error: '缺少 id 或 title' }, { status: 400 });
    }

    const novelDir = safeJoin(LIBRARY_DIR, id);
    const metaPath = path.join(novelDir, 'meta.json');

    // 读取现有 meta
    const metaRaw = await fs.readFile(metaPath, 'utf-8');
    const meta = JSON.parse(metaRaw);

    // 更新标题
    meta.title = title.trim();
    await fs.writeFile(metaPath, JSON.stringify(meta, null, 2), 'utf-8');

    return NextResponse.json({ success: true, title: meta.title });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '重命名失败';
    console.error('Rename library error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/project/create/route.ts">
// ============================================
// POST /api/project/create — 创建写作项目
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';
import type { WritingProject } from '@/types';

const PROJECTS_DIR = path.join(process.cwd(), 'data', 'projects');

export async function POST(req: NextRequest) {
  try {
    const { id, title, sourceNovelIds, sourceRoles } = await req.json();

    if (!id || !title) {
      return NextResponse.json({ error: '缺少 id 或 title' }, { status: 400 });
    }

    const projectDir = safeJoin(PROJECTS_DIR, id);
    await fs.mkdir(projectDir, { recursive: true });
    await fs.mkdir(path.join(projectDir, 'chapters'), { recursive: true });

    const now = new Date().toISOString();
    const project: WritingProject = {
      id,
      title,
      createdAt: now,
      updatedAt: now,
      sourceNovelIds: sourceNovelIds ?? [],
      sourceRoles: sourceRoles ?? [],
      outline: null,
      phases: null,
      volumes: null,
      chapterSets: null,
      chapterPlans: null,
      chapters: [],
      currentLayer: 0,
    };

    await fs.writeFile(
      path.join(projectDir, 'project.json'),
      JSON.stringify(project, null, 2),
      'utf-8',
    );

    return NextResponse.json({ success: true, project });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '创建项目失败';
    console.error('Create project error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/project/delete/route.ts">
// ============================================
// POST /api/project/delete — 删除项目
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import { safeJoin } from '@/lib/safe-path';
import path from 'path';

const PROJECTS_DIR = path.join(process.cwd(), 'data', 'projects');

export async function POST(req: NextRequest) {
  try {
    const { id } = await req.json();
    if (!id) {
      return NextResponse.json({ error: '缺少 id' }, { status: 400 });
    }

    const projectDir = safeJoin(PROJECTS_DIR, id);
    await fs.rm(projectDir, { recursive: true, force: true });

    return NextResponse.json({ success: true });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '删除项目失败';
    console.error('Delete project error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/project/list/route.ts">
// ============================================
// GET /api/project/list — 列出所有项目
// ============================================

import { NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';

const PROJECTS_DIR = path.join(process.cwd(), 'data', 'projects');

export async function GET() {
  try {
    await fs.mkdir(PROJECTS_DIR, { recursive: true });
    const entries = await fs.readdir(PROJECTS_DIR, { withFileTypes: true });

    const projects = [];
    for (const entry of entries) {
      if (!entry.isDirectory()) continue;
      try {
        const raw = await fs.readFile(
          path.join(PROJECTS_DIR, entry.name, 'project.json'),
          'utf-8',
        );
        const project = JSON.parse(raw);
        // 只返回概要，不返回大体积字段
        projects.push({
          id: project.id,
          title: project.title,
          createdAt: project.createdAt,
          updatedAt: project.updatedAt,
          currentLayer: project.currentLayer,
          sourceNovelIds: project.sourceNovelIds,
          sourceRoles: project.sourceRoles,
          chapterCount: project.chapters?.length ?? 0,
        });
      } catch {
        // 跳过无效目录
      }
    }

    return NextResponse.json({ projects });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '读取项目列表失败';
    console.error('List projects error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/project/load/route.ts">
// ============================================
// GET /api/project/load?id=xxx — 读取项目状态
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const PROJECTS_DIR = path.join(process.cwd(), 'data', 'projects');

export async function GET(req: NextRequest) {
  try {
    const id = req.nextUrl.searchParams.get('id');
    if (!id) {
      return NextResponse.json({ error: '缺少 id 参数' }, { status: 400 });
    }

    const projectPath = path.join(safeJoin(PROJECTS_DIR, id), 'project.json');
    const raw = await fs.readFile(projectPath, 'utf-8');
    const project = JSON.parse(raw);

    return NextResponse.json({ project });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '加载项目失败';
    console.error('Load project error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/project/save/route.ts">
// ============================================
// POST /api/project/save — 保存项目全量状态
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const PROJECTS_DIR = path.join(process.cwd(), 'data', 'projects');

export async function POST(req: NextRequest) {
  try {
    const project = await req.json();

    if (!project?.id) {
      return NextResponse.json({ error: '缺少项目 id' }, { status: 400 });
    }

    const projectDir = safeJoin(PROJECTS_DIR, project.id);
    await fs.mkdir(projectDir, { recursive: true });

    await fs.writeFile(
      path.join(projectDir, 'project.json'),
      JSON.stringify(project, null, 2),
      'utf-8',
    );

    return NextResponse.json({ success: true, id: project.id });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '保存项目失败';
    console.error('Save project error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/prompts/save/route.ts">
// ============================================
// POST /api/prompts/save — 保存自定义提示词
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';

const DATA_DIR = path.join(process.cwd(), 'data');
const CUSTOM_PROMPTS_PATH = path.join(DATA_DIR, 'prompts.json');

export async function POST(req: NextRequest) {
  try {
    const { key, content } = await req.json();

    if (!key || typeof content !== 'string') {
      return NextResponse.json({ error: '缺少 key 或 content' }, { status: 400 });
    }

    // 确保 data 目录存在
    await fs.mkdir(DATA_DIR, { recursive: true });

    // 读取现有自定义提示词
    let custom: Record<string, string> = {};
    try {
      const raw = await fs.readFile(CUSTOM_PROMPTS_PATH, 'utf-8');
      custom = JSON.parse(raw);
    } catch {
      // 文件不存在
    }

    if (content.trim() === '') {
      // 空内容 = 删除自定义，恢复默认
      delete custom[key];
    } else {
      custom[key] = content;
    }

    await fs.writeFile(CUSTOM_PROMPTS_PATH, JSON.stringify(custom, null, 2), 'utf-8');

    return NextResponse.json({ success: true, key });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '保存提示词失败';
    console.error('Save prompt error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/source/process/deep-analysis/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '深度分析', maxTokens: 16384 });
</file>

<file path="src/app/api/source/process/event-alignment/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '事件对齐', maxTokens: 16384 });
</file>

<file path="src/app/api/source/process/summary/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '汇总报告', maxTokens: 16384 });
</file>

<file path="src/app/error.tsx">
// ============================================
// 全局错误边界 — 防止白屏
// ============================================

'use client';

import { useEffect } from 'react';
import { Button } from '@/components/ui/button';

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    console.error('Ainovr 渲染错误:', error);
  }, [error]);

  return (
    <div className="flex items-center justify-center min-h-screen p-8">
      <div className="max-w-md text-center space-y-4">
        <h2 className="text-xl font-bold text-foreground">出了点问题</h2>
        <p className="text-sm text-muted-foreground">
          {error.message || '发生了未知错误'}
        </p>
        <Button onClick={reset}>
          重试
        </Button>
      </div>
    </div>
  );
}
</file>

<file path="src/components/chapter/FeedbackInput.tsx">
// ============================================
// 人工反馈输入
// ============================================

'use client';

import { useState } from 'react';

interface FeedbackInputProps {
  onSubmit: (feedback: string) => void;
  disabled?: boolean;
}

export function FeedbackInput({ onSubmit, disabled }: FeedbackInputProps) {
  const [feedback, setFeedback] = useState('');

  const handleSubmit = () => {
    if (!feedback.trim()) return;
    onSubmit(feedback.trim());
    setFeedback('');
  };

  return (
    <div className="rounded-lg border border-border p-4">
      <h3 className="text-sm font-medium text-foreground mb-2">人工反馈</h3>
      <textarea
        value={feedback}
        onChange={(e) => setFeedback(e.target.value)}
        placeholder="对当前章节的修改意见..."
        className="w-full h-20 px-3 py-2 rounded-md border border-border bg-background text-sm resize-none mb-2"
      />
      <button
        onClick={handleSubmit}
        disabled={!feedback.trim() || disabled}
        className="px-3 py-1.5 rounded text-xs bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
      >
        提交反馈并修正
      </button>
    </div>
  );
}
</file>

<file path="src/components/project/SourceRoleSelector.tsx">
// ============================================
// 源小说角色选择器
// ============================================

'use client';

import type { SourceRole } from '@/types';

interface SourceRoleSelectorProps {
  sourceTitle: string;
  role: SourceRole['role'];
  onChange: (role: SourceRole['role']) => void;
}

export function SourceRoleSelector({ sourceTitle, role, onChange }: SourceRoleSelectorProps) {
  const options: { value: SourceRole['role']; label: string }[] = [
    { value: 'style_and_plot', label: '文风 + 情节' },
    { value: 'style', label: '仅文风' },
    { value: 'plot', label: '仅情节' },
  ];

  return (
    <div className="flex items-center gap-2 p-2 rounded-lg border border-border">
      <span className="text-sm text-foreground flex-1 truncate">《{sourceTitle}》</span>
      <div className="flex gap-1">
        {options.map((opt) => (
          <button
            key={opt.value}
            onClick={() => onChange(opt.value)}
            className={`
              px-2 py-1 rounded text-xs transition-colors
              ${role === opt.value
                ? 'bg-primary text-primary-foreground'
                : 'bg-muted text-muted-foreground hover:bg-accent'
              }
            `}
          >
            {opt.label}
          </button>
        ))}
      </div>
    </div>
  );
}
</file>

<file path="src/components/source/MarkdownViewer.tsx">
// ============================================
// Markdown 渲染组件 — 用于文风档案/情节报告
// ============================================

'use client';

import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';

interface MarkdownViewerProps {
  content: string;
  className?: string;
}

export function MarkdownViewer({ content, className = '' }: MarkdownViewerProps) {
  return (
    <div className={`prose prose-sm dark:prose-invert max-w-none ${className}`}>
      <ReactMarkdown remarkPlugins={[remarkGfm]}>
        {content}
      </ReactMarkdown>
    </div>
  );
}
</file>

<file path="src/components/source/ProcessStepIndicator.tsx">
// ============================================
// 步骤进度指示器
// ============================================

'use client';

interface ProcessStepIndicatorProps {
  steps: { key: string; label: string }[];
  currentStep: number;       // 0-based, -1 = 未开始
  completedSteps: Set<number>;
  errorStep?: number;        // 出错的步骤
}

export function ProcessStepIndicator({ steps, currentStep, completedSteps, errorStep }: ProcessStepIndicatorProps) {
  return (
    <div className="flex items-center gap-1">
      {steps.map((step, i) => {
        const isCompleted = completedSteps.has(i);
        const isRunning = currentStep === i;
        const isError = errorStep === i;

        return (
          <div key={step.key} className="flex items-center gap-1">
            <div className="flex items-center gap-1.5">
              <div
                className={`
                  w-6 h-6 rounded-full flex items-center justify-center text-xs font-medium transition-colors
                  ${isCompleted ? 'bg-green-500 text-white' : ''}
                  ${isRunning ? 'bg-primary text-primary-foreground animate-pulse' : ''}
                  ${isError ? 'bg-red-500 text-white' : ''}
                  ${!isCompleted && !isRunning && !isError ? 'bg-muted text-muted-foreground' : ''}
                `}
              >
                {isCompleted ? '✓' : isError ? '✕' : i + 1}
              </div>
              <span className={`text-xs ${isRunning ? 'text-foreground font-medium' : 'text-muted-foreground'}`}>
                {step.label}
              </span>
            </div>
            {i < steps.length - 1 && (
              <div className={`w-6 h-px ${isCompleted ? 'bg-green-500' : 'bg-border'}`} />
            )}
          </div>
        );
      })}
    </div>
  );
}
</file>

<file path="src/components/ui/badge.tsx">
import { mergeProps } from "@base-ui/react/merge-props"
import { useRender } from "@base-ui/react/use-render"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

const badgeVariants = cva(
  "group/badge inline-flex h-5 w-fit shrink-0 items-center justify-center gap-1 overflow-hidden rounded-4xl border border-transparent px-2 py-0.5 text-xs font-medium whitespace-nowrap transition-all focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 aria-invalid:border-destructive aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 [&>svg]:pointer-events-none [&>svg]:size-3!",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground [a]:hover:bg-primary/80",
        secondary:
          "bg-secondary text-secondary-foreground [a]:hover:bg-secondary/80",
        destructive:
          "bg-destructive/10 text-destructive focus-visible:ring-destructive/20 dark:bg-destructive/20 dark:focus-visible:ring-destructive/40 [a]:hover:bg-destructive/20",
        outline:
          "border-border text-foreground [a]:hover:bg-muted [a]:hover:text-muted-foreground",
        ghost:
          "hover:bg-muted hover:text-muted-foreground dark:hover:bg-muted/50",
        link: "text-primary underline-offset-4 hover:underline",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)

function Badge({
  className,
  variant = "default",
  render,
  ...props
}: useRender.ComponentProps<"span"> & VariantProps<typeof badgeVariants>) {
  return useRender({
    defaultTagName: "span",
    props: mergeProps<"span">(
      {
        className: cn(badgeVariants({ variant }), className),
      },
      props
    ),
    render,
    state: {
      slot: "badge",
      variant,
    },
  })
}

export { Badge, badgeVariants }
</file>

<file path="src/components/ui/button.tsx">
import { Button as ButtonPrimitive } from "@base-ui/react/button"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

const buttonVariants = cva(
  "group/button inline-flex shrink-0 items-center justify-center rounded-lg border border-transparent bg-clip-padding text-sm font-medium whitespace-nowrap transition-all outline-none select-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 active:not-aria-[haspopup]:translate-y-px disabled:pointer-events-none disabled:opacity-50 aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:bg-primary/80",
        outline:
          "border-border bg-background hover:bg-muted hover:text-foreground aria-expanded:bg-muted aria-expanded:text-foreground dark:border-input dark:bg-input/30 dark:hover:bg-input/50",
        secondary:
          "bg-secondary text-secondary-foreground hover:bg-[color-mix(in_oklch,var(--secondary),var(--foreground)_5%)] aria-expanded:bg-secondary aria-expanded:text-secondary-foreground",
        ghost:
          "hover:bg-muted hover:text-foreground aria-expanded:bg-muted aria-expanded:text-foreground dark:hover:bg-muted/50",
        destructive:
          "bg-destructive/10 text-destructive hover:bg-destructive/20 focus-visible:border-destructive/40 focus-visible:ring-destructive/20 dark:bg-destructive/20 dark:hover:bg-destructive/30 dark:focus-visible:ring-destructive/40",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default:
          "h-8 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
        xs: "h-6 gap-1 rounded-[min(var(--radius-md),10px)] px-2 text-xs in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3",
        sm: "h-7 gap-1 rounded-[min(var(--radius-md),12px)] px-2.5 text-[0.8rem] in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3.5",
        lg: "h-9 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
        icon: "size-8",
        "icon-xs":
          "size-6 rounded-[min(var(--radius-md),10px)] in-data-[slot=button-group]:rounded-lg [&_svg:not([class*='size-'])]:size-3",
        "icon-sm":
          "size-7 rounded-[min(var(--radius-md),12px)] in-data-[slot=button-group]:rounded-lg",
        "icon-lg": "size-9",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)

function Button({
  className,
  variant = "default",
  size = "default",
  ...props
}: ButtonPrimitive.Props & VariantProps<typeof buttonVariants>) {
  return (
    <ButtonPrimitive
      data-slot="button"
      className={cn(buttonVariants({ variant, size, className }))}
      {...props}
    />
  )
}

export { Button, buttonVariants }
</file>

<file path="src/components/ui/card.tsx">
import * as React from "react"

import { cn } from "@/lib/utils"

function Card({
  className,
  size = "default",
  ...props
}: React.ComponentProps<"div"> & { size?: "default" | "sm" }) {
  return (
    <div
      data-slot="card"
      data-size={size}
      className={cn(
        "group/card flex flex-col gap-4 overflow-hidden rounded-xl bg-card py-4 text-sm text-card-foreground ring-1 ring-foreground/10 has-data-[slot=card-footer]:pb-0 has-[>img:first-child]:pt-0 data-[size=sm]:gap-3 data-[size=sm]:py-3 data-[size=sm]:has-data-[slot=card-footer]:pb-0 *:[img:first-child]:rounded-t-xl *:[img:last-child]:rounded-b-xl",
        className
      )}
      {...props}
    />
  )
}

function CardHeader({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-header"
      className={cn(
        "group/card-header @container/card-header grid auto-rows-min items-start gap-1 rounded-t-xl px-4 group-data-[size=sm]/card:px-3 has-data-[slot=card-action]:grid-cols-[1fr_auto] has-data-[slot=card-description]:grid-rows-[auto_auto] [.border-b]:pb-4 group-data-[size=sm]/card:[.border-b]:pb-3",
        className
      )}
      {...props}
    />
  )
}

function CardTitle({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-title"
      className={cn(
        "font-heading text-base leading-snug font-medium group-data-[size=sm]/card:text-sm",
        className
      )}
      {...props}
    />
  )
}

function CardDescription({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-description"
      className={cn("text-sm text-muted-foreground", className)}
      {...props}
    />
  )
}

function CardAction({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-action"
      className={cn(
        "col-start-2 row-span-2 row-start-1 self-start justify-self-end",
        className
      )}
      {...props}
    />
  )
}

function CardContent({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-content"
      className={cn("px-4 group-data-[size=sm]/card:px-3", className)}
      {...props}
    />
  )
}

function CardFooter({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-footer"
      className={cn(
        "flex items-center rounded-b-xl border-t bg-muted/50 p-4 group-data-[size=sm]/card:p-3",
        className
      )}
      {...props}
    />
  )
}

export {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardAction,
  CardDescription,
  CardContent,
}
</file>

<file path="src/components/ui/checkbox.tsx">
"use client"

import { Checkbox as CheckboxPrimitive } from "@base-ui/react/checkbox"
import { cn } from "@/lib/utils"
import { Check } from "lucide-react"

function Checkbox({
  className,
  checked,
  onCheckedChange,
  disabled,
  ...props
}: {
  className?: string
  checked?: boolean
  onCheckedChange?: (checked: boolean) => void
  disabled?: boolean
}) {
  return (
    <CheckboxPrimitive.Root
      checked={checked}
      onCheckedChange={(checked: boolean) => onCheckedChange?.(checked)}
      disabled={disabled}
      className={cn(
        "peer inline-flex size-4 shrink-0 items-center justify-center rounded border border-primary/30 bg-background transition-colors outline-none focus-visible:ring-2 focus-visible:ring-ring/50 aria-invalid:border-destructive data-checked:border-primary data-checked:bg-primary data-disabled:cursor-not-allowed data-disabled:opacity-50",
        className
      )}
      {...props}
    >
      <CheckboxPrimitive.Indicator className="flex items-center justify-center text-primary-foreground data-[unchecked]:hidden">
        <Check className="size-3" />
      </CheckboxPrimitive.Indicator>
    </CheckboxPrimitive.Root>
  )
}

export { Checkbox }
</file>

<file path="src/components/ui/dialog.tsx">
"use client"

import * as React from "react"
import { Dialog as DialogPrimitive } from "@base-ui/react/dialog"

import { cn } from "@/lib/utils"
import { Button } from "@/components/ui/button"
import { XIcon } from "lucide-react"

function Dialog({ ...props }: DialogPrimitive.Root.Props) {
  return <DialogPrimitive.Root data-slot="dialog" {...props} />
}

function DialogTrigger({ ...props }: DialogPrimitive.Trigger.Props) {
  return <DialogPrimitive.Trigger data-slot="dialog-trigger" {...props} />
}

function DialogPortal({ ...props }: DialogPrimitive.Portal.Props) {
  return <DialogPrimitive.Portal data-slot="dialog-portal" {...props} />
}

function DialogClose({ ...props }: DialogPrimitive.Close.Props) {
  return <DialogPrimitive.Close data-slot="dialog-close" {...props} />
}

function DialogOverlay({
  className,
  ...props
}: DialogPrimitive.Backdrop.Props) {
  return (
    <DialogPrimitive.Backdrop
      data-slot="dialog-overlay"
      className={cn(
        "fixed inset-0 isolate z-50 bg-black/10 duration-100 supports-backdrop-filter:backdrop-blur-xs data-open:animate-in data-open:fade-in-0 data-closed:animate-out data-closed:fade-out-0",
        className
      )}
      {...props}
    />
  )
}

function DialogContent({
  className,
  children,
  showCloseButton = true,
  ...props
}: DialogPrimitive.Popup.Props & {
  showCloseButton?: boolean
}) {
  return (
    <DialogPortal>
      <DialogOverlay />
      <DialogPrimitive.Popup
        data-slot="dialog-content"
        className={cn(
          "fixed top-1/2 left-1/2 z-50 grid w-full max-w-[calc(100%-2rem)] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-xl bg-popover p-4 text-sm text-popover-foreground ring-1 ring-foreground/10 duration-100 outline-none sm:max-w-sm data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95",
          className
        )}
        {...props}
      >
        {children}
        {showCloseButton && (
          <DialogPrimitive.Close
            data-slot="dialog-close"
            render={
              <Button
                variant="ghost"
                className="absolute top-2 right-2"
                size="icon-sm"
              />
            }
          >
            <XIcon
            />
            <span className="sr-only">Close</span>
          </DialogPrimitive.Close>
        )}
      </DialogPrimitive.Popup>
    </DialogPortal>
  )
}

function DialogHeader({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="dialog-header"
      className={cn("flex flex-col gap-2", className)}
      {...props}
    />
  )
}

function DialogFooter({
  className,
  showCloseButton = false,
  children,
  ...props
}: React.ComponentProps<"div"> & {
  showCloseButton?: boolean
}) {
  return (
    <div
      data-slot="dialog-footer"
      className={cn(
        "-mx-4 -mb-4 flex flex-col-reverse gap-2 rounded-b-xl border-t bg-muted/50 p-4 sm:flex-row sm:justify-end",
        className
      )}
      {...props}
    >
      {children}
      {showCloseButton && (
        <DialogPrimitive.Close render={<Button variant="outline" />}>
          Close
        </DialogPrimitive.Close>
      )}
    </div>
  )
}

function DialogTitle({ className, ...props }: DialogPrimitive.Title.Props) {
  return (
    <DialogPrimitive.Title
      data-slot="dialog-title"
      className={cn(
        "font-heading text-base leading-none font-medium",
        className
      )}
      {...props}
    />
  )
}

function DialogDescription({
  className,
  ...props
}: DialogPrimitive.Description.Props) {
  return (
    <DialogPrimitive.Description
      data-slot="dialog-description"
      className={cn(
        "text-sm text-muted-foreground *:[a]:underline *:[a]:underline-offset-3 *:[a]:hover:text-foreground",
        className
      )}
      {...props}
    />
  )
}

export {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogOverlay,
  DialogPortal,
  DialogTitle,
  DialogTrigger,
}
</file>

<file path="src/components/ui/dropdown-menu.tsx">
"use client"

import * as React from "react"
import { Menu as MenuPrimitive } from "@base-ui/react/menu"

import { cn } from "@/lib/utils"
import { ChevronRightIcon, CheckIcon } from "lucide-react"

function DropdownMenu({ ...props }: MenuPrimitive.Root.Props) {
  return <MenuPrimitive.Root data-slot="dropdown-menu" {...props} />
}

function DropdownMenuPortal({ ...props }: MenuPrimitive.Portal.Props) {
  return <MenuPrimitive.Portal data-slot="dropdown-menu-portal" {...props} />
}

function DropdownMenuTrigger({ ...props }: MenuPrimitive.Trigger.Props) {
  return <MenuPrimitive.Trigger data-slot="dropdown-menu-trigger" {...props} />
}

function DropdownMenuContent({
  align = "start",
  alignOffset = 0,
  side = "bottom",
  sideOffset = 4,
  className,
  ...props
}: MenuPrimitive.Popup.Props &
  Pick<
    MenuPrimitive.Positioner.Props,
    "align" | "alignOffset" | "side" | "sideOffset"
  >) {
  return (
    <MenuPrimitive.Portal>
      <MenuPrimitive.Positioner
        className="isolate z-50 outline-none"
        align={align}
        alignOffset={alignOffset}
        side={side}
        sideOffset={sideOffset}
      >
        <MenuPrimitive.Popup
          data-slot="dropdown-menu-content"
          className={cn("z-50 max-h-(--available-height) w-(--anchor-width) min-w-32 origin-(--transform-origin) overflow-x-hidden overflow-y-auto rounded-lg bg-popover p-1 text-popover-foreground shadow-md ring-1 ring-foreground/10 duration-100 outline-none data-[side=bottom]:slide-in-from-top-2 data-[side=inline-end]:slide-in-from-left-2 data-[side=inline-start]:slide-in-from-right-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:overflow-hidden data-closed:fade-out-0 data-closed:zoom-out-95", className )}
          {...props}
        />
      </MenuPrimitive.Positioner>
    </MenuPrimitive.Portal>
  )
}

function DropdownMenuGroup({ ...props }: MenuPrimitive.Group.Props) {
  return <MenuPrimitive.Group data-slot="dropdown-menu-group" {...props} />
}

function DropdownMenuLabel({
  className,
  inset,
  ...props
}: MenuPrimitive.GroupLabel.Props & {
  inset?: boolean
}) {
  return (
    <MenuPrimitive.GroupLabel
      data-slot="dropdown-menu-label"
      data-inset={inset}
      className={cn(
        "px-1.5 py-1 text-xs font-medium text-muted-foreground data-inset:pl-7",
        className
      )}
      {...props}
    />
  )
}

function DropdownMenuItem({
  className,
  inset,
  variant = "default",
  ...props
}: MenuPrimitive.Item.Props & {
  inset?: boolean
  variant?: "default" | "destructive"
}) {
  return (
    <MenuPrimitive.Item
      data-slot="dropdown-menu-item"
      data-inset={inset}
      data-variant={variant}
      className={cn(
        "group/dropdown-menu-item relative flex cursor-default items-center gap-1.5 rounded-md px-1.5 py-1 text-sm outline-hidden select-none focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-inset:pl-7 data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 data-[variant=destructive]:focus:text-destructive dark:data-[variant=destructive]:focus:bg-destructive/20 data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4 data-[variant=destructive]:*:[svg]:text-destructive",
        className
      )}
      {...props}
    />
  )
}

function DropdownMenuSub({ ...props }: MenuPrimitive.SubmenuRoot.Props) {
  return <MenuPrimitive.SubmenuRoot data-slot="dropdown-menu-sub" {...props} />
}

function DropdownMenuSubTrigger({
  className,
  inset,
  children,
  ...props
}: MenuPrimitive.SubmenuTrigger.Props & {
  inset?: boolean
}) {
  return (
    <MenuPrimitive.SubmenuTrigger
      data-slot="dropdown-menu-sub-trigger"
      data-inset={inset}
      className={cn(
        "flex cursor-default items-center gap-1.5 rounded-md px-1.5 py-1 text-sm outline-hidden select-none focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-inset:pl-7 data-popup-open:bg-accent data-popup-open:text-accent-foreground data-open:bg-accent data-open:text-accent-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )}
      {...props}
    >
      {children}
      <ChevronRightIcon className="ml-auto" />
    </MenuPrimitive.SubmenuTrigger>
  )
}

function DropdownMenuSubContent({
  align = "start",
  alignOffset = -3,
  side = "right",
  sideOffset = 0,
  className,
  ...props
}: React.ComponentProps<typeof DropdownMenuContent>) {
  return (
    <DropdownMenuContent
      data-slot="dropdown-menu-sub-content"
      className={cn("w-auto min-w-[96px] rounded-lg bg-popover p-1 text-popover-foreground shadow-lg ring-1 ring-foreground/10 duration-100 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95", className )}
      align={align}
      alignOffset={alignOffset}
      side={side}
      sideOffset={sideOffset}
      {...props}
    />
  )
}

function DropdownMenuCheckboxItem({
  className,
  children,
  checked,
  inset,
  ...props
}: MenuPrimitive.CheckboxItem.Props & {
  inset?: boolean
}) {
  return (
    <MenuPrimitive.CheckboxItem
      data-slot="dropdown-menu-checkbox-item"
      data-inset={inset}
      className={cn(
        "relative flex cursor-default items-center gap-1.5 rounded-md py-1 pr-8 pl-1.5 text-sm outline-hidden select-none focus:bg-accent focus:text-accent-foreground focus:**:text-accent-foreground data-inset:pl-7 data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )}
      checked={checked}
      {...props}
    >
      <span
        className="pointer-events-none absolute right-2 flex items-center justify-center"
        data-slot="dropdown-menu-checkbox-item-indicator"
      >
        <MenuPrimitive.CheckboxItemIndicator>
          <CheckIcon
          />
        </MenuPrimitive.CheckboxItemIndicator>
      </span>
      {children}
    </MenuPrimitive.CheckboxItem>
  )
}

function DropdownMenuRadioGroup({ ...props }: MenuPrimitive.RadioGroup.Props) {
  return (
    <MenuPrimitive.RadioGroup
      data-slot="dropdown-menu-radio-group"
      {...props}
    />
  )
}

function DropdownMenuRadioItem({
  className,
  children,
  inset,
  ...props
}: MenuPrimitive.RadioItem.Props & {
  inset?: boolean
}) {
  return (
    <MenuPrimitive.RadioItem
      data-slot="dropdown-menu-radio-item"
      data-inset={inset}
      className={cn(
        "relative flex cursor-default items-center gap-1.5 rounded-md py-1 pr-8 pl-1.5 text-sm outline-hidden select-none focus:bg-accent focus:text-accent-foreground focus:**:text-accent-foreground data-inset:pl-7 data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )}
      {...props}
    >
      <span
        className="pointer-events-none absolute right-2 flex items-center justify-center"
        data-slot="dropdown-menu-radio-item-indicator"
      >
        <MenuPrimitive.RadioItemIndicator>
          <CheckIcon
          />
        </MenuPrimitive.RadioItemIndicator>
      </span>
      {children}
    </MenuPrimitive.RadioItem>
  )
}

function DropdownMenuSeparator({
  className,
  ...props
}: MenuPrimitive.Separator.Props) {
  return (
    <MenuPrimitive.Separator
      data-slot="dropdown-menu-separator"
      className={cn("-mx-1 my-1 h-px bg-border", className)}
      {...props}
    />
  )
}

function DropdownMenuShortcut({
  className,
  ...props
}: React.ComponentProps<"span">) {
  return (
    <span
      data-slot="dropdown-menu-shortcut"
      className={cn(
        "ml-auto text-xs tracking-widest text-muted-foreground group-focus/dropdown-menu-item:text-accent-foreground",
        className
      )}
      {...props}
    />
  )
}

export {
  DropdownMenu,
  DropdownMenuPortal,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuGroup,
  DropdownMenuLabel,
  DropdownMenuItem,
  DropdownMenuCheckboxItem,
  DropdownMenuRadioGroup,
  DropdownMenuRadioItem,
  DropdownMenuSeparator,
  DropdownMenuShortcut,
  DropdownMenuSub,
  DropdownMenuSubTrigger,
  DropdownMenuSubContent,
}
</file>

<file path="src/components/ui/input.tsx">
import * as React from "react"
import { Input as InputPrimitive } from "@base-ui/react/input"

import { cn } from "@/lib/utils"

function Input({ className, type, ...props }: React.ComponentProps<"input">) {
  return (
    <InputPrimitive
      type={type}
      data-slot="input"
      className={cn(
        "h-8 w-full min-w-0 rounded-lg border border-input bg-transparent px-2.5 py-1 text-base transition-colors outline-none file:inline-flex file:h-6 file:border-0 file:bg-transparent file:text-sm file:font-medium file:text-foreground placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:pointer-events-none disabled:cursor-not-allowed disabled:bg-input/50 disabled:opacity-50 aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 md:text-sm dark:bg-input/30 dark:disabled:bg-input/80 dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40",
        className
      )}
      {...props}
    />
  )
}

export { Input }
</file>

<file path="src/components/ui/label.tsx">
"use client"

import * as React from "react"

import { cn } from "@/lib/utils"

function Label({ className, ...props }: React.ComponentProps<"label">) {
  return (
    <label
      data-slot="label"
      className={cn(
        "flex items-center gap-2 text-sm leading-none font-medium select-none group-data-[disabled=true]:pointer-events-none group-data-[disabled=true]:opacity-50 peer-disabled:cursor-not-allowed peer-disabled:opacity-50",
        className
      )}
      {...props}
    />
  )
}

export { Label }
</file>

<file path="src/components/ui/scroll-area.tsx">
"use client"

import * as React from "react"
import { ScrollArea as ScrollAreaPrimitive } from "@base-ui/react/scroll-area"

import { cn } from "@/lib/utils"

function ScrollArea({
  className,
  children,
  ...props
}: ScrollAreaPrimitive.Root.Props) {
  return (
    <ScrollAreaPrimitive.Root
      data-slot="scroll-area"
      className={cn("relative", className)}
      {...props}
    >
      <ScrollAreaPrimitive.Viewport
        data-slot="scroll-area-viewport"
        className="size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:outline-1"
      >
        {children}
      </ScrollAreaPrimitive.Viewport>
      <ScrollBar />
      <ScrollAreaPrimitive.Corner />
    </ScrollAreaPrimitive.Root>
  )
}

function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}: ScrollAreaPrimitive.Scrollbar.Props) {
  return (
    <ScrollAreaPrimitive.Scrollbar
      data-slot="scroll-area-scrollbar"
      data-orientation={orientation}
      orientation={orientation}
      className={cn(
        "flex touch-none p-px transition-colors select-none data-horizontal:h-2.5 data-horizontal:flex-col data-horizontal:border-t data-horizontal:border-t-transparent data-vertical:h-full data-vertical:w-2.5 data-vertical:border-l data-vertical:border-l-transparent",
        className
      )}
      {...props}
    >
      <ScrollAreaPrimitive.Thumb
        data-slot="scroll-area-thumb"
        className="relative flex-1 rounded-full bg-border"
      />
    </ScrollAreaPrimitive.Scrollbar>
  )
}

export { ScrollArea, ScrollBar }
</file>

<file path="src/components/ui/select.tsx">
"use client"

import * as React from "react"
import { Select as SelectPrimitive } from "@base-ui/react/select"

import { cn } from "@/lib/utils"
import { ChevronDownIcon, CheckIcon, ChevronUpIcon } from "lucide-react"

const Select = SelectPrimitive.Root

function SelectGroup({ className, ...props }: SelectPrimitive.Group.Props) {
  return (
    <SelectPrimitive.Group
      data-slot="select-group"
      className={cn("scroll-my-1 p-1", className)}
      {...props}
    />
  )
}

function SelectValue({ className, ...props }: SelectPrimitive.Value.Props) {
  return (
    <SelectPrimitive.Value
      data-slot="select-value"
      className={cn("flex flex-1 text-left", className)}
      {...props}
    />
  )
}

function SelectTrigger({
  className,
  size = "default",
  children,
  ...props
}: SelectPrimitive.Trigger.Props & {
  size?: "sm" | "default"
}) {
  return (
    <SelectPrimitive.Trigger
      data-slot="select-trigger"
      data-size={size}
      className={cn(
        "flex w-fit items-center justify-between gap-1.5 rounded-lg border border-input bg-transparent py-2 pr-2 pl-2.5 text-sm whitespace-nowrap transition-colors outline-none select-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:cursor-not-allowed disabled:opacity-50 aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 data-placeholder:text-muted-foreground data-[size=default]:h-8 data-[size=sm]:h-7 data-[size=sm]:rounded-[min(var(--radius-md),10px)] *:data-[slot=select-value]:line-clamp-1 *:data-[slot=select-value]:flex *:data-[slot=select-value]:items-center *:data-[slot=select-value]:gap-1.5 dark:bg-input/30 dark:hover:bg-input/50 dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )}
      {...props}
    >
      {children}
      <SelectPrimitive.Icon
        render={
          <ChevronDownIcon className="pointer-events-none size-4 text-muted-foreground" />
        }
      />
    </SelectPrimitive.Trigger>
  )
}

function SelectContent({
  className,
  children,
  side = "bottom",
  sideOffset = 4,
  align = "center",
  alignOffset = 0,
  alignItemWithTrigger = true,
  ...props
}: SelectPrimitive.Popup.Props &
  Pick<
    SelectPrimitive.Positioner.Props,
    "align" | "alignOffset" | "side" | "sideOffset" | "alignItemWithTrigger"
  >) {
  return (
    <SelectPrimitive.Portal>
      <SelectPrimitive.Positioner
        side={side}
        sideOffset={sideOffset}
        align={align}
        alignOffset={alignOffset}
        alignItemWithTrigger={alignItemWithTrigger}
        className="isolate z-50"
      >
        <SelectPrimitive.Popup
          data-slot="select-content"
          data-align-trigger={alignItemWithTrigger}
          className={cn("relative isolate z-50 max-h-(--available-height) w-(--anchor-width) min-w-36 origin-(--transform-origin) overflow-x-hidden overflow-y-auto rounded-lg bg-popover text-popover-foreground shadow-md ring-1 ring-foreground/10 duration-100 data-[align-trigger=true]:animate-none data-[side=bottom]:slide-in-from-top-2 data-[side=inline-end]:slide-in-from-left-2 data-[side=inline-start]:slide-in-from-right-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95", className )}
          {...props}
        >
          <SelectScrollUpButton />
          <SelectPrimitive.List>{children}</SelectPrimitive.List>
          <SelectScrollDownButton />
        </SelectPrimitive.Popup>
      </SelectPrimitive.Positioner>
    </SelectPrimitive.Portal>
  )
}

function SelectLabel({
  className,
  ...props
}: SelectPrimitive.GroupLabel.Props) {
  return (
    <SelectPrimitive.GroupLabel
      data-slot="select-label"
      className={cn("px-1.5 py-1 text-xs text-muted-foreground", className)}
      {...props}
    />
  )
}

function SelectItem({
  className,
  children,
  ...props
}: SelectPrimitive.Item.Props) {
  return (
    <SelectPrimitive.Item
      data-slot="select-item"
      className={cn(
        "relative flex w-full cursor-default items-center gap-1.5 rounded-md py-1 pr-8 pl-1.5 text-sm outline-hidden select-none focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4 *:[span]:last:flex *:[span]:last:items-center *:[span]:last:gap-2",
        className
      )}
      {...props}
    >
      <SelectPrimitive.ItemText className="flex flex-1 shrink-0 gap-2 whitespace-nowrap">
        {children}
      </SelectPrimitive.ItemText>
      <SelectPrimitive.ItemIndicator
        render={
          <span className="pointer-events-none absolute right-2 flex size-4 items-center justify-center" />
        }
      >
        <CheckIcon className="pointer-events-none" />
      </SelectPrimitive.ItemIndicator>
    </SelectPrimitive.Item>
  )
}

function SelectSeparator({
  className,
  ...props
}: SelectPrimitive.Separator.Props) {
  return (
    <SelectPrimitive.Separator
      data-slot="select-separator"
      className={cn("pointer-events-none -mx-1 my-1 h-px bg-border", className)}
      {...props}
    />
  )
}

function SelectScrollUpButton({
  className,
  ...props
}: React.ComponentProps<typeof SelectPrimitive.ScrollUpArrow>) {
  return (
    <SelectPrimitive.ScrollUpArrow
      data-slot="select-scroll-up-button"
      className={cn(
        "top-0 z-10 flex w-full cursor-default items-center justify-center bg-popover py-1 [&_svg:not([class*='size-'])]:size-4",
        className
      )}
      {...props}
    >
      <ChevronUpIcon
      />
    </SelectPrimitive.ScrollUpArrow>
  )
}

function SelectScrollDownButton({
  className,
  ...props
}: React.ComponentProps<typeof SelectPrimitive.ScrollDownArrow>) {
  return (
    <SelectPrimitive.ScrollDownArrow
      data-slot="select-scroll-down-button"
      className={cn(
        "bottom-0 z-10 flex w-full cursor-default items-center justify-center bg-popover py-1 [&_svg:not([class*='size-'])]:size-4",
        className
      )}
      {...props}
    >
      <ChevronDownIcon
      />
    </SelectPrimitive.ScrollDownArrow>
  )
}

export {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectScrollDownButton,
  SelectScrollUpButton,
  SelectSeparator,
  SelectTrigger,
  SelectValue,
}
</file>

<file path="src/components/ui/separator.tsx">
"use client"

import { Separator as SeparatorPrimitive } from "@base-ui/react/separator"

import { cn } from "@/lib/utils"

function Separator({
  className,
  orientation = "horizontal",
  ...props
}: SeparatorPrimitive.Props) {
  return (
    <SeparatorPrimitive
      data-slot="separator"
      orientation={orientation}
      className={cn(
        "shrink-0 bg-border data-horizontal:h-px data-horizontal:w-full data-vertical:w-px data-vertical:self-stretch",
        className
      )}
      {...props}
    />
  )
}

export { Separator }
</file>

<file path="src/components/ui/slider.tsx">
import { Slider as SliderPrimitive } from "@base-ui/react/slider"

import { cn } from "@/lib/utils"

function Slider({
  className,
  defaultValue,
  value,
  min = 0,
  max = 100,
  ...props
}: SliderPrimitive.Root.Props) {
  const _values = Array.isArray(value)
    ? value
    : Array.isArray(defaultValue)
      ? defaultValue
      : [min, max]

  return (
    <SliderPrimitive.Root
      className={cn("data-horizontal:w-full data-vertical:h-full", className)}
      data-slot="slider"
      defaultValue={defaultValue}
      value={value}
      min={min}
      max={max}
      thumbAlignment="edge"
      {...props}
    >
      <SliderPrimitive.Control className="relative flex w-full touch-none items-center select-none data-disabled:opacity-50 data-vertical:h-full data-vertical:min-h-40 data-vertical:w-auto data-vertical:flex-col">
        <SliderPrimitive.Track
          data-slot="slider-track"
          className="relative grow overflow-hidden rounded-full bg-muted select-none data-horizontal:h-1 data-horizontal:w-full data-vertical:h-full data-vertical:w-1"
        >
          <SliderPrimitive.Indicator
            data-slot="slider-range"
            className="bg-primary select-none data-horizontal:h-full data-vertical:w-full"
          />
        </SliderPrimitive.Track>
        {Array.from({ length: _values.length }, (_, index) => (
          <SliderPrimitive.Thumb
            data-slot="slider-thumb"
            key={index}
            className="relative block size-3 shrink-0 rounded-full border border-ring bg-white ring-ring/50 transition-[color,box-shadow] select-none after:absolute after:-inset-2 hover:ring-3 focus-visible:ring-3 focus-visible:outline-hidden active:ring-3 disabled:pointer-events-none disabled:opacity-50"
          />
        ))}
      </SliderPrimitive.Control>
    </SliderPrimitive.Root>
  )
}

export { Slider }
</file>

<file path="src/components/ui/switch.tsx">
"use client"

import { Switch as SwitchPrimitive } from "@base-ui/react/switch"

import { cn } from "@/lib/utils"

function Switch({
  className,
  size = "default",
  ...props
}: SwitchPrimitive.Root.Props & {
  size?: "sm" | "default"
}) {
  return (
    <SwitchPrimitive.Root
      data-slot="switch"
      data-size={size}
      className={cn(
        "peer group/switch relative inline-flex shrink-0 items-center rounded-full border border-transparent transition-all outline-none after:absolute after:-inset-x-3 after:-inset-y-2 focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 data-[size=default]:h-[18.4px] data-[size=default]:w-[32px] data-[size=sm]:h-[14px] data-[size=sm]:w-[24px] dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40 data-checked:bg-primary data-unchecked:bg-input dark:data-unchecked:bg-input/80 data-disabled:cursor-not-allowed data-disabled:opacity-50",
        className
      )}
      {...props}
    >
      <SwitchPrimitive.Thumb
        data-slot="switch-thumb"
        className="pointer-events-none block rounded-full bg-background ring-0 transition-transform group-data-[size=default]/switch:size-4 group-data-[size=sm]/switch:size-3 group-data-[size=default]/switch:data-checked:translate-x-[calc(100%-2px)] group-data-[size=sm]/switch:data-checked:translate-x-[calc(100%-2px)] dark:data-checked:bg-primary-foreground group-data-[size=default]/switch:data-unchecked:translate-x-0 group-data-[size=sm]/switch:data-unchecked:translate-x-0 dark:data-unchecked:bg-foreground"
      />
    </SwitchPrimitive.Root>
  )
}

export { Switch }
</file>

<file path="src/components/ui/tabs.tsx">
"use client"

import { Tabs as TabsPrimitive } from "@base-ui/react/tabs"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

function Tabs({
  className,
  orientation = "horizontal",
  ...props
}: TabsPrimitive.Root.Props) {
  return (
    <TabsPrimitive.Root
      data-slot="tabs"
      data-orientation={orientation}
      className={cn(
        "group/tabs flex gap-2 data-horizontal:flex-col",
        className
      )}
      {...props}
    />
  )
}

const tabsListVariants = cva(
  "group/tabs-list inline-flex w-fit items-center justify-center rounded-lg p-[3px] text-muted-foreground group-data-horizontal/tabs:h-8 group-data-vertical/tabs:h-fit group-data-vertical/tabs:flex-col data-[variant=line]:rounded-none",
  {
    variants: {
      variant: {
        default: "bg-muted",
        line: "gap-1 bg-transparent",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)

function TabsList({
  className,
  variant = "default",
  ...props
}: TabsPrimitive.List.Props & VariantProps<typeof tabsListVariants>) {
  return (
    <TabsPrimitive.List
      data-slot="tabs-list"
      data-variant={variant}
      className={cn(tabsListVariants({ variant }), className)}
      {...props}
    />
  )
}

function TabsTrigger({ className, ...props }: TabsPrimitive.Tab.Props) {
  return (
    <TabsPrimitive.Tab
      data-slot="tabs-trigger"
      className={cn(
        "relative inline-flex h-[calc(100%-1px)] flex-1 items-center justify-center gap-1.5 rounded-md border border-transparent px-1.5 py-0.5 text-sm font-medium whitespace-nowrap text-foreground/60 transition-all group-data-vertical/tabs:w-full group-data-vertical/tabs:justify-start hover:text-foreground focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:outline-1 focus-visible:outline-ring disabled:pointer-events-none disabled:opacity-50 has-data-[icon=inline-end]:pr-1 has-data-[icon=inline-start]:pl-1 aria-disabled:pointer-events-none aria-disabled:opacity-50 dark:text-muted-foreground dark:hover:text-foreground group-data-[variant=default]/tabs-list:data-active:shadow-sm group-data-[variant=line]/tabs-list:data-active:shadow-none [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        "group-data-[variant=line]/tabs-list:bg-transparent group-data-[variant=line]/tabs-list:data-active:bg-transparent dark:group-data-[variant=line]/tabs-list:data-active:border-transparent dark:group-data-[variant=line]/tabs-list:data-active:bg-transparent",
        "data-active:bg-background data-active:text-foreground dark:data-active:border-input dark:data-active:bg-input/30 dark:data-active:text-foreground",
        "after:absolute after:bg-foreground after:opacity-0 after:transition-opacity group-data-horizontal/tabs:after:inset-x-0 group-data-horizontal/tabs:after:bottom-[-5px] group-data-horizontal/tabs:after:h-0.5 group-data-vertical/tabs:after:inset-y-0 group-data-vertical/tabs:after:-right-1 group-data-vertical/tabs:after:w-0.5 group-data-[variant=line]/tabs-list:data-active:after:opacity-100",
        className
      )}
      {...props}
    />
  )
}

function TabsContent({ className, ...props }: TabsPrimitive.Panel.Props) {
  return (
    <TabsPrimitive.Panel
      data-slot="tabs-content"
      className={cn("flex-1 text-sm outline-none", className)}
      {...props}
    />
  )
}

export { Tabs, TabsList, TabsTrigger, TabsContent, tabsListVariants }
</file>

<file path="src/components/ui/textarea.tsx">
import * as React from "react"

import { cn } from "@/lib/utils"

function Textarea({ className, ...props }: React.ComponentProps<"textarea">) {
  return (
    <textarea
      data-slot="textarea"
      className={cn(
        "flex field-sizing-content min-h-16 w-full rounded-lg border border-input bg-transparent px-2.5 py-2 text-base transition-colors outline-none placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:cursor-not-allowed disabled:bg-input/50 disabled:opacity-50 aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 md:text-sm dark:bg-input/30 dark:disabled:bg-input/80 dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40",
        className
      )}
      {...props}
    />
  )
}

export { Textarea }
</file>

<file path="src/lib/ai/prompts/ablation-testing.ts">
// ============================================
// 消融测试 — 系统提示词
// "删掉这段会失去什么？"四分类判定
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说叙事结构分析系统。你的任务不是分析"这段有什么深意"，而是执行消融测试。

## 消融测试方法论

对每个段落，只问一个问题：

**如果删掉这一段，会失去什么？**

结果只有四种可能：

### 1. 剧情崩 (bone)
删除后：
- 主线逻辑出现断层
- 后续情节无法理解
- 关键信息永久丢失
→ 标记为 "bone"

### 2. 人物崩 / 体验降 (muscle)
删除后：
- 主线剧情仍然连贯
- 但人物形象变薄、情感共鸣减弱
- 或世界观氛围变淡
→ 标记为 "muscle"

### 3. 氛围崩 / 气散 (filler_a)
删除后：
- 剧情不变、人物不变
- 但阅读节奏被打乱（该慢的地方突然快了）
- 或沉浸感/陪伴感/生存感下降
→ 标记为 "filler_a"

### 4. 什么都不变 (filler_b)
删除后：
- 剧情、人物、氛围均无变化
- 可能是重复描述、重复感叹、重复解释
→ 标记为 "filler_b"

### 5. 无法判断
如果无法确定归类，标记为 "uncertain"。
**宁可承认不确定，也不强行归类**。

## 输出格式

对每个切片输出 JSON：

{
  "sliceId": "切片ID",
  "category": "bone" | "muscle" | "filler_a" | "filler_b" | "uncertain",
  "lostIfRemoved": "删掉会失去什么（一句话）",
  "confidence": 0-1,
  "reasoning": "判定理由（不超过50字）"
}

## 核心原则

1. **默认假设是"不确定"**：除非有明确证据，否则归类为 uncertain
2. **奥卡姆剃刀**：若无明确证据表明某处是伏笔/隐喻/铺垫，则认为是字面意思
3. **过渡段落的默认解释是"管理节奏"**，而非承载深层主题
4. **网文特性**：大量日常/修炼/准备段落不是"水"，而是在维持"生存感/陪伴感"
`;

export function buildAblationTestingMessages(slicesJson: string) {
  return {
    systemPrompt: getPrompt('ablation-testing', DEFAULT_SYSTEM_PROMPT),
    userMessage: `以下是一本小说的语义切片列表（每个包含标题、内容、语义标签）。请对每个切片执行消融测试。\n\n${slicesJson}\n\n请输出 JSON 数组。`,
  };
}
</file>

<file path="src/lib/ai/prompts/character-dynamics-extraction.ts">
// ============================================
// Step 2.5: 角色动力学 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说逆向工程系统的角色动力学分析模块。
目标：分析角色在叙事中承担的功能，而不是分析角色本身。

禁止：
- 角色小传/人物介绍（"主角是一个少年..."）
- 角色性格描述（"他勇敢、善良..."）
- 作者评价、文学赏析、主观吹捧
- 推测作者意图

原则：
1. 每个结论必须附带原文引用作为证据：【原文："..."】
2. 每个规律至少 2 个独立文本例证
3. 只有 1 个例证的标记为 [推测]
4. 不允许为分析而分析，不允许强行建立关联
5. 所有结论必须说明：这个角色功能如何影响读者体验

你的任务是分析小说文本中的角色动力学，分为 3 个部分。

## 分析内容

### 一、角色叙事功能分类

识别主要角色各自承担的叙事功能。注意：一个角色可以同时承担多个功能。

功能类型（不限于以下，可自行识别新类型）：
- **成长驱动器** — 推动角色成长的关键事件围绕此人
- **谜团载体** — 此人本身就是悬念来源（身份不明/动机不明/能力不明）
- **情绪锚点** — 读者情感投射的对象（共情/代入/牵挂）
- **世界观窗口** — 通过此人的经历/知识展示世界规则
- **冲突制造器** — 制造或激化矛盾的角色
- **镜像角色** — 与主角形成对照，凸显主题
- **催化剂** — 出现即改变局势的角色
- **势能蓄积器** — 此人的存在本身就在积累读者的期待

对每个角色输出：
\`\`\`
### [角色名]
- 叙事功能：[功能1] / [功能2] / ...
- 功能说明：[一句话说明该角色如何承担这些功能]
- 功能证据：
  - 【原文："引用"】（位置）
  - 【原文："引用"】（位置）
- 反例：
  - 【原文："引用"】（位置）——如存在该角色未承担此功能的场景
- 适用率：N%
\`\`\`

### 二、角色关系拓扑

识别角色之间的关系网络，重点分析关系的叙事功能。

关系类型（不限于）：
- 对立（敌对/竞争/理念冲突）
- 同盟（合作/师徒/伙伴）
- 情感（爱情/亲情/友情）
- 镜像（互为对照）
- 因果（一人是另一人的起因）

对每对关键关系输出：
\`\`\`
#### [角色A] ↔ [角色B]
- 关系类型：[类型]
- 关系叙事功能：[这对关系驱动了什么叙事效果]
- 关系演变：[如有变化，简述变化轨迹]
- 证据：
  - 【原文："引用"】
\`\`\`

使用 Markdown 表格输出完整的关系矩阵。

### 三、关键角色弧线（仅主角和 2-3 个核心配角）

对每个关键角色：
\`\`\`
### [角色名] 弧线
- 起始状态：[阅读初期的角色定位/状态]
- 转折事件：[关键转变点]
  - 【原文："引用"】
- 终态：[阅读末期的角色定位/状态]
- 弧线功能：[这个弧线服务于什么叙事目标——不是角色成长了什么，而是这个成长对读者有什么影响]
\`\`\`

## 反证要求

对每个角色功能标签和关系定性：
- 主动在文本中搜索不符合该定性的事例
- 适用率 < 50% 的定性标记为 [过度解读]

## 输出格式

输出为 Markdown 格式的角色动力学报告（character_dynamics.md）。
`;

export const DEFAULT_SUPPLEMENT_PROMPT = `你之前已经分析了该作者的部分作品并生成了一份角色动力学报告。
现在你收到了更多文本。请结合新文本，对角色动力学报告进行补充和修正。

要求：
1. 保留原有 3 个分析部分（角色叙事功能 / 关系拓扑 / 角色弧线）
2. 用新文本中的证据补充或修正结论
3. 更新关系拓扑（新关系、关系变化）
4. 更新角色弧线数据
5. 更新反证数据（支持证据、反例、适用率）
6. 输出完整的更新后的角色动力学报告（不是增量，而是完整版本）
`;

export function buildCharacterDynamicsExtractionMessages(
  slicesText: string,
  narrativeDynamics: string,
) {
  const userMessage = `## 叙事动力学报告（参考）\n\n${narrativeDynamics}\n\n---\n\n## 小说文本\n\n${slicesText}`;
  return { systemPrompt: getPrompt('source-character-dynamics', DEFAULT_SYSTEM_PROMPT), userMessage };
}

export function buildCharacterDynamicsSupplementMessages(
  newChunk: string,
  previousReport: string,
  narrativeDynamics: string,
) {
  const userMessage = `## 之前的角色动力学报告\n\n${previousReport}\n\n---\n\n## 叙事动力学报告（参考）\n\n${narrativeDynamics}\n\n---\n\n## 新的文本片段\n\n${newChunk}\n\n---\n\n请基于以上全部信息，输出完整的、更新后的角色动力学报告。`;
  return { systemPrompt: getPrompt('source-character-dynamics-supplement', DEFAULT_SUPPLEMENT_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/deep-analysis.ts">
// ============================================
// 深度分析 — 逐大切片（统一管线 Step 5）
// 输出结构化 SliceAnalysis JSON
// ============================================

import { getPrompt } from './helpers';
import { withEngineeringConstraints } from './pipeline-common';

export const DEFAULT_SYSTEM_PROMPT = withEngineeringConstraints(`你是小说逆向工程分析程序。你的任务是从原文片段中提取可复现的写作机制，直接指导 AI 仿写。

## 你要提取什么

对给定的原文片段，提取以下 5 个维度的量化机制。每个结论必须有数值支撑和反例。

### 1. styleMechanics — 文风机制
- sentenceLength: { avg, range, climax（高潮段落）, calm（平静段落） }
- dialogueRatio: 对话占比 (0-1)
- descriptionStrategy: 描写策略的可操作规则，如"动作描写用短句堆叠，环境描写用长句铺陈"
- counterExample: 不符合上述规律的原文片段（限30字）
- confidence: 0-1
- generationRule: 一条直接可执行的生成指令

### 2. narrativeMechanics — 叙事机制
- pacingPattern: 节奏模式，如"铺垫20%→递进50%→高潮30%"
- informationControl: 信息控制策略，如"读者知道主角有底牌，对手不知道"
- counterExample, confidence, generationRule（同上格式）

### 3. characterMechanics — 角色机制
对每个重要角色：
- stimulusResponse: 刺激-反应对数组，如 [{ stimulus: "被挑衅", response: "沉默+观察+不口舌" }]
- relationshipChanges: 关系变化，如 [{ with: "南宫婉", from: "盟友", to: "疑虑" }]
- confidence: 0-1

### 4. stimulationPoints — 刺激点
标记每个叙事刺激点：
- type: 冲突升级 | 信息释放 | 关系变化 | 实力跃迁 | 悬念建立 | 日常过渡
- location: 章节或段落位置
- sentenceLengthAtPoint: 该点的平均句长
- tensionChange: -5 到 +5
- confidence: 0-1

### 5. constraints — 约束
- worldRules: 世界观硬规则，如 ["筑基期不能飞行"]
- characterLimits: 角色当前限制，如 ["灵力即将耗尽"]
- plotConstraints: 情节约束，如 ["三章内必须解决威胁"]
- taboos: 绝对不做的，如 ["不写虐主"]

## 同时你会收到全书记忆图谱

图谱包含所有已提取的事件和实体信息。你可以用它来理解当前片段在全书中的位置，但你只分析当前片段的原文。

## 输出格式

输出单个 JSON 对象，严格遵循上述结构。分析不出的字段填 null。`);

export function buildDeepAnalysisMessages(
  sliceText: string,
  sliceIndex: number,
  totalSlices: number,
  eventGraphJson: string,
) {
  const userMessage = `## 切片信息\n- 大切片编号: ${sliceIndex + 1} / ${totalSlices}\n\n## 全书记忆图谱\n${eventGraphJson}\n\n---\n## 原文内容\n${sliceText}\n\n---\n请分析原文，输出 SliceAnalysis JSON。`;

  return {
    systemPrompt: getPrompt('deep-analysis', DEFAULT_SYSTEM_PROMPT),
    userMessage,
  };
}
</file>

<file path="src/lib/ai/prompts/dna-compression.ts">
// ============================================
// Step 6: DNA 压缩 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说逆向工程系统的 DNA 压缩模块。
目标：将前面所有分析步骤的结论压缩为一份跨小说可比的核心 DNA。

这不是再分析一遍。你的任务是提炼——从数万字的分析报告中，提取出真正可迁移、可复用的核心规律。

## 输出格式：YAML

严格输出 YAML 格式。分为核心字段（固定结构）和扩展字段（自由添加）。

### 核心字段（必填）

\`\`\`yaml
reader_need:
  primary: [主要心理缺失，枚举：未知感|成长感|控制感|归属感|崇高感|安全感|神秘感|失控感]
  secondary: [次要心理缺失，可多值，用逗号分隔]

primary_driver: [主翻页驱动力，枚举：世界真相|身份悬念|权力竞争|关系变化|成长突破|危机生存]

mystery_structure: [谜团结构，枚举：mesh|linear|nested|none]

stimulus_cycle:
  - [刺激类型1]
  - [刺激类型2]
  # 按实际循环顺序排列

peak_spacing: [高潮间距，如"约3万字"或"约8-10章"]
cooldown_spacing: [降温间距，如"约5000字"或"约2-3章"]

core_anomaly: [核心结构异常，一句话概括这部小说最独特的叙事特征]

character_functions:
  protagonist: [主角承担的主要叙事功能]
  antagonist: [对手承担的主要叙事功能]
  # 可扩展至其他关键角色

style_signature:
  rhetoric_level: [低修辞|中修辞|高修辞]
  info_density: [低|中|高]
  dialogue_ratio: [低|中|高]
  pov: [叙述视角特征]

potential_type: [主势能类型，枚举：Cognitive|Emotional|Mixed]

narrative_taboos:
  - [禁忌1：这本小说不做的事]
  - [禁忌2]
  # 2-5条核心禁忌

cooling_patterns:
  - [降温模式1]
  - [降温模式2]
\`\`\`

### 扩展字段（根据小说特征自行添加）

在核心字段之后，根据这部小说的独特性，自由添加扩展字段。例如：

\`\`\`yaml
# === 扩展字段（本作特有） ===
world_reveal_pattern: [世界揭示模式，如适用]
power_system_role: [力量体系在叙事中的角色，如适用]
unique_technique: [独特的叙事技术，如适用]
emotional_signature: [情感特征，如适用]
\`\`\`

## 规则

1. 核心字段必须全部填写，缺失的填 null
2. 所有结论必须来自前面的分析报告，不允许凭空生成
3. 每个值应尽量简短（1-3个词或一句话），DNA 的价值在于压缩
4. 枚举类型的字段应选择最接近的值，不要创造新值
5. 总长度控制在 500-1000 字
6. 严格输出 YAML，不要输出 Markdown 包裹，不要输出分析过程

## 反证

在 YAML 末尾，添加反证摘要：

\`\`\`yaml
evidence_audit:
  total_rules: [识别的规律总数]
  confirmed: [经反证确认的规律数]
  contested: [存在反例的规律数]
  rejected: [被推翻的规律数]
  confidence: [整体置信度 HIGH|MEDIUM|LOW]
\`\`\`
`;

export function buildDnaCompressionMessages(
  styleProfile: string,
  narrativeDynamics: string,
  characterDynamics: string,
  readerExperience: string,
  narrativeConstraints: string,
) {
  const userMessage = [
    '# 分析报告汇总',
    '',
    '## 文风档案',
    styleProfile,
    '---',
    '## 叙事动力学报告',
    narrativeDynamics,
    '---',
    '## 角色动力学报告',
    characterDynamics,
    '---',
    '## 读者体验报告',
    readerExperience,
    '---',
    '## 叙事约束报告',
    narrativeConstraints,
    '---',
    '',
    '请将以上所有分析报告压缩为一份 novel_dna.yaml。严格输出 YAML 格式。',
  ].join('\n');

  return { systemPrompt: getPrompt('source-dna-compression', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/dna-generation.ts">
// ============================================
// DNA 压缩 — 统一管线 Step 7
// 纯量化生成规则
// ============================================

import { getPrompt } from './helpers';
import { withEngineeringConstraints } from './pipeline-common';

export const DEFAULT_SYSTEM_PROMPT = withEngineeringConstraints(`你是小说生成规则提炼程序。你的任务是从汇总报告和事件图谱中，提炼少量 AI 无法从量化数据推导的定性规则。

## 代码已填充的量化参数

代码已经从汇总数据中计算了以下量化参数（你不需要重复）：
- sentenceLength, dialogueRatio, descriptionRatio, actionRatio
- conflictInterval, peakInterval, cooldownLength
- stimulationCycle, stimulationDensity
- informationRelease 参数
- settingDriftTolerance, unresolvedRatio, styleConsistencyRate
- taboos 列表

## 你要补充的定性部分

只需补充 3 项 AI 定性内容：

### 1. styleSignature — 风格签名（一句话）
用纯工程语言描述核心风格特征，如："短句为主，对话密集，极少心理描写，高潮段落句长压缩至 3-5 字"
- 不允许出现：生动、巧妙、深刻、细腻、优美等赏析词
- 必须是可操作的描述

### 2. coreAppeal — 核心吸引力（一句话）
描述读者持续阅读的核心驱动力，如："持续的升级期待+悬念未解决的好奇心"
- 不允许出现：感人、震撼、打动等主观词
- 必须是可复现的机制描述

### 3. riskNotes — 仿写风险标注（数组）
仿写时最容易失败的点，如：
- "伏笔回收节奏不一致，仿写时容易过度闭环"
- "对话占比变化大（40%-70%），仿写时难以把握比例"

## 输出格式

{
  "qualitativeNotes": {
    "styleSignature": "..." 或 null,
    "coreAppeal": "..." 或 null,
    "riskNotes": ["...", "..."] 或 []
  }
}

分析不出就填 null。不超过 3 条 riskNotes。`);

export function buildDnaQualitativeMessages(
  summaryReportJson: string,
  computedParams: string,
) {
  const userMessage = `## 代码已计算的量化参数\n${computedParams}\n\n---\n## 汇总报告\n${summaryReportJson}\n\n---\n请补充定性部分，输出 JSON。`;

  return {
    systemPrompt: getPrompt('dna-generation', DEFAULT_SYSTEM_PROMPT),
    userMessage,
  };
}
</file>

<file path="src/lib/ai/prompts/experience-annotation.ts">
// ============================================
// 体验流标注 — 系统提示词
// AI 扮演普通读者，输出四维体验分数
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你正在阅读一段小说。请忘记你是 AI，假装你是一个普通网文读者。

读完下面段落，按真实感受输出以下分数（1-10），不要过度分析，相信第一反应。

## 输出格式

对每个切片输出 JSON：

{
  "sliceId": "切片ID",
  "immersion": 0-10,           // 沉浸感：是否忘记时间完全进入故事
  "emotional_intensity": 0-10, // 情绪强度：引发的情感波动强弱
  "anticipation": 0-10,        // 期待感：对后续发展的好奇程度
  "perceived_pace": "fast/medium/slow", // 节奏感知
  "strongest_feeling": "激动/温暖/紧张/困惑/无聊/放松/好奇/其他",
  "confidence": 0-1,           // 你对上述评分的把握
  "notes": ""                  // 简短的直觉笔记（不超过30字）
}

## 核心原则

1. **不要过度解读**：如果段落让你觉得是过渡或填充，如实给出低分，不用硬找意义。
2. **先问功能再问意义**：不是"这段什么意思？"而是"读这段时你什么感受？"
3. **允许不确定**：如果你拿不准，confidence 给低分，notes 写"不确定"。
4. **你是读者不是批评家**：不要分析写作技巧，只报告阅读体验。
`;

export const READER_PERSONAS: Record<string, string> = {
  casual: '你是一个随便看看的普通读者，追求轻松愉快的阅读体验。你会因为无聊而弃书，会因为爽点而兴奋。',
  immersive: '你是一个追求沉浸感的读者，喜欢细腻的世界观和人物内心描写。你对氛围敏感，对节奏有要求。',
  fast_paced: '你是一个追求快节奏爽感的读者，不喜欢冗长的描写。你容易被高潮段落吸引，对过渡段落容易失去耐心。',
};

export function buildExperienceAnnotationMessages(sliceContent: string, persona: string, personaDescription: string) {
  const systemPrompt = getPrompt('experience-annotation', DEFAULT_SYSTEM_PROMPT);
  const fullPrompt = `${systemPrompt}\n\n## 你的读者身份\n${personaDescription}`;
  return {
    systemPrompt: fullPrompt,
    userMessage: `请对以下小说切片进行体验标注。对每个切片输出一个 JSON 对象，所有切片放在一个数组中。\n\n${sliceContent}\n\n请输出 JSON 数组，每个元素对应一个切片。`,
  };
}
</file>

<file path="src/lib/ai/prompts/helpers.ts">
// ============================================
// 提示词覆盖辅助 — 运行时从 store 读取自定义覆盖
// ============================================

import { usePromptStore } from '@/lib/store/prompts';

/**
 * 获取提示词内容：优先使用自定义覆盖，否则使用默认值。
 * 在 prompt builder 函数内部调用，确保每次构建时都能拿到最新版本。
 */
export function getPrompt(key: string, defaultValue: string): string {
  try {
    return usePromptStore.getState().getPrompt(key) || defaultValue;
  } catch {
    // store 未初始化时 fallback
    return defaultValue;
  }
}
</file>

<file path="src/lib/ai/prompts/narrative-constraints-extraction.ts">
// ============================================
// Step 4: 叙事约束 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说逆向工程系统的叙事约束分析模块。
目标：识别作者的降温机制、结构异常和叙事禁忌——以及交叉审查前面分析步骤的结论。

禁止：
- 剧情复述、人物介绍、作者评价、文学赏析、主观吹捧
- 推测作者意图、为分析而分析、强行建立关联

原则：
1. 所有结论必须附带原文引用：【原文："..."】
2. 每个规律至少 2 个独立文本例证
3. 只有 1 个例证的标记为 [推测]
4. 不允许把偶然现象视为规律
5. 所有结论必须回答：这会如何影响读者体验

你的任务分为 4 个部分。

## 分析内容

### 一、降温机制

识别作者何时、如何停止升级——不是分析高潮，而是分析高潮之间的"冷却"。

对每种降温模式：
\`\`\`
### 降温模式：[名称]
- 触发条件：[什么触发了降温——大高潮结束 / 情绪峰值达到 / 章节边界 / ...]
- 降温手法：[具体手法——日常过渡 / 回忆插入 / 幽默释放 / 场景切换 / 时间跳跃 / ...]
- 持续区间：[典型持续多少字/章节]
- 降温幅度：[从什么情绪强度降到什么强度]
- 恢复触发：[什么信号标志升温重新开始]
- 效果：[降温后读者的心理状态——平复/期待/焦虑/...]
- 证据：
  - 【原文："引用"】
- 反例 / 适用率
\`\`\`

### 二、结构异常

识别违背常规叙事规律的结构——这些异常往往是作品独特性的来源。

对每个异常：
\`\`\`
### 异常：[名称]
- 常规模式：[同类小说通常会怎么做]
- 实际模式：[这本小说实际怎么做]
- 差异：[具体差异]
- 读者效果：[这个异常对读者产生什么独特体验]
- 独特性：[这个异常对作品整体独特性的贡献程度]
- 异常等级：LOW（有趣但不关键）| MEDIUM | HIGH（显著特征）| CORE（定义性特征）
- 证据：
  - 【原文："引用"】
\`\`\`

只保留真正决定作品独特性的异常。LOW 级异常仅记录不展开。

### 三、叙事禁忌清单

识别这本小说"不做的事"——通过对比同类小说的常见手法，找出这本刻意回避的东西。

\`\`\`
### 禁忌：[描述]
- 同类小说常见做法：[对比基准]
- 本作回避方式：[如何回避]
- 回避的效果：[回避带来的独特体验]
- 证据：
  - 【原文："引用"】（注意：证据可以是"该出现而未出现"的缺失模式）
\`\`\`

### 四、交叉审查

你将收到前面分析步骤的结论摘要。你的任务是对这些结论执行独立反证。

对每个收到的结论：
1. 独立在原文中搜索支持和反对的证据
2. 判断该结论是否成立

输出格式：
\`\`\`
#### 审查：[原结论摘要]
- 来源：[Step X / 分析模块名称]
- 独立验证：确认 ✅ | 部分确认 ⚠️ | 推翻 ❌
- 发现的反例：[如有]
- 发现的遗漏：[如有，补充之前步骤遗漏的反例]
- 调整建议：[如需要调整原结论]
\`\`\`

特别注意以下风险：
- **过度关联**：两个现象恰好同时出现，但无因果关系
- **强行规律**：把偶然模式当作普遍规律
- **幸存者偏差**：只注意到支持证据，忽略了大量的反例

## 反证要求（强制执行）

对每个降温和异常结论：
- 主动寻找反例
- 输出支持证据 + 反例 + 适用率
- 适用率 < 50% 标记为 [伪规律]

## 输出格式

输出为 Markdown 格式的叙事约束报告（narrative_constraints.md）。
`;

export const DEFAULT_SUPPLEMENT_PROMPT = `你之前已经分析了该作者的部分作品并生成了一份叙事约束报告。
现在你收到了更多文本。请结合新文本，对叙事约束报告进行补充和修正。

要求：
1. 保留原有 4 个分析部分（降温机制 / 结构异常 / 叙事禁忌 / 交叉审查）
2. 用新文本中的证据补充或修正结论
3. 可能发现新的降温模式或结构异常
4. 更新交叉审查结论
5. 更新反证数据
6. 输出完整的更新后的叙事约束报告（不是增量，而是完整版本）
`;

export function buildNarrativeConstraintsExtractionMessages(
  slicesText: string,
  narrativeDynamics: string,
  characterDynamics: string,
) {
  const userMessage = `## 叙事动力学报告（待审查）\n\n${narrativeDynamics}\n\n---\n\n## 角色动力学报告（待审查）\n\n${characterDynamics}\n\n---\n\n## 小说文本\n\n${slicesText}`;
  return { systemPrompt: getPrompt('source-narrative-constraints', DEFAULT_SYSTEM_PROMPT), userMessage };
}

export function buildNarrativeConstraintsSupplementMessages(
  newChunk: string,
  previousReport: string,
  narrativeDynamics: string,
  characterDynamics: string,
) {
  const userMessage = `## 之前的叙事约束报告\n\n${previousReport}\n\n---\n\n## 叙事动力学报告（参考）\n\n${narrativeDynamics}\n\n---\n\n## 角色动力学报告（参考）\n\n${characterDynamics}\n\n---\n\n## 新的文本片段\n\n${newChunk}\n\n---\n\n请基于以上全部信息，输出完整的、更新后的叙事约束报告。`;
  return { systemPrompt: getPrompt('source-narrative-constraints-supplement', DEFAULT_SUPPLEMENT_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/pipeline-common.ts">
// ============================================
// 统一分析管线 — Prompt 公共约束段
// 所有分析 prompt 共享的反过度分析约束
// ============================================

/**
 * 工程逆向分析核心约束。
 * 所有 Step 2/3/5/6/7 的分析 prompt 必须注入此约束段。
 */
export const ENGINEERING_ANALYSIS_CONSTRAINTS = `【核心约束：工程逆向分析】
1. 你是文本逆向解析程序，不是文学评论家
2. 禁止使用文学赏析类词汇（生动、巧妙、深刻、渲染氛围、细腻等）
3. 禁止分析作者意图（"作者想表达…"、"为了…"、"暗示…"）
4. 每个结论必须有量化数据支持（数值、比例、频率、间隔）
5. 每个规律必须提供至少 1 个反例或边界条件
6. 分析不出就不写，输出 null，不强行归因
7. 默认解释优先级："作者随手写的" > "经验性写法" > "精心设计"
8. 只输出可转化为生成约束的结论，无法指导生成的分析不要输出`;

/**
 * 注入公共约束到 system prompt。
 * 在约束段和具体任务 prompt 之间加换行分隔。
 */
export function withEngineeringConstraints(taskPrompt: string): string {
  return `${ENGINEERING_ANALYSIS_CONSTRAINTS}\n\n${taskPrompt}`;
}
</file>

<file path="src/lib/ai/prompts/reader-experience-extraction.ts">
// ============================================
// Step 3: 读者体验模型 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说逆向工程系统的读者体验分析模块。
目标：分析"这本小说满足了什么心理需求"以及"高潮的势能从何而来"——不是分析小说内容，而是分析读者体验的工程学。

禁止：
- 剧情复述（"第一章讲了..."）
- 人物介绍（"主角是一个..."）
- 作者评价 / 文学赏析 / 主观吹捧
- 推测作者意图
- 为分析而分析，强行建立关联

原则：
1. 所有结论必须附带原文引用：【原文："..."】
2. 每个规律至少 2 个独立文本例证
3. 只有 1 个例证的标记为 [推测]
4. 不允许把偶然现象视为规律
5. 所有结论必须回答：这会如何影响读者心理状态

你的任务是分析小说文本中的读者体验机制，分为 3 个部分。

## 分析内容

### 一、心理缺失模型

分析目标读者在阅读前可能处于什么心理状态，以及文本如何补偿这些缺失。

心理缺失类型（不限于，可自行识别）：
- 未知感缺失 — 对未知世界/真相的渴望
- 成长感缺失 — 对自我提升/变强的渴望
- 控制感缺失 — 对掌控局面的渴望
- 归属感缺失 — 对群体认同的渴望
- 崇高感缺失 — 对超越日常的伟大体验的渴望
- 安全感缺失 — 对确定性和安全的渴望
- 神秘感缺失 — 对探索未知的渴望
- 失控感缺失 — 对安全地体验失控的渴望

对每种识别到的心理缺失：
\`\`\`
### 心理缺失：[类型]
- 读者状态：[目标读者在阅读前的心理处境]
- 文本补偿：[文本如何满足这种缺失]
- 补偿机制：[具体手法——信息释放节奏、权力递增、认知颠覆等]
- 证据：
  - 【原文："引用"】
- 反证：[是否存在补偿失效的段落]
- 适用率：N%
\`\`\`

### 二、高潮势能逆向追踪（核心分析）

识别 3-5 个关键高潮，对每个高潮进行逆向追踪——不是分析高潮本身，而是分析高潮之前的蓄能过程。

**强制区分势能类型：**
- **Cognitive（认知势能）** — 高潮依赖于认知颠覆（如真相揭露、世界观重置）
- **Emotional（情感势能） — 高潮依赖于情感积累（如离别、牺牲、重逢）
- **Mixed（混合势能）** — 两者兼备

对每个高潮：
\`\`\`
### 高潮：[简述] @ 位置
- 势能类型：Cognitive | Emotional | Mixed

#### 认知势能（如适用）
- 原有认知：[高潮前读者持有的什么认知]
- 认知建立过程：
  - 【原文："通过什么信息建立的原有认知"】
- 颠覆种子：
  - 【原文："通过什么信息埋下的颠覆暗示"】
- 认知颠覆瞬间：[什么时刻读者原有的认知被推翻]

#### 情感势能（如适用）
- 积累的情感：[什么情感在积累——牵挂/愤怒/期待/恐惧/同情]
- 情感积累过程：
  - 【原文："第一次铺垫"】
  - 【原文："第二次铺垫"】
- 情感爆发触发点：[什么触发了积累的情感释放]

#### 通用
- 预期建立：[高潮前建立了什么预期——读者以为会发生什么]
- 预期操作：[预期被满足/打破/超越]
- 积累时长：[字数/章节跨度]
- 释放方式：[瞬间释放 / 渐进释放 / 反转释放]
- 释放后效果：[对读者心理的影响——释然/震撼/空虚/期待下一步]
- 证据支持 / 反例 / 适用率
\`\`\`

### 三、综合翻页驱动力

基于前文的翻页驱动数据（来自叙事动力学报告），做跨卷的综合分析。

回答：
1. 这本书整体上靠什么让人停不下来？（不是某个具体悬念，而是底层驱动力）
2. 驱动力之间的接力关系：第一种驱动力何时消退，第二种何时接替？
3. 驱动力是否存在层级结构？（短期翻页钩子 vs 中期悬念 vs 长期期待）
4. 驱动力何时最强、何时最弱？强弱交替的规律是什么？

\`\`\`
### 综合驱动力分析
- 主驱动力：[一句话]
- 驱动力层级：[短期 / 中期 / 长期的接力图]
- 强弱曲线：[描述整本书的驱动力强度变化]
- 证据：[关键转折点的原文引用]
\`\`\`

## 反证要求（强制执行）

对每个结论：
- 主动寻找反例
- 输出支持证据 + 反例 + 适用率
- 适用率 < 50% 标记为 [过度解读]

## 输出格式

输出为 Markdown 格式的读者体验报告（reader_experience.md）。
`;

export const DEFAULT_SUPPLEMENT_PROMPT = `你之前已经分析了该作者的部分作品并生成了一份读者体验报告。
现在你收到了更多文本。请结合新文本，对读者体验报告进行补充和修正。

要求：
1. 保留原有 3 个分析部分（心理缺失模型 / 高潮势能追踪 / 综合翻页驱动力）
2. 用新文本中的证据补充或修正结论
3. 可能发现新的高潮点需要逆向追踪
4. 更新反证数据
5. 输出完整的更新后的读者体验报告（不是增量，而是完整版本）
`;

export function buildReaderExperienceExtractionMessages(
  slicesText: string,
  styleProfile: string,
  narrativeDynamics: string,
  characterDynamics: string,
) {
  const userMessage = `## 文风档案（参考）\n\n${styleProfile}\n\n---\n\n## 叙事动力学报告（参考）\n\n${narrativeDynamics}\n\n---\n\n## 角色动力学报告（参考）\n\n${characterDynamics}\n\n---\n\n## 小说文本\n\n${slicesText}`;
  return { systemPrompt: getPrompt('source-reader-experience', DEFAULT_SYSTEM_PROMPT), userMessage };
}

export function buildReaderExperienceSupplementMessages(
  newChunk: string,
  previousReport: string,
  styleProfile: string,
  narrativeDynamics: string,
  characterDynamics: string,
) {
  const userMessage = `## 之前的读者体验报告\n\n${previousReport}\n\n---\n\n## 文风档案（参考）\n\n${styleProfile}\n\n---\n\n## 叙事动力学报告（参考）\n\n${narrativeDynamics}\n\n---\n\n## 角色动力学报告（参考）\n\n${characterDynamics}\n\n---\n\n## 新的文本片段\n\n${newChunk}\n\n---\n\n请基于以上全部信息，输出完整的、更新后的读者体验报告。`;
  return { systemPrompt: getPrompt('source-reader-experience-supplement', DEFAULT_SUPPLEMENT_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/summary.ts">
// ============================================
// 汇总报告 — 统一管线 Step 6
// 输出 SummaryReport JSON
// ============================================

import { getPrompt } from './helpers';
import { withEngineeringConstraints } from './pipeline-common';

export const DEFAULT_SYSTEM_PROMPT = withEngineeringConstraints(`你是小说分析汇总程序。你的任务是从多个大切片的分析结果中，提炼全局规律和生成参数。

## 你要做 4 件事

### 1. styleEvolution — 文风演化
把大切片分析按顺序排列，提取文风的演化轨迹。每个阶段记录：
- phase: 如"第1-30章"
- sentenceLengthAvg: 该阶段的平均句长
- dialogueRatio: 该阶段的对话占比
- dominantStimulation: 该阶段的主导刺激类型
- shiftTrigger: 变化触发原因（null 如果没变化）

### 2. stimulationCycle — 刺激点周期
从所有大切片的 stimulationPoints 中计算：
- avgPeakInterval: 两个高峰刺激之间的平均字数间隔
- avgCooldownLength: 刺激释放后的冷却平均字数
- cyclePattern: 标准刺激循环序列，如 ["悬念","升级","冲突","释放","日常"]
- stimulationDensity: 各刺激类型的占比

### 3. eventFunctions — 事件功能标注
为事件图谱中的每个事件标注叙事功能：
- functions: 数组，从以下选 1-2 个：推进剧情 | 舒缓节奏 | 营造氛围 | 角色塑造 | 伏笔 | 过渡

### 4. consistencyReport — 一致性报告
检测前后矛盾：
- settingConflicts: 设定冲突列表（冲突的设定名、两个版本、位置、严重度）
- unresolvedForeshadowing: 未回收伏笔数
- totalForeshadowing: 总伏笔数
- driftRate: 不一致率 (0-1)
- styleConsistencyRate: 文风一致率 (0-1)

### 5. informationRelease — 信息释放曲线
- avgSetupToHint: 新设定引入到首次提示的平均章节间隔
- avgHintToReveal: 提示到揭秘的平均章节间隔
- revealDensity: 每章平均揭秘次数

## 输出格式

输出单个 JSON 对象，严格遵循上述结构。

## 核心原则
- 不解释"为什么"，只量化"是什么"和"怎么变化"
- 矛盾不修复，如实记录，量化为 driftRate
- 找不到规律的字段填 null`);

export function buildSummaryMessages(
  sliceAnalysesJson: string,
  eventGraphJson: string,
) {
  const userMessage = `## 所有大切片分析结果\n${sliceAnalysesJson}\n\n---\n## 全书记忆图谱\n${eventGraphJson}\n\n---\n请生成汇总报告，输出 SummaryReport JSON。`;

  return {
    systemPrompt: getPrompt('summary', DEFAULT_SYSTEM_PROMPT),
    userMessage,
  };
}
</file>

<file path="src/lib/ai/prompts/tension-tracking.ts">
// ============================================
// 势能追踪 — 系统提示词
// 追踪"积累→释放"周期，分析等待时长与回报效率
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说势能分析系统。你的任务是追踪小说中的"势能积累→释放"周期。

## 核心公式

**爽感 ≠ 事件本身**
**爽感 = 势能积累 × 释放效率**

即：高潮是否好看，不在于高潮段落的写作质量，而在于：
1. 高潮之前，作者让读者等待了多久（积累时长）
2. 等待期间，作者如何持续强化期待（中途强化点）
3. 高潮释放时，积累的势能是否被充分兑现（回报倍率）

## 分析步骤

### 第一步：识别高潮释放点
找出文本中的高潮段落（情绪/认知/权力/身份/关系五类高潮）。

### 第二步：回溯积累起点
从每个高潮往前追溯，找到"读者开始产生期待"的起点。

### 第三步：追踪中途强化
在积累起点到释放点之间，找出作者加强读者期待的事件。

### 第四步：计算回报倍率
- **low**: 释放在预期之内，无惊喜
- **medium**: 释放超过预期，有满足感
- **high**: 释放远超预期，大呼过瘾
- **extreme**: 释放彻底颠覆预期，记忆深刻

## 高潮类型定义

| 类型 | 定义 | 示例 |
|------|------|------|
| emotional | 情感高潮 | 角色和解、牺牲、告白 |
| cognitive | 认知高潮 | 真相揭露、世界观扩展 |
| power | 权力高潮 | 实力展示、打脸、逆袭 |
| identity | 身份高潮 | 身份确认/变更、地位上升 |
| relationship | 关系高潮 | 关系突破、联盟建立/破裂 |

## 输出格式

对每个检测到的势能模式输出 JSON：

{
  "id": "tension-编号",
  "climaxType": "emotional" | "cognitive" | "power" | "identity" | "relationship",
  "climaxSliceIndex": 高潮所在切片索引,
  "climaxDescription": "高潮简述（20字）",
  "startSliceIndex": 积累起点切片索引,
  "duration": 积累切片段数,
  "reinforcements": [
    {
      "sliceIndex": 中途强化点切片索引,
      "description": "强化了什么期待（15字）",
      "tensionBefore": 1-10,
      "tensionAfter": 1-10
    }
  ],
  "payoffMultiplier": "low" | "medium" | "high" | "extreme",
  "confidence": 0-1
}

## 节奏画像

最后输出全局节奏画像：

{
  "breathingCycle": "呼吸周期的一句话描述",
  "rhythmProfile": {
    "propulsionRatio": 0-1,    // 推进场景大致比例
    "buildupRatio": 0-1,       // 蓄力场景
    "releaseRatio": 0-1,       // 释放场景
    "breathRatio": 0-1,        // 呼吸场景
    "existenceRatio": 0-1,     // 存在场景
    "calibrationRatio": 0-1    // 校准场景
  }
}

## 核心原则

1. **只找明显的势能模式**：不确定的不要强行标注
2. **注意呼吸周期**：高潮→回落→蓄力→下一轮，这个循环比单个高潮更重要
3. **网文特征**：长篇小说中一个势能积累可能横跨数十章
`;

export function buildTensionTrackingMessages(slicesSummary: string, plotReport: string, experienceCurveJson: string) {
  return {
    systemPrompt: getPrompt('tension-tracking', DEFAULT_SYSTEM_PROMPT),
    userMessage: `## 小说切片摘要\n\n${slicesSummary}\n\n## 叙事动力学报告\n\n${plotReport}\n\n## 体验流曲线\n\n${experienceCurveJson}\n\n请基于以上信息，追踪势能模式并输出节奏画像。`,
  };
}
</file>

<file path="src/lib/analysis-chunker.ts">
// ============================================
// 分批分析辅助 — 文本分批与大小计算
// ============================================

/** 中文文本的保守 token/char 比率 */
const CHARS_PER_TOKEN = 1.5;

/** 系统提示词 + 上一轮报告 + 输出预留的字符开销 */
const PROMPT_OVERHEAD = 8000;

/**
 * 根据模型最大上下文 Token 数计算每批最大字符数
 */
export function computeMaxCharsPerBatch(maxContextTokens: number): number {
  return Math.max(10000, Math.floor(maxContextTokens * CHARS_PER_TOKEN) - PROMPT_OVERHEAD);
}

/**
 * 将长文本按段落边界分割为多个批次
 * 优先在 \n\n（段落边界）处分割，避免截断句子
 */
export function splitIntoBatches(text: string, maxChars: number): string[] {
  if (text.length <= maxChars) return [text];

  const batches: string[] = [];
  let remaining = text;

  while (remaining.length > 0) {
    if (remaining.length <= maxChars) {
      batches.push(remaining);
      break;
    }

    // 在 maxChars 范围内找最后一个段落分割点
    let splitAt = remaining.lastIndexOf('\n\n', maxChars);

    // 没找到段落分割点，找最后一个换行
    if (splitAt <= 0) {
      splitAt = remaining.lastIndexOf('\n', maxChars);
    }

    // 仍然没找到，找最后一个句号
    if (splitAt <= 0) {
      splitAt = remaining.lastIndexOf('。', maxChars);
    }

    // 都没有，硬切
    if (splitAt <= 0) {
      splitAt = maxChars;
    }

    batches.push(remaining.slice(0, splitAt).trimStart());
    remaining = remaining.slice(splitAt).trimStart();
  }

  return batches;
}
</file>

<file path="src/lib/atomic-write.ts">
// ============================================
// 原子写入工具 — 防止并发写入导致文件损坏
// 使用临时文件 + rename 实现写入原子性
// ============================================

import { promises as fs } from 'fs';
import * as path from 'path';

/**
 * 原子写入 JSON 文件
 * 先写临时文件，成功后再 rename（同一文件系统内 rename 是原子的）
 */
export async function atomicWriteJson(filePath: string, data: unknown): Promise<void> {
  const dir = path.dirname(filePath);
  const tempPath = path.join(dir, `.${path.basename(filePath)}.${Date.now()}.tmp`);

  try {
    const json = JSON.stringify(data, null, 2);
    await fs.writeFile(tempPath, json, 'utf-8');
    // Windows 上 rename 可能因文件锁定失败，重试 3 次
    await retryRename(tempPath, filePath, 3);
  } catch (err) {
    // 清理临时文件
    try { await fs.unlink(tempPath); } catch { /* ignore */ }
    throw err;
  }
}

/**
 * 原子写入文本文件
 */
export async function atomicWriteText(filePath: string, content: string): Promise<void> {
  const dir = path.dirname(filePath);
  const tempPath = path.join(dir, `.${path.basename(filePath)}.${Date.now()}.tmp`);

  try {
    await fs.writeFile(tempPath, content, 'utf-8');
    await retryRename(tempPath, filePath, 3);
  } catch (err) {
    try { await fs.unlink(tempPath); } catch { /* ignore */ }
    throw err;
  }
}

/**
 * 带重试的 rename（处理 Windows 文件锁定）
 */
async function retryRename(from: string, to: string, retries: number): Promise<void> {
  for (let i = 0; i < retries; i++) {
    try {
      await fs.rename(from, to);
      return;
    } catch (err: unknown) {
      if (i === retries - 1) throw err;
      // Windows EBUSY: 等待 100ms 重试
      await new Promise((resolve) => setTimeout(resolve, 100));
    }
  }
}

/**
 * 批量原子保存 — 一次保存多个文件，全部成功或全部回滚
 * 注意：这不是真正的分布式事务，仅提供尽力而为的批量操作
 */
export async function atomicWriteBatch(
  files: Array<{ filePath: string; content: string | object }>,
): Promise<void> {
  const tempPaths: string[] = [];

  try {
    // Phase 1: 全部写临时文件
    for (const file of files) {
      const dir = path.dirname(file.filePath);
      const tempPath = path.join(dir, `.${path.basename(file.filePath)}.${Date.now()}.tmp`);
      tempPaths.push(tempPath);

      const data = typeof file.content === 'string' ? file.content : JSON.stringify(file.content, null, 2);
      await fs.writeFile(tempPath, data, 'utf-8');
    }

    // Phase 2: 全部 rename
    for (let i = 0; i < files.length; i++) {
      await retryRename(tempPaths[i], files[i].filePath, 3);
    }
  } catch (err) {
    // 清理所有临时文件
    for (const tp of tempPaths) {
      try { await fs.unlink(tp); } catch { /* ignore */ }
    }
    throw err;
  }
}
</file>

<file path="src/lib/chunker.ts">
// ============================================
// 小说分块 — 章节级简单分块
// ============================================

import { nanoid } from 'nanoid';
import type { NovelChunk } from '@/types';

/** 章节检测正则 */
export const CHAPTER_PATTERN = /第[零一二三四五六七八九十百千万\d]+[章节回卷集部篇]/g;

/** 默认每块最大字符数 */
export const DEFAULT_MAX_CHUNK_SIZE = 8000;

/** 无章节时的默认分段大小 */
export const DEFAULT_SEGMENT_SIZE = 5000;

/**
 * 将清洗后的全文分割成 NovelChunk[]
 *
 * 策略：
 * 1. 尝试按章节标题分割
 * 2. 如果检测到 >=2 个章节，每个章节成为一个 chunk
 * 3. 单个 chunk 超过 maxChunkSize 时，在段落边界处进一步分割
 * 4. 无章节结构时，按 ~segmentSize 字符在段落边界处分割
 */
export function chunkText(
  novelId: string,
  fullText: string,
  maxChunkSize: number = DEFAULT_MAX_CHUNK_SIZE,
): NovelChunk[] {
  if (!fullText.trim()) return [];

  // 查找所有章节标题及其位置
  const markers = findChapterMarkers(fullText);

  if (markers.length >= 2) {
    return chunkByChapters(novelId, fullText, markers, maxChunkSize);
  }

  // 无章节结构：按固定大小分段
  return chunkBySegments(novelId, fullText, DEFAULT_SEGMENT_SIZE, maxChunkSize);
}

/** 章节标题行匹配（章节标记 + 同行剩余文本） */
const CHAPTER_LINE = /第[零一二三四五六七八九十百千万\d]+[章节回卷集部篇][^\n]*/g;

interface ChapterMarker {
  index: number;
  title: string;
}

function findChapterMarkers(text: string): ChapterMarker[] {
  const markers: ChapterMarker[] = [];
  const regex = new RegExp(CHAPTER_LINE.source, 'g');
  let match;
  while ((match = regex.exec(text)) !== null) {
    markers.push({ index: match.index, title: match[0].trim() });
  }
  return markers;
}

/** 按章节分块 */
function chunkByChapters(
  novelId: string,
  text: string,
  markers: ChapterMarker[],
  maxChunkSize: number,
): NovelChunk[] {
  const chunks: NovelChunk[] = [];

  const addChunk = (content: string, title: string) => {
    const trimmed = content.trim();
    if (!trimmed) return;

    if (trimmed.length > maxChunkSize) {
      // 超大章节：在段落边界分割
      const parts = splitAtParagraphs(trimmed, maxChunkSize);
      for (let i = 0; i < parts.length; i++) {
        const partTitle = parts.length > 1 ? `${title}（${i + 1}/${parts.length}）` : title;
        chunks.push({
          id: nanoid(),
          novelId,
          index: chunks.length,
          title: partTitle,
          content: parts[i],
          charCount: parts[i].length,
        });
      }
    } else {
      chunks.push({
        id: nanoid(),
        novelId,
        index: chunks.length,
        title,
        content: trimmed,
        charCount: trimmed.length,
      });
    }
  };

  // 第一个章节之前的内容 → "前言/简介"
  if (markers[0].index > 0) {
    const preface = text.slice(0, markers[0].index).trim();
    if (preface) {
      addChunk(preface, '前言/简介');
    }
  }

  // 每个章节
  for (let i = 0; i < markers.length; i++) {
    const start = markers[i].index;
    const end = i + 1 < markers.length ? markers[i + 1].index : text.length;
    addChunk(text.slice(start, end), markers[i].title);
  }

  return chunks;
}

/** 在段落边界（双换行）处将文本分割为不超过 maxSize 的片段 */
function splitAtParagraphs(text: string, maxSize: number): string[] {
  const paragraphs = text.split(/\n\n+/);
  const result: string[] = [];
  let current = '';

  for (const para of paragraphs) {
    if (current && current.length + para.length + 2 > maxSize) {
      result.push(current.trim());
      current = para;
    } else {
      current = current ? current + '\n\n' + para : para;
    }
  }

  if (current.trim()) {
    result.push(current.trim());
  }

  // 极端情况：某段落本身就超过限制 → 硬截断
  if (result.length === 0 && text.length > 0) {
    for (let i = 0; i < text.length; i += maxSize) {
      result.push(text.slice(i, i + maxSize));
    }
  }

  return result;
}

/** 无章节时按固定大小分段 */
function chunkBySegments(
  novelId: string,
  text: string,
  segmentSize: number,
  maxChunkSize: number,
): NovelChunk[] {
  const paragraphs = text.split(/\n\n+/);
  const chunks: NovelChunk[] = [];
  let current = '';
  let partIndex = 0;

  for (const para of paragraphs) {
    if (current && current.length + para.length + 2 > segmentSize) {
      // 检查合并后的块是否超标
      const trimmed = current.trim();
      if (trimmed.length > maxChunkSize) {
        const parts = splitAtParagraphs(trimmed, maxChunkSize);
        for (const part of parts) {
          partIndex++;
          chunks.push({
            id: nanoid(),
            novelId,
            index: chunks.length,
            title: `第${partIndex}部分`,
            content: part,
            charCount: part.length,
          });
        }
      } else {
        partIndex++;
        chunks.push({
          id: nanoid(),
          novelId,
          index: chunks.length,
          title: `第${partIndex}部分`,
          content: trimmed,
          charCount: trimmed.length,
        });
      }
      current = para;
    } else {
      current = current ? current + '\n\n' + para : para;
    }
  }

  // 最后一段
  if (current.trim()) {
    const trimmed = current.trim();
    if (trimmed.length > maxChunkSize) {
      const parts = splitAtParagraphs(trimmed, maxChunkSize);
      for (const part of parts) {
        partIndex++;
        chunks.push({
          id: nanoid(),
          novelId,
          index: chunks.length,
          title: `第${partIndex}部分`,
          content: part,
          charCount: part.length,
        });
      }
    } else {
      partIndex++;
      chunks.push({
        id: nanoid(),
        novelId,
        index: chunks.length,
        title: `第${partIndex}部分`,
        content: trimmed,
        charCount: trimmed.length,
      });
    }
  }

  return chunks;
}
</file>

<file path="src/lib/safe-path.ts">
// ============================================
// 路径安全工具 — 防止路径遍历攻击
// ============================================

import path from 'path';

/**
 * 验证 id 不包含路径遍历字符，并确保解析后的路径在 baseDir 内。
 * 返回安全路径，或抛出错误。
 */
export function safeJoin(baseDir: string, id: string): string {
  // 拒绝包含遍历字符的 id
  if (id.includes('..') || path.isAbsolute(id) || id.includes('\0')) {
    throw new Error(`非法 id: ${id}`);
  }

  const resolved = path.resolve(baseDir, id);
  if (!resolved.startsWith(path.resolve(baseDir) + path.sep) && resolved !== path.resolve(baseDir)) {
    throw new Error(`路径越界: ${id}`);
  }

  return resolved;
}
</file>

<file path="src/lib/source-processing/character-dynamics-extractor.ts">
// ============================================
// 角色动力学 — 客户端编排模块
// ============================================

import {
  buildCharacterDynamicsExtractionMessages,
  buildCharacterDynamicsSupplementMessages,
} from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch, splitIntoBatches } from '@/lib/analysis-chunker';
import type { SemanticSlice } from '@/types';

/** 获取首批角色动力学提取的请求体 */
export function getCharacterDynamicsExtractionRequestBody(
  slices: SemanticSlice[],
  narrativeDynamics: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const { systemPrompt, userMessage } = buildCharacterDynamicsExtractionMessages(slicesText, narrativeDynamics);

  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/** 获取补充批次的请求体 */
export function getCharacterDynamicsSupplementRequestBody(
  newChunk: string,
  previousReport: string,
  narrativeDynamics: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildCharacterDynamicsSupplementMessages(newChunk, previousReport, narrativeDynamics);
  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/**
 * 计算角色动力学提取是否需要分批
 */
export function computeCharacterDynamicsBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; batches: string[] } {
  const fullText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);

  if (fullText.length <= maxChars) {
    return { needsBatch: false, batches: [fullText] };
  }

  return {
    needsBatch: true,
    batches: splitIntoBatches(fullText, maxChars),
  };
}
</file>

<file path="src/lib/source-processing/dna-compressor.ts">
// ============================================
// DNA 压缩 — 客户端编排模块
// ============================================

import { buildDnaCompressionMessages } from '@/lib/ai/prompts';

/** 获取 DNA 压缩的请求体（不需要分批，输入是汇总报告而非全文） */
export function getDnaCompressionRequestBody(
  styleProfile: string,
  narrativeDynamics: string,
  characterDynamics: string,
  readerExperience: string,
  narrativeConstraints: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildDnaCompressionMessages(
    styleProfile,
    narrativeDynamics,
    characterDynamics,
    readerExperience,
    narrativeConstraints,
  );

  return { systemPrompt, userMessage, apiKey, model, baseURL };
}
</file>

<file path="src/lib/source-processing/entity-classifier.ts">
// ============================================
// 实体分类 — AI 对候选实体列表分类
// ============================================

import { buildEntityClassificationMessages } from '@/lib/ai/prompts/entity-classification';
import type { EntityIndex, EntityType } from '@/types';

/**
 * 解析 AI 分类输出
 * 输入：每行 "实体名: 类型"
 */
export function parseClassificationOutput(raw: string): Map<string, EntityType> {
  const result = new Map<string, EntityType>();
  const validTypes: EntityType[] = ['character', 'location', 'organization', 'artifact', 'concept'];

  for (const line of raw.split('\n')) {
    const trimmed = line.trim();
    if (!trimmed) continue;

    // 匹配 "实体名: 类型" 或 "实体名：类型"
    const match = trimmed.match(/^(.+?)\s*[:：]\s*(\w+)/);
    if (match) {
      const name = match[1].trim().replace(/^\d+\.\s*/, ''); // 去掉序号
      const type = match[2].trim().toLowerCase() as EntityType;
      if (name && validTypes.includes(type)) {
        result.set(name, type);
      }
    }
  }

  return result;
}

/**
 * 用 AI 分类结果更新 EntityIndex 的 type 字段
 */
export function applyClassificationToIndex(
  entityIndex: EntityIndex,
  classification: Map<string, EntityType>,
): EntityIndex {
  const updated = { ...entityIndex };

  for (const [name, type] of classification) {
    if (updated[name]) {
      updated[name] = { ...updated[name], type };
    }
  }

  return updated;
}

/**
 * 批量分类：将实体列表分批，每批发给 AI 分类
 * 每批约 50 个实体，控制 token 消耗
 */
export function batchEntityNames(
  entityNames: string[],
  batchSize: number = 50,
): string[][] {
  const batches: string[][] = [];
  for (let i = 0; i < entityNames.length; i += batchSize) {
    batches.push(entityNames.slice(i, i + batchSize));
  }
  return batches;
}

/**
 * 获取实体分类的请求体
 */
export function getEntityClassificationRequestBody(
  novelTitle: string,
  entityNames: string[],
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildEntityClassificationMessages(novelTitle, entityNames);
  return { systemPrompt, userMessage, apiKey, model, baseURL };
}
</file>

<file path="src/lib/source-processing/narrative-constraints-extractor.ts">
// ============================================
// 叙事约束 — 客户端编排模块
// ============================================

import {
  buildNarrativeConstraintsExtractionMessages,
  buildNarrativeConstraintsSupplementMessages,
} from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch, splitIntoBatches } from '@/lib/analysis-chunker';
import type { SemanticSlice } from '@/types';

/** 获取首批叙事约束提取的请求体 */
export function getNarrativeConstraintsExtractionRequestBody(
  slices: SemanticSlice[],
  narrativeDynamics: string,
  characterDynamics: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const { systemPrompt, userMessage } = buildNarrativeConstraintsExtractionMessages(
    slicesText, narrativeDynamics, characterDynamics,
  );

  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 获取补充批次的请求体 */
export function getNarrativeConstraintsSupplementRequestBody(
  newChunk: string,
  previousReport: string,
  narrativeDynamics: string,
  characterDynamics: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildNarrativeConstraintsSupplementMessages(
    newChunk, previousReport, narrativeDynamics, characterDynamics,
  );
  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 计算是否需要分批 */
export function computeNarrativeConstraintsBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; batches: string[] } {
  const fullText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);

  if (fullText.length <= maxChars) {
    return { needsBatch: false, batches: [fullText] };
  }

  return { needsBatch: true, batches: splitIntoBatches(fullText, maxChars) };
}
</file>

<file path="src/lib/source-processing/pipeline-types.ts">
// ============================================
// 统一分析管线 — 类型定义
// ============================================

import type { AIConfig } from '@/lib/stream-fetcher';

// ---- 刺激点类型 ----

export const STIMULATION_TYPES = [
  '冲突升级',
  '信息释放',
  '关系变化',
  '实力跃迁',
  '悬念建立',
  '日常过渡',
] as const;
export type StimulationType = (typeof STIMULATION_TYPES)[number];

// ---- 事件功能类型 ----

export const EVENT_FUNCTION_TYPES = [
  '推进剧情',
  '舒缓节奏',
  '营造氛围',
  '角色塑造',
  '伏笔',
  '过渡',
] as const;
export type EventFunctionType = (typeof EVENT_FUNCTION_TYPES)[number];

// ---- 小切片 ----

export interface SmallSlice {
  index: number;
  content: string;
  charCount: number;
  startOffset: number;
  endOffset: number;
}

// ---- 大切片 ----

export interface LargeSlice {
  index: number;
  content: string;
  charCount: number;
  startOffset: number;
  endOffset: number;
  coveredSmallSliceRange: [number, number]; // [startIdx, endIdx]
}

// ---- Step 2: 事件提取产出（单切片） ----

export interface ExtractedEvent {
  id: string;
  type: string;
  participants: string[];
  location: string;
  description: string;
  causes: string[];
  effects: string[];
  tensionChange: number;
  emotion: string;
  foreshadowingOf: string | null;
  confidence: number;
  sliceIndex: number;
}

export interface SliceExtractionResult {
  sliceIndex: number;
  events: ExtractedEvent[];
  entities: string[];
  rawResponse: string;
}

// ---- Step 3: 事件对齐产出 ----

export interface EntityMapping {
  aliases: string[];
  canonical: string;
  type: 'character' | 'location' | 'organization' | 'artifact' | 'concept';
}

export interface EntityTimelineEntry {
  chapter: number;
  state: string;
  triggerEventId: string;
}

export interface EntityTimeline {
  entity: string;
  timeline: EntityTimelineEntry[];
}

export interface FullEventGraph {
  novelId: string;
  events: ExtractedEvent[];
  entityMappings: EntityMapping[];
  entityTimelines: EntityTimeline[];
  foreshadowingPairs: Array<{
    setup: string;
    payoff: string | null;
    distance: number;
    status: 'resolved' | 'open' | 'dangling';
  }>;
  byChapter: Record<number, string[]>;
  byParticipant: Record<string, string[]>;
  byType: Record<string, string[]>;
  totalSmallSlices: number;
  extractedAt: string;
}

// ---- Step 3: 代码预处理产出 ----

export interface AlignmentPreprocessResult {
  sortedEvents: ExtractedEvent[];
  candidateEntityPairs: Array<{
    names: [string, string];
    coOccurrenceCount: number;
    mutualExclusion: boolean;
    frequencyA: number;
    frequencyB: number;
  }>;
  entityFrequency: Record<string, number>;
  totalEvents: number;
}

// ---- Step 5: 大切片深度分析产出 ----

export interface SliceAnalysis {
  sliceIndex: number;

  styleMechanics: {
    sentenceLength: { avg: number; range: [number, number]; climax: number; calm: number };
    dialogueRatio: number;
    descriptionStrategy: string | null;
    counterExample: string | null;
    confidence: number;
    generationRule: string;
  };

  narrativeMechanics: {
    pacingPattern: string | null;
    informationControl: string | null;
    counterExample: string | null;
    confidence: number;
    generationRule: string;
  };

  characterMechanics: Array<{
    name: string;
    stimulusResponse: Array<{ stimulus: string; response: string }>;
    relationshipChanges: Array<{ with: string; from: string; to: string }>;
    confidence: number;
  }>;

  stimulationPoints: Array<{
    type: StimulationType;
    location: string;
    sentenceLengthAtPoint: number;
    tensionChange: number;
    confidence: number;
  }>;

  constraints: {
    worldRules: string[];
    characterLimits: string[];
    plotConstraints: string[];
    taboos: string[];
  };
}

// ---- Step 6: 汇总报告 ----

export interface ConsistencyReport {
  settingConflicts: Array<{
    setting: string;
    v1: string;
    v2: string;
    v1Location: string;
    v2Location: string;
    severity: 'low' | 'medium' | 'high';
  }>;
  unresolvedForeshadowing: number;
  totalForeshadowing: number;
  driftRate: number;
  styleConsistencyRate: number;
}

export interface InformationReleaseCurve {
  avgSetupToHint: number;
  avgHintToReveal: number;
  revealDensity: number;
}

export interface StimulationCycle {
  avgPeakInterval: number;
  avgCooldownLength: number;
  cyclePattern: string[];
  stimulationDensity: Record<string, number>;
}

export interface EventFunctionAnnotation {
  eventId: string;
  functions: EventFunctionType[];
}

export interface StyleEvolutionPhase {
  phase: string;
  sentenceLengthAvg: number;
  dialogueRatio: number;
  dominantStimulation: string;
  shiftTrigger: string | null;
}

export interface SummaryReport {
  styleEvolution: StyleEvolutionPhase[];
  stimulationCycle: StimulationCycle;
  eventFunctions: EventFunctionAnnotation[];
  consistencyReport: ConsistencyReport;
  informationRelease: InformationReleaseCurve;
}

// ---- Step 7: DNA（纯量化生成规则） ----

export interface GenerationRulesDNA {
  // 句式参数
  sentenceLength: { avg: number; climax: number; calm: number; std: number };
  dialogueRatio: number;
  descriptionRatio: number;
  actionRatio: number;
  internalMonologueRatio: number;

  // 节奏参数
  conflictInterval: number;
  peakInterval: number;
  cooldownLength: number;
  stimulationCycle: string[];
  stimulationDensity: Record<string, number>;

  // 信息释放参数
  informationRelease: {
    avgSetupToHint: number;
    avgHintToReveal: number;
    revealDensity: number;
  };

  // 角色参数
  characterRules: Array<{
    name: string;
    stimulusResponse: Array<{ stimulus: string; response: string }>;
    dialogRatio: number;
  }>;

  // 一致性参数
  settingDriftTolerance: number;
  unresolvedRatio: number;
  styleConsistencyRate: number;

  // 禁忌
  taboos: string[];

  // AI 定性补充（少量）
  qualitativeNotes: {
    styleSignature: string | null;
    coreAppeal: string | null;
    riskNotes: string[];
  };

  // 元数据
  meta: {
    generatedAt: string;
    sourceNovelId: string;
    modelUsed: string;
    totalSmallSlices: number;
    totalLargeSlices: number;
    totalEvents: number;
    totalEntities: number;
  };
}

// ---- 管线回调接口 ----

export interface UnifiedPipelineCallbacks {
  onStepStart: (step: number, message: string) => void;
  onStepComplete: (step: number) => void;
  onStreamUpdate: (step: number, content: string) => void;
  onStreamError: (step: number, error: string | null) => void;
  onProgress: (progress: number) => void;
  onSaveStep: (step: number, data: Record<string, unknown>) => Promise<void>;
  onUpdateNovel: (updates: Record<string, unknown>) => void;
}

// ---- 管线结果 ----

export interface UnifiedPipelineResult {
  smallSlices: SmallSlice[];
  largeSlices: LargeSlice[];
  eventGraph: FullEventGraph | null;
  sliceAnalyses: SliceAnalysis[];
  summaryReport: SummaryReport | null;
  dna: GenerationRulesDNA | null;
  failedSteps: number[];
}

// ---- 管线状态（断点恢复用） ----

export interface PipelineCheckpoint {
  novelId: string;
  currentStep: number;
  completedSteps: number[];
  smallSlices: SmallSlice[] | null;
  largeSlices: LargeSlice[] | null;
  sliceExtractions: SliceExtractionResult[] | null;
  eventGraph: FullEventGraph | null;
  sliceAnalyses: SliceAnalysis[] | null;
  summaryReport: SummaryReport | null;
}

// ---- 常量 ----

/** 小切片目标尺寸（字符） */
export const SMALL_SLICE_TARGET_SIZE = 20_000;

/** 小切片最小尺寸（低于此值不单独切） */
export const SMALL_SLICE_MIN_SIZE = 5_000;

/** 重试次数 */
export const MAX_RETRIES = 2;

/** 管线总步数 */
export const TOTAL_PIPELINE_STEPS = 7;

/** 管线步骤标签（UI 展示用） */
export const STEP_LABELS = [
  '小切片',
  '事件提取',
  '图谱对齐',
  '大切片',
  '深度分析',
  '汇总报告',
  'DNA 压缩',
] as const;
</file>

<file path="src/lib/source-processing/plot-extractor.ts">
// ============================================
// 情节规律提取 — 客户端编排模块
// ============================================

import {
  buildPlotExtractionMessages,
  buildPlotSupplementMessages,
} from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch, splitIntoBatches } from '@/lib/analysis-chunker';
import type { SemanticSlice } from '@/types';

/** 获取首批情节提取的请求体 */
export function getPlotExtractionRequestBody(
  slices: SemanticSlice[],
  styleProfile: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const { systemPrompt, userMessage } = buildPlotExtractionMessages(slicesText, styleProfile);

  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/** 获取补充批次的请求体 */
export function getPlotSupplementRequestBody(
  newChunk: string,
  previousReport: string,
  styleProfile: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildPlotSupplementMessages(newChunk, previousReport, styleProfile);
  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/**
 * 计算情节提取是否需要分批
 */
export function computePlotBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; batches: string[] } {
  const fullText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);

  if (fullText.length <= maxChars) {
    return { needsBatch: false, batches: [fullText] };
  }

  return {
    needsBatch: true,
    batches: splitIntoBatches(fullText, maxChars),
  };
}
</file>

<file path="src/lib/source-processing/reader-experience-extractor.ts">
// ============================================
// 读者体验模型 — 客户端编排模块
// ============================================

import {
  buildReaderExperienceExtractionMessages,
  buildReaderExperienceSupplementMessages,
} from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch, splitIntoBatches } from '@/lib/analysis-chunker';
import type { SemanticSlice } from '@/types';

/** 获取首批读者体验提取的请求体 */
export function getReaderExperienceExtractionRequestBody(
  slices: SemanticSlice[],
  styleProfile: string,
  narrativeDynamics: string,
  characterDynamics: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const { systemPrompt, userMessage } = buildReaderExperienceExtractionMessages(
    slicesText, styleProfile, narrativeDynamics, characterDynamics,
  );

  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 获取补充批次的请求体 */
export function getReaderExperienceSupplementRequestBody(
  newChunk: string,
  previousReport: string,
  styleProfile: string,
  narrativeDynamics: string,
  characterDynamics: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildReaderExperienceSupplementMessages(
    newChunk, previousReport, styleProfile, narrativeDynamics, characterDynamics,
  );
  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 计算是否需要分批 */
export function computeReaderExperienceBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; batches: string[] } {
  const fullText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);

  if (fullText.length <= maxChars) {
    return { needsBatch: false, batches: [fullText] };
  }

  return { needsBatch: true, batches: splitIntoBatches(fullText, maxChars) };
}
</file>

<file path="src/lib/source-processing/unified-pipeline.ts">
// ============================================
// 统一分析管线 — 7 步编排器
// ============================================
//
// Step 1: 小切片（代码，~2万字/片）
// Step 2: 逐片事件提取（N 次并行 AI，原文只读 1 次）
// Step 3: 事件对齐（代码预处理 + 1 次 AI）→ 全书记忆图谱
// Step 4: 大切片（代码，按 maxContextTokens 自适应）
// Step 5: 逐大切片深度分析（并行 AI，带完整图谱 JSON）
// Step 6: 汇总 + 刺激点分析 + 一致性报告（1 次 AI）
// Step 7: DNA 压缩（代码填充量化参数 + AI 定性）→ 纯量化生成规则
//
// 错误策略：重试 2 次后管线停止，已完成结果保留，支持断点恢复
// 短文本：统一流程，切片数=1

import type { AIConfig } from '@/lib/stream-fetcher';
import { createStreamFetcher } from '@/lib/stream-fetcher';
import { buildEventExtractionMessages, buildEventAlignmentMessages, buildDeepAnalysisMessages, buildSummaryMessages, buildDnaQualitativeMessages } from '@/lib/ai/prompts';
import type {
  SmallSlice,
  LargeSlice,
  SliceExtractionResult,
  ExtractedEvent,
  FullEventGraph,
  SliceAnalysis,
  SummaryReport,
  GenerationRulesDNA,
  UnifiedPipelineCallbacks,
  UnifiedPipelineResult,
  PipelineCheckpoint,
  AlignmentPreprocessResult,
} from './pipeline-types';
import {
  SMALL_SLICE_TARGET_SIZE,
  SMALL_SLICE_MIN_SIZE,
  MAX_RETRIES,
  TOTAL_PIPELINE_STEPS,
} from './pipeline-types';

// ============================================
// 辅助：带重试的 fetch
// ============================================

async function fetchWithRetry(
  fetcher: ReturnType<typeof createStreamFetcher>,
  url: string,
  body: object,
  step: number,
  callbacks: UnifiedPipelineCallbacks,
  label: string,
): Promise<string> {
  let lastError: string | null = null;

  for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
    if (attempt > 0) {
      callbacks.onStreamUpdate(step, `[重试第 ${attempt} 次: ${label}]`);
    }

    const { result, state } = await fetcher.fetch(url, body);

    if (result !== null) {
      return result;
    }

    lastError = state.error;
  }

  // 所有重试失败
  const errorMsg = `${label}失败（已重试 ${MAX_RETRIES} 次）: ${lastError}`;
  callbacks.onStreamError(step, errorMsg);
  throw new Error(errorMsg);
}

// ============================================
// Step 2: 逐片事件提取（并行 AI）
// ============================================

/** 解析 AI 输出为 ExtractedEvent[] */
function parseExtractedEvents(raw: string, sliceIndex: number): ExtractedEvent[] {
  try {
    const jsonMatch = raw.match(/\[[\s\S]*\]/);
    if (!jsonMatch) return [];
    const parsed = JSON.parse(jsonMatch[0]);
    if (!Array.isArray(parsed)) return [];

    return parsed
      .filter((e: Record<string, unknown>) =>
        typeof e.type === 'string' && typeof e.description === 'string'
        && (e.confidence === undefined || Number(e.confidence) >= 0.5),
      )
      .map((e: Record<string, unknown>, idx: number): ExtractedEvent => ({
        id: String(e.id ?? `E-S${sliceIndex}-${idx}`),
        type: String(e.type ?? '其他'),
        participants: Array.isArray(e.participants) ? e.participants.map(String) : [],
        location: String(e.location ?? '未知'),
        description: String(e.description ?? '').slice(0, 50),
        causes: Array.isArray(e.causes) ? e.causes.map(String) : [],
        effects: Array.isArray(e.effects) ? e.effects.map(String) : [],
        tensionChange: Math.min(5, Math.max(-5, Number(e.tension_change ?? 0))),
        emotion: String(e.emotion ?? ''),
        foreshadowingOf: e.foreshadowing_of ? String(e.foreshadowing_of) : null,
        confidence: Math.min(1, Math.max(0, Number(e.confidence ?? 0.7))),
        sliceIndex,
      }));
  } catch {
    return [];
  }
}

/** 从 AI 输出中提取实体列表 */
function extractEntitiesFromRaw(raw: string, events: ExtractedEvent[]): string[] {
  // 从事件的 participants 中收集，再去重
  const entities = new Set<string>();
  for (const event of events) {
    for (const p of event.participants) {
      entities.add(p);
    }
  }
  return [...entities];
}

async function runStep2EventExtraction(
  smallSlices: SmallSlice[],
  aiConfig: AIConfig,
  fetcher: ReturnType<typeof createStreamFetcher>,
  callbacks: UnifiedPipelineCallbacks,
): Promise<SliceExtractionResult[]> {
  const totalSlices = smallSlices.length;

  // 全量并行
  const results = await Promise.all(
    smallSlices.map(async (slice) => {
      const label = `切片 ${slice.index + 1}/${totalSlices} 事件提取`;
      const { systemPrompt, userMessage } = buildEventExtractionMessages(
        slice.content, slice.index, totalSlices,
      );

      try {
        const raw = await fetchWithRetry(
          fetcher,
          '/api/source/process/event-extraction',
          { systemPrompt, userMessage, apiKey: aiConfig.apiKey, model: aiConfig.model, baseURL: aiConfig.baseURL },
          1, callbacks, label,
        );

        callbacks.onStreamUpdate(1, `[${label} 完成]`);

        const events = parseExtractedEvents(raw, slice.index);
        const entities = extractEntitiesFromRaw(raw, events);

        return {
          sliceIndex: slice.index,
          events,
          entities,
          rawResponse: raw,
        } satisfies SliceExtractionResult;
      } catch {
        // 重试耗尽 → 管线停止（严格报错策略）
        throw new Error(`切片 ${slice.index + 1}/${totalSlices} 事件提取失败`);
      }
    }),
  );

  return results;
}

// ============================================
// Step 3: 事件对齐（代码预处理 + 1 次 AI）
// ============================================

async function runStep3EventAlignment(
  sliceExtractions: SliceExtractionResult[],
  novelId: string,
  aiConfig: AIConfig,
  fetcher: ReturnType<typeof createStreamFetcher>,
  callbacks: UnifiedPipelineCallbacks,
): Promise<FullEventGraph> {
  // 代码预处理：排序、候选实体对、频率统计
  const preprocessed = preprocessForAlignment(sliceExtractions);

  // 如果只有 1 个切片，代码直接构建图谱（无需 AI 对齐）
  if (sliceExtractions.length <= 1) {
    return buildEventGraphFromExtraction(preprocessed, novelId);
  }

  // 准备 AI 输入
  const allEventsJson = JSON.stringify(preprocessed.sortedEvents, null, 2);
  const candidatePairsStr = preprocessed.candidateEntityPairs.length > 0
    ? JSON.stringify(preprocessed.candidateEntityPairs.slice(0, 50), null, 2) // 最多传 50 个候选对
    : '无候选实体对';
  const entityFrequencyStr = JSON.stringify(
    Object.fromEntries(
      Object.entries(preprocessed.entityFrequency)
        .sort(([, a], [, b]) => b - a)
        .slice(0, 100), // 最多传 100 个实体
    ), null, 2,
  );

  const { systemPrompt, userMessage } = buildEventAlignmentMessages(
    allEventsJson,
    candidatePairsStr,
    entityFrequencyStr,
    preprocessed.totalEvents,
    sliceExtractions.length,
  );

  const raw = await fetchWithRetry(
    fetcher,
    '/api/source/process/event-alignment',
    { systemPrompt, userMessage, apiKey: aiConfig.apiKey, model: aiConfig.model, baseURL: aiConfig.baseURL },
    2, callbacks, '事件对齐',
  );

  callbacks.onStreamUpdate(2, '[事件对齐完成，正在构建图谱…]');

  // 解析 AI 输出为 FullEventGraph
  return parseAlignmentOutput(raw, preprocessed, novelId);
}

/** 从 AI 对齐输出构建 FullEventGraph */
function parseAlignmentOutput(
  raw: string,
  preprocessed: AlignmentPreprocessResult,
  novelId: string,
): FullEventGraph {
  try {
    const jsonMatch = raw.match(/\{[\s\S]*\}/);
    if (!jsonMatch) {
      // AI 输出不是 JSON，回退到代码构建
      return buildEventGraphFromExtraction(preprocessed, novelId);
    }

    const parsed = JSON.parse(jsonMatch[0]);

    // 使用 AI 重新输出的事件（已归一化），如果不存在则用原始事件
    const events: ExtractedEvent[] = Array.isArray(parsed.events)
      ? parsed.events.map((e: Record<string, unknown>, idx: number) => ({
          id: String(e.id ?? `E-${idx}`),
          type: String(e.type ?? '其他'),
          participants: Array.isArray(e.participants) ? e.participants.map(String) : [],
          location: String(e.location ?? '未知'),
          description: String(e.description ?? '').slice(0, 50),
          causes: Array.isArray(e.causes) ? e.causes.map(String) : [],
          effects: Array.isArray(e.effects) ? e.effects.map(String) : [],
          tensionChange: Math.min(5, Math.max(-5, Number(e.tension_change ?? e.tensionChange ?? 0))),
          emotion: String(e.emotion ?? ''),
          foreshadowingOf: e.foreshadowing_of ? String(e.foreshadowing_of) : null,
          confidence: Math.min(1, Math.max(0, Number(e.confidence ?? 0.7))),
          sliceIndex: Number(e.sliceIndex ?? 0),
        }))
      : preprocessed.sortedEvents;

    // 构建索引
    const byChapter: Record<number, string[]> = {};
    const byParticipant: Record<string, string[]> = {};
    const byType: Record<string, string[]> = {};
    for (const e of events) {
      (byChapter[e.sliceIndex] ??= []).push(e.id); // 用 sliceIndex 代替 chapter
      for (const p of e.participants) {
        (byParticipant[p] ??= []).push(e.id);
      }
      (byType[e.type] ??= []).push(e.id);
    }

    return {
      novelId,
      events,
      entityMappings: Array.isArray(parsed.entityMappings) ? parsed.entityMappings : [],
      entityTimelines: Array.isArray(parsed.entityTimelines) ? parsed.entityTimelines : [],
      foreshadowingPairs: Array.isArray(parsed.foreshadowingPairs)
        ? parsed.foreshadowingPairs.map((p: Record<string, unknown>) => ({
            setup: String(p.setup ?? ''),
            payoff: p.payoff ? String(p.payoff) : null,
            distance: Number(p.distance ?? 0),
            status: String(p.status ?? 'open') as 'resolved' | 'open' | 'dangling',
          }))
        : [],
      byChapter,
      byParticipant,
      byType,
      totalSmallSlices: new Set(events.map(e => e.sliceIndex)).size,
      extractedAt: new Date().toISOString(),
    };
  } catch {
    // 解析失败，回退到代码构建
    return buildEventGraphFromExtraction(preprocessed, novelId);
  }
}

/** 纯代码构建图谱（无 AI 对齐，用于单切片或 AI 对齐失败时的回退） */
function buildEventGraphFromExtraction(
  preprocessed: AlignmentPreprocessResult,
  novelId: string,
): FullEventGraph {
  const events = preprocessed.sortedEvents;
  const byChapter: Record<number, string[]> = {};
  const byParticipant: Record<string, string[]> = {};
  const byType: Record<string, string[]> = {};
  for (const e of events) {
    (byChapter[e.sliceIndex] ??= []).push(e.id);
    for (const p of e.participants) {
      (byParticipant[p] ??= []).push(e.id);
    }
    (byType[e.type] ??= []).push(e.id);
  }

  // 代码检测伏笔配对
  const foreshadowingPairs: FullEventGraph['foreshadowingPairs'] = [];
  const eventById = new Map(events.map(e => [e.id, e]));
  for (const e of events) {
    if (e.foreshadowingOf) {
      const payoff = eventById.get(e.foreshadowingOf);
      foreshadowingPairs.push({
        setup: e.id,
        payoff: payoff?.id ?? null,
        distance: payoff ? Math.abs(payoff.sliceIndex - e.sliceIndex) : 0,
        status: payoff ? 'resolved' : 'dangling',
      });
    }
  }

  return {
    novelId,
    events,
    entityMappings: [],
    entityTimelines: [],
    foreshadowingPairs,
    byChapter,
    byParticipant,
    byType,
    totalSmallSlices: new Set(events.map(e => e.sliceIndex)).size,
    extractedAt: new Date().toISOString(),
  };
}

// ============================================
// Step 5: 逐大切片深度分析（并行 AI）
// ============================================

function parseSliceAnalysis(raw: string, sliceIndex: number): SliceAnalysis | null {
  try {
    const jsonMatch = raw.match(/\{[\s\S]*\}/);
    if (!jsonMatch) return null;
    const parsed = JSON.parse(jsonMatch[0]);

    const defaultSentenceLength = { avg: 0, range: [0, 0] as [number, number], climax: 0, calm: 0 };
    const sm = parsed.styleMechanics;
    const styleMechanics = {
      sentenceLength: (sm?.sentenceLength && typeof sm.sentenceLength === 'object')
        ? { avg: Number(sm.sentenceLength.avg) || 0, range: (Array.isArray(sm.sentenceLength.range) ? sm.sentenceLength.range : [0, 0]) as [number, number], climax: Number(sm.sentenceLength.climax) || 0, calm: Number(sm.sentenceLength.calm) || 0 }
        : defaultSentenceLength,
      dialogueRatio: Number(sm?.dialogueRatio) || 0,
      descriptionStrategy: sm?.descriptionStrategy ?? null,
      counterExample: sm?.counterExample ?? null,
      confidence: Number(sm?.confidence) || 0,
      generationRule: String(sm?.generationRule ?? ''),
    };

    const nm = parsed.narrativeMechanics;
    const narrativeMechanics = {
      pacingPattern: nm?.pacingPattern ?? null,
      informationControl: nm?.informationControl ?? null,
      counterExample: nm?.counterExample ?? null,
      confidence: Number(nm?.confidence) || 0,
      generationRule: String(nm?.generationRule ?? ''),
    };

    return {
      sliceIndex,
      styleMechanics,
      narrativeMechanics,
      characterMechanics: Array.isArray(parsed.characterMechanics) ? parsed.characterMechanics : [],
      stimulationPoints: Array.isArray(parsed.stimulationPoints) ? parsed.stimulationPoints : [],
      constraints: parsed.constraints ?? { worldRules: [], characterLimits: [], plotConstraints: [], taboos: [] },
    };
  } catch {
    return null;
  }
}

async function runStep5DeepAnalysis(
  largeSlices: LargeSlice[],
  eventGraph: FullEventGraph | null,
  aiConfig: AIConfig,
  fetcher: ReturnType<typeof createStreamFetcher>,
  callbacks: UnifiedPipelineCallbacks,
): Promise<SliceAnalysis[]> {
  const graphJson = eventGraph ? JSON.stringify(eventGraph) : '{}';

  const results = await Promise.all(
    largeSlices.map(async (slice) => {
      const label = `大切片 ${slice.index + 1}/${largeSlices.length} 深度分析`;
      const { systemPrompt, userMessage } = buildDeepAnalysisMessages(
        slice.content, slice.index, largeSlices.length, graphJson,
      );

      const raw = await fetchWithRetry(
        fetcher,
        '/api/source/process/deep-analysis',
        { systemPrompt, userMessage, apiKey: aiConfig.apiKey, model: aiConfig.model, baseURL: aiConfig.baseURL },
        4, callbacks, label,
      );

      callbacks.onStreamUpdate(4, `[${label} 完成]`);
      return parseSliceAnalysis(raw, slice.index);
    }),
  );

  return results.filter((r): r is SliceAnalysis => r !== null);
}

// ============================================
// Step 6: 汇总 + 刺激点分析 + 一致性报告
// ============================================

function parseSummaryReport(raw: string): SummaryReport | null {
  try {
    const jsonMatch = raw.match(/\{[\s\S]*\}/);
    if (!jsonMatch) return null;
    const parsed = JSON.parse(jsonMatch[0]);
    // 注入默认值防止 NaN 流入下游
    return {
      styleEvolution: Array.isArray(parsed.styleEvolution) ? parsed.styleEvolution : [],
      stimulationCycle: parsed.stimulationCycle ?? {
        avgPeakInterval: 0, avgCooldownLength: 0, cyclePattern: [], stimulationDensity: {},
      },
      eventFunctions: Array.isArray(parsed.eventFunctions) ? parsed.eventFunctions : [],
      consistencyReport: parsed.consistencyReport ?? {
        settingConflicts: [], unresolvedForeshadowing: 0, totalForeshadowing: 0, driftRate: 0, styleConsistencyRate: 1,
      },
      informationRelease: parsed.informationRelease ?? {
        avgSetupToHint: 0, avgHintToReveal: 0, revealDensity: 0,
      },
    };
  } catch {
    return null;
  }
}

async function runStep6Summary(
  sliceAnalyses: SliceAnalysis[],
  eventGraph: FullEventGraph | null,
  aiConfig: AIConfig,
  fetcher: ReturnType<typeof createStreamFetcher>,
  callbacks: UnifiedPipelineCallbacks,
): Promise<SummaryReport> {
  const analysesJson = JSON.stringify(sliceAnalyses);
  const graphJson = eventGraph ? JSON.stringify(eventGraph) : '{}';

  const { systemPrompt, userMessage } = buildSummaryMessages(analysesJson, graphJson);

  const raw = await fetchWithRetry(
    fetcher,
    '/api/source/process/summary',
    { systemPrompt, userMessage, apiKey: aiConfig.apiKey, model: aiConfig.model, baseURL: aiConfig.baseURL },
    5, callbacks, '汇总报告',
  );

  callbacks.onStreamUpdate(5, '[汇总完成]');

  const report = parseSummaryReport(raw);
  if (!report) {
    throw new Error('汇总报告解析失败');
  }
  return report;
}

// ============================================
// Step 7: DNA 压缩（代码填充 + AI 定性）
// ============================================

function computeQuantitativeDna(
  eventGraph: FullEventGraph | null,
  summaryReport: SummaryReport | null,
  sliceAnalyses: SliceAnalysis[],
  novelId: string,
  model: string,
): { dna: GenerationRulesDNA; computedParams: string } {
  // 从 sliceAnalyses 计算句式参数
  const lengths = sliceAnalyses.map(a => a.styleMechanics.sentenceLength.avg).filter(n => n > 0);
  const dialogRatios = sliceAnalyses.map(a => a.styleMechanics.dialogueRatio).filter(n => n > 0);
  const avgSentenceLen = lengths.length > 0 ? lengths.reduce((a, b) => a + b, 0) / lengths.length : 0;
  const avgDialog = dialogRatios.length > 0 ? dialogRatios.reduce((a, b) => a + b, 0) / dialogRatios.length : 0;

  const sentenceLengthStd = lengths.length > 1
    ? Math.sqrt(lengths.reduce((s, n) => s + (n - avgSentenceLen) ** 2, 0) / (lengths.length - 1))
    : 0;

  const climaxLengths = sliceAnalyses.map(a => a.styleMechanics.sentenceLength.climax).filter(n => n > 0);
  const calmLengths = sliceAnalyses.map(a => a.styleMechanics.sentenceLength.calm).filter(n => n > 0);

  // 从刺激点计算节奏参数
  const allStimPoints = sliceAnalyses.flatMap(a => a.stimulationPoints);
  const peakPoints = allStimPoints.filter(p => p.tensionChange >= 3);
  // 用总字数 / 高峰间隔数估算平均高峰间距
  const totalChars = sliceAnalyses.reduce((s, a) => {
    // 从句长 × 句数估算片段字数，若无数据则取 0
    const charEstimate = a.styleMechanics.sentenceLength.avg * (a.stimulationPoints.length || 1) * 5;
    return s + charEstimate;
  }, 0);
  const avgPeakInterval = peakPoints.length > 1
    ? Math.round(totalChars / (peakPoints.length - 1))
    : 0;
  const cooldownPoints = allStimPoints.filter(p => p.type === '日常过渡');

  // 刺激类型密度
  const stimDensity: Record<string, number> = {};
  for (const p of allStimPoints) {
    stimDensity[p.type] = (stimDensity[p.type] || 0) + 1;
  }
  const totalStim = allStimPoints.length || 1;
  for (const k of Object.keys(stimDensity)) {
    stimDensity[k] = Math.round((stimDensity[k] / totalStim) * 100) / 100;
  }

  // 从汇总报告获取参数
  const cycle = summaryReport?.stimulationCycle;
  const consistency = summaryReport?.consistencyReport;
  const infoRelease = summaryReport?.informationRelease;

  // 角色规则
  const characterRules = sliceAnalyses
    .flatMap(a => a.characterMechanics)
    .reduce((map, cm) => {
      if (!map.has(cm.name)) {
        map.set(cm.name, { name: cm.name, stimulusResponse: cm.stimulusResponse, dialogRatio: 0 });
      } else {
        const existing = map.get(cm.name)!;
        existing.stimulusResponse.push(...cm.stimulusResponse);
      }
      return map;
    }, new Map<string, GenerationRulesDNA['characterRules'][0]>());

  // 禁忌
  const taboos = [...new Set(sliceAnalyses.flatMap(a => a.constraints.taboos))];

  const dna: GenerationRulesDNA = {
    sentenceLength: {
      avg: Math.round(avgSentenceLen),
      climax: climaxLengths.length > 0 ? Math.round(climaxLengths.reduce((a, b) => a + b, 0) / climaxLengths.length) : 0,
      calm: calmLengths.length > 0 ? Math.round(calmLengths.reduce((a, b) => a + b, 0) / calmLengths.length) : 0,
      std: Math.round(sentenceLengthStd),
    },
    dialogueRatio: Math.round(avgDialog * 100) / 100,
    descriptionRatio: Math.round((1 - avgDialog) * 0.5 * 100) / 100,
    actionRatio: Math.round((1 - avgDialog) * 0.3 * 100) / 100,
    internalMonologueRatio: Math.round((1 - avgDialog) * 0.2 * 100) / 100,
    conflictInterval: cycle?.avgPeakInterval ?? avgPeakInterval,
    peakInterval: cycle?.avgPeakInterval ?? avgPeakInterval,
    cooldownLength: cycle?.avgCooldownLength ?? (cooldownPoints.length > 0 ? 2000 : 0),
    stimulationCycle: cycle?.cyclePattern ?? Object.keys(stimDensity),
    stimulationDensity: stimDensity,
    informationRelease: {
      avgSetupToHint: infoRelease?.avgSetupToHint ?? 0,
      avgHintToReveal: infoRelease?.avgHintToReveal ?? 0,
      revealDensity: infoRelease?.revealDensity ?? 0,
    },
    characterRules: [...characterRules.values()],
    settingDriftTolerance: consistency?.driftRate ?? 0,
    unresolvedRatio: consistency
      ? (consistency.unresolvedForeshadowing ?? 0) / Math.max(1, consistency.totalForeshadowing ?? 1)
      : 0,
    styleConsistencyRate: consistency?.styleConsistencyRate ?? 1,
    taboos,
    qualitativeNotes: { styleSignature: null, coreAppeal: null, riskNotes: [] },
    meta: {
      generatedAt: new Date().toISOString(),
      sourceNovelId: novelId,
      modelUsed: model,
      totalSmallSlices: eventGraph?.totalSmallSlices ?? 0,
      totalLargeSlices: sliceAnalyses.length,
      totalEvents: eventGraph?.events.length ?? 0,
      totalEntities: eventGraph?.entityMappings.length ?? 0,
    },
  };

  return { dna, computedParams: JSON.stringify(dna, null, 2) };
}

function parseQualitativeNotes(raw: string): GenerationRulesDNA['qualitativeNotes'] {
  try {
    const jsonMatch = raw.match(/\{[\s\S]*\}/);
    if (!jsonMatch) return { styleSignature: null, coreAppeal: null, riskNotes: [] };
    const parsed = JSON.parse(jsonMatch[0]);
    return parsed.qualitativeNotes ?? { styleSignature: null, coreAppeal: null, riskNotes: [] };
  } catch {
    return { styleSignature: null, coreAppeal: null, riskNotes: [] };
  }
}

async function runStep7Dna(
  eventGraph: FullEventGraph | null,
  summaryReport: SummaryReport | null,
  sliceAnalyses: SliceAnalysis[],
  novelId: string,
  aiConfig: AIConfig,
  fetcher: ReturnType<typeof createStreamFetcher>,
  callbacks: UnifiedPipelineCallbacks,
): Promise<GenerationRulesDNA> {
  // 代码填充量化参数
  let dna: GenerationRulesDNA;
  let computedParams: string;
  try {
    const result = computeQuantitativeDna(
      eventGraph, summaryReport, sliceAnalyses, novelId, aiConfig.model,
    );
    dna = result.dna;
    computedParams = result.computedParams;
  } catch (err) {
    // 量化计算失败 → 返回最小默认 DNA，不阻塞管线
    const msg = err instanceof Error ? err.message : '量化计算异常';
    callbacks.onStreamUpdate(6, `[DNA 量化计算失败: ${msg}，使用默认值]`);
    dna = {
      sentenceLength: { avg: 0, climax: 0, calm: 0, std: 0 },
      dialogueRatio: 0, descriptionRatio: 0, actionRatio: 0, internalMonologueRatio: 0,
      conflictInterval: 0, peakInterval: 0, cooldownLength: 0,
      stimulationCycle: [], stimulationDensity: {},
      informationRelease: { avgSetupToHint: 0, avgHintToReveal: 0, revealDensity: 0 },
      characterRules: [],
      settingDriftTolerance: 0, unresolvedRatio: 0, styleConsistencyRate: 1,
      taboos: [],
      qualitativeNotes: { styleSignature: null, coreAppeal: null, riskNotes: [] },
      meta: { generatedAt: new Date().toISOString(), sourceNovelId: novelId, modelUsed: aiConfig.model, totalSmallSlices: 0, totalLargeSlices: sliceAnalyses.length, totalEvents: eventGraph?.events.length ?? 0, totalEntities: 0 },
    };
    computedParams = JSON.stringify(dna, null, 2);
  }

  // AI 定性（1 次调用）
  if (summaryReport) {
    try {
      const { systemPrompt, userMessage } = buildDnaQualitativeMessages(
        JSON.stringify(summaryReport), computedParams,
      );

      const raw = await fetchWithRetry(
        fetcher,
        '/api/source/process/dna-compression',
        { systemPrompt, userMessage, apiKey: aiConfig.apiKey, model: aiConfig.model, baseURL: aiConfig.baseURL },
        6, callbacks, 'DNA 定性',
      );

      dna.qualitativeNotes = parseQualitativeNotes(raw);
    } catch {
      // AI 定性失败不阻塞，保留代码填充的量化部分
      callbacks.onStreamUpdate(6, '[DNA 定性失败，仅使用量化参数]');
    }
  }

  return dna;
}

// ============================================
// Step 1: 小切片（纯代码）
// ============================================

export function createSmallSlices(rawText: string): SmallSlice[] {
  const totalChars = rawText.length;

  // 短文本：整个文本作为一个切片
  if (totalChars <= SMALL_SLICE_TARGET_SIZE * 1.3) {
    return [{
      index: 0,
      content: rawText,
      charCount: totalChars,
      startOffset: 0,
      endOffset: totalChars,
    }];
  }

  const slices: SmallSlice[] = [];
  let offset = 0;

  while (offset < totalChars) {
    let endOffset = Math.min(offset + SMALL_SLICE_TARGET_SIZE, totalChars);

    // 在段落/换行边界截断，避免切断句子
    if (endOffset < totalChars) {
      const searchStart = Math.max(offset + SMALL_SLICE_MIN_SIZE, endOffset - 2000);
      const searchRange = rawText.slice(searchStart, endOffset + 2000);
      // 找最后一个换行符作为切割点
      const lastNewline = searchRange.lastIndexOf('\n');
      if (lastNewline > 0) {
        endOffset = searchStart + lastNewline + 1;
      }
    }

    const content = rawText.slice(offset, endOffset);
    if (content.trim().length > 0) {
      slices.push({
        index: slices.length,
        content,
        charCount: content.length,
        startOffset: offset,
        endOffset,
      });
    }

    offset = endOffset;
  }

  return slices;
}

// ============================================
// Step 3 代码预处理：候选实体对
// ============================================

export function preprocessForAlignment(
  extractions: SliceExtractionResult[],
): AlignmentPreprocessResult {
  // 收集所有事件，按 sliceIndex 排序
  const allEvents = extractions
    .flatMap(e => e.events)
    .sort((a, b) => a.sliceIndex - b.sliceIndex);

  // 统计实体频率
  const entityFrequency: Record<string, number> = {};
  const entitySlicePresence: Record<string, Set<number>> = {};

  for (const event of allEvents) {
    for (const participant of event.participants) {
      entityFrequency[participant] = (entityFrequency[participant] || 0) + 1;
      if (!entitySlicePresence[participant]) {
        entitySlicePresence[participant] = new Set();
      }
      entitySlicePresence[participant].add(event.sliceIndex);
    }
  }

  // 找候选实体对（疑似同一实体的不同称呼）
  const entityNames = Object.keys(entityFrequency);
  const candidatePairs: AlignmentPreprocessResult['candidateEntityPairs'] = [];

  for (let i = 0; i < entityNames.length; i++) {
    for (let j = i + 1; j < entityNames.length; j++) {
      const a = entityNames[i];
      const b = entityNames[j];

      // 跳过低频实体
      if (entityFrequency[a] < 2 || entityFrequency[b] < 2) continue;

      const slicesA = entitySlicePresence[a];
      const slicesB = entitySlicePresence[b];

      // 计算共现和互斥
      let coOccurrence = 0;
      for (const s of slicesA) {
        if (slicesB.has(s)) coOccurrence++;
      }

      const mutualExclusion = coOccurrence === 0 && slicesA.size > 1 && slicesB.size > 1;

      // 只保留有可能的候选：互斥出现（从不同时出现在同一切片）
      if (mutualExclusion || coOccurrence > 0) {
        candidatePairs.push({
          names: [a, b],
          coOccurrenceCount: coOccurrence,
          mutualExclusion,
          frequencyA: entityFrequency[a],
          frequencyB: entityFrequency[b],
        });
      }
    }
  }

  // 按互斥优先排序
  candidatePairs.sort((a, b) => {
    if (a.mutualExclusion && !b.mutualExclusion) return -1;
    if (!a.mutualExclusion && b.mutualExclusion) return 1;
    return (b.frequencyA + b.frequencyB) - (a.frequencyA + a.frequencyB);
  });

  return {
    sortedEvents: allEvents,
    candidateEntityPairs: candidatePairs,
    entityFrequency,
    totalEvents: allEvents.length,
  };
}

// ============================================
// Step 4: 大切片（纯代码）
// ============================================

export function createLargeSlices(rawText: string, maxContextTokens: number): LargeSlice[] {
  // 估算：1 token ≈ 1.5 中文字符，留 30% 空间给图谱和 prompt
  const availableChars = Math.floor(maxContextTokens * 1.5 * 0.7);

  const totalChars = rawText.length;

  // 短文本：整个文本作为一个切片
  if (totalChars <= availableChars) {
    return [{
      index: 0,
      content: rawText,
      charCount: totalChars,
      startOffset: 0,
      endOffset: totalChars,
      coveredSmallSliceRange: [0, 0],
    }];
  }

  const slices: LargeSlice[] = [];
  let offset = 0;

  while (offset < totalChars) {
    let endOffset = Math.min(offset + availableChars, totalChars);

    // 在段落/换行边界截断
    if (endOffset < totalChars) {
      const searchStart = Math.max(offset + availableChars * 0.8, endOffset - 2000);
      const searchRange = rawText.slice(searchStart, endOffset + 2000);
      const lastNewline = searchRange.lastIndexOf('\n');
      if (lastNewline > 0) {
        endOffset = searchStart + lastNewline + 1;
      }
    }

    const content = rawText.slice(offset, endOffset);
    if (content.trim().length > 0) {
      slices.push({
        index: slices.length,
        content,
        charCount: content.length,
        startOffset: offset,
        endOffset,
        coveredSmallSliceRange: [0, 0], // 将在管线中根据 offset 映射填充
      });
    }

    offset = endOffset;
  }

  return slices;
}

// ============================================
// 主管线入口
// ============================================

export async function runUnifiedPipeline(
  novelId: string,
  rawText: string,
  aiConfig: AIConfig,
  checkpoint: PipelineCheckpoint | null,
  callbacks: UnifiedPipelineCallbacks,
  externalFetcher?: ReturnType<typeof createStreamFetcher>,
): Promise<UnifiedPipelineResult> {
  const fetcher = externalFetcher ?? createStreamFetcher();
  const failedSteps: number[] = [];

  // 断点恢复：从已完成步骤集合确定跳过
  const completedSteps = new Set(checkpoint?.completedSteps ?? []);

  // 中间产物
  let smallSlices: SmallSlice[] = checkpoint?.smallSlices ?? [];
  let largeSlices: LargeSlice[] = checkpoint?.largeSlices ?? [];
  let sliceExtractions: SliceExtractionResult[] = checkpoint?.sliceExtractions ?? [];
  let eventGraph: FullEventGraph | null = checkpoint?.eventGraph ?? null;
  let sliceAnalyses: SliceAnalysis[] = checkpoint?.sliceAnalyses ?? [];
  let summaryReport: SummaryReport | null = checkpoint?.summaryReport ?? null;
  let dna: GenerationRulesDNA | null = null;

  try {
    // ---- Step 1: 小切片 ----
    if (!completedSteps.has(0)) {
      callbacks.onStepStart(0, '正在切分小切片…');
      callbacks.onProgress(Math.round(0 * 100 / TOTAL_PIPELINE_STEPS));

      smallSlices = createSmallSlices(rawText);

      await callbacks.onSaveStep(0, { smallSlices });
      callbacks.onStepComplete(0);
      completedSteps.add(0);
    }

    // ---- Step 2: 逐片事件提取（并行 AI） ----
    if (!completedSteps.has(1)) {
      callbacks.onStepStart(1, `正在提取事件（${smallSlices.length} 个小切片并行）…`);
      callbacks.onProgress(Math.round(1 * 100 / TOTAL_PIPELINE_STEPS));

      sliceExtractions = await runStep2EventExtraction(
        smallSlices, aiConfig, fetcher, callbacks,
      );

      await callbacks.onSaveStep(1, { sliceExtractions });
      callbacks.onStepComplete(1);
      completedSteps.add(1);
    }

    // ---- Step 3: 事件对齐（代码预处理 + 1 次 AI） ----
    if (!completedSteps.has(2)) {
      callbacks.onStepStart(2, '正在对齐事件图谱…');
      callbacks.onProgress(Math.round(2 * 100 / TOTAL_PIPELINE_STEPS));

      eventGraph = await runStep3EventAlignment(
        sliceExtractions, novelId, aiConfig, fetcher, callbacks,
      );

      await callbacks.onSaveStep(2, { eventGraph });
      callbacks.onStepComplete(2);
      completedSteps.add(2);
    }

    // ---- Step 4: 大切片 ----
    if (!completedSteps.has(3)) {
      callbacks.onStepStart(3, '正在切分大切片…');
      callbacks.onProgress(Math.round(3 * 100 / TOTAL_PIPELINE_STEPS));

      largeSlices = createLargeSlices(rawText, aiConfig.maxContextTokens);
      // 映射 coveredSmallSliceRange
      for (const ls of largeSlices) {
        const startIdx = smallSlices.findIndex(ss => ss.endOffset > ls.startOffset);
        const endIdx = smallSlices.findLastIndex(ss => ss.startOffset < ls.endOffset);
        ls.coveredSmallSliceRange = [
          Math.max(0, startIdx),
          Math.max(0, endIdx),
        ];
      }

      await callbacks.onSaveStep(3, { largeSlices });
      callbacks.onStepComplete(3);
      completedSteps.add(3);
    }

    // ---- Step 5: 逐大切片深度分析（并行 AI） ----
    if (!completedSteps.has(4)) {
      callbacks.onStepStart(4, `正在深度分析（${largeSlices.length} 个大切片并行）…`);
      callbacks.onProgress(Math.round(4 * 100 / TOTAL_PIPELINE_STEPS));

      sliceAnalyses = await runStep5DeepAnalysis(
        largeSlices, eventGraph, aiConfig, fetcher, callbacks,
      );

      await callbacks.onSaveStep(4, { sliceAnalyses });
      callbacks.onStepComplete(4);
      completedSteps.add(4);
    }

    // ---- Step 6: 汇总 + 刺激点分析 + 一致性报告 ----
    if (!completedSteps.has(5)) {
      callbacks.onStepStart(5, '正在汇总分析…');
      callbacks.onProgress(Math.round(5 * 100 / TOTAL_PIPELINE_STEPS));

      summaryReport = await runStep6Summary(
        sliceAnalyses, eventGraph, aiConfig, fetcher, callbacks,
      );

      await callbacks.onSaveStep(5, { summaryReport });
      callbacks.onStepComplete(5);
      completedSteps.add(5);
    }

    // ---- Step 7: DNA 压缩 ----
    if (!completedSteps.has(6)) {
      callbacks.onStepStart(6, '正在压缩 DNA…');
      callbacks.onProgress(Math.round(6 * 100 / TOTAL_PIPELINE_STEPS));

      dna = await runStep7Dna(
        eventGraph, summaryReport, sliceAnalyses, novelId, aiConfig, fetcher, callbacks,
      );

      await callbacks.onSaveStep(6, { dna });
      callbacks.onStepComplete(6);
      completedSteps.add(6);
    }

    callbacks.onProgress(100);
  } catch (error) {
    // 错误已由 fetchWithRetry 抛出
    // 已完成的步骤结果保留在闭包变量中
    const failedStep = [...Array(TOTAL_PIPELINE_STEPS).keys()].find(
      s => !completedSteps.has(s)
    );
    if (failedStep !== undefined) {
      failedSteps.push(failedStep);
    }
    throw error;
  }

  return {
    smallSlices,
    largeSlices,
    eventGraph,
    sliceAnalyses,
    summaryReport,
    dna,
    failedSteps,
  };
}
</file>

<file path="src/lib/store/navigation.ts">
// ============================================
// 导航 Store — 视图路由，独立于项目数据
// ============================================

import { create } from 'zustand';
import type { ActiveView, PipelineTask } from '@/types';

export interface NavigationState {
  activeView: ActiveView;
  activeTask: PipelineTask | null;

  setActiveView: (view: ActiveView) => void;
  setActiveTask: (task: PipelineTask | null) => void;
}

export const useNavigationStore = create<NavigationState>()((set) => ({
  activeView: 'source-library',
  activeTask: null,

  setActiveView: (view) => set({ activeView: view }),
  setActiveTask: (task) => set({ activeTask: task }),
}));
</file>

<file path="start.ps1">
# ========================================
#   Ainovr 启动器 (PowerShell)
# ========================================

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  Ainovr 启动器" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 杀掉占用 3000 端口的旧进程
Write-Host "[1/2] 清理旧进程..." -ForegroundColor Yellow
$ports = Get-NetTCPConnection -LocalPort 3000 -State Listen -ErrorAction SilentlyContinue
foreach ($conn in $ports) {
    Write-Host "      关闭旧进程 PID $($conn.OwningProcess)"
    Stop-Process -Id $conn.OwningProcess -Force -ErrorAction SilentlyContinue
}
Write-Host "      完成"
Write-Host ""

# 启动服务并自动打开浏览器
Write-Host "[2/2] 启动服务..." -ForegroundColor Yellow
Start-Job -Name 'open-browser' -ScriptBlock {
    Start-Sleep -Seconds 3
    Start-Process 'http://localhost:3000'
} | Out-Null
Write-Host "      浏览器将自动打开..."
Write-Host ""

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  服务运行中" -ForegroundColor Green
Write-Host "  地址: http://localhost:3000" -ForegroundColor Green
Write-Host "  关闭此窗口 = 停止服务" -ForegroundColor DarkGray
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

npm run dev
</file>

<file path="test-client.html">
<script>
// Simulate what AnalyzeView does
async function test() {
  try {
    const response = await fetch('/api/analyze', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        novelTexts: ['测试文本，主角站在山巅。'],
        apiKey: 'test-key',
        model: 'deepseek-v4-flash',
        baseURL: 'https://api.deepseek.com'
      }),
    });
    if (!response.ok) {
      const err = await response.json().catch(() => ({ error: '请求失败' }));
      console.log('Error:', err);
      return;
    }
    const reader = response.body?.getReader();
    const decoder = new TextDecoder();
    let text = '';
    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      text += decoder.decode(value, { stream: true });
    }
    console.log('Success, length:', text.length);
  } catch (e) {
    console.log('Fetch error:', e.message);
  }
}
test();
</script>
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
</file>

<file path="src/app/api/generation/chapter-plan-detail/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterPlanDetailMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '每章计划详细生成',
  promptBuilder: (b) => buildChapterPlanDetailMessages(b.setContent as string, b.planFramework as string, b.chapterIndex as number, b.minorPlotPatterns as string),
});
</file>

<file path="src/app/api/generation/chapter-plan-framework/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterPlanFrameworkMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '每章计划框架生成',
  promptBuilder: (b) => buildChapterPlanFrameworkMessages(b.setContent as string, b.minorPlotPatterns as string),
});
</file>

<file path="src/app/api/generation/chapter-set-detail/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterSetDetailMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '章节集合详细生成',
  promptBuilder: (b) => buildChapterSetDetailMessages(b.volumeContent as string, b.setFramework as string, b.setIndex as number, b.minorPlotPatterns as string),
});
</file>

<file path="src/app/api/generation/chapter-set-framework/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterSetFrameworkMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '章节集合框架生成',
  promptBuilder: (b) => buildChapterSetFrameworkMessages(b.volumeContent as string, b.minorPlotPatterns as string),
});
</file>

<file path="src/app/api/generation/phase-detail/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildPhaseDetailMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '阶段详细生成',
  promptBuilder: (b) => buildPhaseDetailMessages(b.outline as string, b.phaseFramework as string, b.phaseIndex as number, b.plotGuide as string),
});
</file>

<file path="src/app/api/generation/phase-framework/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildPhaseFrameworkMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '阶段框架生成',
  promptBuilder: (b) => buildPhaseFrameworkMessages(b.outline as string, b.plotGuide as string),
});
</file>

<file path="src/app/api/generation/volume-detail/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildVolumeDetailMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '分卷详细生成',
  promptBuilder: (b) => buildVolumeDetailMessages(b.outline as string, b.phaseContent as string, b.volumeFramework as string, b.volumeIndex as number, b.minorPlotPatterns as string),
});
</file>

<file path="src/app/api/generation/volume-framework/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildVolumeFrameworkMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '分卷框架生成',
  promptBuilder: (b) => buildVolumeFrameworkMessages(b.outline as string, b.phaseContent as string, b.minorPlotPatterns as string),
});
</file>

<file path="src/app/api/library/upload/route.ts">
// ============================================
// POST /api/library/upload — 上传清洗后文本到素材库
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

/** 最大请求体大小：50MB（防止 OOM） */
const MAX_BODY_SIZE = 50 * 1024 * 1024;

export async function POST(req: NextRequest) {
  try {
    // 检查 Content-Length 防止 OOM
    const contentLength = parseInt(req.headers.get('content-length') || '0', 10);
    if (contentLength > MAX_BODY_SIZE) {
      return NextResponse.json({ error: '文件过大，最大支持 50MB' }, { status: 413 });
    }
    const { id, title, rawText, totalChars, importConfig } = await req.json();

    if (!id || !title || typeof rawText !== 'string') {
      return NextResponse.json({ error: '缺少必要字段' }, { status: 400 });
    }

    const novelDir = safeJoin(LIBRARY_DIR, id);
    await fs.mkdir(novelDir, { recursive: true });

    // 保存清洗后原文
    await fs.writeFile(path.join(novelDir, 'raw.txt'), rawText, 'utf-8');

    // 保存 meta
    const meta = {
      id,
      title,
      totalChars: totalChars ?? rawText.length,
      importConfig: importConfig ?? null,
      status: 'raw',
      createdAt: new Date().toISOString(),
      processedAt: null,
      sliceCount: 0,
      hasStyleProfile: false,
      hasPlotReport: false,
      sampleCount: 0,
    };
    await fs.writeFile(path.join(novelDir, 'meta.json'), JSON.stringify(meta, null, 2), 'utf-8');

    return NextResponse.json({ success: true, id, title });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '上传失败';
    console.error('Upload to library error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/novels/detail/route.ts">
// ============================================
// GET /api/novels/detail?id=xxx — 读取小说完整数据
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const NOVELS_DIR = path.join(process.cwd(), 'data', 'novels');

export async function GET(req: NextRequest) {
  try {
    const { searchParams } = new URL(req.url);
    const id = searchParams.get('id');
    if (!id) {
      return NextResponse.json({ error: '缺少 id 参数' }, { status: 400 });
    }

    const bookDir = safeJoin(NOVELS_DIR, id);

    // 读取 meta.json
    const metaRaw = await fs.readFile(path.join(bookDir, 'meta.json'), 'utf-8');
    const meta = JSON.parse(metaRaw);

    // 读取 full.txt
    const fullText = await fs.readFile(path.join(bookDir, 'full.txt'), 'utf-8');

    // 读取 chunks
    let chunks: {
      id: string;
      index: number;
      title: string;
      charCount: number;
      content: string;
    }[] = [];

    try {
      const chunksMetaRaw = await fs.readFile(path.join(bookDir, 'chunks.json'), 'utf-8');
      const chunksMeta = JSON.parse(chunksMetaRaw);

      const chunksDir = path.join(bookDir, 'chunks');
      for (const cm of chunksMeta) {
        const content = await fs.readFile(path.join(chunksDir, cm.filename), 'utf-8');
        chunks.push({
          id: cm.id,
          index: cm.index,
          title: cm.title,
          charCount: cm.charCount,
          content,
        });
      }
    } catch {
      // chunks 目录可能不存在（旧格式或无章节小说）
      chunks = [];
    }

    return NextResponse.json({
      novel: {
        id: meta.id,
        title: meta.title,
        totalChars: meta.totalChars,
        fullText,
        chunks,
        rawText: null,
        importConfig: null,
      },
    });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '读取失败';
    console.error('Detail novel error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/novels/list/route.ts">
// ============================================
// GET /api/novels/list — 列出 data/novels/ 下的全部小说
// ============================================

import { NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';

const NOVELS_DIR = path.join(process.cwd(), 'data', 'novels');

export async function GET() {
  try {
    await fs.mkdir(NOVELS_DIR, { recursive: true });

    const entries = await fs.readdir(NOVELS_DIR, { withFileTypes: true });
    const novels: { id: string; title: string; totalChars: number; chunkCount: number }[] = [];

    for (const entry of entries) {
      if (!entry.isDirectory()) continue;

      try {
        const metaPath = path.join(NOVELS_DIR, entry.name, 'meta.json');
        const metaRaw = await fs.readFile(metaPath, 'utf-8');
        const meta = JSON.parse(metaRaw);
        novels.push({
          id: meta.id,
          title: meta.title,
          totalChars: meta.totalChars,
          chunkCount: meta.chunkCount,
        });
      } catch {
        // 跳过无 meta.json 的目录（可能是旧格式文件或损坏的目录）
        continue;
      }
    }

    return NextResponse.json({ novels });
  } catch {
    return NextResponse.json({ novels: [] });
  }
}
</file>

<file path="src/app/api/source/process/ablation-testing/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '消融测试' });
</file>

<file path="src/app/api/source/process/character-dynamics/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '角色动力学分析' });
</file>

<file path="src/app/api/source/process/classify-entities/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '实体分类', maxTokens: 2048 });
</file>

<file path="src/app/api/source/process/dna-compression/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: 'DNA 压缩', maxTokens: 4096 });
</file>

<file path="src/app/api/source/process/event-extraction/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '事件提取', maxTokens: 16384 });
</file>

<file path="src/app/api/source/process/experience-annotation/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '体验流标注' });
</file>

<file path="src/app/api/source/process/narrative-constraints/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '叙事约束分析' });
</file>

<file path="src/app/api/source/process/plot/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '情节提取' });
</file>

<file path="src/app/api/source/process/reader-experience/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '读者体验分析' });
</file>

<file path="src/app/api/source/process/samples/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '样本选取' });
</file>

<file path="src/app/api/source/process/slice/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '智能切片' });
</file>

<file path="src/app/api/source/process/style/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '文风提取' });
</file>

<file path="src/app/api/source/process/tension-tracking/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';

export const POST = createStreamRoute({ label: '势能追踪' });
</file>

<file path="src/components/chapter/ReviewPanel.tsx">
// ============================================
// 审查面板 — 5维度评分展示
// ============================================

'use client';

import type { ChapterReview, ReviewDimension } from '@/types';

interface ReviewPanelProps {
  reviews: ChapterReview[];
  onRevise: () => void;
  canRevise: boolean;
  revisionCount: number;
}

const DIMENSION_LABELS: Record<ReviewDimension, string> = {
  style_consistency: '文风一致性',
  plot_coherence: '情节逻辑',
  pacing: '节奏',
  pattern_execution: '模式执行',
  foreshadow_execution: '伏笔执行',
  qi_consistency: '气的一致性',
};

export function ReviewPanel({ reviews, onRevise, canRevise, revisionCount }: ReviewPanelProps) {
  if (reviews.length === 0) return null;

  const allPassed = reviews.every((r) => r.score >= 7);

  return (
    <div className="rounded-lg border border-border p-4">
      <div className="flex items-center justify-between mb-3">
        <h3 className="text-sm font-medium text-foreground">
          自动审查
          {allPassed && <span className="ml-2 text-green-500 text-xs">全部通过 ✓</span>}
        </h3>

        {!allPassed && (
          <button
            onClick={onRevise}
            disabled={!canRevise}
            className="px-3 py-1 rounded text-xs bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
          >
            修正（{revisionCount}/3）
          </button>
        )}
      </div>

      <div className="space-y-2">
        {reviews.map((review) => {
          const isLow = review.score < 7;
          return (
            <div key={review.dimension} className="flex items-start gap-3">
              <div className={`
                w-7 h-7 rounded flex items-center justify-center text-xs font-bold
                ${isLow ? 'bg-red-100 text-red-600 dark:bg-red-900/30 dark:text-red-400' : 'bg-green-100 text-green-600 dark:bg-green-900/30 dark:text-green-400'}
              `}>
                {review.score}
              </div>
              <div className="flex-1 min-w-0">
                <p className="text-xs font-medium text-foreground">
                  {DIMENSION_LABELS[review.dimension]}
                </p>
                {review.issues.length > 0 && (
                  <p className="text-xs text-red-500 mt-0.5">{review.issues.join('; ')}</p>
                )}
                {review.suggestions.length > 0 && (
                  <p className="text-xs text-muted-foreground mt-0.5">{review.suggestions.join('; ')}</p>
                )}
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}
</file>

<file path="src/components/generation/HierarchyTree.tsx">
// ============================================
// 层级树 — 可视化大纲/阶段/卷/集合/章的层级
// ============================================

'use client';

import { useState } from 'react';
import { ChevronRight, ChevronDown, FileText } from 'lucide-react';
import type { Phase, Volume, ChapterSet, ChapterPlan, BookOutline } from '@/types';

interface HierarchyTreeProps {
  outline: BookOutline | null;
  phases: Phase[] | null;
  volumes: Volume[] | null;
  chapterSets: ChapterSet[] | null;
  chapterPlans: ChapterPlan[] | null;
}

/** 可折叠的树节点 */
function TreeNode({
  label,
  icon,
  children,
  defaultExpanded = false,
  content,
}: {
  label: string;
  icon?: React.ReactNode;
  children?: React.ReactNode;
  defaultExpanded?: boolean;
  content?: string;
}) {
  const [expanded, setExpanded] = useState(defaultExpanded);
  const [contentExpanded, setContentExpanded] = useState(false);
  const hasLongContent = !!(content && content.length > 100);

  return (
    <div className="mb-1">
      {/* 标题行：展开全文按钮紧跟标题 */}
      <div
        onClick={() => { setExpanded((e) => !e); if (!expanded) setContentExpanded(false); }}
        className="flex items-center gap-1 px-2 py-1 rounded hover:bg-accent/50 cursor-pointer"
      >
        {expanded ? <ChevronDown className="w-3 h-3 shrink-0" /> : <ChevronRight className="w-3 h-3 shrink-0" />}
        {icon}
        <span className="font-medium truncate">{label}</span>
        {hasLongContent && expanded && !contentExpanded && (
          <button
            onClick={(e) => { e.stopPropagation(); setContentExpanded(true); }}
            className="shrink-0 text-primary hover:underline text-xs ml-1"
          >
            展开全文
          </button>
        )}
      </div>

      {expanded && (
        <div className="ml-4 relative">
          {content && (
            <div className="px-2 py-1 text-muted-foreground whitespace-pre-wrap">
              <span className={contentExpanded ? '' : 'line-clamp-3'}>{content}</span>
            </div>
          )}
          {/* 展开后 sticky 收起按钮，跟随滚动 */}
          {hasLongContent && contentExpanded && (
            <div className="sticky bottom-0 flex justify-center py-1 bg-background/80 backdrop-blur-sm border-t border-border/50">
              <button
                onClick={(e) => { e.stopPropagation(); setContentExpanded(false); }}
                className="px-3 py-0.5 rounded text-xs text-primary hover:bg-primary/10 transition-colors"
              >
                收起全文
              </button>
            </div>
          )}
          {children}
        </div>
      )}
    </div>
  );
}

export function HierarchyTree({ outline, phases, volumes, chapterSets, chapterPlans }: HierarchyTreeProps) {
  if (!outline) {
    return (
      <div className="p-3 text-xs text-muted-foreground text-center">
        尚未生成大纲
      </div>
    );
  }

  const phasesForVolume = (phaseId: string) => volumes?.filter((v) => v.phaseId === phaseId) ?? [];
  const setsForVolume = (volumeId: string) => chapterSets?.filter((s) => s.volumeId === volumeId) ?? [];
  const plansForSet = (setId: string) => chapterPlans?.filter((p) => p.chapterSetId === setId) ?? [];

  return (
    <div className="text-xs">
      {/* Layer 1: 大纲 */}
      <TreeNode
        label="全书大纲"
        icon={<FileText className="w-3 h-3 text-primary shrink-0" />}
        defaultExpanded={false}
        content={outline.content}
      />

      {/* Layer 2: 阶段 */}
      {phases?.map((phase) => (
        <div key={phase.id} className="ml-2">
          <TreeNode
            label={phase.title}
            content={phase.content}
          >
            {/* Layer 3: 卷 */}
            {phasesForVolume(phase.id).map((vol) => (
              <div key={vol.id} className="ml-2">
                <TreeNode
                  label={vol.title}
                  content={vol.content}
                >
                  {/* Layer 4: 章节集合 */}
                  {setsForVolume(vol.id).map((set) => (
                    <div key={set.id} className="ml-2">
                      <TreeNode
                        label={set.title}
                        content={set.content}
                      >
                        {/* Layer 5: 章节计划 */}
                        {plansForSet(set.id).map((plan) => (
                          <div key={plan.id} className="ml-2 px-2 py-0.5 text-muted-foreground">
                            <FileText className="w-3 h-3 inline mr-1" />
                            {plan.title}
                          </div>
                        ))}
                      </TreeNode>
                    </div>
                  ))}
                </TreeNode>
              </div>
            ))}
          </TreeNode>
        </div>
      ))}
    </div>
  );
}
</file>

<file path="src/components/generation/LayerCard.tsx">
// ============================================
// 层级卡片 — 单层状态和操作（展开 + 编辑 + 重新规划）
// ============================================

'use client';

import { useState, useRef, useEffect } from 'react';
import { Loader2, ChevronDown, ChevronUp, Pencil, RefreshCw } from 'lucide-react';
import { StreamingText } from '@/components/StreamingText';

interface LayerCardProps {
  layerNumber: number;
  title: string;
  description: string;
  status: 'pending' | 'generating' | 'framework_done' | 'done';
  content: string | null;
  streamContent: string;
  isStreaming: boolean;
  canGenerate: boolean;
  onGenerate: () => void;
  onEdit?: (newContent: string) => void;
  onRegenWithPrompt?: (customPrompt: string) => void;
}

export function LayerCard({
  layerNumber,
  title,
  description,
  status,
  content,
  streamContent,
  isStreaming,
  canGenerate,
  onGenerate,
  onEdit,
  onRegenWithPrompt,
}: LayerCardProps) {
  const [expanded, setExpanded] = useState(false);
  const [isEditing, setIsEditing] = useState(false);
  const [editText, setEditText] = useState('');
  const [showRegenInput, setShowRegenInput] = useState(false);
  const [regenPrompt, setRegenPrompt] = useState('');
  const editRef = useRef<HTMLTextAreaElement>(null);

  useEffect(() => {
    if (isEditing && editRef.current) editRef.current.focus();
  }, [isEditing]);

  const startEdit = () => {
    setEditText(content ?? '');
    setIsEditing(true);
  };

  const saveEdit = () => {
    if (editText.trim() !== content) {
      onEdit?.(editText.trim());
    }
    setIsEditing(false);
  };

  const cancelEdit = () => {
    setIsEditing(false);
    setEditText('');
  };

  const submitRegen = () => {
    if (regenPrompt.trim()) {
      onRegenWithPrompt?.(regenPrompt.trim());
      setRegenPrompt('');
      setShowRegenInput(false);
    }
  };

  return (
    <div className="rounded-lg border border-border p-4">
      {/* 标题行 */}
      <div className="flex items-center justify-between mb-2">
        <div className="flex items-center gap-2">
          <span className="text-xs font-bold text-muted-foreground bg-muted px-2 py-0.5 rounded">
            L{layerNumber}
          </span>
          <h3 className="text-sm font-medium text-foreground">{title}</h3>
          {status === 'done' && <span className="text-xs text-green-500">✓</span>}
          {status === 'generating' && <Loader2 className="w-3 h-3 animate-spin text-primary" />}
        </div>

        <div className="flex items-center gap-1">
          {/* 编辑按钮（已完成时显示） */}
          {status === 'done' && onEdit && !isEditing && (
            <button
              onClick={startEdit}
              className="p-1 rounded text-xs text-muted-foreground hover:text-foreground hover:bg-accent/50 transition-colors"
              title="手动编辑"
            >
              <Pencil className="w-3 h-3" />
            </button>
          )}

          {/* 重新规划按钮（已完成或当前层时显示） */}
          {(status === 'done' || canGenerate) && onRegenWithPrompt && (
            <button
              onClick={() => setShowRegenInput((v) => !v)}
              className="p-1 rounded text-xs text-muted-foreground hover:text-primary hover:bg-primary/10 transition-colors"
              title="自定义提示词重新规划"
            >
              <RefreshCw className="w-3 h-3" />
            </button>
          )}

          {/* 展开/收起 */}
          {content && !streamContent && !isEditing && (
            <button
              onClick={() => setExpanded((e) => !e)}
              className="flex items-center gap-1 px-2 py-0.5 rounded text-xs text-muted-foreground hover:text-foreground hover:bg-accent/50 transition-colors"
            >
              {expanded ? <ChevronUp className="w-3 h-3" /> : <ChevronDown className="w-3 h-3" />}
              {expanded ? '收起' : '展开'}
            </button>
          )}

          {/* 生成按钮 */}
          <button
            onClick={onGenerate}
            disabled={!canGenerate || isStreaming}
            className={`
              px-3 py-1 rounded text-xs transition-colors
              ${status === 'done'
                ? 'bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400'
                : canGenerate
                  ? 'bg-primary text-primary-foreground hover:bg-primary/90'
                  : 'bg-muted text-muted-foreground cursor-not-allowed'
              }
            `}
          >
            {status === 'generating' ? '生成中...' : status === 'done' ? '重新生成' : '生成'}
          </button>
        </div>
      </div>

      <p className="text-xs text-muted-foreground mb-2">{description}</p>

      {/* 自定义提示词输入框 */}
      {showRegenInput && (
        <div className="mb-2 p-3 rounded-lg border border-primary/20 bg-primary/5 space-y-2">
          <label className="text-xs font-medium text-foreground">自定义提示词重新规划</label>
          <textarea
            value={regenPrompt}
            onChange={(e) => setRegenPrompt(e.target.value)}
            placeholder="输入你的额外要求或修改方向..."
            className="w-full h-20 px-3 py-2 rounded-md border border-border bg-background text-xs resize-none"
            onKeyDown={(e) => { if (e.key === 'Enter' && e.metaKey) submitRegen(); }}
          />
          <div className="flex justify-end gap-2">
            <button onClick={() => setShowRegenInput(false)} className="px-2 py-1 rounded text-xs text-muted-foreground hover:bg-accent">取消</button>
            <button
              onClick={submitRegen}
              disabled={!regenPrompt.trim()}
              className="px-3 py-1 rounded text-xs bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50 disabled:cursor-not-allowed"
            >
              开始重新规划
            </button>
          </div>
        </div>
      )}

      {/* 编辑模式 */}
      {isEditing && (
        <div className="mt-2 space-y-2">
          <textarea
            ref={editRef}
            value={editText}
            onChange={(e) => setEditText(e.target.value)}
            className="w-full h-64 px-3 py-2 rounded-md border border-border bg-background text-xs font-mono resize-y"
          />
          <div className="flex justify-end gap-2">
            <button onClick={cancelEdit} className="px-3 py-1.5 rounded text-xs border border-border text-muted-foreground hover:bg-accent">取消</button>
            <button onClick={saveEdit} className="px-3 py-1.5 rounded text-xs bg-primary text-primary-foreground hover:bg-primary/90">保存</button>
          </div>
        </div>
      )}

      {/* 流式输出 */}
      {(streamContent || isStreaming) && !isEditing && (
        <div className="mt-2 p-3 rounded bg-muted/50 max-h-64 overflow-y-auto">
          <StreamingText content={streamContent} isStreaming={isStreaming} />
        </div>
      )}

      {/* 已完成的内容（非编辑、非流式时显示） */}
      {content && !streamContent && !isEditing && (
        <div className="mt-2 p-3 rounded bg-muted/50">
          <pre className={`text-xs text-muted-foreground whitespace-pre-wrap ${expanded ? '' : 'line-clamp-6'}`}>{content}</pre>
        </div>
      )}
    </div>
  );
}
</file>

<file path="src/components/StreamingText.tsx">
// ============================================
// 流式文本渲染组件
// ============================================

'use client';

import { useRef, useEffect } from 'react';

interface StreamingTextProps {
  content: string;
  isStreaming: boolean;
  className?: string;
}

export function StreamingText({ content, isStreaming, className = '' }: StreamingTextProps) {
  const scrollRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (scrollRef.current && isStreaming) {
      scrollRef.current.scrollTop = scrollRef.current.scrollHeight;
    }
  }, [content, isStreaming]);

  if (!content) {
    return null;
  }

  return (
    <div
      ref={scrollRef}
      className={`prose prose-sm max-w-none whitespace-pre-wrap leading-relaxed text-foreground ${className}`}
    >
      {content}
      {isStreaming && (
        <span className="inline-block w-2 h-4 ml-0.5 bg-foreground/50 animate-pulse" />
      )}
    </div>
  );
}
</file>

<file path="src/components/ui/sonner.tsx">
"use client"

import { Toaster as Sonner, type ToasterProps } from "sonner"
import { CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon, Loader2Icon } from "lucide-react"

const Toaster = ({ ...props }: ToasterProps) => {
  return (
    <Sonner
      theme="light"
      className="toaster group"
      icons={{
        success: (
          <CircleCheckIcon className="size-4" />
        ),
        info: (
          <InfoIcon className="size-4" />
        ),
        warning: (
          <TriangleAlertIcon className="size-4" />
        ),
        error: (
          <OctagonXIcon className="size-4" />
        ),
        loading: (
          <Loader2Icon className="size-4 animate-spin" />
        ),
      }}
      style={
        {
          "--normal-bg": "var(--popover)",
          "--normal-text": "var(--popover-foreground)",
          "--normal-border": "var(--border)",
          "--border-radius": "var(--radius)",
        } as React.CSSProperties
      }
      toastOptions={{
        classNames: {
          toast: "cn-toast",
        },
      }}
      {...props}
    />
  )
}

export { Toaster }
</file>

<file path="src/lib/ai/prompts/entity-classification.ts">
// ============================================
// Step -1A AI补充：实体分类 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说实体分类器。你会收到一批候选实体名称，以及小说标题作为上下文。

你的任务：对每个实体判断其类型。

类型定义：
- character（角色）：人物、角色名、代称、称号。注意有些看似普通名词的词在特定小说中是角色名（如《诡秘之主》中"太阳"是角色代号）
- location（地点）：城市、国家、地区、建筑、地理区域
- organization（组织/势力）：门派、组织、势力、团队、种族
- artifact（物品/能力）：武器、法宝、特殊物品、超凡能力名称
- concept（概念/其他）：不属于以上四类的词汇

输出格式（每行一个，严格按 YAML）：
实体名: 类型

示例：
克莱恩: character
廷根: location
值夜者: organization
灰雾之上: concept
阿蒙: character
`;

export function buildEntityClassificationMessages(
  novelTitle: string,
  entityNames: string[],
) {
  const userMessage = `小说标题：《${novelTitle}》\n\n候选实体列表：\n${entityNames.map((n, i) => `${i + 1}. ${n}`).join('\n')}`;
  return { systemPrompt: getPrompt('entity-classification', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/event-extraction.ts">
// ============================================
// 事件提取 — 逐切片模式（统一管线 Step 2）
// 从小切片原文中提取结构化事件，带因果链
// ============================================

import { getPrompt } from './helpers';
import { withEngineeringConstraints } from './pipeline-common';

export const DEFAULT_SYSTEM_PROMPT = withEngineeringConstraints(`你是一部小说的结构化事件提取程序。你的任务是从原文片段中提取事件，建立事件之间的因果链。

## 什么是"事件"

事件 = 一个改变了故事状态的具体行动或发生。

- ✅ "韩立拜墨居仁为师" → 事件（状态：无师→有师）
- ✅ "发现神秘小瓶可以催熟药草" → 事件（状态：未知→已知）
- ❌ "韩立走在路上" → 不是事件（状态未改变）
- ❌ "天很蓝" → 不是事件（环境描写，不是行动）

## 事件类型

使用以下标准类型之一：
- **拜师/收徒**：师徒关系建立
- **战斗/冲突**：武力对抗
- **死亡/牺牲**：角色死亡
- **揭露/发现**：关键信息被揭示
- **突破/升级**：实力/境界提升
- **交易/获得**：重要物品的获取或交易
- **背叛/决裂**：关系破裂
- **结盟/合作**：新联盟建立
- **离别/重逢**：角色离开或重聚
- **决策/选择**：角色做出影响后续的重大决策
- **日常/过渡**：状态维护性事件（修炼、赶路、准备）
- **其他**

## 输出格式

对每个识别到的事件输出 JSON：

{
  "id": "E-S{sliceIndex}-{序号}",
  "chapter": 章节号,
  "type": "事件类型",
  "participants": ["角色1", "角色2"],
  "location": "地点",
  "description": "一句话描述（不超过50字）",
  "causes": [],
  "effects": [],
  "tension_change": -5~5,
  "emotion": "情绪词",
  "foreshadowing_of": null,
  "confidence": 0-1
}

## 因果链标注规则

1. **只标注本切片内的因果**：事件 A 在本切片内直接导致事件 B → A.effects 包含 B.id
2. **不猜测跨切片因果**：跨切片的因果关系由后续步骤处理，不要在这里猜测
3. **伏笔标注**：如果本事件明显为后续剧情埋线索 → 标注 foreshadowing_of（仅限本切片内能确认的）
4. **拿不准就留空**：因果关系不确定时留空，宁缺毋滥

## 核心原则

- 只提取原文中**实际发生**的事件，不推测、不编造
- 置信度 < 0.5 的事件不要输出
- 同时记录出现的所有实体名（角色、地点、势力、物品）
- 只做事实提取，不分析作者意图，不解读象征意义`);

/**
 * 构建逐切片事件提取的消息（统一管线 Step 2）
 */
export function buildEventExtractionMessages(
  sliceContent: string,
  sliceIndex: number,
  totalSlices: number,
) {
  const userMessage = `## 切片信息\n- 切片编号: ${sliceIndex + 1} / ${totalSlices}\n- 事件 ID 格式: E-S${sliceIndex}-{序号}\n\n## 原文内容\n${sliceContent}\n\n---\n请提取本切片中的所有事件，输出 JSON 数组。同时列出本切片中出现的所有实体名。`;

  return {
    systemPrompt: getPrompt('event-extraction', DEFAULT_SYSTEM_PROMPT),
    userMessage,
  };
}

/**
 * 构建事件对齐的消息（统一管线 Step 3）
 * AI 看到所有事件 JSON + 候选实体对，输出完整重建的图谱
 */
export const DEFAULT_ALIGNMENT_PROMPT = withEngineeringConstraints(`你是事件图谱对齐程序。你的任务是：
1. 实体归一：识别不同切片中对同一实体的不同称呼（别名、错别字、称号变化）
2. 跨切片因果链：建立跨切片的事件因果关系
3. 伏笔配对：识别埋设和回收的伏笔对
4. 实体时间线：为每个实体构建状态演变轨迹

## 输入
你将收到：所有事件 JSON、候选实体对（代码预匹配的疑似同一实体）、实体频率统计。

## 输出格式（完整 JSON）

{
  "entityMappings": [
    { "aliases": ["韩立", "韩老魔", "韩历"], "canonical": "韩立", "type": "character" }
  ],
  "crossSliceCausalLinks": [
    { "cause": "E-S0-005", "effect": "E-S3-012", "confidence": 0.8 }
  ],
  "foreshadowingPairs": [
    { "setup": "E-S1-003", "payoff": "E-S8-021", "distance": 42, "status": "resolved" }
  ],
  "entityTimelines": [
    { "entity": "韩立", "timeline": [
      { "chapter": 1, "state": "散修", "triggerEventId": "E-S0-001" },
      { "chapter": 25, "state": "筑基", "triggerEventId": "E-S2-008" }
    ]}
  ],
  "events": [ ... ]
  // 重新输出所有事件，其中：
  // - participants 替换为 canonical 名称
  // - causes/effects 包含跨切片因果链接
  // - id 保持不变
]

## 核心原则
- 不分析作者意图，不做文学解读
- 实体归一需要证据（从不同时出现、类型匹配、上下文线索）
- 拿不准的因果链不标注，置信度 < 0.5 的不输出
- 未回收的伏笔 status 标为 "open"，无任何提示的标为 "dangling"`);

export function buildEventAlignmentMessages(
  allEventsJson: string,
  candidatePairs: string,
  entityFrequency: string,
  totalEvents: number,
  totalSlices: number,
) {
  const userMessage = `## 输入概要
- 共 ${totalEvents} 个事件，来自 ${totalSlices} 个切片

## 所有事件（已按切片排序）
${allEventsJson}

## 候选实体对（代码预匹配）
${candidatePairs}

## 实体频率统计
${entityFrequency}

---
请执行实体归一、跨切片因果链接、伏笔配对、实体时间线构建。输出完整 JSON。`;

  return {
    systemPrompt: getPrompt('event-alignment', DEFAULT_ALIGNMENT_PROMPT),
    userMessage,
  };
}
</file>

<file path="src/lib/api/stream-route.ts">
// ============================================
// 统一流式路由工厂 — 消除 25+ 个 API route 的重复样板
// ============================================

import { chatCompletionStream } from '@/lib/ai/providers';
import { DEFAULT_MODEL, DEFAULT_BASE_URL } from '@/lib/constants';

interface StreamRouteConfig {
  /** 路由中文标签，用于错误信息 */
  label: string;
  /** maxTokens，默认 8192 */
  maxTokens?: number;
}

/**
 * 从请求中提取 AI 配置和提示词，调用 chatCompletionStream 返回流式响应。
 * 适用于 source/process/* 路由（客户端预构建 systemPrompt + userMessage）。
 */
export function handlePassThroughStream(
  req: Request,
  config: StreamRouteConfig,
): Promise<Response> {
  return handleStreamRequest(req, config, null);
}

/**
 * 从请求中提取 AI 配置和业务字段，调用 promptBuilder 构建 prompt，再调用 chatCompletionStream。
 * 适用于 generation/* 和 chapter/* 路由。
 */
export function handlePromptBuiltStream(
  req: Request,
  config: StreamRouteConfig,
  promptBuilder: (body: Record<string, unknown>) => { systemPrompt: string; userMessage: string },
): Promise<Response> {
  return handleStreamRequest(req, config, promptBuilder);
}

/**
 * 内部统一实现 — 异步包装
 */
async function handleStreamRequest(
  req: Request,
  config: StreamRouteConfig,
  promptBuilder: ((body: Record<string, unknown>) => { systemPrompt: string; userMessage: string }) | null,
): Promise<Response> {
  const body = await req.json() as Record<string, unknown>;
  const { apiKey, model, baseURL } = body;
  const thinkingMode = body.thinkingMode as boolean | undefined;

  if (!apiKey) {
    return new Response(JSON.stringify({ error: '请先配置 API Key' }), { status: 400 });
  }

  let systemPrompt: string;
  let userMessage: string;

  if (promptBuilder) {
    ({ systemPrompt, userMessage } = promptBuilder(body));
  } else {
    systemPrompt = body.systemPrompt as string;
    userMessage = body.userMessage as string;
    if (!userMessage) {
      return new Response(JSON.stringify({ error: '缺少文本内容' }), { status: 400 });
    }
  }

  const stream = chatCompletionStream(
    { apiKey: apiKey as string, model: (model as string) || DEFAULT_MODEL, baseURL: (baseURL as string) || DEFAULT_BASE_URL, thinkingMode },
    { system: systemPrompt, messages: [{ role: 'user', content: userMessage }], maxTokens: config.maxTokens ?? 8192 },
  );

  return new Response(stream, {
    headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Cache-Control': 'no-cache' },
  });
}

/**
 * 创建完整的 POST route handler，自动包 try/catch。
 *
 * 用法：
 * ```ts
 * // source/process/slice/route.ts
 * export const POST = createStreamRoute({ label: '智能切片' });
 *
 * // generation/outline/route.ts
 * export const POST = createStreamRoute({
 *   label: '大纲生成',
 *   promptBuilder: (body) => buildOutlineGenerationMessages(body.styleGuide, body.plotGuide, ...),
 * });
 * ```
 */
export function createStreamRoute(
  config: StreamRouteConfig & {
    /** 可选：prompt 构建函数。不传则为 pass-through 模式（客户端发 systemPrompt + userMessage） */
    promptBuilder?: (body: Record<string, unknown>) => { systemPrompt: string; userMessage: string };
  },
): (req: Request) => Promise<Response> {
  return async (req: Request) => {
    try {
      if (config.promptBuilder) {
        return await handlePromptBuiltStream(req, config, config.promptBuilder);
      }
      return await handlePassThroughStream(req, config);
    } catch (error: unknown) {
      const message = error instanceof Error ? error.message : `${config.label}失败`;
      console.error(`${config.label}错误:`, error);
      return new Response(JSON.stringify({ error: message }), { status: 500 });
    }
  };
}
</file>

<file path="src/lib/constants.ts">
// ============================================
// 共享常量 — 避免默认值在多处硬编码导致不一致
// ============================================

/** 默认模型名称 */
export const DEFAULT_MODEL = 'deepseek-v4-flash';

/** 默认 API Base URL */
export const DEFAULT_BASE_URL = 'https://api.deepseek.com';

/** 允许的 API 协议（防止 SSRF） */
export const ALLOWED_API_PROTOCOLS = ['https:'] as const;
</file>

<file path="src/lib/hooks/use-streaming-fetch.ts">
// ============================================
// 统一流式请求 hook — 自动 abort + 卸载清理
// ============================================

'use client';

import { useState, useCallback, useRef, useEffect } from 'react';

export function useStreamingFetch() {
  const abortRef = useRef<AbortController | null>(null);
  const [streamContent, setStreamContent] = useState('');
  const [isStreaming, setIsStreaming] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const abort = useCallback(() => {
    abortRef.current?.abort();
    abortRef.current = null;
  }, []);

  // 组件卸载时自动取消进行中的请求
  useEffect(() => abort, [abort]);

  const startFetch = useCallback(async (url: string, body: object): Promise<string | null> => {
    // 取消上一次请求
    abort();

    const controller = new AbortController();
    abortRef.current = controller;

    setIsStreaming(true);
    setStreamContent('');
    setError(null);

    try {
      const response = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
        signal: controller.signal,
      });

      if (!response.ok) {
        const err = await response.json().catch(() => ({ error: '请求失败' }));
        throw new Error(err.error || `${response.status} 请求失败`);
      }

      const reader = response.body?.getReader();
      if (!reader) throw new Error('无法读取响应流');

      const decoder = new TextDecoder();
      let fullText = '';

      while (true) {
        const { done, value } = await reader.read();
        if (done) break;

        const chunk = decoder.decode(value, { stream: true });
        fullText += chunk;
        setStreamContent(fullText);
      }

      // 空响应处理：如果流结束但没有内容，可能是 API 调用失败
      if (!fullText.trim()) {
        throw new Error('模型返回了空响应，请检查 API Key 或模型配置');
      }

      return fullText;
    } catch (err: unknown) {
      // 主动 abort 不算错误
      if (err instanceof DOMException && err.name === 'AbortError') {
        return null;
      }
      const message = err instanceof Error ? err.message : '未知错误';
      setError(message);
      return null;
    } finally {
      // 只有当前 controller 仍然是活跃的才重置状态
      // 避免被 abort 的旧请求覆盖新请求的 isStreaming 状态
      if (abortRef.current === controller) {
        setIsStreaming(false);
      }
    }
  }, [abort]);

  return { streamContent, isStreaming, error, startFetch, abort, setStreamContent, abortRef };
}
</file>

<file path="src/lib/source-processing/ablation-tester.ts">
// ============================================
// 消融测试器 — 客户端编排模块
// "删掉这段会失去什么？"四分类判定
// ============================================

import { buildAblationTestingMessages } from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch } from '@/lib/analysis-chunker';
import type { SemanticSlice, AblationResult, AblationCategory } from '@/types';

const VALID_CATEGORIES: ReadonlySet<string> = new Set(['bone', 'muscle', 'filler_a', 'filler_b', 'uncertain']);

/** 构建消融测试请求体 */
export function getAblationTestingRequestBody(
  slices: SemanticSlice[],
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesJson = slices.map((s) =>
    JSON.stringify({
      sliceId: s.id,
      index: s.index,
      title: s.title,
      semanticTags: s.semanticTags,
      plotArc: s.plotArc,
      emotionalTone: s.emotionalTone,
      narrativeFunction: s.narrativeFunction,
      contentPreview: s.content.slice(0, 800),
      charCount: s.charCount,
    })
  ).join('\n');

  const { systemPrompt, userMessage } = buildAblationTestingMessages(slicesJson);
  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 解析消融测试 AI 输出 */
export function parseAblationResults(raw: string, slices: SemanticSlice[]): AblationResult[] {
  try {
    const jsonMatch = raw.match(/\[[\s\S]*\]/);
    if (!jsonMatch) return fallbackResults(slices);
    const parsed = JSON.parse(jsonMatch[0]);
    if (!Array.isArray(parsed)) return fallbackResults(slices);

    // 按 sliceId 建立 slices 索引（AI 输出可能乱序）
    const sliceById = new Map<string, SemanticSlice>();
    const sliceByIndex = new Map<number, SemanticSlice>();
    for (const s of slices) {
      sliceById.set(s.id, s);
      sliceByIndex.set(s.index, s);
    }

    return parsed
      .map((item: Record<string, unknown>) => {
        const category = String(item.category ?? 'uncertain');
        // 优先按 sliceId 匹配，回退到 sliceIndex
        const matchedSlice = (typeof item.sliceId === 'string' && sliceById.get(item.sliceId))
          || sliceByIndex.get(Number(item.sliceIndex ?? 0));
        return {
          sliceId: matchedSlice?.id ?? `unknown-${Number(item.sliceIndex ?? 0)}`,
          sliceIndex: matchedSlice?.index ?? Number(item.sliceIndex ?? 0),
          category: VALID_CATEGORIES.has(category) ? (category as AblationCategory) : 'uncertain',
          lostIfRemoved: String(item.lostIfRemoved ?? '不确定'),
          confidence: Math.min(1, Math.max(0, Number(item.confidence ?? 0.3))),
          reasoning: String(item.reasoning ?? '无法判定'),
        };
      })
      .filter((r: AblationResult) => sliceById.has(r.sliceId) || sliceByIndex.has(r.sliceIndex));
  } catch (err) {
    console.error('消融测试结果解析失败:', err);
    return fallbackResults(slices);
  }
}

/** 当 AI 输出无法解析时的回退结果（全部标记为 uncertain） */
function fallbackResults(slices: SemanticSlice[]): AblationResult[] {
  return slices.map((s) => ({
    sliceId: s.id,
    sliceIndex: s.index,
    category: 'uncertain' as AblationCategory,
    lostIfRemoved: '解析失败，无法判定',
    confidence: 0,
    reasoning: 'AI 输出格式异常，回退为 uncertain',
  }));
}

/** 计算消融测试批次 */
export function computeAblationBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; sliceGroups: SemanticSlice[][] } {
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);
  const groups: SemanticSlice[][] = [];
  let currentGroup: SemanticSlice[] = [];
  let currentChars = 0;

  // 每切片大约 1000 字符开销（摘要JSON）
  const CHARS_PER_SLICE_OVERHEAD = 1000;

  for (const slice of slices) {
    if (currentChars + CHARS_PER_SLICE_OVERHEAD > maxChars && currentGroup.length > 0) {
      groups.push(currentGroup);
      currentGroup = [];
      currentChars = 0;
    }
    currentGroup.push(slice);
    currentChars += CHARS_PER_SLICE_OVERHEAD;
  }
  if (currentGroup.length > 0) groups.push(currentGroup);

  return { needsBatch: groups.length > 1, sliceGroups: groups };
}

/** 聚合消融结果的统计摘要 */
export function summarizeAblationResults(results: AblationResult[]): {
  boneCount: number;
  muscleCount: number;
  fillerACount: number;
  fillerBCount: number;
  uncertainCount: number;
  totalCount: number;
} {
  const counts = { boneCount: 0, muscleCount: 0, fillerACount: 0, fillerBCount: 0, uncertainCount: 0, totalCount: results.length };
  for (const r of results) {
    switch (r.category) {
      case 'bone': counts.boneCount++; break;
      case 'muscle': counts.muscleCount++; break;
      case 'filler_a': counts.fillerACount++; break;
      case 'filler_b': counts.fillerBCount++; break;
      case 'uncertain': counts.uncertainCount++; break;
    }
  }
  return counts;
}
</file>

<file path="src/lib/source-processing/experience-annotator.ts">
// ============================================
// 体验流标注器 — 客户端编排模块
// AI 扮演读者（非批评家），多 reader persona 投票
// ============================================

import { buildExperienceAnnotationMessages, READER_PERSONAS } from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch, splitIntoBatches } from '@/lib/analysis-chunker';
import type { SemanticSlice, ExperienceAnnotation, ExperienceCurve } from '@/types';

/** 构建体验流标注请求体 */
export function getExperienceAnnotationRequestBody(
  slices: SemanticSlice[],
  persona: string,
  personaDescription: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesJson = slices.map((s) =>
    `## 切片 ${s.index}: ${s.title}\n- 语义标签: ${s.semanticTags.join(', ')}\n- 情节弧线: ${s.plotArc}\n- 情绪基调: ${s.emotionalTone}\n\n${s.content.slice(0, 1500)}`
  ).join('\n\n---\n\n');

  const { systemPrompt, userMessage } = buildExperienceAnnotationMessages(slicesJson, persona, personaDescription);
  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 解析体验流标注 AI 输出 */
export function parseExperienceAnnotations(raw: string, slices: SemanticSlice[], persona: string): ExperienceAnnotation[] {
  try {
    const jsonMatch = raw.match(/\[[\s\S]*\]/);
    if (!jsonMatch) return [];
    const parsed = JSON.parse(jsonMatch[0]);
    if (!Array.isArray(parsed)) return [];

    // 按 sliceId 建立 slices 索引（AI 输出可能乱序）
    const sliceById = new Map<string, SemanticSlice>();
    const sliceByIndex = new Map<number, SemanticSlice>();
    for (const s of slices) {
      sliceById.set(s.id, s);
      sliceByIndex.set(s.index, s);
    }

    return parsed
      .filter((item: Record<string, unknown>) =>
        typeof item.immersion === 'number' &&
        typeof item.emotional_intensity === 'number' &&
        typeof item.anticipation === 'number')
      .map((item: Record<string, unknown>) => {
        // 优先按 sliceId 匹配，回退到位置索引
        const matchedSlice = (typeof item.sliceId === 'string' && sliceById.get(item.sliceId))
          || sliceByIndex.get(Number(item.sliceIndex));
        return {
          sliceId: matchedSlice?.id ?? `unknown-${Number(item.sliceIndex ?? 0)}`,
          sliceIndex: matchedSlice?.index ?? Number(item.sliceIndex ?? 0),
          immersion: Math.min(10, Math.max(1, Math.round(Number(item.immersion)))),
          emotionalIntensity: Math.min(10, Math.max(1, Math.round(Number(item.emotional_intensity)))),
          anticipation: Math.min(10, Math.max(1, Math.round(Number(item.anticipation)))),
          perceivedPace: (['fast', 'medium', 'slow'].includes(String(item.perceived_pace)) ? String(item.perceived_pace) : 'medium') as 'fast' | 'medium' | 'slow',
          confidence: Math.min(1, Math.max(0, Number(item.confidence ?? 0.5))),
          readerPersona: persona,
          notes: String(item.notes ?? ''),
        };
      })
      .filter((ann: ExperienceAnnotation) => sliceById.has(ann.sliceId) || sliceByIndex.has(ann.sliceIndex));
  } catch (err) {
    console.error('体验流标注解析失败:', err);
    return [];
  }
}

/** 聚合多 persona 的标注结果为体验曲线 */
export function aggregateExperienceCurves(
  allAnnotations: ExperienceAnnotation[],
  slices: SemanticSlice[],
): ExperienceCurve[] {
  // 按 sliceId 分组
  const grouped = new Map<string, ExperienceAnnotation[]>();
  for (const ann of allAnnotations) {
    const existing = grouped.get(ann.sliceId) || [];
    existing.push(ann);
    grouped.set(ann.sliceId, existing);
  }

  return slices.map((slice) => {
    const annotations = grouped.get(slice.id) || [];
    if (annotations.length === 0) {
      return {
        sliceId: slice.id,
        sliceIndex: slice.index,
        avgImmersion: 0,
        avgEmotionalIntensity: 0,
        avgAnticipation: 0,
        dominantPace: 'medium' as const,
        scoreSpread: 0,
        highPoints: false,
      };
    }

    const n = annotations.length;
    const avgImmersion = annotations.reduce((s, a) => s + a.immersion, 0) / n;
    const avgEmotionalIntensity = annotations.reduce((s, a) => s + a.emotionalIntensity, 0) / n;
    const avgAnticipation = annotations.reduce((s, a) => s + a.anticipation, 0) / n;

    // 计算标准差（衡量主观性）
    const immersionVariance = annotations.reduce((s, a) => s + (a.immersion - avgImmersion) ** 2, 0) / n;

    // 主导节奏（投票）
    const paceVotes = { fast: 0, medium: 0, slow: 0 };
    for (const a of annotations) { paceVotes[a.perceivedPace]++; }
    const dominantPace = (Object.entries(paceVotes).sort(([, a], [, b]) => b - a)[0][0]) as 'fast' | 'medium' | 'slow';

    return {
      sliceId: slice.id,
      sliceIndex: slice.index,
      avgImmersion: Math.round(avgImmersion * 10) / 10,
      avgEmotionalIntensity: Math.round(avgEmotionalIntensity * 10) / 10,
      avgAnticipation: Math.round(avgAnticipation * 10) / 10,
      dominantPace,
      scoreSpread: Math.round(Math.sqrt(immersionVariance) * 10) / 10,
      highPoints: avgImmersion >= 7 && Math.sqrt(immersionVariance) <= 2,
    };
  });
}

/** 计算体验标注是否需要分批 */
export function computeExperienceBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; sliceGroups: SemanticSlice[][] } {
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);
  const groups: SemanticSlice[][] = [];
  let currentGroup: SemanticSlice[] = [];
  let currentChars = 0;

  for (const slice of slices) {
    const sliceChars = slice.content.length + 200; // 标题+标签开销
    if (currentChars + sliceChars > maxChars && currentGroup.length > 0) {
      groups.push(currentGroup);
      currentGroup = [];
      currentChars = 0;
    }
    currentGroup.push(slice);
    currentChars += sliceChars;
  }
  if (currentGroup.length > 0) groups.push(currentGroup);

  return { needsBatch: groups.length > 1, sliceGroups: groups };
}
</file>

<file path="src/lib/source-processing/program-indexer.ts">
// ============================================
// Step -1A: 程序索引 — 从原文提取实体索引
// 90% 程序 + 10% AI（仅最后分类用）
// ============================================

import type { EntityIndex } from '@/types';

// ---- 中文停用词表（核心子集） ----
const STOP_WORDS = new Set([
  // 代词
  '他', '她', '它', '我', '你', '我们', '你们', '他们', '她们', '它们',
  '自己', '这', '那', '这个', '那个', '什么', '怎么', '为什么',
  '哪', '哪里', '谁', '多少', '怎样',
  // 介词/连词
  '的', '了', '在', '是', '我', '有', '和', '就', '不', '人', '都', '一',
  '一个', '上', '也', '很', '到', '说', '要', '去', '你', '会', '着', '没有',
  '看', '好', '自己', '这', '他', '她', '它', '吗', '吧', '啊', '呢', '哦',
  '哈', '嗯', '呀', '嘛', '啦', '喔', '嘿', '哎', '唉', '嗨',
  // 量词
  '个', '只', '把', '条', '件', '次', '位', '种', '样', '份', '群', '批',
  '些', '点', '段', '间', '年', '月', '日', '天', '时', '分', '秒',
  // 方位
  '里', '外', '前', '后', '左', '右', '上', '下', '中', '内', '旁',
  // 常见动词/形容词（太泛，无区分度）
  '知道', '可以', '已经', '应该', '可能', '需要', '开始', '继续',
  '正在', '一直', '虽然', '但是', '因为', '所以', '如果', '不过',
  '然后', '之后', '之后', '以前', '以后', '之间', '关于', '对于',
  '但是', '而且', '或者', '还是', '不过', '只是', '已经', '正在',
  '这个', '那个', '这些', '那些', '什么', '怎样', '如何', '为什么',
  '今天', '明天', '昨天', '现在', '时候', '地方', '东西', '问题',
  '时候', '样子', '事情', '道理', '办法', '关系', '意思', '感觉',
  '忽然', '突然', '立刻', '马上', '终于', '竟然', '果然', '居然',
  '慢慢', '渐渐', '轻轻', '默默', '悄悄',
  // 时间词
  '早上', '中午', '下午', '晚上', '白天', '夜晚', '早上', '今天',
  '昨天', '明天', '早上', '上午',
]);

// ---- 对话标记（出现在角色名附近的词） ----
const DIALOGUE_MARKERS = [
  '说道', '道', '问道', '喊道', '叫道', '笑道', '冷笑道', '低声道',
  '说道', '怒道', '沉声道', '大声道', '轻声道', '缓缓道', '淡淡道',
  '看着', '看向', '望着', '盯着', '凝视着', '瞥了', '扫了一眼',
  '回答', '问道', '说了', '吼道', '喝道', '催促道', '建议道',
  '说', '问', '喊', '叫', '笑', '吼', '喝',
];

// ---- 章节边界正则 ----
const CHAPTER_REGEX = /第[零一二三四五六七八九十百千万\d]+[章节回卷集部篇]/g;

/**
 * 从原文中提取候选实体（第一层：纯程序）
 * 使用 n-gram 统计 + 对话标记检测
 */
export function extractCandidateEntities(rawText: string): Map<string, { frequency: number; contexts: string[] }> {
  const candidates = new Map<string, { frequency: number; contexts: string[] }>();

  // 1. 提取2-4字 n-gram
  // 只在非空白、非标点的中文字符序列上统计
  const chineseText = rawText.replace(/[^一-鿿]/g, ' ');

  for (let n = 2; n <= 4; n++) {
    const segments = chineseText.split(/\s+/).filter(s => s.length >= n);
    for (const seg of segments) {
      for (let i = 0; i <= seg.length - n; i++) {
        const gram = seg.slice(i, i + n);
        if (STOP_WORDS.has(gram)) continue;
        // 过滤纯数字、纯标点
        if (/^[零一二三四五六七八九十百千万\d]+$/.test(gram)) continue;

        const entry = candidates.get(gram) || { frequency: 0, contexts: [] };
        entry.frequency++;
        candidates.set(gram, entry);
      }
    }
  }

  // 2. 利用对话标记提取特殊实体
  // 匹配模式："XX说道" "XX看着" 等
  for (const marker of DIALOGUE_MARKERS) {
    const regex = new RegExp(`([\\u4e00-\\u9fff]{2,4})${escapeRegex(marker)}`, 'g');
    let match;
    while ((match = regex.exec(rawText)) !== null) {
      const name = match[1];
      if (!STOP_WORDS.has(name) && name.length >= 2) {
        const entry = candidates.get(name) || { frequency: 0, contexts: [] };
        entry.frequency++;
        if (entry.contexts.length < 3) {
          // 保存一小段上下文（前后各10字）
          const start = Math.max(0, match.index - 10);
          const end = Math.min(rawText.length, match.index + name.length + marker.length + 10);
          entry.contexts.push(rawText.slice(start, end));
        }
        candidates.set(name, entry);
      }
    }
  }

  return candidates;
}

/**
 * 统计过滤（第二层）
 * 过滤停用词、低频项、普通名词
 */
export function filterCandidates(
  candidates: Map<string, { frequency: number; contexts: string[] }>,
  totalChars: number,
): Map<string, { frequency: number; contexts: string[] }> {
  // 动态最低频率：按文本长度调整
  // 10万字以下：3次；50万字：5次；200万字：8次
  const minFreq = totalChars < 100000 ? 3 : totalChars < 500000 ? 5 : 8;

  // 最大保留实体数
  const maxEntities = totalChars < 50000 ? 80 : totalChars < 500000 ? 200 : 300;

  const filtered = new Map<string, { frequency: number; contexts: string[] }>();

  // 按频率排序
  const sorted = [...candidates.entries()]
    .filter(([name, data]) => {
      if (data.frequency < minFreq) return false;
      if (STOP_WORDS.has(name)) return false;
      // 过滤纯方位词、纯时间词
      if (/^[上下左右前后内外中东西南北]+$/.test(name)) return false;
      // 过滤以"的""了"结尾的短语
      if (name.endsWith('的') || name.endsWith('了') || name.endsWith('着')) return false;
      // 过滤章节标题词
      if (/^第[零一二三四五六七八九十百千万\d]+/.test(name)) return false;
      return true;
    })
    .sort((a, b) => b[1].frequency - a[1].frequency);

  for (let i = 0; i < Math.min(sorted.length, maxEntities); i++) {
    const [name, data] = sorted[i];
    filtered.set(name, data);
  }

  return filtered;
}

/**
 * 检测别名（同一个实体的不同称呼）
 * 简单策略：如果两个高频实体A和B，且B完全包含在A的上下文中频繁共现，可能是别名
 */
export function detectAliases(
  filtered: Map<string, { frequency: number; contexts: string[] }>,
): Map<string, string[]> {
  const aliases = new Map<string, string[]>();
  const names = [...filtered.keys()];

  for (let i = 0; i < names.length; i++) {
    for (let j = i + 1; j < names.length; j++) {
      const a = names[i];
      const b = names[j];

      // 如果一个名字包含另一个，可能是全名/简称
      if (a.includes(b) || b.includes(a)) {
        // 较长的作为主名
        const primary = a.length >= b.length ? a : b;
        const secondary = a.length >= b.length ? b : a;
        const existing = aliases.get(primary) || [];
        if (!existing.includes(secondary)) {
          existing.push(secondary);
          aliases.set(primary, existing);
        }
      }
    }
  }

  return aliases;
}

/**
 * 构建每个实体出现在哪些章节/位置
 */
export function buildEntityOccurrences(
  rawText: string,
  entityNames: string[],
): Map<string, string[]> {
  const occurrences = new Map<string, string[]>();

  // 先找章节边界
  const chapters: { title: string; start: number; end: number }[] = [];
  let match;
  const regex = new RegExp(CHAPTER_REGEX.source, 'g');
  while ((match = regex.exec(rawText)) !== null) {
    chapters.push({
      title: match[0],
      start: match.index,
      end: 0,
    });
  }
  // 设置 end
  for (let i = 0; i < chapters.length; i++) {
    chapters[i].end = i + 1 < chapters.length ? chapters[i + 1].start : rawText.length;
  }

  // 对每个实体，统计出现在哪些章节
  for (const name of entityNames) {
    const positions: number[] = [];
    let searchStart = 0;
    while (true) {
      const idx = rawText.indexOf(name, searchStart);
      if (idx === -1) break;
      positions.push(idx);
      searchStart = idx + name.length;
      // 最多采样1000个位置
      if (positions.length >= 1000) break;
    }

    // 映射到章节
    const chapterTitles = new Set<string>();
    for (const pos of positions) {
      for (const ch of chapters) {
        if (pos >= ch.start && pos < ch.end) {
          chapterTitles.add(ch.title);
          break;
        }
      }
    }

    occurrences.set(name, [...chapterTitles]);
  }

  return occurrences;
}

/**
 * 构建完整的 EntityIndex（综合上面所有步骤）
 * 注意：type 字段留给 AI 分类步骤填充，这里默认 'concept'
 */
export function buildEntityIndex(
  rawText: string,
  candidates: Map<string, { frequency: number; contexts: string[] }>,
  aliases: Map<string, string[]>,
  occurrences: Map<string, string[]>,
): EntityIndex {
  const index: EntityIndex = {};

  for (const [name, data] of candidates) {
    const entityAliases = aliases.get(name) || [];
    index[name] = {
      type: 'concept', // 默认类型，AI分类步骤会更新
      aliases: entityAliases,
      frequency: data.frequency,
      occurrences: occurrences.get(name) || [],
      segments: [], // Step -1B 填充
    };
  }

  return index;
}

/**
 * 按类型拆分 EntityIndex 为子索引
 */
export function splitEntityIndex(index: EntityIndex): {
  characterIndex: EntityIndex;
  locationIndex: EntityIndex;
  organizationIndex: EntityIndex;
  artifactIndex: EntityIndex;
  conceptIndex: EntityIndex;
} {
  const characterIndex: EntityIndex = {};
  const locationIndex: EntityIndex = {};
  const organizationIndex: EntityIndex = {};
  const artifactIndex: EntityIndex = {};
  const conceptIndex: EntityIndex = {};

  for (const [name, entry] of Object.entries(index)) {
    switch (entry.type) {
      case 'character':
        characterIndex[name] = entry;
        break;
      case 'location':
        locationIndex[name] = entry;
        break;
      case 'organization':
        organizationIndex[name] = entry;
        break;
      case 'artifact':
        artifactIndex[name] = entry;
        break;
      case 'concept':
        conceptIndex[name] = entry;
        break;
    }
  }

  return { characterIndex, locationIndex, organizationIndex, artifactIndex, conceptIndex };
}

/**
 * 主入口：执行完整的 Step -1A 程序索引（不含AI分类）
 */
export function runProgramIndexing(rawText: string): {
  entityIndex: EntityIndex;
  candidateNames: string[];
} {
  // Layer 1: 提取候选实体
  const candidates = extractCandidateEntities(rawText);

  // Layer 2: 过滤
  const filtered = filterCandidates(candidates, rawText.length);

  // 别名检测
  const aliases = detectAliases(filtered);

  // 构建位置索引
  const names = [...filtered.keys()];
  const occurrences = buildEntityOccurrences(rawText, names);

  // 综合为 EntityIndex
  const entityIndex = buildEntityIndex(rawText, filtered, aliases, occurrences);

  return {
    entityIndex,
    candidateNames: names,
  };
}

function escapeRegex(str: string): string {
  return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
</file>

<file path="src/lib/source-processing/smart-slicer.ts">
// ============================================
// 智能切片 — 客户端编排模块
// ============================================

import type { SemanticSlice } from '@/types';
import { buildSlicingMessages } from '@/lib/ai/prompts';
import { chunkText, DEFAULT_MAX_CHUNK_SIZE } from '@/lib/chunker';

/** 解析 AI 输出的切片 markdown 为 SemanticSlice[] */
export function parseSliceOutput(raw: string, novelId: string): SemanticSlice[] {
  const slices: SemanticSlice[] = [];
  const sections = raw.split(/^## 切片 /m).filter(Boolean);

  for (const section of sections) {
    const lines = section.split('\n');
    const headerLine = lines[0] ?? '';
    const titleMatch = headerLine.match(/^\d+:\s*(.+)/);

    // 提取元数据
    let semanticTags: string[] = [];
    let plotArc = '';
    let emotionalTone = '';
    let contentStartIndex = 0;

    for (let i = 1; i < lines.length; i++) {
      const line = lines[i].trim();
      if (line.startsWith('- 语义标签:')) {
        semanticTags = line.replace('- 语义标签:', '').split(',').map((s) => s.trim()).filter(Boolean);
      } else if (line.startsWith('- 情节弧线:')) {
        plotArc = line.replace('- 情节弧线:', '').trim();
      } else if (line.startsWith('- 情绪基调:')) {
        emotionalTone = line.replace('- 情绪基调:', '').trim();
      } else if (line === '---' || (line && !line.startsWith('-') && contentStartIndex === 0)) {
        // 找到正文开始位置
        if (contentStartIndex === 0) contentStartIndex = i;
        break;
      }
    }

    // 如果没找到明确的正文开始位置，跳过元数据行后开始
    if (contentStartIndex === 0) {
      contentStartIndex = lines.findIndex((l, i) => i > 0 && !l.startsWith('-') && l.trim() !== '' && l.trim() !== '---');
      if (contentStartIndex === -1) contentStartIndex = lines.length;
    }

    const content = lines.slice(contentStartIndex).join('\n').replace(/^---\s*/m, '').trim();

    if (content) {
      slices.push({
        id: `${novelId}-slice-${slices.length}`,
        index: slices.length,
        title: titleMatch?.[1]?.trim() ?? `切片${slices.length + 1}`,
        content,
        charCount: content.length,
        semanticTags,
        plotArc,
        emotionalTone,
        // 增强字段默认值（后续由 Memory Graph 填充）
        segmentId: '',
        volume: null,
        chapterRange: '',
        characterRefs: [],
        narrativeFunction: '',
        tensionLevel: 0,
        dependencies: [],
      });
    }
  }

  return slices;
}

/** 降级：使用硬分块 + 空语义标签 */
export function fallbackSlice(novelId: string, fullText: string): SemanticSlice[] {
  const chunks = chunkText(novelId, fullText, DEFAULT_MAX_CHUNK_SIZE);
  return chunks.map((chunk, index) => ({
    id: chunk.id,
    index,
    title: chunk.title,
    content: chunk.content,
    charCount: chunk.charCount,
    semanticTags: [],
    plotArc: '',
    emotionalTone: '',
    segmentId: '',
    volume: null,
    chapterRange: '',
    characterRefs: [],
    narrativeFunction: '',
    tensionLevel: 0,
    dependencies: [],
  }));
}

/** 获取切片 API 的请求体 */
export function getSlicingRequestBody(
  rawText: string,
  apiKey: string,
  model: string,
  baseURL: string,
  targetSliceSize: number = 5000,
) {
  const { systemPrompt, userMessage } = buildSlicingMessages(rawText, targetSliceSize);
  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/**
 * 判断文本是否足够短，可以跳过 AI 切片，直接作为单一切片
 */
export function shouldSkipSlicing(rawText: string, targetSliceSize: number = 5000): boolean {
  return rawText.length <= targetSliceSize * 1.3;
}

/**
 * 短文本直接创建单一切片（无需 AI 调用）
 */
export function createSingleSlice(novelId: string, rawText: string): SemanticSlice[] {
  return [{
    id: `${novelId}-slice-0`,
    index: 0,
    title: '全文',
    content: rawText,
    charCount: rawText.length,
    semanticTags: [],
    plotArc: '全文',
    emotionalTone: '',
    segmentId: '',
    volume: null,
    chapterRange: '',
    characterRefs: [],
    narrativeFunction: '',
    tensionLevel: 0,
    dependencies: [],
  }];
}
</file>

<file path="src/lib/source-processing/style-extractor.ts">
// ============================================
// 文风提取 — 客户端编排模块
// ============================================

import {
  buildStyleExtractionMessages,
  buildStyleSupplementMessages,
} from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch, splitIntoBatches } from '@/lib/analysis-chunker';
import type { SemanticSlice } from '@/types';

/** 获取首批文风提取的请求体 */
export function getStyleExtractionRequestBody(
  slices: SemanticSlice[],
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const slicesText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const { systemPrompt, userMessage } = buildStyleExtractionMessages(slicesText);

  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/** 获取补充批次的请求体（用于多批处理） */
export function getStyleSupplementRequestBody(
  newChunk: string,
  previousProfile: string,
  apiKey: string,
  model: string,
  baseURL: string,
) {
  const { systemPrompt, userMessage } = buildStyleSupplementMessages(newChunk, previousProfile);
  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}

/**
 * 计算文风提取是否需要分批
 * 返回 null 表示无需分批，否则返回分批信息
 */
export function computeStyleBatches(
  slices: SemanticSlice[],
  maxContextTokens: number,
): { needsBatch: boolean; batches: string[] } {
  const fullText = slices.map((s) => `### ${s.title}\n\n${s.content}`).join('\n\n');
  const maxChars = computeMaxCharsPerBatch(maxContextTokens);

  if (fullText.length <= maxChars) {
    return { needsBatch: false, batches: [fullText] };
  }

  return {
    needsBatch: true,
    batches: splitIntoBatches(fullText, maxChars),
  };
}
</file>

<file path="src/lib/source-processing/tension-tracker.ts">
// ============================================
// 势能追踪器 — 客户端编排模块
// 追踪"积累→释放"周期，分析势能模式与呼吸节奏
// ============================================

import { buildTensionTrackingMessages } from '@/lib/ai/prompts';
import type { SemanticSlice, TensionPattern, TensionAnalysis, TensionAnchor, RhythmProfile, ExperienceCurve } from '@/types';

const VALID_CLIMAX_TYPES: ReadonlySet<string> = new Set(['emotional', 'cognitive', 'power', 'identity', 'relationship']);
const VALID_PAYOFFS: ReadonlySet<string> = new Set(['low', 'medium', 'high', 'extreme']);

/** 构建势能追踪请求体 */
export function getTensionTrackingRequestBody(
  slices: SemanticSlice[],
  plotReport: string,
  experienceCurve: ExperienceCurve[],
  apiKey: string,
  model: string,
  baseURL: string,
) {
  // 切片摘要（只传关键元数据，不传全文以节省 token）
  const slicesSummary = slices.map((s) =>
    `[${s.index}] ${s.title} | 标签: ${s.semanticTags.join(',')} | 弧线: ${s.plotArc} | 情绪: ${s.emotionalTone} | 叙事功能: ${s.narrativeFunction} | 紧张度: ${s.tensionLevel}`
  ).join('\n');

  const experienceCurveJson = JSON.stringify(
    experienceCurve.map((c) => ({
      sliceIndex: c.sliceIndex,
      immersion: c.avgImmersion,
      emotionalIntensity: c.avgEmotionalIntensity,
      anticipation: c.avgAnticipation,
      pace: c.dominantPace,
      isHighPoint: c.highPoints,
    })),
    null, 2
  );

  const { systemPrompt, userMessage } = buildTensionTrackingMessages(slicesSummary, plotReport, experienceCurveJson);
  return { systemPrompt, userMessage, apiKey, model, baseURL };
}

/** 解析势能追踪 AI 输出 */
export function parseTensionAnalysis(raw: string, slices: SemanticSlice[]): TensionAnalysis | null {
  try {
    // 尝试提取 JSON 块
    const jsonMatch = raw.match(/\{[\s\S]*"rhythmProfile"[\s\S]*\}/);
    if (!jsonMatch) return null;
    const parsed = JSON.parse(jsonMatch[0]);

    const patterns: TensionPattern[] = (Array.isArray(parsed.patterns) ? parsed.patterns : [])
      .filter((p: Record<string, unknown>) =>
        typeof p.climaxType === 'string' && VALID_CLIMAX_TYPES.has(p.climaxType) &&
        typeof p.climaxSliceIndex === 'number' &&
        typeof p.startSliceIndex === 'number' &&
        typeof p.duration === 'number')
      .map((p: Record<string, unknown>, index: number) => ({
        id: `tension-${index}`,
        climaxType: p.climaxType as TensionPattern['climaxType'],
        startIndex: Number(p.startSliceIndex),
        duration: Number(p.duration),
        reinforcements: Array.isArray(p.reinforcements)
          ? p.reinforcements.map((r: Record<string, unknown>): TensionAnchor => ({
            sliceId: slices[Number(r.sliceIndex)]?.id ?? '',
            sliceIndex: Number(r.sliceIndex),
            chapter: slices[Number(r.sliceIndex)]?.chapterRange ?? '',
            type: 'reinforcement' as const,
            description: String(r.description ?? ''),
            tensionBefore: Math.min(10, Math.max(1, Number(r.tensionBefore ?? 5))),
            tensionAfter: Math.min(10, Math.max(1, Number(r.tensionAfter ?? 7))),
          }))
          : [],
        release: {
          sliceId: slices[Number(p.climaxSliceIndex)]?.id ?? '',
          sliceIndex: Number(p.climaxSliceIndex),
          chapter: slices[Number(p.climaxSliceIndex)]?.chapterRange ?? '',
          type: 'release' as const,
          description: String(p.climaxDescription ?? ''),
          tensionBefore: Math.min(10, Math.max(1, Number(p.tension_before ?? 9))),
          tensionAfter: Math.min(10, Math.max(1, Number(p.tension_after ?? 3))),
        },
        payoffMultiplier: VALID_PAYOFFS.has(String(p.payoffMultiplier)) ? String(p.payoffMultiplier) as TensionPattern['payoffMultiplier'] : 'medium',
        description: String(p.climaxDescription ?? ''),
      }));

    const rhythmProfile: RhythmProfile = {
      propulsionRatio: Number(parsed.rhythmProfile?.propulsionRatio ?? 0),
      buildupRatio: Number(parsed.rhythmProfile?.buildupRatio ?? 0),
      releaseRatio: Number(parsed.rhythmProfile?.releaseRatio ?? 0),
      breathRatio: Number(parsed.rhythmProfile?.breathRatio ?? 0),
      existenceRatio: Number(parsed.rhythmProfile?.existenceRatio ?? 0),
      calibrationRatio: Number(parsed.rhythmProfile?.calibrationRatio ?? 0),
    };

    return {
      patterns,
      breathingCycle: String(parsed.breathingCycle ?? '未知'),
      rhythmProfile,
    };
  } catch (err) {
    console.error('势能追踪结果解析失败:', err);
    return null;
  }
}
</file>

<file path="src/lib/source-processing/value-sampler.ts">
// ============================================
// 价值采样器 — 从事件图谱选取核心切片
// 不平均采样，按叙事价值选取 30-50 个
// ============================================

import type { EventGraph, NovelEvent, SemanticSlice, TextChunk } from '@/types';

/** 采样配置 */
export interface ValueSamplingConfig {
  maxSamples: number;            // 最大采样数（默认 50）
  climaxRatio: number;           // 高潮占比
  turningPointRatio: number;     // 转折点占比
  foreshadowingRatio: number;    // 伏笔-回收对占比
  breathRatio: number;           // 呼吸段落占比
  userAnchorCount: number;       // 用户锚点数量
}

const DEFAULT_CONFIG: ValueSamplingConfig = {
  maxSamples: 50,
  climaxRatio: 0.30,
  turningPointRatio: 0.20,
  foreshadowingRatio: 0.25,
  breathRatio: 0.15,
  userAnchorCount: 5,
};

/** 采样结果 */
export interface ValueSamplingResult {
  selectedSliceIndices: number[];    // 选中的切片索引
  samplingRationale: string;         // 采样理由摘要
  byCategory: Record<string, number[]>; // 按类别的切片索引
}

/** 事件图谱的高潮转折类型集合 */
const CLIMAX_TYPES = new Set(['战斗/冲突', '突破/升级', '死亡/牺牲', '揭露/发现']);
const TURNING_TYPES = new Set(['背叛/决裂', '决策/选择', '离别/重逢', '结盟/合作']);
const BREATH_TYPES = new Set(['日常/过渡']);

/**
 * 从事件图谱中执行价值采样
 * @param eventGraph 事件图谱
 * @param slices 原始切片（SemanticSlice[]）
 * @param config 采样配置
 * @param userAnchorSliceIndices 用户标注的锚点切片索引
 */
export function sampleByValue(
  eventGraph: EventGraph,
  slices: SemanticSlice[],
  config: Partial<ValueSamplingConfig> = {},
  userAnchorSliceIndices: number[] = [],
): ValueSamplingResult {
  const cfg = { ...DEFAULT_CONFIG, ...config };
  const events = eventGraph.events;
  const eventById = new Map(events.map((e) => [e.id, e] as const));
  const selected = new Set<number>();
  const byCategory: Record<string, number[]> = {
    climax: [],
    turning: [],
    foreshadowing: [],
    breath: [],
    userAnchor: [],
  };

  // ── 1. 高潮点 ──
  const climaxEvents = events
    .filter((e) => CLIMAX_TYPES.has(e.type) || Math.abs(e.tensionChange) >= 3)
    .sort((a, b) => b.confidence - a.confidence || Math.abs(b.tensionChange) - Math.abs(a.tensionChange));

  const climaxCount = Math.floor(cfg.maxSamples * cfg.climaxRatio);
  for (const e of climaxEvents.slice(0, climaxCount)) {
    const sliceIdx = findSliceIndex(e, slices);
    if (sliceIdx >= 0 && !selected.has(sliceIdx)) {
      selected.add(sliceIdx);
      byCategory.climax.push(sliceIdx);
    }
  }

  // ── 2. 转折点 ──
  const turningEvents = events
    .filter((e) => TURNING_TYPES.has(e.type))
    .sort((a, b) => b.confidence - a.confidence);

  const turningCount = Math.floor(cfg.maxSamples * cfg.turningPointRatio);
  for (const e of turningEvents.slice(0, turningCount)) {
    const sliceIdx = findSliceIndex(e, slices);
    if (sliceIdx >= 0 && !selected.has(sliceIdx)) {
      selected.add(sliceIdx);
      byCategory.turning.push(sliceIdx);
    }
  }

  // ── 3. 伏笔-回收对 ──
  const foreshadowCount = Math.floor(cfg.maxSamples * cfg.foreshadowingRatio);
  const pairs = eventGraph.foreshadowingPairs
    .filter((p) => p.distance > 5) // 只取长程伏笔
    .sort((a, b) => b.distance - a.distance);

  for (const pair of pairs.slice(0, Math.ceil(foreshadowCount / 2))) {
    const setupEvent = eventById.get(pair.setup);
    const payoffEvent = eventById.get(pair.payoff);
    for (const e of [setupEvent, payoffEvent]) {
      if (!e) continue;
      const sliceIdx = findSliceIndex(e, slices);
      if (sliceIdx >= 0 && !selected.has(sliceIdx)) {
        selected.add(sliceIdx);
        byCategory.foreshadowing.push(sliceIdx);
      }
    }
  }

  // ── 4. 呼吸段落 ──
  const breathCount = Math.floor(cfg.maxSamples * cfg.breathRatio);
  const breathEvents = events
    .filter((e) => BREATH_TYPES.has(e.type) || (Math.abs(e.tensionChange) <= 1 && !CLIMAX_TYPES.has(e.type)))
    .sort((a, b) => a.chapter - b.chapter); // 均匀分布在时间线上

  if (breathEvents.length > 0) {
    const step = Math.max(1, Math.floor(breathEvents.length / breathCount));
    for (let i = 0; i < breathEvents.length && byCategory.breath.length < breathCount; i += step) {
      const sliceIdx = findSliceIndex(breathEvents[i], slices);
      if (sliceIdx >= 0 && !selected.has(sliceIdx)) {
        selected.add(sliceIdx);
        byCategory.breath.push(sliceIdx);
      }
    }
  }

  // ── 5. 用户锚点 ──
  for (const idx of userAnchorSliceIndices.slice(0, cfg.userAnchorCount)) {
    if (idx >= 0 && idx < slices.length && !selected.has(idx)) {
      selected.add(idx);
      byCategory.userAnchor.push(idx);
    }
  }

  // ── 补充未达标 ──
  if (selected.size < cfg.maxSamples * 0.6) {
    // 按置信度随机补充
    const remaining = events
      .filter((e) => !selected.has(findSliceIndex(e, slices)))
      .sort((a, b) => b.confidence - a.confidence);

    for (const e of remaining) {
      if (selected.size >= cfg.maxSamples) break;
      const sliceIdx = findSliceIndex(e, slices);
      if (sliceIdx >= 0 && !selected.has(sliceIdx)) {
        selected.add(sliceIdx);
      }
    }
  }

  const rationale = `价值采样：高潮${byCategory.climax.length}个 + 转折${byCategory.turning.length}个 + 伏笔对${byCategory.foreshadowing.length}个 + 呼吸${byCategory.breath.length}个 + 用户锚点${byCategory.userAnchor.length}个 = 共${selected.size}个切片`;

  return {
    selectedSliceIndices: [...selected].sort((a, b) => a - b),
    samplingRationale: rationale,
    byCategory,
  };
}

/**
 * 从事件找到对应的切片索引
 * 假设 chunk index ≈ slice index（1:1 线性映射），就近搜索匹配角色引用的切片
 */
function findSliceIndex(event: NovelEvent, slices: SemanticSlice[]): number {
  // chunk 索引直接作为 slice 索引的初始估计（假设粒度一致）
  const estimatedSlice = Math.min(event.chunkIndex, slices.length - 1);
  // 就近搜索带匹配角色引用的切片
  const searchStart = Math.max(0, estimatedSlice - 3);
  const searchEnd = Math.min(slices.length - 1, estimatedSlice + 3);

  for (let i = searchStart; i <= searchEnd; i++) {
    const slice = slices[i];
    if (slice && event.participants.some((p) => slice.characterRefs?.includes(p))) {
      return i;
    }
  }

  return estimatedSlice >= 0 && estimatedSlice < slices.length ? estimatedSlice : -1;
}
</file>

<file path="src/lib/store/project.ts">
// ============================================
// 写作项目 Store — 服务端为主，浏览器缓存
// ============================================

import { create } from 'zustand';
import type {
  WritingProject,
  BookOutline,
  Phase,
  Volume,
  ChapterSet,
  ChapterPlan,
  GeneratedChapter,
} from '@/types';

export interface ProjectState {
  /** 浏览器内存缓存 */
  projects: WritingProject[];
  /** 当前活跃项目 ID */
  activeProjectId: string | null;

  // 数据加载
  loadProjects: () => Promise<void>;

  // 导航
  setActiveProjectId: (id: string | null) => void;

  // 项目 CRUD
  addProject: (project: WritingProject) => void;
  removeProject: (id: string) => void;
  updateProject: (id: string, updates: Partial<WritingProject>) => void;

  // 五层产出设置
  setOutline: (projectId: string, outline: BookOutline) => void;
  setPhases: (projectId: string, phases: Phase[]) => void;
  setVolumes: (projectId: string, volumes: Volume[]) => void;
  setChapterSets: (projectId: string, sets: ChapterSet[]) => void;
  setChapterPlans: (projectId: string, plans: ChapterPlan[]) => void;

  // 章节生成
  addGeneratedChapter: (projectId: string, chapter: GeneratedChapter) => void;
  updateGeneratedChapter: (projectId: string, chapterId: string, updates: Partial<GeneratedChapter>) => void;
}

export const useProjectStore = create<ProjectState>()((set, get) => ({
  projects: [],
  activeProjectId: null,

  loadProjects: async () => {
    try {
      const res = await fetch('/api/project/list');
      if (!res.ok) return;
      const data = await res.json();
      set({ projects: data.projects ?? [] });
    } catch (err) {
      console.error('加载项目列表失败:', err);
    }
  },

  setActiveProjectId: (id) => set({ activeProjectId: id }),

  addProject: (project) => {
    set((state) => ({ projects: [...state.projects, project] }));
  },

  removeProject: (id) => {
    set((state) => ({
      projects: state.projects.filter((p) => p.id !== id),
      activeProjectId: state.activeProjectId === id ? null : state.activeProjectId,
    }));
  },

  updateProject: (id, updates) => {
    set((state) => ({
      projects: state.projects.map((p) =>
        p.id === id ? { ...p, ...updates, updatedAt: new Date().toISOString() } : p
      ),
    }));
  },

  setOutline: (projectId, outline) => {
    get().updateProject(projectId, { outline, currentLayer: 1 });
  },

  setPhases: (projectId, phases) => {
    get().updateProject(projectId, { phases, currentLayer: 2 });
  },

  setVolumes: (projectId, volumes) => {
    get().updateProject(projectId, { volumes, currentLayer: 3 });
  },

  setChapterSets: (projectId, chapterSets) => {
    get().updateProject(projectId, { chapterSets, currentLayer: 4 });
  },

  setChapterPlans: (projectId, chapterPlans) => {
    get().updateProject(projectId, { chapterPlans, currentLayer: 5 });
  },

  addGeneratedChapter: (projectId, chapter) => {
    set((state) => ({
      projects: state.projects.map((p) =>
        p.id === projectId
          ? { ...p, chapters: [...p.chapters, chapter], updatedAt: new Date().toISOString() }
          : p
      ),
    }));
  },

  updateGeneratedChapter: (projectId, chapterId, updates) => {
    set((state) => ({
      projects: state.projects.map((p) =>
        p.id === projectId
          ? {
              ...p,
              chapters: p.chapters.map((c) =>
                c.id === chapterId ? { ...c, ...updates } : c
              ),
              updatedAt: new Date().toISOString(),
            }
          : p
      ),
    }));
  },
}));
</file>

<file path="src/lib/text-cleaner.ts">
// ============================================
// TXT 文本清洗 — 过滤网络小说噪声 + 标点规范化
// ============================================
// 参考项目：txt_reform、text_edit、novel-proofer、nanoWriter、novel-proofreader

import type { CleaningConfig, CleaningStepId } from '@/types';

/** 全部 10 个清洗步骤 ID */
export const ALL_CLEANING_STEPS: CleaningStepId[] = [
  'encoding', 'urls', 'promos', 'authorNotes', 'watermarks',
  'nav', 'separators', 'toc', 'punctuation', 'blankLines',
];

/** 预设对应的步骤列表（模块级常量，避免每次调用重新分配） */
const STEPS_STANDARD = ALL_CLEANING_STEPS.filter(
  (s) => s !== 'promos' && s !== 'authorNotes',
);
const STEPS_LIGHT: CleaningStepId[] = ['encoding', 'punctuation', 'blankLines'];

/** 根据 CleaningConfig 解析出需要执行的步骤列表 */
export function resolveCleaningSteps(config: CleaningConfig): CleaningStepId[] {
  switch (config.preset) {
    case 'aggressive':
      return ALL_CLEANING_STEPS;
    case 'standard':
      return STEPS_STANDARD;
    case 'light':
      return STEPS_LIGHT;
    case 'none':
      return config.enabledSteps;
    default:
      // 防御性回退：未知预设 → 运行全部步骤
      console.warn(`Unknown cleaning preset: "${config.preset}", falling back to aggressive`);
      return ALL_CLEANING_STEPS;
  }
}

// ---- Step 1: 编码清理 ----

/** 去掉 BOM、零宽字符、替换字符、控制字符，统一换行 */
export function stripEncodingArtifacts(text: string): string {
  return text
    .replace(/^﻿/, '')                                          // UTF-8 BOM
    .replace(/[​‌‍﻿­�]/g, '')         // 零宽/软连字符/替换
    .replace(/\r\n/g, '\n')                                          // CRLF → LF
    .replace(/\r/g, '\n')                                            // CR → LF
    .replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, '');             // 控制字符
}

// ---- Step 2: URL 行 ----

const URL_LINE_RE = /^(?:https?:\/\/|www\.)\S+$/i;

/** 删除整行都是 URL 的行 */
export function removeUrlLines(lines: string[]): string[] {
  return lines.filter((line) => !URL_LINE_RE.test(line.trim()));
}

// ---- Step 3: 广告/推广行 ----

const PROMO_PATTERNS: RegExp[] = [
  /本章未完.*继续/,
  /最新章节/,
  /本书由.*首发/,
  /无弹窗/,
  /免费阅读/,
  /记住.*网址/,
  /纯文字.*首发/,
  /更新最快/,
  /天才一秒记住/,
  /新笔下文学/,
  /吾爱文学/,
  /笔趣阁/,
  /起点中文网/,
  /创世中文网/,
  /纵横中文网/,
  /小说阅读网/,
];

/** 删除广告推广行 */
export function removePromoLines(lines: string[]): string[] {
  return lines.filter((line) => {
    const t = line.trim();
    if (!t) return true; // 保留空行
    return !PROMO_PATTERNS.some((p) => p.test(t));
  });
}

// ---- Step 4: 作者碎碎念 ----

const AUTHOR_NOTE_PATTERNS: RegExp[] = [
  /^ps[\s:：]/i,
  /^作者[说有感]/,
  /求[月票推荐票收藏打赏订阅阅]/,
  /投[推荐票月票]/,
  /本章[说福利]/,
];

/** 删除作者碎碎念行（安全阀：>40字保留，以。结尾保留） */
export function removeAuthorNoteLines(lines: string[]): string[] {
  return lines.filter((line) => {
    const t = line.trim();
    if (!t) return true;
    if (t.length > 40) return true;        // 长度安全阀
    if (t.endsWith('。')) return true;      // 句末标点排除（txt_reform StrictEnd 思路）
    return !AUTHOR_NOTE_PATTERNS.some((p) => p.test(t));
  });
}

// ---- Step 5: 水印/来源行 ----

const WATERMARK_PATTERNS: RegExp[] = [
  /本小说来自/,
  /[Tt][Xx][Tt]小说下载/,
  /电子书下载/,
  /更多好书/,
  /全集下载/,
  /\.txt下载/i,
  /小说论坛/,
  /书友[群号]/,
  /QQ[群号]/,
  /微信[公号群]/,
];

/** 删除水印/来源行（40字安全阀） */
export function removeWatermarkLines(lines: string[]): string[] {
  return lines.filter((line) => {
    const t = line.trim();
    if (!t) return true;
    if (t.length > 40) return true;
    return !WATERMARK_PATTERNS.some((p) => p.test(t));
  });
}

// ---- Step 6: 导航行 ----

const NAV_EXACT = new Set([
  '上一章', '下一章', '返回目录', '返回书页', '返回列表', '上一页', '下一页',
  '上一章]', '[下一章', '[返回目录]',
]);

/** 删除导航行（20字安全阀） */
export function removeNavLines(lines: string[]): string[] {
  return lines.filter((line) => {
    const t = line.trim();
    if (!t) return true;
    if (t.length > 20) return true;
    // 精确匹配
    if (NAV_EXACT.has(t)) return false;
    // 方括号包裹的导航 [xxx]
    if (/^\[.+\]$/.test(t) && t.length <= 12) {
      const inner = t.slice(1, -1);
      if (NAV_EXACT.has(inner)) return false;
    }
    return true;
  });
}

// ---- Step 7: 分隔线 ----

/** 由重复符号组成的分隔线（至少3个连续相同符号），>5字才删 */
export function removeSeparatorLines(lines: string[]): string[] {
  const SEP_RE = /^([-=*_—])\1{2,}$/;
  return lines.filter((line) => {
    const t = line.trim();
    if (!t) return true;
    if (t.length <= 5) return true;        // 短行保留
    return !SEP_RE.test(t);
  });
}

// ---- Step 8: 目录块 ----

const CHAPTER_TITLE_RE = /^第[零一二三四五六七八九十百千万〇\d]+[章节回卷集部篇].{0,30}$/;
const BOOK_PART_RE = /^(序章|序言|前言|引子|楔子|尾声|后记|番外[一二三四五六七八九十\d]{0,3})(\s.*)?$/;

function isChapterTitleLine(line: string): boolean {
  const t = line.trim();
  return t.length > 0 && t.length <= 35 && (CHAPTER_TITLE_RE.test(t) || BOOK_PART_RE.test(t));
}

/** 删除连续 3+ 个章节标题行组成的目录块 */
export function removeTocBlocks(lines: string[]): string[] {
  const marks = lines.map((l) => isChapterTitleLine(l));

  const toRemove = new Set<number>();
  let runStart = -1;
  let runCount = 0;

  const flushRun = (end: number) => {
    if (runCount >= 3) {
      for (let j = runStart; j < end; j++) {
        if (marks[j] || lines[j].trim() === '') {
          toRemove.add(j);
        }
      }
    }
    runStart = -1;
    runCount = 0;
  };

  for (let i = 0; i < lines.length; i++) {
    if (marks[i]) {
      if (runStart === -1) runStart = i;
      runCount++;
    } else if (lines[i].trim() === '') {
      // 空行在目录块中间 → 继续扫描
      continue;
    } else {
      flushRun(i);
    }
  }
  flushRun(lines.length);

  return lines.filter((_, i) => !toRemove.has(i));
}

// ---- Step 9: 标点规范化（novel-proofer CJK 上下文感知） ----

/** CJK 字符范围，用于上下文感知判断 */
const CJK = '\\u3400-\\u4dbf\\u4e00-\\u9fff\\u3040-\\u30ff\\uac00-\\ud7af';

/** 省略号/破折号/标点规范化（只在中日韩上下文中转换） */
export function normalizePunctuation(text: string): string {
  let t = text;

  // 省略号规范化
  t = t.replace(/\.{3,}/g, '……');            // ... → ……
  t = t.replace(/[。．｡]{3,}/g, '……');       // 。。。→ ……
  t = t.replace(/…{3,}/g, '……');              // ……… → ……

  // 破折号规范化
  t = t.replace(/[-—]{2,}/g, '——');            // ---/———→ ——

  // CJK 上下文感知：ASCII 标点 → 全角（数字中不变）
  // 逗号：CJK 旁边的 , → ，（数字中不转）
  t = t.replace(new RegExp(`(?<=[${CJK}])(?<!\\d),(?!\\d)`, 'g'), '，');
  t = t.replace(new RegExp(`(?<!\\d),(?!\\d)(?=[${CJK}])`, 'g'), '，');

  // 分号
  t = t.replace(new RegExp(`(?<=[${CJK}]);`, 'g'), '；');

  // 冒号
  t = t.replace(new RegExp(`(?<=[${CJK}]):`, 'g'), '：');

  // 问号
  t = t.replace(new RegExp(`(?<=[${CJK}])\\?`, 'g'), '？');

  // 感叹号
  t = t.replace(new RegExp(`(?<=[${CJK}])!`, 'g'), '！');

  // 句号：CJK 后面且后面是 CJK/空白/结尾/右标点（数字中不转）
  const CLOSING = '"\'"\'"\\u201c\\u201d\\u2018\\u2019)\\]\\u3011\\u300b\\u300d\\u300f';
  t = t.replace(
    new RegExp(`(?<=[${CJK}])\\.(?=(?:[${CJK}]|\\s|$|[${CLOSING}]))`, 'g'),
    '。',
  );

  // 括号：CJK 旁边 () → （）
  t = t.replace(new RegExp(`(?<=[${CJK}])\\(`, 'g'), '（');
  t = t.replace(new RegExp(`\\)(?=[${CJK}])`, 'g'), '）');
  t = t.replace(new RegExp(`\\((?=[${CJK}])`, 'g'), '（');

  // CJK 和标点之间的空格删除
  t = t.replace(new RegExp(`(?<=[${CJK}])[ \\t]+(?=[，。！？；：、,.!?;:])`, 'g'), '');
  t = t.replace(new RegExp(`(?<=[，。！？；：、,.!?;:])[ \\t]+(?=[${CJK}])`, 'g'), '');

  return t;
}

// ---- Step 10: 空行整理 ----

/** 3+ 连续空行压缩为 2，首尾 trim */
export function collapseBlankLines(text: string): string {
  return text
    .replace(/\n{4,}/g, '\n\n\n')
    .trim();
}

// ============================================
// 主管道
// ============================================

/** 清洗网络小说 TXT 文本（可选 CleaningConfig，不传则默认运行全部步骤） */
export function cleanNovelText(raw: string, config?: CleaningConfig): string {
  const steps = config ? resolveCleaningSteps(config) : ALL_CLEANING_STEPS;

  // Step 1: 编码清理
  let text = steps.includes('encoding') ? stripEncodingArtifacts(raw) : raw;

  // Step 2-8: 按行过滤
  let lines = text.split('\n');
  if (steps.includes('urls')) lines = removeUrlLines(lines);
  if (steps.includes('promos')) lines = removePromoLines(lines);
  if (steps.includes('authorNotes')) lines = removeAuthorNoteLines(lines);
  if (steps.includes('watermarks')) lines = removeWatermarkLines(lines);
  if (steps.includes('nav')) lines = removeNavLines(lines);
  if (steps.includes('separators')) lines = removeSeparatorLines(lines);
  if (steps.includes('toc')) lines = removeTocBlocks(lines);

  // 合并
  text = lines.join('\n');

  // Step 9: 标点规范化
  if (steps.includes('punctuation')) text = normalizePunctuation(text);

  // Step 10: 空行整理
  if (steps.includes('blankLines')) text = collapseBlankLines(text);

  return text;
}
</file>

<file path="AGENTS.md">
当需要设计管线分析时 必须能适度指导模型生成 不做为了分析而分析的事 同时分析小说时 允许模型回答 我不知道 或暂未分析出来 不要强行思考 牵强附会
</file>

<file path="CLAUDE.md">
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

墨韵仿写 — AI 小说风格模仿创作工具。上传小说 → AI 分析文风/剧情/人物 → 按风格仿写新故事。

## Commands

```bash
npm run dev     # 开发服务器 (localhost:3000)
npm run build   # 生产构建
npm run lint    # ESLint 检查
```

无测试框架，无测试命令。有一个手动管线测试脚本：`node data/test-pipeline.mjs [base_url] [api_key] [model]`。

## ⚠️ Next.js Version

**Next.js 16.2.6** — 这不是你训练数据中的 Next.js。API、约定、文件结构可能有 breaking changes。写代码前先读 `node_modules/next/dist/docs/` 中的相关指南。注意 deprecation 警告。

## Architecture

### 单页应用，无 URL 路由

整个应用只有一个 `src/app/page.tsx`，通过 Zustand store 的 `activeView` 状态切换视图组件。不使用 Next.js 的动态路由或嵌套布局。

当前活跃视图：`source-library` | `source-detail` | `writing-project` | `prompt-management` | `layer-generation` | `chapter-generation`（后两个标记为废弃，正迁移到 `writing-project` 内嵌标签页）。

### 数据存储：文件系统，无数据库

所有数据存放在项目根目录 `data/` 下，纯 JSON/TXT/MD/YAML 文件，通过 `fs.promises` 直接读写：

- `data/source_library/{id}/` — 素材库：分析报告、切片、DNA、元数据
- `data/projects/{id}/` — 写作项目 JSON
- `data/novels/{id}/` — 旧版小说数据（兼容）
- `data/prompts.json` — 自定义 prompt 覆盖

所有文件写入必须通过 `safeJoin()`（`src/lib/safe-path.ts`）防止路径遍历。写入使用 `atomic-write.ts` 的临时文件 + rename 模式保证原子性。浏览器端 IndexedDB 仅作缓存/兼容层（`src/lib/sync/idb-helpers.ts`）。

### AI 集成：自研流式客户端

`src/lib/ai/providers.ts` — 手写的 OpenAI-compatible SSE 流式客户端（原生 fetch + ReadableStream）。**不要使用 `ai` 或 `@ai-sdk/openai` 包**，它们在 dependencies 中但运行时未使用。

AI 配置（apiKey/model/baseURL/thinkingMode）存在浏览器 localStorage（Zustand persist），每次请求从客户端传到服务端。服务端不存储 AI 凭证。默认使用 DeepSeek API（`https://api.deepseek.com`，模型 `deepseek-v4-flash`）。

### 流式请求基础设施

两层抽象：

| 模块 | 位置 | 用途 |
|------|------|------|
| `providers.ts` | `src/lib/ai/` | 底层 SSE 流解析，`chatCompletionStream()` 返回 `ReadableStream` |
| `stream-fetcher.ts` | `src/lib/` | 上层封装，`createStreamFetcher()` 提供 `fetch()` + `abort()`，自动注入 `thinkingMode` |
| `stream-route.ts` | `src/lib/api/` | API route 工厂，`createStreamRoute()` 消除 route 的重复样板 |

`stream-route.ts` 提供三种模式：
- `handlePassThroughStream` — 客户端预构建 systemPrompt + userMessage（source/process/* 路由用）
- `handlePromptBuiltStream` — 服务端通过 promptBuilder 构建（generation/* 和 chapter/* 路由用）
- `createStreamRoute` — 一行创建完整 POST handler，自动包 try/catch

### Prompt 系统

Prompt 构建器在 `src/lib/ai/prompts/`，28 个构建器文件，每个导出 `build*Messages()` + `DEFAULT_SYSTEM_PROMPT`。统一从 `src/lib/ai/prompts/index.ts` 导出。

Prompt 注册表在 `src/lib/store/prompts.ts`（`PROMPT_REGISTRY`），34 个可自定义 prompt，分为四个类别：统一管线（5）、素材处理（14）、层级生成（9+1大纲）、章节写作（3）。用户可通过 PromptManagementView 自定义任意 prompt，覆盖存储在服务端 `data/prompts.json`。

统一管线 prompt 使用公共约束段（`pipeline-common.ts`），所有分析 prompt 注入「工程逆向分析」核心约束，禁止文学赏析、强制量化输出。

### Zustand Stores（`src/lib/store/`）

| Store | 职责 |
|-------|------|
| `useSettingsStore` | AI 配置，localStorage 持久化（当前 migration v8），含 thinkingMode |
| `useNavigationStore` | 当前视图路由（`activeView`）+ 管线任务追踪 |
| `useSourceLibraryStore` | 素材库列表与当前选中，启动时加载，处理中状态自动重置为 error |
| `useSourceProcessingStore` | 统一 7 步素材分析管线状态，支持断点恢复 |
| `useProjectStore` | 写作项目 CRUD、5 层生成输出、章节管理 |
| `usePromptStore` | 自定义 prompt 覆盖，启动时从服务端加载 |

### API Routes（`src/app/api/`）

46 个 Route Handler，按领域分组：
- `source/process/` — 15 个：统一管线步骤（event-extraction, event-alignment, deep-analysis, summary, dna-compression）+ 旧版步骤（slice, style, plot, character-dynamics, reader-experience, narrative-constraints, samples, classify-entities）+ 进阶分析（experience-annotation, ablation-testing, tension-tracking）
- `generation/` — 9 个层级生成（outline, phase-framework/detail, volume-framework/detail, chapter-set-framework/detail, chapter-plan-framework/detail）
- `chapter/` — 3 个章节操作（generate, review, revise）
- `library/` — 7 个素材管理（list, get, upload, save, save-step, delete, rename）
- `project/` — 5 个项目 CRUD（create, save, load, list, delete）
- `prompts/` — 2 个 prompt 管理（list, save）
- `novels/` — 4 个旧版兼容（list, detail, save, delete）

source/process/* 和 generation/* 路由（共 28 个）使用 `createStreamRoute()` 一行创建，模式统一。

### 素材处理管线（统一 7 步架构）

**统一管线**（`src/lib/source-processing/unified-pipeline.ts`）：
1. 小切片（代码，~2万字/片）
2. 逐片事件提取（N 次并行 AI）
3. 事件对齐（代码预处理 + 1 次 AI）→ 全书记忆图谱
4. 大切片（代码，按 maxContextTokens 自适应）
5. 逐大切片深度分析（并行 AI，带完整图谱）
6. 汇总 + 刺激点分析 + 一致性报告（1 次 AI）
7. DNA 压缩（代码填充量化参数 + AI 定性）→ 纯量化生成规则

核心转向：从「文学鉴赏式分析」转向「工程逆向式分析」。只提取可量化的生成规则，不分析作者意图。
类型定义在 `pipeline-types.ts`。所有分析 prompt 注入公共约束段（`pipeline-common.ts`）。
支持断点恢复（`PipelineCheckpoint`），重试 2 次后停止，已完成结果保留。

旧版管线（basic-pipeline.ts、dao-pipeline.ts）仍存在但已不使用，逐步清理中。

### 5 层生成管线

`src/lib/generation/layer-pipeline.ts` — "框架-后细节"模式：
大纲 → 阶段规划 → 卷规划 → 章节集 → 章节计划

每层先生成框架结构，再逐个填充细节。上下文由 `context-assembler.ts` 按层组装，优先使用新 `GenerationRulesDNA`（量化参数），回退到旧 DNA 格式。

## Key Files

| File | Purpose |
|------|---------|
| `src/app/page.tsx` | 应用入口，视图路由 |
| `src/app/layout.tsx` | 根布局，metadata + Toaster |
| `src/lib/ai/providers.ts` | 自研 SSE 流式 AI 客户端 |
| `src/lib/ai/prompts/index.ts` | Prompt 统一导出 |
| `src/lib/ai/prompts/pipeline-common.ts` | 统一公共约束段（工程逆向分析） |
| `src/lib/api/stream-route.ts` | API route 统一工厂（消除重复样板） |
| `src/lib/stream-fetcher.ts` | 共享流式 fetch 工具（自动注入 thinkingMode） |
| `src/lib/store/settings.ts` | AI 配置 store（migration v8） |
| `src/lib/store/source-processing.ts` | 统一 7 步管线编排 store |
| `src/lib/store/prompts.ts` | Prompt 注册表（34 个）+ 自定义覆盖 store |
| `src/lib/source-processing/unified-pipeline.ts` | 统一 7 步管线编排器 |
| `src/lib/source-processing/pipeline-types.ts` | 管线类型定义（343 行） |
| `src/lib/generation/layer-pipeline.ts` | 5 层生成管线 |
| `src/lib/generation/context-assembler.ts` | 层级上下文组装（含 GenerationRulesDNA 支持） |
| `src/types/index.ts` | 全局类型定义（679 行） |
| `src/lib/safe-path.ts` | 路径遍历防护 |
| `src/lib/atomic-write.ts` | 原子写入（临时文件 + rename） |
| `src/lib/constants.ts` | 共享常量（默认模型/URL/协议白名单） |

## Conventions

- **中文代码库**：UI 文本、注释、prompt 内容均为中文
- **ID 生成**：统一使用 `nanoid`
- **SSRF 防护**：`providers.ts` 校验 URL 协议（仅 https），屏蔽内网地址
- **Tailwind CSS v4** + **shadcn/ui**（base-nova 风格），OKLCH 色彩空间，暖纸色调主题
- **路径别名**：`@/*` → `./src/*`
- **文件写入**：必须通过 `safeJoin()` 防路径遍历，使用 `atomicWrite*()` 保证原子性
- **API 凭证**：服务端不存储，每次请求从客户端传入
</file>

<file path="next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: false,
};

export default nextConfig;
</file>

<file path="src/app/api/chapter/generate/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterWritingMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '章节生成',
  promptBuilder: (b) => buildChapterWritingMessages(
    (b.styleGuide ?? '') as string, (b.hierarchyContext ?? '') as string, (b.chapterTask ?? '') as string,
    (b.previousState ?? '') as string, b.daoContext as string | undefined, b.rhythmPrescription as string | undefined,
  ),
});
</file>

<file path="src/app/api/chapter/review/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterReviewMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '审查',
  maxTokens: 4096,
  promptBuilder: (b) => buildChapterReviewMessages(
    b.chapterContent as string, (b.styleGuide ?? '') as string, (b.chapterTask ?? '') as string,
    b.daoContext as string | undefined,
  ),
});
</file>

<file path="src/app/api/chapter/revise/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildChapterRevisionMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '修正',
  promptBuilder: (b) => buildChapterRevisionMessages(
    b.chapterContent as string, (b.reviews ?? '') as string, (b.humanFeedback ?? null) as string | null,
    (b.styleGuide ?? '') as string, (b.chapterTask ?? '') as string, b.daoContext as string | undefined,
  ),
});
</file>

<file path="src/app/api/generation/outline/route.ts">
import { createStreamRoute } from '@/lib/api/stream-route';
import { buildOutlineGenerationMessages } from '@/lib/ai/prompts';

export const POST = createStreamRoute({
  label: '大纲生成',
  promptBuilder: (b) => buildOutlineGenerationMessages(
    b.styleGuide as string, b.plotGuide as string, b.userConcept as string,
    b.daoContext as string | undefined, b.rhythmPrescription as string | undefined,
  ),
});
</file>

<file path="src/app/api/library/get/route.ts">
// ============================================
// GET /api/library/get?id=xxx — 读取单个源小说全量数据
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

export async function GET(req: NextRequest) {
  try {
    const id = req.nextUrl.searchParams.get('id');
    if (!id) {
      return NextResponse.json({ error: '缺少 id 参数' }, { status: 400 });
    }

    const novelDir = safeJoin(LIBRARY_DIR, id);

    // 读取 meta.json
    const metaRaw = await fs.readFile(path.join(novelDir, 'meta.json'), 'utf-8');
    const meta = JSON.parse(metaRaw);

    // 读取 raw.txt（可能不存在）
    let rawText: string | null = null;
    try {
      rawText = await fs.readFile(path.join(novelDir, 'raw.txt'), 'utf-8');
    } catch { /* raw.txt 可能不存在 */ }

    // 读取 slices.json（旧管线）
    let slices = null;
    try {
      const slicesRaw = await fs.readFile(path.join(novelDir, 'slices.json'), 'utf-8');
      slices = JSON.parse(slicesRaw);
    } catch { /* slices 可能不存在 */ }

    // 读取统一管线中间产物
    let smallSlices = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'small_slices.json'), 'utf-8');
      smallSlices = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    let sliceExtractions = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'slice_extractions.json'), 'utf-8');
      sliceExtractions = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    let eventGraph = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'event_graph.json'), 'utf-8');
      eventGraph = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    let largeSlices = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'large_slices.json'), 'utf-8');
      largeSlices = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    let sliceAnalyses = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'slice_analyses.json'), 'utf-8');
      sliceAnalyses = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    let summaryReport = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'summary_report.json'), 'utf-8');
      summaryReport = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    let generationRulesDna = null;
    try {
      const raw = await fs.readFile(path.join(novelDir, 'generation_rules_dna.json'), 'utf-8');
      generationRulesDna = JSON.parse(raw);
    } catch { /* 文件可能不存在 */ }

    // 读取 style_profile.md
    let styleProfile: string | null = null;
    try {
      styleProfile = await fs.readFile(path.join(novelDir, 'style_profile.md'), 'utf-8');
    } catch { /* style_profile 可能不存在 */ }

    // 读取 plot_report.md (叙事动力学，向后兼容)
    let plotReport: string | null = null;
    try {
      plotReport = await fs.readFile(path.join(novelDir, 'plot_report.md'), 'utf-8');
    } catch { /* plot_report 可能不存在 */ }

    // 读取 character_dynamics.md
    let characterDynamics: string | null = null;
    try {
      characterDynamics = await fs.readFile(path.join(novelDir, 'character_dynamics.md'), 'utf-8');
    } catch { /* character_dynamics 可能不存在 */ }

    // 读取 reader_experience.md
    let readerExperience: string | null = null;
    try {
      readerExperience = await fs.readFile(path.join(novelDir, 'reader_experience.md'), 'utf-8');
    } catch { /* reader_experience 可能不存在 */ }

    // 读取 narrative_constraints.md
    let narrativeConstraints: string | null = null;
    try {
      narrativeConstraints = await fs.readFile(path.join(novelDir, 'narrative_constraints.md'), 'utf-8');
    } catch { /* narrative_constraints 可能不存在 */ }

    // 读取 novel_dna.yaml
    let novelDna: string | null = null;
    try {
      novelDna = await fs.readFile(path.join(novelDir, 'novel_dna.yaml'), 'utf-8');
    } catch { /* novel_dna 可能不存在 */ }

    // 读取 samples.json
    let samples = null;
    try {
      const samplesRaw = await fs.readFile(path.join(novelDir, 'samples.json'), 'utf-8');
      samples = JSON.parse(samplesRaw);
    } catch { /* samples 可能不存在 */ }

    return NextResponse.json({
      ...meta,
      rawText,
      slices,
      styleProfile,
      plotReport,
      characterDynamics,
      readerExperience,
      narrativeConstraints,
      representativeSamples: samples,
      novelDna,
      // 统一管线中间产物
      smallSlices,
      sliceExtractions,
      eventGraph,
      largeSlices,
      sliceAnalyses,
      summaryReport,
      generationRulesDna,
    });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '读取源小说失败';
    console.error('Get library item error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/library/save/route.ts">
// ============================================
// POST /api/library/save — 保存处理结果到素材库
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';
import { atomicWriteJson, atomicWriteText } from '@/lib/atomic-write';
import type { SourceNovel } from '@/types';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

export async function POST(req: NextRequest) {
  try {
    const sourceNovel: SourceNovel = await req.json();

    if (!sourceNovel.id || !sourceNovel.title) {
      return NextResponse.json({ error: '缺少 id 或 title' }, { status: 400 });
    }

    const novelDir = safeJoin(LIBRARY_DIR, sourceNovel.id);
    const { mkdir } = await import('fs/promises');
    await mkdir(novelDir, { recursive: true });

    // meta.json（不含大体积字段）
    const meta = {
      id: sourceNovel.id,
      title: sourceNovel.title,
      totalChars: sourceNovel.totalChars,
      importConfig: sourceNovel.importConfig,
      status: sourceNovel.status,
      createdAt: sourceNovel.createdAt,
      processedAt: sourceNovel.processedAt,
      sliceCount: sourceNovel.slices?.length ?? 0,
      hasStyleProfile: !!sourceNovel.styleProfile,
      hasPlotReport: !!sourceNovel.plotReport,
      hasCharacterDynamics: !!sourceNovel.characterDynamics,
      hasReaderExperience: !!sourceNovel.readerExperience,
      hasNarrativeConstraints: !!sourceNovel.narrativeConstraints,
      hasNovelDna: !!sourceNovel.novelDna,
      sampleCount: sourceNovel.representativeSamples?.length ?? 0,
    };
    await atomicWriteJson(path.join(novelDir, 'meta.json'), meta);

    // slices.json
    if (sourceNovel.slices) {
      await atomicWriteJson(path.join(novelDir, 'slices.json'), sourceNovel.slices);
    }

    // style_profile.md
    if (sourceNovel.styleProfile) {
      await atomicWriteText(path.join(novelDir, 'style_profile.md'), sourceNovel.styleProfile);
    }

    // plot_report.md
    if (sourceNovel.plotReport) {
      await atomicWriteText(path.join(novelDir, 'plot_report.md'), sourceNovel.plotReport);
    }

    // character_dynamics.md
    if (sourceNovel.characterDynamics) {
      await atomicWriteText(path.join(novelDir, 'character_dynamics.md'), sourceNovel.characterDynamics);
    }

    // reader_experience.md
    if (sourceNovel.readerExperience) {
      await atomicWriteText(path.join(novelDir, 'reader_experience.md'), sourceNovel.readerExperience);
    }

    // narrative_constraints.md
    if (sourceNovel.narrativeConstraints) {
      await atomicWriteText(path.join(novelDir, 'narrative_constraints.md'), sourceNovel.narrativeConstraints);
    }

    // novel_dna.yaml
    if (sourceNovel.novelDna) {
      await atomicWriteText(path.join(novelDir, 'novel_dna.yaml'), sourceNovel.novelDna);
    }

    // samples.json
    if (sourceNovel.representativeSamples) {
      await atomicWriteJson(path.join(novelDir, 'samples.json'), sourceNovel.representativeSamples);
    }

    return NextResponse.json({ success: true, id: sourceNovel.id });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '保存素材失败';
    console.error('Save library error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/novels/delete/route.ts">
// ============================================
// POST /api/novels/delete — 删除 data/novels/{id}/ 目录
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const NOVELS_DIR = path.join(process.cwd(), 'data', 'novels');

async function removeDir(dirPath: string) {
  try {
    const entries = await fs.readdir(dirPath, { withFileTypes: true });
    for (const entry of entries) {
      const full = path.join(dirPath, entry.name);
      if (entry.isDirectory()) {
        await removeDir(full);
      } else {
        await fs.unlink(full);
      }
    }
    await fs.rmdir(dirPath);
  } catch (err: unknown) {
    if ((err as NodeJS.ErrnoException).code !== 'ENOENT') throw err;
  }
}

export async function POST(req: NextRequest) {
  try {
    const { id } = await req.json();
    if (!id) {
      return NextResponse.json({ error: '缺少 id' }, { status: 400 });
    }

    const bookDir = safeJoin(NOVELS_DIR, id);
    await removeDir(bookDir);

    return NextResponse.json({ success: true });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '删除失败';
    console.error('Delete novel error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/novels/save/route.ts">
// ============================================
// POST /api/novels/save — 保存小说到 data/novels/{id}/
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';

const NOVELS_DIR = path.join(process.cwd(), 'data', 'novels');

export async function POST(req: NextRequest) {
  try {
    const { id, title, fullText, chunks } = await req.json();

    if (!id || !title || typeof fullText !== 'string') {
      return NextResponse.json(
        { error: '缺少必要字段 id / title / fullText' },
        { status: 400 },
      );
    }

    const bookDir = safeJoin(NOVELS_DIR, id);
    await fs.mkdir(bookDir, { recursive: true });

    // 元数据
    const meta = {
      id,
      title,
      totalChars: fullText.length,
      chunkCount: chunks?.length ?? 0,
      createdAt: new Date().toISOString(),
    };
    await fs.writeFile(path.join(bookDir, 'meta.json'), JSON.stringify(meta, null, 2), 'utf-8');

    // 全文
    await fs.writeFile(path.join(bookDir, 'full.txt'), fullText, 'utf-8');

    // 分块索引和内容
    if (chunks && chunks.length > 0) {
      const chunksMeta = chunks.map((c: { id: string; index: number; title: string; charCount: number }, i: number) => ({
        id: c.id,
        index: c.index ?? i,
        title: c.title,
        charCount: c.charCount,
        filename: `chunk_${String(i + 1).padStart(4, '0')}.txt`,
      }));
      await fs.writeFile(path.join(bookDir, 'chunks.json'), JSON.stringify(chunksMeta, null, 2), 'utf-8');

      const chunksDir = path.join(bookDir, 'chunks');
      await fs.mkdir(chunksDir, { recursive: true });
      for (const [i, chunk] of chunks.entries()) {
        const filename = `chunk_${String(i + 1).padStart(4, '0')}.txt`;
        await fs.writeFile(path.join(chunksDir, filename), chunk.content, 'utf-8');
      }
    }

    return NextResponse.json({ success: true, id, title });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '保存失败';
    console.error('Save novel error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/components/source/SourceNovelCard.tsx">
// ============================================
// 源小说卡片 — 简洁状态显示 + 改名
// ============================================

'use client';

import { useState, useRef, useEffect } from 'react';
import { FileText, CheckCircle, Loader2, AlertCircle, Pencil, Trash2 } from 'lucide-react';
import type { SourceNovel, SourceNovelStatus } from '@/types';

interface SourceNovelCardProps {
  novel: SourceNovel;
  onClick: () => void;
  onRename?: (id: string, newTitle: string) => void;
  onDelete?: (id: string) => void;
}

const STATUS_CONFIG: Record<SourceNovelStatus, { label: string; color: string; icon: typeof CheckCircle }> = {
  raw: { label: '待处理', color: 'text-muted-foreground', icon: FileText },
  indexing: { label: '索引中', color: 'text-blue-500', icon: Loader2 },
  segmenting: { label: '分段中', color: 'text-blue-500', icon: Loader2 },
  slicing: { label: '切片中', color: 'text-blue-500', icon: Loader2 },
  extracting: { label: '分析中', color: 'text-blue-500', icon: Loader2 },
  character_dynamics: { label: '角色分析', color: 'text-blue-500', icon: Loader2 },
  deep_analyzing: { label: '深度分析', color: 'text-blue-500', icon: Loader2 },
  selecting: { label: '选取中', color: 'text-blue-500', icon: Loader2 },
  evolution_modeling: { label: '演化建模', color: 'text-blue-500', icon: Loader2 },
  compressing: { label: '压缩中', color: 'text-blue-500', icon: Loader2 },
  ready: { label: '已处理', color: 'text-green-500', icon: CheckCircle },
  error: { label: '出错', color: 'text-red-500', icon: AlertCircle },
};

function formatChars(n: number): string {
  if (n >= 10000) return `${(n / 10000).toFixed(1)}万`;
  if (n >= 1000) return `${(n / 1000).toFixed(1)}千`;
  return `${n}`;
}

export function SourceNovelCard({ novel, onClick, onRename, onDelete }: SourceNovelCardProps) {
  const config = STATUS_CONFIG[novel.status];
  const Icon = config.icon;
  const processingStatuses: SourceNovelStatus[] = ['indexing', 'segmenting', 'slicing', 'extracting', 'character_dynamics', 'deep_analyzing', 'selecting', 'evolution_modeling', 'compressing'];
  const isProcessing = processingStatuses.includes(novel.status);

  const [isEditing, setIsEditing] = useState(false);
  const [editTitle, setEditTitle] = useState(novel.title);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (isEditing) inputRef.current?.focus();
  }, [isEditing]);

  const handleRenameSubmit = () => {
    const trimmed = editTitle.trim();
    if (trimmed && trimmed !== novel.title && onRename) {
      onRename(novel.id, trimmed);
    } else {
      setEditTitle(novel.title);
    }
    setIsEditing(false);
  };

  return (
    <div
      className="group p-4 rounded-lg border border-border bg-card hover:bg-accent/50 transition-colors"
    >
      <div className="flex items-start justify-between gap-2">
        <div
          className="flex items-center gap-2 min-w-0 flex-1 cursor-pointer"
          onClick={onClick}
        >
          <FileText className="w-4 h-4 text-primary shrink-0" />
          {isEditing ? (
            <input
              ref={inputRef}
              value={editTitle}
              onChange={(e) => setEditTitle(e.target.value)}
              onBlur={handleRenameSubmit}
              onKeyDown={(e) => {
                if (e.key === 'Enter') handleRenameSubmit();
                if (e.key === 'Escape') { setEditTitle(novel.title); setIsEditing(false); }
              }}
              onClick={(e) => e.stopPropagation()}
              className="text-sm font-medium text-foreground bg-transparent border-b border-primary outline-none flex-1 min-w-0"
            />
          ) : (
            <span className="text-sm font-medium text-foreground truncate">
              《{novel.title}》
            </span>
          )}
        </div>

        {/* 操作按钮 — hover 时显示 */}
        <div className="flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity shrink-0">
          <button
            onClick={(e) => { e.stopPropagation(); setIsEditing(true); setEditTitle(novel.title); }}
            className="p-1 rounded hover:bg-accent text-muted-foreground hover:text-foreground transition-colors"
            title="重命名"
          >
            <Pencil className="w-3 h-3" />
          </button>
          {onDelete && (
            <button
              onClick={(e) => { e.stopPropagation(); onDelete(novel.id); }}
              className="p-1 rounded hover:bg-destructive/10 text-muted-foreground hover:text-destructive transition-colors"
              title="删除"
            >
              <Trash2 className="w-3 h-3" />
            </button>
          )}
        </div>
      </div>

      <div className="flex items-center gap-3 mt-2 cursor-pointer" onClick={onClick}>
        <span className="text-xs text-muted-foreground">{formatChars(novel.totalChars)}字</span>
        <div className={`flex items-center gap-1 text-xs ${config.color}`}>
          <Icon className={`w-3 h-3 ${isProcessing ? 'animate-spin' : ''}`} />
          {config.label}
        </div>
      </div>

      {/* 处理完成：显示分析维度状态 */}
      {novel.status === 'ready' && (
        <div className="flex gap-2 mt-2 text-xs text-muted-foreground cursor-pointer flex-wrap" onClick={onClick}>
          <span className="text-green-600 dark:text-green-400">文风✓</span>
          <span className="text-green-600 dark:text-green-400">叙事✓</span>
          <span className="text-green-600 dark:text-green-400">角色✓</span>
          <span className="text-green-600 dark:text-green-400">体验✓</span>
          {novel.novelDna && <span className="text-primary font-medium">DNA✓</span>}
        </div>
      )}

      {/* 出错状态 */}
      {novel.status === 'error' && (
        <div className="mt-2 text-xs text-red-500 cursor-pointer" onClick={onClick}>
          处理失败，可重新处理
        </div>
      )}
    </div>
  );
}
</file>

<file path="src/components/source/StylePlotLibraryView.tsx">
// ============================================
// 文风/情节库 — 双列卡片网格，展示所有小说的提取结果
// ============================================

'use client';

import { FileText, CheckCircle, XCircle, Eye } from 'lucide-react';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { useNavigationStore } from '@/lib/store/navigation';
import type { SourceNovel } from '@/types';

function formatChars(n: number): string {
  if (n >= 10000) return `${(n / 10000).toFixed(1)}万`;
  if (n >= 1000) return `${(n / 1000).toFixed(1)}千`;
  return `${n}`;
}

interface ResultCardProps {
  novel: SourceNovel;
  type: 'style' | 'plot';
  onViewDetail: (novelId: string) => void;
}

function ResultCard({ novel, type, onViewDetail }: ResultCardProps) {
  // list API 不返回完整内容，用 status 判断是否有结果
  const hasContent = novel.status === 'ready';
  const label = type === 'style' ? '文风档案' : '情节报告';

  return (
    <div
      className="p-4 rounded-lg border border-border bg-card hover:bg-accent/50 transition-colors"
    >
      <div className="flex items-center gap-2 mb-2">
        <FileText className="w-4 h-4 text-primary shrink-0" />
        <span className="text-sm font-medium text-foreground truncate">
          《{novel.title}》
        </span>
      </div>

      <div className="flex items-center gap-3 text-xs text-muted-foreground mb-3">
        <span>{formatChars(novel.totalChars)}字</span>
        <div className="flex items-center gap-1">
          {hasContent ? (
            <>
              <CheckCircle className="w-3 h-3 text-green-500" />
              <span className="text-green-600 dark:text-green-400">{label}已提取</span>
            </>
          ) : (
            <>
              <XCircle className="w-3 h-3 text-muted-foreground" />
              <span>未提取</span>
            </>
          )}
        </div>
      </div>

      {hasContent && (
        <button
          onClick={() => onViewDetail(novel.id)}
          className="flex items-center gap-1.5 text-xs text-primary hover:text-primary/80 transition-colors"
        >
          <Eye className="w-3.5 h-3.5" />
          查看详情
        </button>
      )}
    </div>
  );
}

export function StylePlotLibraryView() {
  const { sourceNovels } = useSourceLibraryStore();
  const { setActiveSourceId, setActiveView } = {
    setActiveSourceId: useSourceLibraryStore((s) => s.setActiveSourceId),
    setActiveView: useNavigationStore((s) => s.setActiveView),
  };

  // 只显示已处理或有部分结果的小说
  const processedNovels = sourceNovels.filter(
    (n) => n.status === 'ready' || n.status === 'extracting' || n.status === 'selecting'
  );

  const handleViewDetail = (novelId: string) => {
    setActiveSourceId(novelId);
    setActiveView('source-detail');
  };

  if (processedNovels.length === 0) {
    return (
      <div className="flex flex-col items-center justify-center h-64 text-muted-foreground">
        <FileText className="w-10 h-10 mb-3 opacity-30" />
        <p className="text-sm">还没有提取结果</p>
        <p className="text-xs mt-1 opacity-60">先在「小说管理」中处理小说，文风和情节会出现在这里</p>
      </div>
    );
  }

  return (
    <div className="flex-1 overflow-y-auto">
      <div className="grid grid-cols-2 gap-4">
        {/* 文风档案列 */}
        <div>
          <h3 className="text-sm font-medium text-foreground mb-3 flex items-center gap-2">
            <div className="w-1 h-4 rounded-full bg-blue-500" />
            文风档案
          </h3>
          <div className="space-y-3">
            {processedNovels.map((novel) => (
              <ResultCard
                key={`style-${novel.id}`}
                novel={novel}
                type="style"
                onViewDetail={handleViewDetail}
              />
            ))}
          </div>
        </div>

        {/* 情节报告列 */}
        <div>
          <h3 className="text-sm font-medium text-foreground mb-3 flex items-center gap-2">
            <div className="w-1 h-4 rounded-full bg-purple-500" />
            情节报告
          </h3>
          <div className="space-y-3">
            {processedNovels.map((novel) => (
              <ResultCard
                key={`plot-${novel.id}`}
                novel={novel}
                type="plot"
                onViewDetail={handleViewDetail}
              />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/lib/ai/prompts/chapter-plan-detail.ts">
// ============================================
// Layer 5 填充: 单章详细计划
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是为指定章节生成详细计划。

要求：
- 约 100 字
- 包含：3-5 个场景节拍、小情节模式结构（触发→发展→高潮→收尾）、情节关键词、节奏指令、伏笔操作、铺垫任务
- 最大自由度：集合只给方向和情节模式结构，具体场景自由发挥
`;

export function buildChapterPlanDetailMessages(
  setContent: string,
  planFramework: string,
  chapterIndex: number,
  minorPlotPatterns: string,
) {
  const userMessage = `## 集合规划\n\n${setContent}\n\n---\n\n## 章节计划框架\n\n${planFramework}\n\n---\n\n## 小情节模式库\n\n${minorPlotPatterns}\n\n---\n\n请为第 ${chapterIndex + 1} 章生成详细计划。`;
  return { systemPrompt: getPrompt('chapter-plan-detail', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/chapter-plan-framework.ts">
// ============================================
// Layer 5 框架: 每章计划 — 确定每章标题和模式
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据章节集合规划，为每章生成计划框架。

要求：
- 输出每章的标题、对应的小情节模式（如适用）、情节关键词
- 不要填充详细内容，仅输出框架列表
`;

export function buildChapterPlanFrameworkMessages(
  setContent: string,
  minorPlotPatterns: string,
) {
  const userMessage = `## 集合规划\n\n${setContent}\n\n---\n\n## 小情节模式库\n\n${minorPlotPatterns}`;
  return { systemPrompt: getPrompt('chapter-plan-framework', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/chapter-set-detail.ts">
// ============================================
// Layer 4 填充: 单集合详细规划
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是为指定章节集合生成详细规划。

要求：
- 约 200 字
- 包含：集合主题、情节推进方向、小情节分配（哪章用哪个模式）、节奏安排（快/慢/中）、伏笔操作点、铺垫任务
- 弹性约束：小情节模式的选择可以灵活替换
`;

export function buildChapterSetDetailMessages(
  volumeContent: string,
  setFramework: string,
  setIndex: number,
  minorPlotPatterns: string,
) {
  const userMessage = `## 卷规划\n\n${volumeContent}\n\n---\n\n## 集合框架\n\n${setFramework}\n\n---\n\n## 小情节模式库\n\n${minorPlotPatterns}\n\n---\n\n请为第 ${setIndex + 1} 个集合生成详细规划。`;
  return { systemPrompt: getPrompt('chapter-set-detail', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/chapter-set-framework.ts">
// ============================================
// Layer 4 框架: 章节集合 — 确定数量和主题
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据卷规划，为指定卷规划章节集合框架。

要求：
- 每卷 3-5 个集合
- 每个集合 5-8 章
- 输出每个集合的标题、情节推进方向、预估章节范围
- 安排小情节模式的出场顺序
- 不要填充详细内容，仅输出框架
`;

export function buildChapterSetFrameworkMessages(
  volumeContent: string,
  minorPlotPatterns: string,
) {
  const userMessage = `## 卷规划\n\n${volumeContent}\n\n---\n\n## 小情节模式库\n\n${minorPlotPatterns}`;
  return { systemPrompt: getPrompt('chapter-set-framework', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/phase-detail.ts">
// ============================================
// Layer 2 填充: 单个阶段详细规划
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是为指定阶段生成详细规划。

要求：
- 约 500 字
- 包含：阶段主题、关键事件（3-5个）、伏笔计划、节奏安排（小爽点位置+大高潮位置）、缓冲空间
- 严格遵循上级大纲的约束
`;

export function buildPhaseDetailMessages(
  outline: string,
  phaseFramework: string,
  phaseIndex: number,
  plotGuide: string,
) {
  const userMessage = `## 全书大纲\n\n${outline}\n\n---\n\n## 阶段框架\n\n${phaseFramework}\n\n---\n\n## 节奏与伏笔规则\n\n${plotGuide}\n\n---\n\n请为第 ${phaseIndex + 1} 个阶段生成详细规划。`;
  return { systemPrompt: getPrompt('phase-detail', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/phase-framework.ts">
// ============================================
// Layer 2 框架: 阶段规划 — 确定数量和主题
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据全书大纲和节奏/伏笔规则，规划阶段框架。

要求：
- 输出 4-6 个阶段的标题和预估章节范围
- 每个阶段对应大纲中的哪些大情节节点
- 标注大高潮位置和伏笔埋设计划
- 不要填充详细内容，仅输出框架

输出格式：
1. 阶段标题（第X-Y章）：对应大情节节点 / 预估字数
2. ...
`;

export function buildPhaseFrameworkMessages(
  outline: string,
  plotGuide: string,
) {
  const userMessage = `## 全书大纲\n\n${outline}\n\n---\n\n## 节奏与伏笔规则\n\n${plotGuide}`;
  return { systemPrompt: getPrompt('phase-framework', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/plot-extraction.ts">
// ============================================
// Step 2b: 情节规律提取 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说逆向工程系统的叙事动力学分析模块。
目标：提取可迁移、可复用的叙事规律——不是"小说讲了什么"，而是"小说如何驱动读者不停翻页"。

禁止：剧情复述、人物介绍、作者评价、文学赏析、主观吹捧、推测意图、为分析而分析。

你的任务是分析小说文本，提取叙事动力学规律，分为 8 个部分。

## 提取内容

### 一、大情节框架
- 识别小说的整体结构类型（如退婚流、废柴逆袭、重生流等）
- 提取大情节的核心节点和典型章节位置
- 提取关键词标签

### 二、小情节模式库（10-20个）
- 识别反复出现的情节模式（如装逼打脸、奇遇、拍卖会等）
- 对每个模式：命名、拆解结构、统计频率、标注典型章节、提取关键词

### 三、伏笔手法
- 提前量、回收方式、密度、关键伏笔示例

### 四、节奏规律
- 小爽点间隔、大高潮间隔、战斗/过渡/日常占比

### 五、谜团网络
识别文本中的谜团体系，输出网络结构（不是线性列表）。

对每个谜团节点：
- 谜团问题：这个谜团悬置的核心问题是什么？
- 引入时机：在什么位置、通过什么方式引入
- 解决时机：在什么位置、通过什么方式解决（未解决的标注"悬置中"）
- 关联谜团：与其他哪些谜团有逻辑/情感关联（显式标注连接关系）
- 叙事功能：这个谜团在整体叙事中承担什么功能（驱动翻页/制造悬念/延迟满足/制造意外等）

输出为 Markdown 表格，并用"→"标注谜团间的连接关系，形成可追溯的网络。

### 六、翻页驱动机制
识别持续驱动读者阅读的深层机制。

对每种驱动力：
- 驱动类型（信息缺口 / 危机升级 / 身份悬念 / 权力竞争 / 世界探索 / 关系变化）
- 在文本中的分布位置和强度变化
- 驱动力之间的接力模式（一种驱动力消退时，什么接替它）
- 驱动力之间的叠加模式（哪些驱动力同时生效，产生叠加效应）

重点分析：这些驱动力如何制造"阅读惯性"——不是某个具体事件让人想读下去，而是一种持续的不愿停下的状态。

### 七、关键场景刺激-情绪映射
选取 5-10 个关键转折点（不是全部场景），分析情绪转换过程。

对每个转折点：
- 场景位置与描述
- 刺激类型（揭秘 / 成长 / 危机 / 胜利 / 失败 / 牺牲 / 智斗 / 爱情）
- 情绪前态 → 情绪后态
- 转换手法：作者用什么技术实现了这个情绪转换
- 转换持续时间：是瞬间爆发还是渐变

重点：分析转换过程本身，而不是记录情绪标签。

### 八、高光片段技术动作
识别 3-5 个最高光的叙事片段，拆解其技术动作。

对每个高光片段：
- 片段位置与简要描述
- 视角变化（如有）
- 节奏变化（加速/减速/骤停）
- 信息密度变化（信息涌入/信息控制/信息隐瞒）
- 留白手法（如有）
- 反转结构（如有）
- 尺度切换（微观→宏观或反之，如有）
- 技术动作的目的和对读者情绪的影响

技术动作必须结合具体文本分析，不允许泛泛而谈。

## 反证要求（强制执行）

对每个识别的规律/模式/结论：

### 规律：[规律描述]
- 支持证据：
  - 【原文："引用内容"】（位置）
- 反例：
  - 【原文："引用内容"】（位置）——如果存在
- 适用率：N% (支持 X 个 / 总 Y 个)
- 标记：规律 | 非普遍规律(例外条件) | 伪规律

## 输出格式

输出为 Markdown 格式的叙事动力学报告（narrative_dynamics.md），使用表格、列表、标题层级组织。
`;

export const DEFAULT_SUPPLEMENT_PROMPT = `你之前已经分析了该作者的部分作品并生成了一份叙事动力学报告。
现在你收到了更多文本。请结合新文本，对叙事动力学报告进行补充和修正。

要求：
1. 保留原有 8 个分析部分（大情节框架 / 小情节模式库 / 伏笔手法 / 节奏规律 / 谜团网络 / 翻页驱动 / 刺激-情绪映射 / 高光技术动作）
2. 用新文本中的证据补充或修正每个部分的结论
3. 更新谜团网络（新谜团、已解决的谜团、新的关联关系）
4. 更新翻页驱动力的分布和强度数据
5. 更新反证数据（支持证据、反例、适用率）
6. 输出完整的更新后的叙事动力学报告（不是增量，而是完整版本）
`;

export function buildPlotExtractionMessages(slicesText: string, styleProfile: string) {
  const userMessage = `## 文风档案（参考）\n\n${styleProfile}\n\n---\n\n## 小说文本\n\n${slicesText}`;
  return { systemPrompt: getPrompt('source-plot', DEFAULT_SYSTEM_PROMPT), userMessage };
}

export function buildPlotSupplementMessages(newChunk: string, previousReport: string, styleProfile: string) {
  const userMessage = `## 之前的情节规律报告\n\n${previousReport}\n\n---\n\n## 文风档案（参考）\n\n${styleProfile}\n\n---\n\n## 新的文本片段\n\n${newChunk}\n\n---\n\n请基于以上全部信息，输出完整的、更新后的情节规律报告。`;
  return { systemPrompt: getPrompt('source-plot-supplement', DEFAULT_SUPPLEMENT_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/sample-selection.ts">
// ============================================
// Step 3: 代表性切片选取 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是从全部切片中精选 3-5 个代表性切片。

要求：
- 每个对应一种典型的写作场景（战斗/日常/高潮/对话/描写/高潮蓄能/情绪转换/异常结构）
- 最能体现该作者的文风特征和叙事特征
- 优先选取被读者体验分析和叙事约束分析标记为关键的场景
- 附上选取理由

输出格式（Markdown）：

## 样本 {序号}: {标题}
- 场景类型: {战斗/日常/高潮/对话/描写/高潮蓄能/情绪转换/异常结构}
- 选取理由: {一句话说明为什么这个切片最能代表该场景的文风和叙事特征}

{切片正文}

---
`;

export function buildSampleSelectionMessages(
  slicesSummary: string,
  styleProfile: string,
  plotReport: string,
  readerExperience?: string,
  narrativeConstraints?: string,
) {
  const sections = [
    `## 文风档案\n\n${styleProfile}`,
    `## 叙事动力学报告\n\n${plotReport}`,
    readerExperience ? `## 读者体验报告\n\n${readerExperience}` : '',
    narrativeConstraints ? `## 叙事约束报告\n\n${narrativeConstraints}` : '',
    `## 切片列表\n\n${slicesSummary}`,
  ].filter(Boolean).join('\n\n---\n\n');

  return { systemPrompt: getPrompt('source-samples', DEFAULT_SYSTEM_PROMPT), userMessage: sections };
}
</file>

<file path="src/lib/ai/prompts/source-slicing.ts">
// ============================================
// Step 0: 智能切片 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是分析小说文本，按叙事弧线进行智能切片。

要求：
- 以情节完整性为最高优先级（不在高潮中间切断）
- 以章节边界为自然切割点
- 每个切片包含一个相对完整的叙事单元
- 为每个切片标注：序号、标题、语义标签、情节弧线、情绪基调
- 每个切片的目标字数：{targetSliceSize} 字（允许 ±30% 浮动，情节完整性优先于字数约束）

切片粒度说明：
- 2000字以下：精细切片，适合短篇分析或需要逐场景拆解
- 5000字左右（默认）：标准切片，平衡语义完整性和分析效率
- 10000字以上：粗粒度切片，适合快速浏览长篇小说的整体结构
- 30000字以上：按卷/大弧线切片，每个切片对应一个完整的叙事大段落

输出格式（Markdown）：

## 切片 {序号}: {标题}
- 语义标签: {标签1}, {标签2}
- 情节弧线: {弧线名称}
- 情绪基调: {基调}
- 字数: {字数}

{切片正文}

---
`;

export function buildSlicingMessages(novelText: string, targetSliceSize: number = 5000) {
  const systemPrompt = getPrompt('source-slicing', DEFAULT_SYSTEM_PROMPT)
    .replace('{targetSliceSize}', String(targetSliceSize));
  return { systemPrompt, userMessage: novelText };
}
</file>

<file path="src/lib/ai/prompts/style-extraction.ts">
// ============================================
// Step 2a: 文风提取 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你是小说逆向工程系统的文风分析模块。
目标：提取可迁移、可复用的文风规律。

禁止：剧情复述、人物介绍、作者评价、文学赏析、主观吹捧、推测意图。

你的任务是深入分析小说文本，提取 9 个维度的写作风格特征。

## 分析维度

### 基础六维
1. 句式特征（长短句比例、排比/对仗使用）
2. 修辞偏好（比喻、拟人、夸张等手法的使用频率和方式）
3. 词汇特征（文言/白话、口语/书面语、特定词汇频率）
4. 叙事视角（人称、全知/有限视角、语言调性）
5. 对话风格（对话长度、节奏、个性化程度）
6. 描写手法（环境描写、动作描写、心理描写的偏好和方式）

### 增强三维
7. **章节首尾技术**
   - 开头 hook 类型分布（悬念式 / 场景式 / 对话式 / 时间跳跃式 / 回忆式）及各自占比
   - 结尾类型分布（悬念切断 / 情绪释放 / 反转揭示 / 缓冲过渡 / 伏笔埋设）及各自占比
   - 首尾组合模式（例如：对话开头+悬念结尾 的组合频率）

8. **叙述与场景比例（telling vs showing）**
   - 不同节奏区域的叙述/场景比例（高潮期、过渡期、日常期）
   - 从叙述切换到场景的触发条件（什么信号让作者从"讲"切换到"演"）
   - 从场景切换到叙述的触发条件（什么信号让作者从"演"切换到"讲"）

9. **紧张语言标记**
   - 语言如何随紧张度变化：短句率、对话密度、描写量、句子碎片化程度
   - 紧张场景与平静场景的量化语言差异
   - 紧张度渐变时的语言渐变模式

## 反证要求（强制执行）

对每个文风规律：
- 主动在文本中搜索违反该规律的实例
- 输出格式：

### 规律：[规律描述]
- 支持证据：
  - 【原文："引用内容"】（位置）
- 反例：
  - 【原文："引用内容"】（位置）
- 适用率：N% (支持 X 个 / 总 Y 个)
- 标记：规律 | 非普遍规律(例外条件) | 伪规律

## 输出格式

输出为 Markdown 格式的文风档案（style_profile.md），使用表格、列表、标题层级组织。
`;

export const DEFAULT_SUPPLEMENT_PROMPT = `你之前已经分析了该作者的部分作品并生成了一份文风档案。
现在你收到了更多文本。请结合新文本，对文风档案进行补充和修正。

要求：
1. 保留原有 9 个分析维度（基础六维 + 章节首尾技术 + 叙述/场景比例 + 紧张语言标记）
2. 用新文本中的证据补充或修正每个维度的结论
3. 更新反证数据（支持证据、反例、适用率）
4. 输出完整的更新后的文风档案（不是增量，而是完整版本）
`;

export function buildStyleExtractionMessages(slicesText: string) {
  return { systemPrompt: getPrompt('source-style', DEFAULT_SYSTEM_PROMPT), userMessage: slicesText };
}

export function buildStyleSupplementMessages(newChunk: string, previousProfile: string) {
  const userMessage = `## 之前的文风档案\n\n${previousProfile}\n\n---\n\n## 新的文本片段\n\n${newChunk}\n\n---\n\n请基于以上全部信息，输出完整的、更新后的文风档案。`;
  return { systemPrompt: getPrompt('source-style-supplement', DEFAULT_SUPPLEMENT_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/volume-detail.ts">
// ============================================
// Layer 3 填充: 单卷详细规划
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是为指定卷生成详细规划。

要求：
- 约 300 字
- 包含：卷主题、核心冲突、主要事件列表+匹配的小情节模式、人物出场安排
- 弹性约束：可调整事件顺序和形式
`;

export function buildVolumeDetailMessages(
  outline: string,
  phaseContent: string,
  volumeFramework: string,
  volumeIndex: number,
  minorPlotPatterns: string,
) {
  const userMessage = `## 全书大纲\n\n${outline}\n\n---\n\n## 阶段规划\n\n${phaseContent}\n\n---\n\n## 分卷框架\n\n${volumeFramework}\n\n---\n\n## 小情节模式库\n\n${minorPlotPatterns}\n\n---\n\n请为第 ${volumeIndex + 1} 卷生成详细规划。`;
  return { systemPrompt: getPrompt('volume-detail', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/volume-framework.ts">
// ============================================
// Layer 3 框架: 分卷 — 确定数量和主题
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据阶段规划和小情节模式库，为指定阶段规划分卷框架。

要求：
- 每阶段 1-2 卷
- 每卷约 20-30 章
- 输出每卷的标题、核心冲突、预估章节范围
- 为每卷匹配适合的小情节模式
- 不要填充详细内容，仅输出框架
`;

export function buildVolumeFrameworkMessages(
  outline: string,
  phaseContent: string,
  minorPlotPatterns: string,
) {
  const userMessage = `## 全书大纲\n\n${outline}\n\n---\n\n## 阶段规划\n\n${phaseContent}\n\n---\n\n## 小情节模式库\n\n${minorPlotPatterns}`;
  return { systemPrompt: getPrompt('volume-framework', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/source-processing/basic-pipeline.ts">
// ============================================
// 基础 8 步分析管线 — 独立编排器
// 切片 → 文风 → 叙事动力学 → 角色动力学 → 读者体验 ‖ 叙事约束 → 样本 → DNA
// 支持断点恢复：根据已有数据跳过已完成步骤
// ============================================

import { parseSliceOutput, fallbackSlice, getSlicingRequestBody, shouldSkipSlicing, createSingleSlice } from '@/lib/source-processing/smart-slicer';
import { getStyleExtractionRequestBody, computeStyleBatches, getStyleSupplementRequestBody } from '@/lib/source-processing/style-extractor';
import { getPlotExtractionRequestBody, computePlotBatches, getPlotSupplementRequestBody } from '@/lib/source-processing/plot-extractor';
import { getCharacterDynamicsExtractionRequestBody, computeCharacterDynamicsBatches, getCharacterDynamicsSupplementRequestBody } from '@/lib/source-processing/character-dynamics-extractor';
import { getReaderExperienceExtractionRequestBody, computeReaderExperienceBatches, getReaderExperienceSupplementRequestBody } from '@/lib/source-processing/reader-experience-extractor';
import { getNarrativeConstraintsExtractionRequestBody, computeNarrativeConstraintsBatches, getNarrativeConstraintsSupplementRequestBody } from '@/lib/source-processing/narrative-constraints-extractor';
import { getSampleSelectionRequestBody, parseSampleOutput } from '@/lib/source-processing/sample-selector';
import { getDnaCompressionRequestBody } from '@/lib/source-processing/dna-compressor';
import { createStreamFetcher, type AIConfig, type StreamState } from '@/lib/stream-fetcher';
import type { SemanticSlice, SourceNovel, RepresentativeSample } from '@/types';

// ---- 类型 ----

/** 步骤总数 */
export const TOTAL_STEPS = 8;

/** 回调接口 — store 通过此接口接收管线事件 */
export interface BasicPipelineCallbacks {
  onStepStart: (step: number, message: string) => void;
  onStepComplete: (step: number) => void;
  onStreamUpdate: (step: number, content: string) => void;
  onStreamError: (step: number, error: string | null) => void;
  onProgress: (progress: number) => void;
  /** 保存单步结果到磁盘，管线内部不直接 fetch */
  onSaveStep: (step: number, data: Record<string, unknown>) => Promise<void>;
  /** 更新源小说数据（内存 + 触发 UI 更新） */
  onUpdateNovel: (updates: Record<string, unknown>) => void;
}

/** 管线运行结果 */
export interface BasicPipelineResult {
  slices: SemanticSlice[];
  styleProfile: string;
  plotReport: string;
  characterDynamics: string;
  readerExperience: string;
  narrativeConstraints: string;
  representativeSamples: RepresentativeSample[];
  novelDna: string | null;
  /** 哪些步骤是从断点恢复跳过的 */
  skippedSteps: number[];
}

// ---- 断点检测 ----

/**
 * 根据已有数据判断应从哪一步开始
 * 返回第一个未完成步骤的索引，0 表示从头开始
 */
export function determineResumeStep(novel: SourceNovel | null): number {
  if (!novel) return 0;
  if (!novel.slices || novel.slices.length === 0) return 0;
  if (!novel.styleProfile) return 1;
  if (!novel.plotReport) return 2;
  if (!novel.characterDynamics) return 3;
  if (!novel.readerExperience) return 4;
  if (!novel.narrativeConstraints) return 5;
  if (!novel.representativeSamples) return 6;
  return 7;
}

// ---- 多批处理 ----

/** 流式 fetcher 类型（来自 stream-fetcher） */
interface StreamFetcher {
  abort(): void;
  fetch(url: string, body: object): Promise<{ result: string | null; state: StreamState }>;
}

/**
 * 多批处理辅助 — 自动分批调用 AI 接口
 * 用于 Step 1-5（文风、叙事、角色、读者体验、叙事约束）
 */
async function runMultiBatch(
  slices: SemanticSlice[],
  batchInfo: { needsBatch: boolean; batches: string[] },
  apiPath: string,
  buildFirstBody: () => object,
  buildSupplementBody: (chunk: string, prevResult: string) => object,
  streamStep: number,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string> {
  let result = '';

  if (!batchInfo.needsBatch) {
    const body = buildFirstBody();
    const res = await fetcher.fetch(apiPath, body);
    if (res.result) {
      result = res.result;
      callbacks.onStreamUpdate(streamStep, res.state.content);
    }
  } else {
    for (let i = 0; i < batchInfo.batches.length; i++) {
      const body = i === 0
        ? buildFirstBody()
        : buildSupplementBody(batchInfo.batches[i], result);
      const res = await fetcher.fetch(apiPath, body);
      if (res.result) {
        result = res.result;
        callbacks.onStreamUpdate(streamStep, res.state.content);
      } else break;
    }
  }

  // 仅在成功时清除错误；失败时保留上游设置的 error 状态
  if (result) {
    callbacks.onStreamError(streamStep, null);
  }
  return result;
}

// ---- 单步函数 ----

/** Step 0: 智能切片 */
async function runStep0(
  rawText: string,
  novelId: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<SemanticSlice[]> {
  callbacks.onStepStart(0, '智能切片中...');
  callbacks.onProgress(0);
  callbacks.onUpdateNovel({ status: 'slicing' });

  let slices: SemanticSlice[];

  if (shouldSkipSlicing(rawText)) {
    slices = createSingleSlice(novelId, rawText);
    callbacks.onStreamUpdate(0, `文本较短（${rawText.length}字），跳过智能切片，直接作为单一切片处理。`);
  } else {
    const sliceBody = getSlicingRequestBody(rawText, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL);
    const sliceResult = await fetcher.fetch('/api/source/process/slice', sliceBody);

    if (!sliceResult.result) {
      const errMsg = sliceResult.state.error || '智能切片失败，请检查 API 配置';
      callbacks.onStreamError(0, errMsg);
      throw new Error(errMsg);
    }

    callbacks.onStreamUpdate(0, sliceResult.state.content);
    slices = parseSliceOutput(sliceResult.result, novelId);
    if (slices.length === 0) {
      slices = fallbackSlice(novelId, rawText);
    }
  }

  callbacks.onUpdateNovel({ slices });
  await callbacks.onSaveStep(0, { slices });
  callbacks.onStepComplete(0);
  return slices;
}

/** Step 1: 文风提取 */
async function runStep1(
  slices: SemanticSlice[],
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string> {
  callbacks.onStepStart(1, '文风提取中...');
  callbacks.onProgress(Math.round(100 / TOTAL_STEPS));
  callbacks.onUpdateNovel({ status: 'extracting' });

  const styleBatchInfo = computeStyleBatches(slices, aiConfig.maxContextTokens);
  const profile = await runMultiBatch(
    slices, styleBatchInfo,
    '/api/source/process/style',
    () => getStyleExtractionRequestBody(slices, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    (chunk, prev) => getStyleSupplementRequestBody(chunk, prev, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    1, fetcher, callbacks,
  );

  if (!profile) {
    throw new Error('文风提取失败，请检查 API 配置');
  }

  callbacks.onUpdateNovel({ styleProfile: profile });
  await callbacks.onSaveStep(1, { styleProfile: profile });
  callbacks.onStepComplete(1);
  return profile;
}

/** Step 2: 叙事动力学（依赖 profile） */
async function runStep2(
  slices: SemanticSlice[],
  profile: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string> {
  callbacks.onStepStart(2, '叙事动力学分析中...');

  const narrativeBatchInfo = computePlotBatches(slices, aiConfig.maxContextTokens);
  const narrativeReport = await runMultiBatch(
    slices, narrativeBatchInfo,
    '/api/source/process/plot',
    () => getPlotExtractionRequestBody(slices, profile, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    (chunk, prev) => getPlotSupplementRequestBody(chunk, prev, profile, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    2, fetcher, callbacks,
  );

  if (!narrativeReport) {
    throw new Error('叙事动力学分析失败，请检查 API 配置');
  }

  callbacks.onUpdateNovel({ plotReport: narrativeReport });
  await callbacks.onSaveStep(2, { plotReport: narrativeReport });
  callbacks.onStepComplete(2);
  return narrativeReport;
}

/** Step 3: 角色动力学（依赖 narrativeReport） */
async function runStep3(
  slices: SemanticSlice[],
  narrativeReport: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string> {
  callbacks.onStepStart(3, '角色动力学分析中...');
  callbacks.onProgress(Math.round(3 * 100 / TOTAL_STEPS));
  callbacks.onUpdateNovel({ status: 'character_dynamics' });

  const charDynBatchInfo = computeCharacterDynamicsBatches(slices, aiConfig.maxContextTokens);
  const characterDynamics = await runMultiBatch(
    slices, charDynBatchInfo,
    '/api/source/process/character-dynamics',
    () => getCharacterDynamicsExtractionRequestBody(slices, narrativeReport, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    (chunk, prev) => getCharacterDynamicsSupplementRequestBody(chunk, prev, narrativeReport, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    3, fetcher, callbacks,
  );

  if (!characterDynamics) {
    throw new Error('角色动力学分析失败，请检查 API 配置');
  }

  callbacks.onUpdateNovel({ characterDynamics });
  await callbacks.onSaveStep(3, { characterDynamics });
  callbacks.onStepComplete(3);
  return characterDynamics;
}

/** Step 4: 读者体验 */
async function runStep4(
  slices: SemanticSlice[],
  profile: string,
  narrativeReport: string,
  characterDynamics: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string> {
  const readerExpBatchInfo = computeReaderExperienceBatches(slices, aiConfig.maxContextTokens);
  const result = await runMultiBatch(
    slices, readerExpBatchInfo,
    '/api/source/process/reader-experience',
    () => getReaderExperienceExtractionRequestBody(slices, profile, narrativeReport, characterDynamics, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    (chunk, prev) => getReaderExperienceSupplementRequestBody(chunk, prev, profile, narrativeReport, characterDynamics, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    4, fetcher, callbacks,
  );

  if (!result) {
    throw new Error('读者体验分析失败，请检查 API 配置');
  }

  callbacks.onUpdateNovel({ readerExperience: result });
  await callbacks.onSaveStep(4, { readerExperience: result });
  return result;
}

/** Step 5: 叙事约束 */
async function runStep5(
  slices: SemanticSlice[],
  narrativeReport: string,
  characterDynamics: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string> {
  const narConBatchInfo = computeNarrativeConstraintsBatches(slices, aiConfig.maxContextTokens);
  const result = await runMultiBatch(
    slices, narConBatchInfo,
    '/api/source/process/narrative-constraints',
    () => getNarrativeConstraintsExtractionRequestBody(slices, narrativeReport, characterDynamics, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    (chunk, prev) => getNarrativeConstraintsSupplementRequestBody(chunk, prev, narrativeReport, characterDynamics, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL),
    5, fetcher, callbacks,
  );

  if (!result) {
    throw new Error('叙事约束分析失败，请检查 API 配置');
  }

  callbacks.onUpdateNovel({ narrativeConstraints: result });
  await callbacks.onSaveStep(5, { narrativeConstraints: result });
  return result;
}

/** Step 6: 样本选取 */
async function runStep6(
  slices: SemanticSlice[],
  profile: string,
  narrativeReport: string,
  readerExperience: string,
  narrativeConstraints: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<RepresentativeSample[]> {
  callbacks.onStepStart(6, '样本选取中...');
  callbacks.onProgress(Math.round(6 * 100 / TOTAL_STEPS));
  callbacks.onUpdateNovel({ status: 'selecting' });

  const sampleBody = getSampleSelectionRequestBody(
    slices, profile, narrativeReport,
    aiConfig.apiKey, aiConfig.model, aiConfig.baseURL,
    readerExperience, narrativeConstraints,
  );
  const sampleResult = await fetcher.fetch('/api/source/process/samples', sampleBody);

  if (!sampleResult.result) {
    callbacks.onStreamError(6, sampleResult.state.error || '样本选取失败');
    throw new Error('样本选取失败，请检查 API 配置');
  }

  callbacks.onStreamUpdate(6, sampleResult.state.content);
  const samples = parseSampleOutput(sampleResult.result);

  callbacks.onUpdateNovel({ representativeSamples: samples });
  await callbacks.onSaveStep(6, { representativeSamples: samples });
  callbacks.onStepComplete(6);
  return samples;
}

/** Step 7: DNA 压缩（失败不中止管线） */
async function runStep7(
  profile: string,
  narrativeReport: string,
  characterDynamics: string,
  readerExperience: string,
  narrativeConstraints: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  callbacks: BasicPipelineCallbacks,
): Promise<string | null> {
  callbacks.onStepStart(7, 'DNA 压缩中...');
  callbacks.onProgress(Math.round(7 * 100 / TOTAL_STEPS));
  callbacks.onUpdateNovel({ status: 'compressing' });

  const dnaBody = getDnaCompressionRequestBody(
    profile, narrativeReport, characterDynamics,
    readerExperience, narrativeConstraints,
    aiConfig.apiKey, aiConfig.model, aiConfig.baseURL,
  );
  const dnaResult = await fetcher.fetch('/api/source/process/dna-compression', dnaBody);

  let novelDna: string | null = null;

  if (!dnaResult.result) {
    // DNA 压缩失败不影响整体流程，但通知用户
    const dnaError = dnaResult.state.error || '未知错误';
    console.warn('DNA 压缩失败:', dnaError);
    callbacks.onStreamUpdate(7, `⚠️ DNA 压缩失败: ${dnaError}`);
    callbacks.onStreamError(7, dnaError);
  } else {
    callbacks.onStreamUpdate(7, dnaResult.state.content);
    novelDna = dnaResult.result;
    callbacks.onUpdateNovel({ novelDna });
    await callbacks.onSaveStep(7, { novelDna });
  }

  callbacks.onStepComplete(7);
  return novelDna;
}

// ---- 主编排函数 ----

/**
 * 运行基础 8 步分析管线
 *
 * @param novelId 小说 ID
 * @param rawText 原始文本（新上传时提供）
 * @param aiConfig AI 配置
 * @param novel 已有小说数据（恢复时传入，新上传传 null）
 * @param callbacks 回调接口
 * @returns 管线结果 + 跳过的步骤列表
 */
export async function runBasicPipeline(
  novelId: string,
  rawText: string | null,
  aiConfig: AIConfig,
  novel: SourceNovel | null,
  callbacks: BasicPipelineCallbacks,
  externalFetcher?: ReturnType<typeof createStreamFetcher>,
): Promise<BasicPipelineResult> {
  const fetcher = externalFetcher ?? createStreamFetcher();
  const resumeStep = determineResumeStep(novel);
  const skippedSteps: number[] = [];

  // 从已有数据加载已完成步骤的结果
  let slices: SemanticSlice[] = novel?.slices ?? [];
  let profile = novel?.styleProfile ?? '';
  let narrativeReport = novel?.plotReport ?? '';
  let characterDynamics = novel?.characterDynamics ?? '';
  let readerExperience = novel?.readerExperience ?? '';
  let narrativeConstraints = novel?.narrativeConstraints ?? '';
  let samples: RepresentativeSample[] = novel?.representativeSamples ?? [];
  let novelDna: string | null = novel?.novelDna ?? null;

  // 记录跳过的步骤
  for (let i = 0; i < resumeStep; i++) {
    skippedSteps.push(i);
  }

  // ── Step 0: 智能切片 ──
  if (resumeStep <= 0) {
    if (!rawText) throw new Error('新上传需要 rawText，恢复时需要已有 slices');
    slices = await runStep0(rawText, novelId, aiConfig, fetcher, callbacks);
  }

  // ── Step 1: 文风提取 ──
  if (resumeStep <= 1) {
    profile = await runStep1(slices, aiConfig, fetcher, callbacks);
  }

  // ── Step 2: 叙事动力学（依赖 profile） ──
  if (resumeStep <= 2) {
    narrativeReport = await runStep2(slices, profile, aiConfig, fetcher, callbacks);
  }

  // ── Step 3: 角色动力学（依赖 narrativeReport） ──
  if (resumeStep <= 3) {
    characterDynamics = await runStep3(slices, narrativeReport, aiConfig, fetcher, callbacks);
  }

  // ── Step 4 + Step 5: 读者体验 ‖ 叙事约束（并行） ──
  if (resumeStep <= 4) {
    callbacks.onStepStart(4, '读者体验 + 叙事约束分析中（并行）...');
    callbacks.onProgress(Math.round(4 * 100 / TOTAL_STEPS));
    callbacks.onUpdateNovel({ status: 'deep_analyzing' });

    // Step 5 不依赖 Step 4 结果，两者可并行
    const needsReaderExp = resumeStep <= 4;
    const needsNarCon = resumeStep <= 5;

    const results = await Promise.all([
      needsReaderExp
        ? runStep4(slices, profile, narrativeReport, characterDynamics, aiConfig, fetcher, callbacks)
        : Promise.resolve(readerExperience),
      needsNarCon
        ? runStep5(slices, narrativeReport, characterDynamics, aiConfig, fetcher, callbacks)
        : Promise.resolve(narrativeConstraints),
    ]);

    readerExperience = results[0];
    narrativeConstraints = results[1];

    // onSaveStep 已在 runStep4/runStep5 内部调用，此处无需重复
    callbacks.onStepComplete(4);
    callbacks.onStepComplete(5);
  } else if (resumeStep <= 5) {
    // Step 4 已完成但 Step 5 未完成（极端边界情况）
    narrativeConstraints = await runStep5(slices, narrativeReport, characterDynamics, aiConfig, fetcher, callbacks);
  }

  // ── Step 6: 样本选取 ──
  if (resumeStep <= 6) {
    samples = await runStep6(slices, profile, narrativeReport, readerExperience, narrativeConstraints, aiConfig, fetcher, callbacks);
  }

  // ── Step 7: DNA 压缩（失败不中止） ──
  if (resumeStep <= 7) {
    novelDna = await runStep7(profile, narrativeReport, characterDynamics, readerExperience, narrativeConstraints, aiConfig, fetcher, callbacks);
  }

  // 全部完成
  const now = new Date().toISOString();
  callbacks.onUpdateNovel({ status: 'ready', processedAt: now });
  await callbacks.onSaveStep(7, { status: 'ready', processedAt: now });
  callbacks.onProgress(100);

  return {
    slices, styleProfile: profile, plotReport: narrativeReport,
    characterDynamics, readerExperience, narrativeConstraints,
    representativeSamples: samples, novelDna, skippedSteps,
  };
}
</file>

<file path="src/lib/source-processing/sample-selector.ts">
// ============================================
// 代表性切片选取 — 客户端编排模块
// ============================================

import { buildSampleSelectionMessages } from '@/lib/ai/prompts';
import type { SemanticSlice, RepresentativeSample } from '@/types';

/** 解析 AI 输出的样本 markdown 为 RepresentativeSample[] */
export function parseSampleOutput(raw: string): RepresentativeSample[] {
  const samples: RepresentativeSample[] = [];
  const sections = raw.split(/^## 样本 /m).filter(Boolean);

  for (const section of sections) {
    const lines = section.split('\n');
    const headerLine = lines[0] ?? '';
    const titleMatch = headerLine.match(/^\d+:\s*(.+)/);

    let selectionReason = '';
    let contentStartIndex = 0;

    for (let i = 1; i < lines.length; i++) {
      const line = lines[i].trim();
      if (line.startsWith('- 场景类型:') || line.startsWith('- 选取理由:')) {
        if (line.startsWith('- 选取理由:')) {
          selectionReason = line.replace('- 选取理由:', '').trim();
        }
      } else if (line === '---' || (line && !line.startsWith('-') && contentStartIndex === 0)) {
        if (contentStartIndex === 0) contentStartIndex = i;
        break;
      }
    }

    if (contentStartIndex === 0) {
      contentStartIndex = lines.findIndex((l, i) => i > 0 && !l.startsWith('-') && l.trim() !== '' && l.trim() !== '---');
      if (contentStartIndex === -1) contentStartIndex = lines.length;
    }

    const content = lines.slice(contentStartIndex).join('\n').replace(/^---\s*/m, '').trim();

    if (content) {
      samples.push({
        sliceId: `sample-${samples.length}`,
        sliceIndex: samples.length,
        title: titleMatch?.[1]?.trim() ?? `样本${samples.length + 1}`,
        content,
        selectionReason,
      });
    }
  }

  return samples;
}

/** 获取样本选取 API 的请求体 */
export function getSampleSelectionRequestBody(
  slices: SemanticSlice[],
  styleProfile: string,
  plotReport: string,
  apiKey: string,
  model: string,
  baseURL: string,
  readerExperience?: string,
  narrativeConstraints?: string,
) {
  // 只传切片摘要，不传全文（节省 token）
  const slicesSummary = slices
    .map((s) => `- ${s.title} (${s.charCount}字) [${s.semanticTags.join(', ')}] ${s.emotionalTone}`)
    .join('\n');

  const { systemPrompt, userMessage } = buildSampleSelectionMessages(
    slicesSummary, styleProfile, plotReport, readerExperience, narrativeConstraints,
  );

  return {
    systemPrompt,
    userMessage,
    apiKey,
    model,
    baseURL,
  };
}
</file>

<file path="src/lib/source-processing/windowed-event-extractor.ts">
// ============================================
// 重叠窗口事件提取器 — 5窗口并行→合并→事件图谱
// 替代原 Step 0-3 的多批次文本摘要模式
// ============================================

import { buildEventExtractionMessages } from '@/lib/ai/prompts';
import { computeMaxCharsPerBatch } from '@/lib/analysis-chunker';
import { type AIConfig, type StreamState } from '@/lib/stream-fetcher';
import type { TextChunk, NovelEvent, EventGraph } from '@/types';

// ---- 类型 ----

interface StreamFetcher {
  fetch(url: string, body: object): Promise<{ result: string | null; state: StreamState }>;
}

/** 单窗口配置 */
interface WindowConfig {
  id: string;             // W1, W2, ...
  startChunk: number;
  endChunk: number;
}

// ---- 窗口划分 ----

/**
 * 将 chunks 划分为重叠窗口
 * 5 个窗口 × ~45万字，重叠 10%
 */
export function computeWindows(chunks: TextChunk[], totalChars: number): WindowConfig[] {
  const WINDOW_CHARS = 450_000;  // 每窗口约 45 万字
  const OVERLAP_RATIO = 0.10;    // 重叠 10%

  const windows: WindowConfig[] = [];
  let currentStart = 0;
  let windowIndex = 0;

  while (currentStart < chunks.length) {
    // 计算当前窗口的结束位置
    let charCount = 0;
    let endChunk = currentStart;
    for (let i = currentStart; i < chunks.length; i++) {
      charCount += chunks[i].charCount;
      if (charCount >= WINDOW_CHARS) {
        endChunk = i;
        break;
      }
      endChunk = i;
    }
    if (endChunk >= chunks.length) endChunk = chunks.length - 1;

    windows.push({
      id: `W${windowIndex + 1}`,
      startChunk: currentStart,
      endChunk,
    });

    if (endChunk >= chunks.length - 1) break;

    // 下一窗口从当前窗口的 ~90% 位置开始（重叠 10%）
    const windowCharCount = chunks.slice(currentStart, endChunk + 1).reduce((s, c) => s + c.charCount, 0);
    const overlapSize = Math.floor(windowCharCount * OVERLAP_RATIO);
    let overlapChars = 0;
    currentStart = endChunk;
    for (let i = endChunk; i >= currentStart; i--) {
      overlapChars += chunks[i].charCount;
      if (overlapChars >= overlapSize) {
        currentStart = i;
        break;
      }
    }

    windowIndex++;
  }

  return windows;
}

// ---- 主流程 ----

export interface WindowedExtractionResult {
  eventGraph: EventGraph;
  rawWindowResults: Array<{ windowId: string; events: NovelEvent[] }>;
}

/**
 * 执行重叠窗口事件提取
 * @param chunks 文本分块
 * @param entityDict 实体字典文本
 * @param novelId 小说 ID
 * @param aiConfig AI 配置
 * @param fetcher 流式 fetch 工具
 * @param onProgress 进度回调
 */
export async function runWindowedEventExtraction(
  chunks: TextChunk[],
  entityDict: string,
  novelId: string,
  aiConfig: AIConfig,
  fetcher: StreamFetcher,
  onProgress?: (window: string, progress: number) => void,
): Promise<WindowedExtractionResult> {
  const totalChars = chunks.reduce((s, c) => s + c.charCount, 0);
  const windows = computeWindows(chunks, totalChars);

  // ═══ 并行提取所有窗口的事件 ═══
  const windowResults = await Promise.all(
    windows.map(async (win) => {
      onProgress?.(win.id, 0);

      // 组装该窗口的原文
      const windowChunks = chunks.slice(win.startChunk, win.endChunk + 1);
      const chunksText = windowChunks
        .map((c) => `## Chunk ${c.index}: ${c.title}\n\n${c.content}`)
        .join('\n\n---\n\n');

      // TODO: 重叠区事件引用机制尚未实现，后续可传入前窗口事件 ID 提升合并质量
      const { systemPrompt, userMessage } = buildEventExtractionMessages(
        chunksText, win.startChunk, windows.length,
      );

      const res = await fetcher.fetch('/api/source/process/event-extraction', {
        systemPrompt, userMessage,
        apiKey: aiConfig.apiKey, model: aiConfig.model, baseURL: aiConfig.baseURL,
      });

      if (!res.result) {
        console.error(`窗口 ${win.id} 事件提取失败:`, res.state.error);
        return { windowId: win.id, events: [] };
      }

      const events = parseEventOutput(res.result, win.id, windowChunks);
      onProgress?.(win.id, 1);
      return { windowId: win.id, events };
    }),
  );

  // ═══ 合并去重（重叠区事件匹配） ═══
  const mergedEvents = mergeOverlappingEvents(windowResults, chunks);

  // ═══ 构建事件图谱 ═══
  const eventGraph = buildEventGraph(mergedEvents, novelId, chunks);

  return { eventGraph, rawWindowResults: windowResults };
}

// ---- 解析 AI 输出 ----

function parseEventOutput(raw: string, windowId: string, windowChunks: TextChunk[]): NovelEvent[] {
  try {
    const jsonMatch = raw.match(/\[[\s\S]*\]/);
    if (!jsonMatch) return [];
    const parsed = JSON.parse(jsonMatch[0]);
    if (!Array.isArray(parsed)) return [];

    return parsed
      .filter((e: Record<string, unknown>) =>
        typeof e.type === 'string' && typeof e.description === 'string' && (e.confidence === undefined || Number(e.confidence) >= 0.5))
      .map((e: Record<string, unknown>, idx: number): NovelEvent => {
        const chunkIdx = typeof e.chapter === 'number'
          ? windowChunks.findIndex((c) => c.chapterStart <= Number(e.chapter) && c.chapterEnd >= Number(e.chapter))
          : 0;

        return {
          id: String(e.id ?? `E-${windowId}-${idx}`),
          chapter: Number(e.chapter ?? 0),
          chunkIndex: chunkIdx >= 0 ? chunkIdx : 0,
          type: String(e.type ?? '其他'),
          participants: Array.isArray(e.participants) ? e.participants.map(String) : [],
          location: String(e.location ?? '未知'),
          description: String(e.description ?? '').slice(0, 50),
          causes: Array.isArray(e.causes) ? e.causes.map(String) : [],
          effects: Array.isArray(e.effects) ? e.effects.map(String) : [],
          tensionChange: Math.min(5, Math.max(-5, Number(e.tension_change ?? 0))),
          emotion: String(e.emotion ?? ''),
          foreshadowingOf: e.foreshadowing_of ? String(e.foreshadowing_of) : null,
          foreshadowingFrom: null, // 合并阶段填充
          confidence: Math.min(1, Math.max(0, Number(e.confidence ?? 0.7))),
        };
      });
  } catch (err) {
    console.error(`窗口 ${windowId} 事件解析失败:`, err);
    return [];
  }
}

// ---- 重叠区合并 ----

function mergeOverlappingEvents(
  windowResults: Array<{ windowId: string; events: NovelEvent[] }>,
  chunks: TextChunk[],
): NovelEvent[] {
  const allEvents: NovelEvent[] = [];
  const mergedNextEventIds = new Set<string>(); // 被合并的下一窗口事件 ID

  for (let i = 0; i < windowResults.length; i++) {
    const currentWin = windowResults[i];
    const nextWin = windowResults[i + 1];

    for (const event of currentWin.events) {
      // 检查此事件是否与下一窗口中的某个事件一致（重叠区检测）
      let merged = false;
      if (nextWin) {
        for (const nextEvent of nextWin.events) {
          // 匹配条件：同章节 + 同类型 + 共享至少一个参与者
          const sameChapter = Math.abs(event.chapter - nextEvent.chapter) <= 2;
          const sameType = event.type === nextEvent.type;
          const sharedParticipant = event.participants.some((p) => nextEvent.participants.includes(p));

          if (sameChapter && sameType && sharedParticipant) {
            // 这是同一个事件在不同窗口中的两次提取 → 合并
            const mergedEvent = {
              ...event,
              // 取更长的描述
              description: event.description.length >= nextEvent.description.length ? event.description : nextEvent.description,
              // 合并参与者
              participants: [...new Set([...event.participants, ...nextEvent.participants])],
              // 取更高的置信度
              confidence: Math.max(event.confidence, nextEvent.confidence),
              // 合并因果
              causes: [...new Set([...event.causes, ...nextEvent.causes])],
              effects: [...new Set([...event.effects, ...nextEvent.effects])],
            };
            allEvents.push(mergedEvent);
            mergedNextEventIds.add(nextEvent.id);
            merged = true;
            break;
          }
        }
      }

      if (!merged) {
        allEvents.push(event);
      }
    }

    // 添加下一窗口中未被合并的事件
    if (nextWin) {
      for (const nextEvent of nextWin.events) {
        if (!mergedNextEventIds.has(nextEvent.id)) {
          allEvents.push(nextEvent);
        }
      }
    }
  }

  // 去重（最终按 ID）
  const uniqueById = new Map<string, NovelEvent>();
  for (const e of allEvents) {
    const existing = uniqueById.get(e.id);
    if (!existing || e.confidence > existing.confidence) {
      uniqueById.set(e.id, e);
    }
  }

  return [...uniqueById.values()].sort((a, b) => a.chapter - b.chapter || a.chunkIndex - b.chunkIndex);
}

// ---- 构建事件图谱 ----

function buildEventGraph(events: NovelEvent[], novelId: string, chunks: TextChunk[]): EventGraph {
  // 索引
  const byChapter: Record<number, string[]> = {};
  const byParticipant: Record<string, string[]> = {};
  const byType: Record<string, string[]> = {};
  const eventById = new Map<string, NovelEvent>();
  for (const e of events) {
    eventById.set(e.id, e);
    (byChapter[e.chapter] ??= []).push(e.id);
    for (const p of e.participants) {
      (byParticipant[p] ??= []).push(e.id);
    }
    (byType[e.type] ??= []).push(e.id);
  }

  // 伏笔-回收对
  const foreshadowingPairs: EventGraph['foreshadowingPairs'] = [];
  for (const e of events) {
    if (e.foreshadowingOf) {
      const payoff = eventById.get(e.foreshadowingOf);
      if (payoff) {
        foreshadowingPairs.push({
          setup: e.id,
          payoff: payoff.id,
          distance: payoff.chapter - e.chapter,
        });
        // 反向标记
        payoff.foreshadowingFrom = e.id;
      }
    }
  }

  return {
    novelId,
    events,
    totalChapters: chunks[chunks.length - 1]?.chapterEnd ?? 0,
    totalChunks: chunks.length,
    extractedAt: new Date().toISOString(),
    byChapter,
    byParticipant,
    byType,
    foreshadowingPairs,
  };
}
</file>

<file path="src/lib/store/settings.ts">
// ============================================
// 设置 Store — API 配置，持久化到 localStorage
// ============================================

import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import { DEFAULT_MODEL, DEFAULT_BASE_URL } from '@/lib/constants';
import type { AIConfig } from '@/lib/stream-fetcher';

export interface SettingsState {
  apiKey: string;
  model: string;
  baseURL: string;
  maxContextTokens: number;
  thinkingMode: boolean;

  setAISettings: (settings: Partial<Pick<SettingsState, 'apiKey' | 'model' | 'baseURL' | 'maxContextTokens' | 'thinkingMode'>>) => void;
  getEffectiveApiKey: () => string;
  getAIConfig: () => AIConfig;
}

export const useSettingsStore = create<SettingsState>()(
  persist(
    (set, get) => ({
      apiKey: '',
      model: DEFAULT_MODEL,
      baseURL: DEFAULT_BASE_URL,
      maxContextTokens: 1000000,
      thinkingMode: true,

      setAISettings: (settings) => set(settings),

      getEffectiveApiKey: () => {
        const stored = get().apiKey;
        if (stored.trim()) return stored;
        return process.env.NEXT_PUBLIC_DEEPSEEK_API_KEY ?? '';
      },

      getAIConfig: () => ({
        apiKey: get().getEffectiveApiKey(),
        model: get().model,
        baseURL: get().baseURL,
        maxContextTokens: get().maxContextTokens,
        thinkingMode: get().thinkingMode,
      }),
    }),
    {
      name: 'ainovr-settings',
      version: 8,
      migrate: (persisted, version) => {
        let state = persisted as Record<string, unknown>;

        // v1-2: 添加 baseURL
        if (version <= 2) {
          if (!state.baseURL) {
            state = { ...state, baseURL: DEFAULT_BASE_URL, model: state.model || DEFAULT_MODEL };
          }
        }
        // v3: 清理废弃字段
        if (version <= 3) {
          delete state.providerType;
          delete state.customProviders;
          if (!state.baseURL) {
            state = { ...state, baseURL: DEFAULT_BASE_URL };
          }
        }
        // v4: 重置 apiKey
        if (version <= 4) {
          state = { ...state, apiKey: '' };
        }
        // v5-6: 清理废弃字段
        if (version <= 6) {
          delete state.thinkingMode;
          delete state.thinkingEffort;
          delete state.chatMessages;
          delete state.importConfig;
          delete state.selectedBookIds;
          delete state.novels;
          delete state.analysisReport;
          delete state.isAnalyzing;
          delete state.writeResult;
          delete state.isWriting;
          delete state.activeView;
          state = {
            ...state,
            maxContextTokens: (state.maxContextTokens as number) || 1000000,
          };
        }
        // v8: thinkingMode 重新引入（默认开启）
        if (version <= 7) {
          state = { ...state, thinkingMode: true };
        }
        return state;
      },
    }
  )
);
</file>

<file path="src/lib/store/source-library.ts">
// ============================================
// 素材库 Store — 服务端为主，浏览器缓存
// ============================================

import { create } from 'zustand';
import type { SourceNovel, SourceNovelStatus, PipelineTask } from '@/types';

/** 处理中的过渡态 — 页面刷新后不应残留 */
const TRANSIENT_STATUSES: SourceNovelStatus[] = [
  'indexing', 'segmenting', 'slicing', 'extracting',
  'character_dynamics', 'deep_analyzing', 'selecting',
  'evolution_modeling', 'compressing',
];

export interface SourceLibraryState {
  /** 浏览器内存缓存 */
  sourceNovels: SourceNovel[];
  /** 当前正在查看/处理的源小说 ID */
  activeSourceId: string | null;
  /** 当前管线任务 */
  activeTask: PipelineTask | null;

  // 数据加载
  loadSourceNovels: () => Promise<void>;

  // 增删改
  addSourceNovel: (novel: SourceNovel) => void;
  removeSourceNovel: (id: string) => void;
  updateSourceNovel: (id: string, updates: Partial<SourceNovel>) => void;

  // 导航
  setActiveSourceId: (id: string | null) => void;
  setActiveTask: (task: PipelineTask | null) => void;
}

export const useSourceLibraryStore = create<SourceLibraryState>()((set) => ({
  sourceNovels: [],
  activeSourceId: null,
  activeTask: null,

  loadSourceNovels: async () => {
    try {
      const res = await fetch('/api/library/list');
      if (!res.ok) return;
      const data = await res.json();
      const novels: SourceNovel[] = (data.novels ?? []).map((n: SourceNovel) =>
        TRANSIENT_STATUSES.includes(n.status)
          ? { ...n, status: 'error' as SourceNovelStatus }
          : n,
      );
      set({ sourceNovels: novels });
    } catch (err) {
      console.error('加载素材库失败:', err);
    }
  },

  addSourceNovel: (novel) => {
    set((state) => ({ sourceNovels: [...state.sourceNovels, novel] }));
  },

  removeSourceNovel: (id) => {
    set((state) => ({
      sourceNovels: state.sourceNovels.filter((n) => n.id !== id),
      activeSourceId: state.activeSourceId === id ? null : state.activeSourceId,
    }));
  },

  updateSourceNovel: (id, updates) => {
    set((state) => ({
      sourceNovels: state.sourceNovels.map((n) =>
        n.id === id ? { ...n, ...updates } : n
      ),
    }));
  },

  setActiveSourceId: (id) => set({ activeSourceId: id }),
  setActiveTask: (task) => set({ activeTask: task }),
}));
</file>

<file path="src/lib/stream-fetcher.ts">
// ============================================
// 共享流式 fetch 工具 — 统一所有管线的 HTTP 流式请求
// dao-pipeline、source-processing store、layer-pipeline 均使用此模块
// 自动注入 thinkingMode，无需每个 get*RequestBody() 手动传递
// ============================================

/** AI 配置（统一类型，替代各文件中重复定义的同名接口） */
export interface AIConfig {
  apiKey: string;
  model: string;
  baseURL: string;
  maxContextTokens: number;
  thinkingMode?: boolean;
}

// ---- 流式状态 ----

export interface StreamState {
  content: string;
  isStreaming: boolean;
  error: string | null;
}

// ---- 流式 fetch 工具 ----

export function createStreamFetcher() {
  let abortController: AbortController | null = null;

  return {
    abort() {
      abortController?.abort();
      abortController = null;
    },

    async fetch(url: string, body: object): Promise<{ result: string | null; state: StreamState }> {
      const controller = new AbortController();
      abortController = controller;
      const state: StreamState = { content: '', isStreaming: true, error: null };

      try {
        // 从 settings store 自动注入 thinkingMode
        let thinkingMode: boolean | undefined;
        try { thinkingMode = (await import('@/lib/store/settings')).useSettingsStore.getState().thinkingMode; } catch { /* store 未初始化 */ }

        const response = await fetch(url, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ ...body, thinkingMode }),
          signal: controller.signal,
        });

        if (!response.ok) {
          const err = await response.json().catch(() => ({ error: '请求失败' }));
          throw new Error(err.error || `${response.status} 请求失败`);
        }

        const reader = response.body?.getReader();
        if (!reader) throw new Error('无法读取响应流');

        const decoder = new TextDecoder();
        const chunks: string[] = [];
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          chunks.push(decoder.decode(value, { stream: true }));
          state.content = chunks.join('');
        }

        const fullText = chunks.join('');
        if (!fullText.trim()) throw new Error('模型返回了空响应');
        // 检测上游编码的错误（providers.ts 用文本而非 controller.error 传递）
        if (fullText.startsWith('[STREAM_ERROR]')) {
          throw new Error(fullText.replace(/^\[STREAM_ERROR\]\s*/, ''));
        }
        state.isStreaming = false;
        return { result: fullText, state };
      } catch (err: unknown) {
        if (err instanceof DOMException && err.name === 'AbortError') {
          state.isStreaming = false;
          return { result: null, state };
        }
        state.error = err instanceof Error ? err.message : '未知错误';
        state.isStreaming = false;
        return { result: null, state };
      } finally {
        if (abortController === controller) abortController = null;
      }
    },
  };
}
</file>

<file path="src/lib/utils.ts">
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

/** 过滤文件名中的非法字符 */
export function safeFilename(name: string): string {
  return name.replace(/[\\/:*?"<>|]/g, '_');
}

/** 安全解析数字输入，避免 NaN 扩散 */
export function safeInt(value: string, fallback: number = 0): number {
  const n = Number(value);
  return Number.isFinite(n) ? Math.max(0, Math.round(n)) : fallback;
}

import type { NovelChunk } from '@/types';

/** 客户端保存小说到服务端 data/novels/ */
export async function saveNovelToServer(novel: {
  id: string;
  title: string;
  fullText: string;
  chunks: NovelChunk[];
}): Promise<void> {
  try {
    const res = await fetch('/api/novels/save', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        id: novel.id,
        title: novel.title,
        fullText: novel.fullText,
        chunks: novel.chunks,
      }),
    });
    if (!res.ok) {
      console.error('Failed to save novel to server:', res.status);
    }
  } catch (err) {
    console.error('Failed to save novel to server:', err);
  }
}
</file>

<file path="package.json">
{
  "name": "ainovr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.67",
    "@base-ui/react": "^1.5.0",
    "ai": "^6.0.193",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "file-saver": "^2.0.5",
    "lucide-react": "^1.17.0",
    "nanoid": "^5.1.11",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "shadcn": "^4.9.0",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.6.0",
    "tw-animate-css": "^1.4.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/file-saver": "^2.0.7",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
</file>

<file path="README.md">
# ✦ Ainovr — AI 小说风格模仿创作

上传你喜欢的小说，AI 深度分析文风、剧情、人物塑造等特征，生成量化 DNA 规则，然后按风格仿写全新故事。

## 功能

- 📖 **上传参考小说** — 支持 TXT 文件，可同时上传多本
- 🔬 **统一 7 步分析管线** — 小切片 → 事件提取 → 事件对齐（全书记忆图谱）→ 大切片 → 深度分析 → 汇总报告 → DNA 压缩
- 🧬 **工程逆向 DNA** — 量化提取可度量的写作规则（非文学赏析），支持断点恢复
- ✍️ **5 层层级生成** — 大纲 → 阶段规划 → 分卷 → 章节集 → 每章计划，先框架后细节
- 🔄 **章节审查** — 5 维度自动审查（文风/情节/节奏/模式/伏笔），支持最多 3 轮修正
- 📤 **导出** — 仿写结果导出为 TXT 文件
- 🔧 **34 个可自定义 Prompt** — 所有 AI 提示词均可通过界面定制

## 技术栈

- **前端**：Next.js 16.2.6 + React 19.2.4 + TypeScript 5（strict）
- **样式**：Tailwind CSS v4 + shadcn/ui（base-nova 风格，OKLCH 色彩空间）
- **AI**：自研 SSE 流式客户端（原生 fetch + ReadableStream，OpenAI 兼容协议），默认 DeepSeek API
- **存储**：文件系统（JSON/TXT/MD/YAML），`data/` 目录下持久化，原子写入保证数据安全
- **状态**：Zustand v5，6 个 store

## 快速开始

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

浏览器打开 http://localhost:3000

## 使用流程

1. 点击右上角 ⚙️ 配置 AI 后端（推荐 DeepSeek）
2. 上传你喜欢的小说 TXT 文件
3. 点击「一键分析」→ 等待统一 7 步管线分析完成
4. 创建写作项目 → 指定源小说角色（文风参考 / 情节参考 / 两者）
5. 逐层生成：大纲 → 阶段 → 分卷 → 章节集 → 每章计划
6. 生成章节正文 → 自动审查 → 修正
7. 导出完成的作品

## 项目结构

```
src/
├── app/
│   ├── page.tsx              # 应用入口（SPA 视图路由）
│   ├── layout.tsx            # 根布局 + metadata + Toaster
│   └── api/                  # 46 个 API Route Handler
│       ├── source/process/   # 素材分析（15 个：统一管线 5 步 + 旧版 8 步 + 进阶 3 步）
│       ├── generation/       # 5 层生成（9 个 route）
│       ├── chapter/          # 章节生成/审查/修正
│       ├── library/          # 素材库 CRUD（7 个）
│       ├── project/          # 写作项目 CRUD（5 个）
│       ├── prompts/          # 自定义 prompt 管理
│       └── novels/           # 旧版兼容接口
├── lib/
│   ├── ai/
│   │   ├── providers.ts      # 自研 SSE 流式 AI 客户端
│   │   └── prompts/          # 28 个 prompt 构建器 + 公共约束段
│   ├── api/
│   │   └── stream-route.ts   # API route 统一工厂
│   ├── store/                # 6 个 Zustand store
│   │   ├── settings.ts       # AI 配置（migration v8）
│   │   ├── navigation.ts     # 视图路由
│   │   ├── source-library.ts # 素材库列表
│   │   ├── source-processing.ts # 统一管线状态 + 断点恢复
│   │   ├── project.ts        # 写作项目 + 层级生成
│   │   └── prompts.ts        # 34 个 prompt 注册表 + 覆盖
│   ├── source-processing/    # 分析管线（unified-pipeline + 旧版 + 提取模块）
│   ├── generation/           # 5 层生成管线 + 上下文组装
│   ├── stream-fetcher.ts     # 共享流式 fetch 工具
│   ├── safe-path.ts          # 路径遍历防护
│   ├── atomic-write.ts       # 原子文件写入
│   └── constants.ts          # 共享常量
├── components/
│   ├── ui/                   # shadcn/ui 基础组件（16 个）
│   ├── source/               # 素材库/详情/文风情节库视图
│   ├── project/              # 写作项目视图
│   ├── generation/           # 层级生成视图
│   ├── chapter/              # 章节生成/审查视图
│   ├── prompts/              # Prompt 管理视图
│   ├── Sidebar.tsx           # 侧边栏导航
│   └── SettingsDialog.tsx    # 设置弹窗
└── types/
    └── index.ts              # 全局类型定义（679 行）
```

## 架构概览

```
┌──────────────────────────────────────────────────────────────┐
│  统一分析管线（7 步 · 支持断点恢复 · 短文本自动简化）         │
│  小切片 → 事件提取 → 事件对齐 → 大切片 → 深度分析 → 汇总     │
│       → DNA 压缩 → 量化生成规则                               │
├──────────────────────────────────────────────────────────────┤
│  写作生成管线（5 层层次化 · 先框架后细节）                    │
│  大纲 → 阶段 → 分卷 → 章节集 → 每章计划 → 正文 → 审查 → 修正 │
└──────────────────────────────────────────────────────────────┘
```

## 开发

```bash
npm run dev     # 开发服务器（localhost:3000）
npm run build   # 生产构建
npm run lint    # ESLint 检查
```

### 手动测试管线

```bash
# 需要先启动 dev server
node data/test-pipeline.mjs http://localhost:3000 YOUR_API_KEY deepseek-chat
```

## 许可

私有项目。
</file>

<file path="src/app/api/library/save-step/route.ts">
// ============================================
// POST /api/library/save-step — 增量保存单步处理结果
// ============================================

import { NextRequest, NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import { safeJoin } from '@/lib/safe-path';
import { atomicWriteJson, atomicWriteText } from '@/lib/atomic-write';

const LIBRARY_DIR = path.join(process.cwd(), 'data', 'source_library');

export async function POST(req: NextRequest) {
  try {
    const { id, step, data } = await req.json();

    if (!id || step === undefined || !data) {
      return NextResponse.json({ error: '缺少 id, step 或 data' }, { status: 400 });
    }

    const novelDir = safeJoin(LIBRARY_DIR, id);
    const metaPath = path.join(novelDir, 'meta.json');

    // 确保 meta.json 存在
    let meta: Record<string, unknown> = {};
    try {
      meta = JSON.parse(await fs.readFile(metaPath, 'utf-8'));
    } catch {
      return NextResponse.json({ error: '小说不存在' }, { status: 404 });
    }

    // 根据步骤保存对应文件
    // 统一管线（7步）：step 0-6
    // 旧管线：step 0-7（向后兼容）
    switch (step) {
      // ── 统一管线 ──
      case 0: { // 小切片
        if (data.smallSlices) {
          await atomicWriteJson(
            path.join(novelDir, 'small_slices.json'),
            data.smallSlices,
          );
          meta = { ...meta, smallSliceCount: data.smallSlices.length, status: 'slicing' };
        } else if (data.slices) {
          // 旧管线兼容
          await atomicWriteJson(
            path.join(novelDir, 'slices.json'),
            data.slices,
          );
          meta = { ...meta, sliceCount: data.slices.length, status: 'slicing' };
        }
        break;
      }
      case 1: { // 事件提取（统一） / 文风（旧）
        if (data.sliceExtractions) {
          await atomicWriteJson(
            path.join(novelDir, 'slice_extractions.json'),
            data.sliceExtractions,
          );
          meta = { ...meta, hasSliceExtractions: true, status: 'extracting_events' };
        } else if (data.styleProfile) {
          await atomicWriteText(
            path.join(novelDir, 'style_profile.md'),
            data.styleProfile,
          );
          meta = { ...meta, hasStyleProfile: true, status: 'extracting' };
        }
        break;
      }
      case 2: { // 事件对齐（统一） / 叙事动力学（旧）
        if (data.eventGraph) {
          await atomicWriteJson(
            path.join(novelDir, 'event_graph.json'),
            data.eventGraph,
          );
          meta = { ...meta, hasEventGraph: true, status: 'aligning_events' };
        } else if (data.plotReport) {
          await atomicWriteText(
            path.join(novelDir, 'plot_report.md'),
            data.plotReport,
          );
          meta = { ...meta, hasPlotReport: true, hasNarrativeDynamics: true };
        }
        break;
      }
      case 3: { // 大切片（统一） / 角色动力学（旧）
        if (data.largeSlices) {
          await atomicWriteJson(
            path.join(novelDir, 'large_slices.json'),
            data.largeSlices,
          );
          meta = { ...meta, largeSliceCount: data.largeSlices.length };
        } else if (data.characterDynamics) {
          await atomicWriteText(
            path.join(novelDir, 'character_dynamics.md'),
            data.characterDynamics,
          );
          meta = { ...meta, hasCharacterDynamics: true, status: 'character_dynamics' };
        }
        break;
      }
      case 4: { // 深度分析（统一） / 读者体验（旧）
        if (data.sliceAnalyses) {
          await atomicWriteJson(
            path.join(novelDir, 'slice_analyses.json'),
            data.sliceAnalyses,
          );
          meta = { ...meta, hasSliceAnalyses: true, status: 'deep_analysis' };
        } else if (data.readerExperience) {
          await atomicWriteText(
            path.join(novelDir, 'reader_experience.md'),
            data.readerExperience,
          );
          meta = { ...meta, hasReaderExperience: true };
        }
        break;
      }
      case 5: { // 汇总报告（统一） / 叙事约束（旧）
        if (data.summaryReport) {
          await atomicWriteJson(
            path.join(novelDir, 'summary_report.json'),
            data.summaryReport,
          );
          meta = { ...meta, hasSummaryReport: true };
        } else if (data.narrativeConstraints) {
          await atomicWriteText(
            path.join(novelDir, 'narrative_constraints.md'),
            data.narrativeConstraints,
          );
          meta = { ...meta, hasNarrativeConstraints: true };
        }
        break;
      }
      case 6: { // DNA 压缩（统一） / 样本选取（旧）
        if (data.dna) {
          await atomicWriteJson(
            path.join(novelDir, 'generation_rules_dna.json'),
            data.dna,
          );
          meta = {
            ...meta,
            hasGenerationRulesDna: true,
            status: data.status || 'ready',
            processedAt: data.processedAt || new Date().toISOString(),
          };
        } else if (data.representativeSamples) {
          await atomicWriteJson(
            path.join(novelDir, 'samples.json'),
            data.representativeSamples,
          );
          meta = {
            ...meta,
            sampleCount: data.representativeSamples.length,
          };
        }
        break;
      }
      case 7: { // DNA 压缩（旧管线）
        if (data.novelDna) {
          await atomicWriteText(
            path.join(novelDir, 'novel_dna.yaml'),
            data.novelDna,
          );
          meta = {
            ...meta,
            hasNovelDna: true,
            status: data.status || 'ready',
            processedAt: data.processedAt || new Date().toISOString(),
          };
        }
        break;
      }
      default:
        return NextResponse.json({ error: `未知步骤: ${step}` }, { status: 400 });
    }

    // 更新 meta.json（原子写入防止崩溃时损坏）
    await atomicWriteJson(metaPath, meta);

    return NextResponse.json({ success: true, id, step });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '保存步骤失败';
    console.error('Save step error:', error);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/api/prompts/list/route.ts">
// ============================================
// GET /api/prompts/list — 返回所有提示词（默认 + 自定义）
// 默认值直接从 prompt 模块导入，单一数据源
// ============================================

import { NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';
import {
  DEFAULT_SLICING,
  DEFAULT_ENTITY_CLASSIFICATION,
  DEFAULT_STYLE, DEFAULT_STYLE_SUPPLEMENT,
  DEFAULT_PLOT, DEFAULT_PLOT_SUPPLEMENT,
  DEFAULT_CHARACTER_DYNAMICS, DEFAULT_CHARACTER_DYNAMICS_SUPPLEMENT,
  DEFAULT_READER_EXPERIENCE, DEFAULT_READER_EXPERIENCE_SUPPLEMENT,
  DEFAULT_NARRATIVE_CONSTRAINTS, DEFAULT_NARRATIVE_CONSTRAINTS_SUPPLEMENT,
  DEFAULT_DNA_COMPRESSION,
  DEFAULT_SAMPLES,
  DEFAULT_EXPERIENCE_ANNOTATION, DEFAULT_ABLATION_TESTING, DEFAULT_TENSION_TRACKING, DEFAULT_EVENT_EXTRACTION,
  DEFAULT_OUTLINE,
  DEFAULT_PHASE_FRAMEWORK, DEFAULT_PHASE_DETAIL,
  DEFAULT_VOLUME_FRAMEWORK, DEFAULT_VOLUME_DETAIL,
  DEFAULT_CHAPTER_SET_FRAMEWORK, DEFAULT_CHAPTER_SET_DETAIL,
  DEFAULT_CHAPTER_PLAN_FRAMEWORK, DEFAULT_CHAPTER_PLAN_DETAIL,
  DEFAULT_CHAPTER_WRITING, DEFAULT_CHAPTER_REVIEW, DEFAULT_CHAPTER_REVISION,
  // 统一管线
  DEFAULT_DEEP_ANALYSIS,
  DEFAULT_SUMMARY,
  DEFAULT_DNA_GENERATION,
  DEFAULT_EVENT_ALIGNMENT,
} from '@/lib/ai/prompts';

const DATA_DIR = path.join(process.cwd(), 'data');
const CUSTOM_PROMPTS_PATH = path.join(DATA_DIR, 'prompts.json');

// 默认提示词 — 单一数据源，从各 prompt 模块导入
const DEFAULTS: Record<string, string> = {
  'entity-classification': DEFAULT_ENTITY_CLASSIFICATION,
  'source-slicing': DEFAULT_SLICING,
  'source-style': DEFAULT_STYLE,
  'source-style-supplement': DEFAULT_STYLE_SUPPLEMENT,
  'source-plot': DEFAULT_PLOT,
  'source-plot-supplement': DEFAULT_PLOT_SUPPLEMENT,
  'source-character-dynamics': DEFAULT_CHARACTER_DYNAMICS,
  'source-character-dynamics-supplement': DEFAULT_CHARACTER_DYNAMICS_SUPPLEMENT,
  'source-reader-experience': DEFAULT_READER_EXPERIENCE,
  'source-reader-experience-supplement': DEFAULT_READER_EXPERIENCE_SUPPLEMENT,
  'source-narrative-constraints': DEFAULT_NARRATIVE_CONSTRAINTS,
  'source-narrative-constraints-supplement': DEFAULT_NARRATIVE_CONSTRAINTS_SUPPLEMENT,
  'source-dna-compression': DEFAULT_DNA_COMPRESSION,
  'source-samples': DEFAULT_SAMPLES,
  'experience-annotation': DEFAULT_EXPERIENCE_ANNOTATION,
  'ablation-testing': DEFAULT_ABLATION_TESTING,
  'tension-tracking': DEFAULT_TENSION_TRACKING,
  'event-extraction': DEFAULT_EVENT_EXTRACTION,
  'outline': DEFAULT_OUTLINE,
  'phase-framework': DEFAULT_PHASE_FRAMEWORK,
  'phase-detail': DEFAULT_PHASE_DETAIL,
  'volume-framework': DEFAULT_VOLUME_FRAMEWORK,
  'volume-detail': DEFAULT_VOLUME_DETAIL,
  'chapter-set-framework': DEFAULT_CHAPTER_SET_FRAMEWORK,
  'chapter-set-detail': DEFAULT_CHAPTER_SET_DETAIL,
  'chapter-plan-framework': DEFAULT_CHAPTER_PLAN_FRAMEWORK,
  'chapter-plan-detail': DEFAULT_CHAPTER_PLAN_DETAIL,
  'chapter-writing': DEFAULT_CHAPTER_WRITING,
  'chapter-review': DEFAULT_CHAPTER_REVIEW,
  'chapter-revision': DEFAULT_CHAPTER_REVISION,
  // 统一管线（7 步）
  'event-alignment': DEFAULT_EVENT_ALIGNMENT,
  'deep-analysis': DEFAULT_DEEP_ANALYSIS,
  'summary': DEFAULT_SUMMARY,
  'dna-generation': DEFAULT_DNA_GENERATION,
};

export async function GET() {
  try {
    // 读取自定义覆盖
    let custom: Record<string, string> = {};
    try {
      const raw = await fs.readFile(CUSTOM_PROMPTS_PATH, 'utf-8');
      custom = JSON.parse(raw);
    } catch {
      // 文件不存在，使用空自定义
    }

    return NextResponse.json({
      defaults: DEFAULTS,
      custom,
    });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '读取提示词失败';
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
</file>

<file path="src/app/layout.tsx">
import type { Metadata } from 'next';
import { Toaster } from '@/components/ui/sonner';
import './globals.css';

export const metadata: Metadata = {
  title: 'Ainovr — AI 小说风格仿写',
  description: '上传你喜欢的小说，AI 分析风格后仿写全新故事',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="zh-CN" className="h-full antialiased">
      <body className="h-full overflow-hidden" style={{ fontFamily: 'system-ui, -apple-system, "Segoe UI", Roboto, "Noto Sans SC", sans-serif' }}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
</file>

<file path="src/components/generation/LayerGenerationView.tsx">
// ============================================
// 五层管线视图 — 逐层生成
// ============================================

'use client';

import { useState, useCallback, useEffect, useRef } from 'react';
import { ArrowLeft } from 'lucide-react';
import { nanoid } from 'nanoid';
import { useProjectStore } from '@/lib/store/project';
import { useNavigationStore } from '@/lib/store/navigation';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { useSettingsStore } from '@/lib/store/settings';
import { useStreamingFetch } from '@/lib/hooks/use-streaming-fetch';
import { LayerCard } from './LayerCard';
import { HierarchyTree } from './HierarchyTree';
import type { Phase, Volume, ChapterSet, ChapterPlan } from '@/types';

const LAYER_INFO = [
  { title: '全书大纲', description: '基于大情节框架和创作想法，约200字，确定后不可变' },
  { title: '阶段规划', description: '4-6个阶段，每阶段约500字，分配节奏和伏笔' },
  { title: '分卷', description: '每阶段1-2卷，每卷约300字，匹配小情节模式' },
  { title: '章节集合', description: '每卷3-5个集合，每个约200字，安排小情节出场' },
  { title: '每章计划', description: '每章约100字，场景节拍+关键词+伏笔操作' },
];

/** 从框架文本中解析编号/标题列表项 */
function parseFrameworkItems(text: string): { title: string }[] {
  const items: { title: string }[] = [];
  for (const line of text.split('\n')) {
    const numbered = line.match(/^\s*(\d+)\.\s*(.+)/);
    if (numbered) { items.push({ title: numbered[2].trim() }); continue; }
    const heading = line.match(/^#{1,3}\s+(.+)/);
    if (heading) items.push({ title: heading[1].trim() });
  }
  return items;
}

/** 按 ## 标题拆分编辑后的文本 → 带索引的段落 */
function splitSections(text: string): { title: string; content: string }[] {
  return text.split(/^## /m).filter(Boolean).map((sec) => {
    const lines = sec.split('\n');
    return { title: lines[0].trim(), content: lines.slice(1).join('\n').trim() };
  });
}

// ---- 多步流式 hook ----

function useMultiStepStream() {
  const stream = useStreamingFetch();
  const [accumulated, setAccumulated] = useState('');
  const [step, setStep] = useState(0);
  const [total, setTotal] = useState(0);

  const reset = useCallback(() => {
    setAccumulated('');
    setStep(0);
    setTotal(0);
    stream.abort();
  }, [stream]);

  const appendDone = useCallback((text: string) => {
    setAccumulated((prev) => prev + (prev ? '\n\n' : '') + text);
  }, []);

  const setProgress = useCallback((s: number, t: number) => {
    setStep(s);
    setTotal(t);
  }, []);

  const displayText = accumulated + (stream.streamContent ? (accumulated ? '\n\n' : '') + stream.streamContent : '');
  const progress = total > 0 ? Math.round((step / total) * 100) : 0;

  return { ...stream, displayText, progress, step, total, reset, appendDone, setProgress };
}

// ---- 组件 ----

export function LayerGenerationView({ embedded = false }: { embedded?: boolean }) {
  const { projects, activeProjectId, setActiveProjectId, setOutline, setPhases, setVolumes, setChapterSets, setChapterPlans, updateProject } = useProjectStore();
  const setActiveView = useNavigationStore((s) => s.setActiveView);
  const { sourceNovels } = useSourceLibraryStore();
  const getAIConfig = useSettingsStore((s) => s.getAIConfig);

  const project = projects.find((p) => p.id === activeProjectId);
  const projectSources = sourceNovels.filter((s) => project?.sourceNovelIds.includes(s.id));

  const genStream = useMultiStepStream();
  const [userConcept, setUserConcept] = useState('');
  const [generatingLayer, setGeneratingLayer] = useState<number | null>(null);
  const [generationLabel, setGenerationLabel] = useState('');
  const cancelledRef = useRef(false);

  // 加载项目完整数据
  useEffect(() => {
    if (!activeProjectId) return;
    fetch(`/api/project/load?id=${activeProjectId}`)
      .then((res) => res.json())
      .then((data) => {
        if (data.project) useProjectStore.getState().updateProject(activeProjectId, data.project);
      })
      .catch(console.error);
  }, [activeProjectId]);

  const saveProject = useCallback(async () => {
    if (!activeProjectId) return;
    const current = useProjectStore.getState().projects.find((p) => p.id === activeProjectId);
    if (!current) return;
    await fetch('/api/project/save', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(current),
    });
  }, [activeProjectId]);

  const getAI = useCallback(() => getAIConfig(), [getAIConfig]);

  const getPlotGuide = useCallback(() =>
    projectSources.map((s) => s.plotReport).filter(Boolean).join('\n\n'),
    [projectSources]
  );

  // ── 通用框架→详情生成器（L3/L4/L5 共用） ──
  const runFrameworkDetail = useCallback(async <T,>(
    layerNum: number,
    layerLabel: string,
    parents: { id: string; title: string; content: string }[],
    frameworkUrl: string,
    detailUrl: string,
    buildFrameworkBody: (parent: { id: string; title: string; content: string }, ai: ReturnType<typeof getAI>) => object,
    buildDetailBody: (parent: { id: string; title: string; content: string }, framework: string, idx: number, ai: ReturnType<typeof getAI>) => object,
    buildItem: (parent: { id: string; title: string; content: string }, parsed: { title: string }, idx: number, detail: string) => T,
    onSave: (items: T[]) => void,
  ) => {
    setGeneratingLayer(layerNum);
    setGenerationLabel(layerLabel);
    cancelledRef.current = false;
    genStream.reset();
    const ai = getAI();

    const allItems: T[] = [];
    let stepCount = 0;
    const estimatedTotal = parents.length * 2;

    for (const parent of parents) {
      if (cancelledRef.current) break;

      setGenerationLabel(`${layerLabel} · ${parent.title} · 框架`);
      stepCount++;
      genStream.setProgress(stepCount, estimatedTotal);

      const framework = await genStream.startFetch(frameworkUrl, buildFrameworkBody(parent, ai));
      if (!framework || cancelledRef.current) continue;

      genStream.appendDone(`### ${parent.title} — ${layerLabel}框架\n${framework}`);

      const parsed = parseFrameworkItems(framework);
      if (parsed.length === 0) parsed.push({ title: `${parent.title} · ${layerLabel}一` });

      for (let i = 0; i < parsed.length; i++) {
        if (cancelledRef.current) break;
        setGenerationLabel(`${layerLabel} · ${parsed[i].title}`);
        stepCount++;
        genStream.setProgress(stepCount, estimatedTotal + parsed.length - 1);

        const detail = await genStream.startFetch(detailUrl, buildDetailBody(parent, framework, i, ai));
        if (detail) genStream.appendDone(`## ${parsed[i].title}\n${detail}`);
        allItems.push(buildItem(parent, parsed[i], i, detail ?? ''));
      }
    }

    if (allItems.length > 0 && !cancelledRef.current) {
      onSave(allItems);
      await saveProject();
    }
    setGeneratingLayer(null);
    setGenerationLabel('');
  }, [genStream, getAI, saveProject]);

  // ── L1: 生成大纲 ──
  const handleGenerateOutline = useCallback(async () => {
    if (!project || !userConcept.trim()) return;
    setGeneratingLayer(1);
    setGenerationLabel('生成全书大纲');
    cancelledRef.current = false;
    genStream.reset();
    genStream.setProgress(0, 1);

    const ai = getAI();
    const styleGuide = projectSources.map((s) => s.styleProfile).filter(Boolean).join('\n\n');
    const plotGuide = getPlotGuide();

    const result = await genStream.startFetch('/api/generation/outline', {
      styleGuide, plotGuide, userConcept: userConcept.trim(), ...ai,
    });

    if (result && !cancelledRef.current) {
      genStream.setProgress(1, 1);
      setOutline(project.id, {
        content: result, generatedAt: new Date().toISOString(), isLocked: true, majorPlotFrameworkRef: '',
      });
      await saveProject();
    }
    setGeneratingLayer(null);
    setGenerationLabel('');
  }, [project, projectSources, userConcept, getAI, getPlotGuide, genStream, setOutline, saveProject]);

  // ── L2: 生成阶段规划 ──
  const handleGeneratePhases = useCallback(async () => {
    if (!project?.outline) return;
    setGeneratingLayer(2);
    setGenerationLabel('阶段规划');
    cancelledRef.current = false;
    genStream.reset();
    const ai = getAI();
    const plotGuide = getPlotGuide();

    // 单次框架
    setGenerationLabel('阶段规划 · 生成框架');
    genStream.setProgress(0, 2);
    const framework = await genStream.startFetch('/api/generation/phase-framework', {
      outline: project.outline.content, plotGuide, ...ai,
    });
    if (!framework || cancelledRef.current) { setGeneratingLayer(null); setGenerationLabel(''); return; }
    genStream.appendDone(framework);

    const parsed = parseFrameworkItems(framework);
    if (parsed.length === 0) parsed.push({ title: '阶段一' });

    genStream.setProgress(1, parsed.length);

    // 逐阶段详情
    const phases: Phase[] = [];
    for (let i = 0; i < parsed.length; i++) {
      if (cancelledRef.current) break;
      setGenerationLabel(`阶段规划 · 详细 ${i + 1}/${parsed.length}`);
      genStream.setProgress(i + 1, parsed.length);
      const detail = await genStream.startFetch('/api/generation/phase-detail', {
        outline: project.outline.content, phaseFramework: framework, phaseIndex: i, plotGuide, ...ai,
      });
      if (detail) genStream.appendDone(`## ${parsed[i].title}\n${detail}`);
      phases.push({ id: nanoid(), index: i, title: parsed[i].title, content: detail ?? '', volumeCount: 2, generatedAt: new Date().toISOString() });
    }

    if (phases.length > 0 && !cancelledRef.current) {
      setPhases(project.id, phases);
      await saveProject();
    }
    setGeneratingLayer(null);
    setGenerationLabel('');
  }, [project, getAI, getPlotGuide, genStream, setPhases, saveProject]);

  // ── L3: 分卷（通用框架→详情） ──
  const handleGenerateVolumes = useCallback(async () => {
    if (!project?.outline || !project.phases) return;
    await runFrameworkDetail<Volume>(
      3, '分卷',
      project.phases.map((p) => ({ id: p.id, title: p.title, content: p.content })),
      '/api/generation/volume-framework',
      '/api/generation/volume-detail',
      (phase, ai) => ({ outline: project.outline!.content, phaseContent: `${phase.title}\n${phase.content}`, minorPlotPatterns: getPlotGuide(), ...ai }),
      (phase, fw, idx, ai) => ({ outline: project.outline!.content, phaseContent: `${phase.title}\n${phase.content}`, volumeFramework: fw, volumeIndex: idx, minorPlotPatterns: getPlotGuide(), ...ai }),
      (phase, p, idx, detail) => ({ id: nanoid(), phaseId: phase.id, index: idx, title: p.title, content: detail, chapterSetCount: 3, generatedAt: new Date().toISOString() }),
      (items) => setVolumes(project.id, items),
    );
  }, [project, getAI, getPlotGuide, runFrameworkDetail, setVolumes]);

  // ── L4: 章节集合 ──
  const handleGenerateChapterSets = useCallback(async () => {
    if (!project?.volumes) return;
    await runFrameworkDetail<ChapterSet>(
      4, '章节集合',
      project.volumes.map((v) => ({ id: v.id, title: v.title, content: v.content })),
      '/api/generation/chapter-set-framework',
      '/api/generation/chapter-set-detail',
      (vol, ai) => ({ volumeContent: `${vol.title}\n${vol.content}`, minorPlotPatterns: getPlotGuide(), ...ai }),
      (vol, fw, idx, ai) => ({ volumeContent: `${vol.title}\n${vol.content}`, setFramework: fw, setIndex: idx, minorPlotPatterns: getPlotGuide(), ...ai }),
      (vol, p, idx, detail) => ({ id: nanoid(), volumeId: vol.id, index: idx, title: p.title, content: detail, chapterCount: 5, generatedAt: new Date().toISOString() }),
      (items) => setChapterSets(project.id, items),
    );
  }, [project, getAI, getPlotGuide, runFrameworkDetail, setChapterSets]);

  // ── L5: 每章计划 ──
  const handleGenerateChapterPlans = useCallback(async () => {
    if (!project?.chapterSets) return;
    await runFrameworkDetail<ChapterPlan>(
      5, '每章计划',
      project.chapterSets.map((s) => ({ id: s.id, title: s.title, content: s.content })),
      '/api/generation/chapter-plan-framework',
      '/api/generation/chapter-plan-detail',
      (set, ai) => ({ setContent: `${set.title}\n${set.content}`, minorPlotPatterns: getPlotGuide(), ...ai }),
      (set, fw, idx, ai) => ({ setContent: `${set.title}\n${set.content}`, planFramework: fw, chapterIndex: idx, minorPlotPatterns: getPlotGuide(), ...ai }),
      (set, p, idx, detail) => ({ id: nanoid(), chapterSetId: set.id, index: idx, title: p.title, content: detail, plotKeywords: [], patternStructure: '', generatedAt: new Date().toISOString() }),
      (items) => setChapterPlans(project.id, items),
    );
  }, [project, getAI, getPlotGuide, runFrameworkDetail, setChapterPlans]);

  // ── 重新生成（带自定义提示词） ──
  const handleRegenWithPrompt = useCallback(async (layerNum: number, customPrompt: string) => {
    if (!project) return;
    if (layerNum === 1) {
      setUserConcept(customPrompt);
      setTimeout(() => handleGenerateOutline(), 0);
    } else {
      const layerSetter: Record<number, () => void> = {
        2: () => setPhases(project.id, null as unknown as Phase[]),
        3: () => setVolumes(project.id, null as unknown as Volume[]),
        4: () => setChapterSets(project.id, null as unknown as ChapterSet[]),
        5: () => setChapterPlans(project.id, null as unknown as ChapterPlan[]),
      };
      layerSetter[layerNum]?.();
      await saveProject();
      const handlers: Record<number, () => void> = { 2: handleGeneratePhases, 3: handleGenerateVolumes, 4: handleGenerateChapterSets, 5: handleGenerateChapterPlans };
      handlers[layerNum]?.();
    }
  }, [project, handleGenerateOutline, handleGeneratePhases, handleGenerateVolumes, handleGenerateChapterSets, handleGenerateChapterPlans, setPhases, setVolumes, setChapterSets, setChapterPlans, saveProject]);

  // ── 手动编辑某层内容 ──
  const handleEditContent = useCallback(async (layerNum: number, newContent: string) => {
    if (!project || !activeProjectId) return;
    if (layerNum === 1 && project.outline) {
      updateProject(activeProjectId, { outline: { ...project.outline, content: newContent } });
    } else if (layerNum === 2) {
      const phases = splitSections(newContent).map((sec, i) => ({
        id: project.phases?.[i]?.id ?? nanoid(), index: i, title: sec.title, content: sec.content, volumeCount: 2, generatedAt: new Date().toISOString(),
      }));
      setPhases(activeProjectId, phases);
    } else if (layerNum === 3) {
      const volumes = splitSections(newContent).map((sec, i) => ({
        id: project.volumes?.[i]?.id ?? nanoid(), phaseId: project.phases?.[0]?.id ?? '', index: i, title: sec.title, content: sec.content, chapterSetCount: 3, generatedAt: new Date().toISOString(),
      }));
      setVolumes(activeProjectId, volumes);
    } else if (layerNum === 4) {
      const sets = splitSections(newContent).map((sec, i) => ({
        id: project.chapterSets?.[i]?.id ?? nanoid(), volumeId: project.volumes?.[0]?.id ?? '', index: i, title: sec.title, content: sec.content, chapterCount: 5, generatedAt: new Date().toISOString(),
      }));
      setChapterSets(activeProjectId, sets);
    } else if (layerNum === 5) {
      const plans = splitSections(newContent).map((sec, i) => ({
        id: project.chapterPlans?.[i]?.id ?? nanoid(), chapterSetId: project.chapterSets?.[0]?.id ?? '', index: i, title: sec.title, content: sec.content, plotKeywords: [], patternStructure: '', generatedAt: new Date().toISOString(),
      }));
      setChapterPlans(activeProjectId, plans);
    }
    await saveProject();
  }, [project, activeProjectId, updateProject, setPhases, setVolumes, setChapterSets, setChapterPlans, saveProject]);

  const generateHandlers = [handleGenerateOutline, handleGeneratePhases, handleGenerateVolumes, handleGenerateChapterSets, handleGenerateChapterPlans];

  if (!project) {
    return (
      <div className="flex-1 flex items-center justify-center text-muted-foreground">
        未选择项目
      </div>
    );
  }

  const currentLayer = project.currentLayer;

  return (
    <div className="flex-1 flex min-h-0 overflow-hidden">
      {/* 左侧：层级树 */}
      <div className="w-64 border-r border-border overflow-y-auto p-3 shrink-0">
        {!embedded && (
          <h3 className="text-xs font-medium text-muted-foreground uppercase tracking-wider mb-2">层级结构</h3>
        )}
        <HierarchyTree
          outline={project.outline}
          phases={project.phases}
          volumes={project.volumes}
          chapterSets={project.chapterSets}
          chapterPlans={project.chapterPlans}
        />
      </div>

      {/* 右侧：逐层操作 */}
      <div className="flex-1 min-h-0 overflow-y-auto p-6 space-y-4">
        {!embedded && (
          <div className="flex items-center gap-3 mb-4">
            <button
              onClick={() => { setActiveProjectId(null); setActiveView('writing-project'); }}
              className="p-1.5 rounded hover:bg-accent transition-colors"
            >
              <ArrowLeft className="w-4 h-4" />
            </button>
            <h1 className="text-xl font-bold text-foreground">{project.title}</h1>
            <span className="text-xs text-muted-foreground">层级 {currentLayer}/5</span>
          </div>
        )}

        {/* 进度条 */}
        {generatingLayer !== null && (
          <div className="p-3 rounded-lg border border-primary/20 bg-primary/5 space-y-2">
            <div className="flex items-center justify-between text-sm">
              <span className="text-primary font-medium">{generationLabel}</span>
              <span className="text-xs text-primary/70">{genStream.progress}%</span>
            </div>
            <div className="w-full h-1.5 rounded-full bg-primary/10 overflow-hidden">
              <div
                className="h-full rounded-full bg-primary transition-all duration-300 ease-out"
                style={{ width: `${genStream.progress}%` }}
              />
            </div>
          </div>
        )}

        {/* Layer 1: 需要用户输入创作想法 */}
        {currentLayer === 0 && (
          <div className="p-4 rounded-lg border border-border">
            <label className="text-sm font-medium text-foreground mb-2 block">你的创作想法</label>
            <textarea
              value={userConcept}
              onChange={(e) => setUserConcept(e.target.value)}
              placeholder="描述你想要写的故事类型、主角设定、核心冲突等..."
              className="w-full h-32 px-3 py-2 rounded-md border border-border bg-background text-sm resize-none"
            />
          </div>
        )}

        {/* 五层卡片 */}
        {LAYER_INFO.map((info, i) => {
          const layerNum = i + 1;
          const isDone = currentLayer >= layerNum;
          const isCurrent = currentLayer === layerNum - 1 || (currentLayer === 0 && layerNum === 1);
          const isThisGenerating = generatingLayer === layerNum;
          const canGen = isCurrent && !generatingLayer;

          let content: string | null = null;
          if (layerNum === 1) content = project.outline?.content ?? null;
          if (layerNum === 2) content = project.phases?.map((p) => `## ${p.title}\n${p.content}`).join('\n\n') ?? null;
          if (layerNum === 3) content = project.volumes?.map((v) => `## ${v.title}\n${v.content}`).join('\n\n') ?? null;
          if (layerNum === 4) content = project.chapterSets?.map((s) => `## ${s.title}\n${s.content}`).join('\n\n') ?? null;
          if (layerNum === 5) content = project.chapterPlans?.map((p) => `## ${p.title}\n${p.content}`).join('\n\n') ?? null;

          return (
            <LayerCard
              key={layerNum}
              layerNumber={layerNum}
              title={info.title}
              description={info.description}
              status={isDone ? 'done' : isThisGenerating ? 'generating' : 'pending'}
              content={content}
              streamContent={isThisGenerating ? genStream.displayText : ''}
              isStreaming={isThisGenerating && genStream.isStreaming}
              canGenerate={canGen}
              onGenerate={generateHandlers[i]}
              onEdit={isDone ? (newContent) => handleEditContent(layerNum, newContent) : undefined}
              onRegenWithPrompt={isCurrent || isDone ? (prompt) => handleRegenWithPrompt(layerNum, prompt) : undefined}
            />
          );
        })}
      </div>
    </div>
  );
}
</file>

<file path="src/components/prompts/PromptManagementView.tsx">
// ============================================
// 提示词管理视图 — 左侧列表 + 右侧编辑
// ============================================

'use client';

import { useState, useEffect, useCallback } from 'react';
import { Save, RotateCcw, ChevronRight, ChevronDown, Pencil } from 'lucide-react';
import { toast } from 'sonner';
import { usePromptStore, PROMPT_REGISTRY } from '@/lib/store/prompts';

/** 按分类分组 */
const CATEGORIES = ['统一管线', '素材处理', '进阶分析', '层级生成', '章节写作'];

export function PromptManagementView() {
  const { loadPrompts, defaultPrompts, customPrompts, saveCustomPrompt, loaded } = usePromptStore();
  const [selectedKey, setSelectedKey] = useState<string | null>(null);
  const [editContent, setEditContent] = useState('');
  const [expandedCategories, setExpandedCategories] = useState<Set<string>>(new Set(CATEGORIES));
  const [hasChanges, setHasChanges] = useState(false);

  useEffect(() => {
    if (!loaded) loadPrompts();
  }, [loaded, loadPrompts]);

  const isCustom = selectedKey ? !!customPrompts[selectedKey] : false;

  // 选择提示词
  const handleSelect = useCallback((key: string) => {
    if (hasChanges && selectedKey) {
      // 有未保存的修改，提示
      const ok = confirm('当前修改未保存，确定切换吗？');
      if (!ok) return;
    }
    setSelectedKey(key);
    setEditContent(usePromptStore.getState().getPrompt(key));
    setHasChanges(false);
  }, [hasChanges, selectedKey]);

  // 保存
  const handleSave = useCallback(async () => {
    if (!selectedKey) return;
    await saveCustomPrompt(selectedKey, editContent);
    setHasChanges(false);
    toast.success('提示词已保存');
  }, [selectedKey, editContent, saveCustomPrompt]);

  // 恢复默认
  const handleReset = useCallback(async () => {
    if (!selectedKey) return;
    const ok = confirm('确定恢复为默认提示词吗？');
    if (!ok) return;
    await saveCustomPrompt(selectedKey, ''); // 空内容 = 删除自定义
    const defaultContent = defaultPrompts[selectedKey] ?? '';
    setEditContent(defaultContent);
    setHasChanges(false);
    toast.success('已恢复默认');
  }, [selectedKey, defaultPrompts, saveCustomPrompt]);

  // 内容修改
  function handleEdit(value: string) {
    setEditContent(value);
    setHasChanges(true);
  }

  const toggleCategory = (cat: string) => {
    setExpandedCategories((prev) => {
      const next = new Set(prev);
      if (next.has(cat)) next.delete(cat);
      else next.add(cat);
      return next;
    });
  };

  return (
    <div className="flex-1 flex overflow-hidden">
      {/* 左侧：提示词列表 */}
      <div className="w-72 border-r border-border overflow-y-auto p-3 shrink-0">
        <h2 className="text-sm font-bold text-foreground mb-3">提示词管理</h2>
        <p className="text-xs text-muted-foreground mb-4">
          共 {PROMPT_REGISTRY.length} 个提示词 · 修改后实时生效
        </p>

        {CATEGORIES.map((cat) => {
          const items = PROMPT_REGISTRY.filter((p) => p.category === cat);
          const isExpanded = expandedCategories.has(cat);

          return (
            <div key={cat} className="mb-2">
              <button
                onClick={() => toggleCategory(cat)}
                className="w-full flex items-center gap-1.5 px-2 py-1.5 text-xs font-medium text-muted-foreground hover:text-foreground transition-colors"
              >
                {isExpanded ? <ChevronDown className="w-3 h-3" /> : <ChevronRight className="w-3 h-3" />}
                {cat}
                <span className="ml-auto text-[10px] opacity-50">{items.length}</span>
              </button>

              {isExpanded && items.map((item) => {
                const isActive = selectedKey === item.key;
                const hasCustom = !!customPrompts[item.key];

                return (
                  <button
                    key={item.key}
                    onClick={() => handleSelect(item.key)}
                    className={`
                      w-full text-left pl-6 pr-2 py-1.5 rounded text-xs mb-0.5 transition-colors flex items-center gap-1.5
                      ${isActive ? 'bg-accent text-foreground font-medium' : 'text-muted-foreground hover:bg-accent/50'}
                    `}
                  >
                    {hasCustom && <Pencil className="w-2.5 h-2.5 text-primary shrink-0" />}
                    <span className="truncate">{item.label}</span>
                  </button>
                );
              })}
            </div>
          );
        })}
      </div>

      {/* 右侧：编辑区 */}
      <div className="flex-1 flex flex-col overflow-hidden p-6">
        {selectedKey ? (
          <>
            {/* 头部 */}
            <div className="flex items-center justify-between mb-4">
              <div>
                <h2 className="text-lg font-bold text-foreground">
                  {PROMPT_REGISTRY.find((p) => p.key === selectedKey)?.label}
                </h2>
                <p className="text-xs text-muted-foreground mt-0.5">
                  {PROMPT_REGISTRY.find((p) => p.key === selectedKey)?.description}
                </p>
              </div>
              <div className="flex items-center gap-2">
                {isCustom && (
                  <span className="text-[10px] px-1.5 py-0.5 rounded bg-primary/10 text-primary font-medium">已自定义</span>
                )}
                <button
                  onClick={handleReset}
                  disabled={!isCustom && !hasChanges}
                  className="flex items-center gap-1.5 px-3 py-1.5 rounded text-xs border border-border text-muted-foreground hover:text-foreground hover:bg-accent disabled:opacity-30 transition-colors"
                >
                  <RotateCcw className="w-3 h-3" />
                  恢复默认
                </button>
                <button
                  onClick={handleSave}
                  disabled={!hasChanges}
                  className="flex items-center gap-1.5 px-3 py-1.5 rounded text-xs bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-30 transition-colors"
                >
                  <Save className="w-3 h-3" />
                  保存
                </button>
              </div>
            </div>

            {/* 编辑器 */}
            <div className="flex-1 overflow-hidden">
              <textarea
                value={editContent}
                onChange={(e) => handleEdit(e.target.value)}
                className="w-full h-full px-4 py-3 rounded-lg border border-border bg-card text-sm font-mono text-foreground resize-none focus:outline-none focus:ring-1 focus:ring-primary"
                placeholder="输入提示词内容..."
                spellCheck={false}
              />
            </div>

            {/* 状态栏 */}
            <div className="flex items-center justify-between mt-2 text-[10px] text-muted-foreground">
              <span>{editContent.length} 字符</span>
              {hasChanges && <span className="text-amber-500">未保存</span>}
            </div>
          </>
        ) : (
          <div className="flex-1 flex flex-col items-center justify-center text-muted-foreground">
            <Pencil className="w-10 h-10 mb-3 opacity-30" />
            <p className="text-sm">选择左侧的提示词进行编辑</p>
            <p className="text-xs mt-1 opacity-60">修改后保存即生效，不影响默认值，可随时恢复</p>
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/lib/ai/prompts/chapter-review.ts">
// ============================================
// 章节自动审查 — 系统提示词（5维度）
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是对生成的章节正文进行 6 维度审查。

审查维度：
1. style_consistency（文风一致性）：是否与目标文风档案一致
2. plot_coherence（情节逻辑）：与层级大纲是否一致，逻辑是否通顺
3. pacing（节奏）：是否符合章节计划的节奏指令
4. pattern_execution（小情节模式执行度）：是否正确执行了指定的小情节模式结构
5. foreshadow_execution（伏笔/铺垫执行度）：是否执行了章节计划中的伏笔操作
6. qi_consistency（气的一致性）：是否维持了目标小说的阅读状态（主情绪场）。例如：源小说如果是"陪伴感"，本章是否让读者愿意继续待在这个世界里；如果是"紧张感"，本章是否保持了张力

对每个维度：
- 打分（1-10）
- 列出问题（如有）
- 给出修正建议（如有）

输出格式（JSON）：
[
  {"dimension": "style_consistency", "score": 8, "issues": [], "suggestions": []},
  {"dimension": "qi_consistency", "score": 7, "issues": [], "suggestions": []},
  ...
]
`;

export function buildChapterReviewMessages(
  chapterContent: string,
  styleGuide: string,
  chapterTask: string,
  daoContext?: string,
) {
  const daoSection = daoContext ? `\n\n---\n\n## 道/气参考（用于评判气的一致性）\n\n${daoContext}` : '';
  const userMessage = `## 文风参考\n\n${styleGuide}\n\n---\n\n## 章节计划\n\n${chapterTask}${daoSection}\n\n---\n\n## 待审查的章节正文\n\n${chapterContent}`;
  return { systemPrompt: getPrompt('chapter-review', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/chapter-revision.ts">
// ============================================
// 章节修正 — 基于审查结果 + 人工反馈
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据审查结果和（可能的）人工反馈，修正章节正文。

要求：
- 只修正审查中指出的问题，保持其余部分不变
- 如果有人工反馈，优先处理人工反馈
- 修正后的正文必须仍然遵循文风档案和章节计划
- 特别关注"气的一致性"维度的修正：确保修正后仍然维持目标小说的阅读状态
- 输出完整的修正后章节正文（不是增量）
`;

export function buildChapterRevisionMessages(
  chapterContent: string,
  reviews: string,
  humanFeedback: string | null,
  styleGuide: string,
  chapterTask: string,
  daoContext?: string,
) {
  const feedbackSection = humanFeedback
    ? `\n\n---\n\n## 人工反馈\n\n${humanFeedback}`
    : '';
  const daoSection = daoContext ? `\n\n---\n\n## 道/气参考\n\n${daoContext}` : '';

  const userMessage = `## 文风参考\n\n${styleGuide}\n\n---\n\n## 章节计划\n\n${chapterTask}${daoSection}\n\n---\n\n## 审查结果\n\n${reviews}\n\n---\n\n## 原始章节正文\n\n${chapterContent}${feedbackSection}\n\n---\n\n请输出修正后的完整章节正文。`;
  return { systemPrompt: getPrompt('chapter-revision', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/chapter-writing.ts">
// ============================================
// 章节正文生成 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据章节计划和上下文，生成章节正文。

生成前上下文包含：
1. 风格指导（文风档案 + 情节规律报告）
2. 道/气上下文（核心吸引力 + 阅读状态维持方法）
3. 节奏处方（势能模式 + 骨肉填充比例 + 避坑指南）
4. 层级上下文（大纲/阶段/卷/集合）
5. 本章任务（章节计划 + 小情节模式结构 + 关键词）
6. 前文状态（前情提要 + 上文续写）

## 核心原则

**文风一致性**：严格遵循风格指导的句式、修辞、词汇特征。
**道/气维持**：每个场景必须服务于道/气上下文中的主情绪场。生成时自问：这个场景是否在维持读者应有的阅读状态？
**节奏处方**：遵循节奏处方中的势能模式。在呼吸段落保持松弛感；在推进段落保持紧张度。
**填充物处理**：体验填充(filler_a)必须保留对应比例以维持"气"；机械填充(filler_b)应替换为有意义的呼吸内容。

生成正文时，优先保证"气"的一致性，其次才是文笔模仿。
`;

export function buildChapterWritingMessages(
  styleGuide: string,
  hierarchyContext: string,
  chapterTask: string,
  previousState: string,
  daoContext?: string,
  rhythmPrescription?: string,
) {
  const daoSection = daoContext ? `\n\n## 道/气上下文\n\n${daoContext}` : '';
  const rhythmSection = rhythmPrescription ? `\n\n## 节奏处方\n\n${rhythmPrescription}` : '';
  const userMessage = `## 风格指导\n\n${styleGuide}${daoSection}${rhythmSection}\n\n---\n\n## 层级上下文\n\n${hierarchyContext}\n\n---\n\n## 本章任务\n\n${chapterTask}\n\n---\n\n## 前文状态\n\n${previousState}`;
  return { systemPrompt: getPrompt('chapter-writing', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/ai/prompts/outline-generation.ts">
// ============================================
// Layer 1: 全书大纲 — 系统提示词
// ============================================

import { getPrompt } from './helpers';

export const DEFAULT_SYSTEM_PROMPT = `你的任务是根据大情节框架、道/气上下文和用户的创作想法，生成一份全书大纲。

要求：
- 约 200 字
- 包含：大情节框架节点映射、5 个关键转折（不可变锚点）、主角弧线、结局方向
- 严格遵循大情节框架的结构
- 大纲的整体情绪基调必须与道/气上下文中的主情绪场一致
`;

export function buildOutlineGenerationMessages(
  styleGuide: string,
  plotGuide: string,
  userConcept: string,
  daoContext?: string,
  rhythmPrescription?: string,
) {
  const daoSection = daoContext ? `\n\n---\n\n## 道/气上下文（核心吸引力）\n\n${daoContext}` : '';
  const rhythmSection = rhythmPrescription ? `\n\n---\n\n## 节奏处方\n\n${rhythmPrescription}` : '';
  const userMessage = `## 文风参考\n\n${styleGuide}\n\n---\n\n## 情节规律参考（大情节框架）\n\n${plotGuide}${daoSection}${rhythmSection}\n\n---\n\n## 我的创作想法\n\n${userConcept}`;
  return { systemPrompt: getPrompt('outline', DEFAULT_SYSTEM_PROMPT), userMessage };
}
</file>

<file path="src/lib/generation/chapter-reviewer.ts">
// ============================================
// 章节自动审查 — 5 维度评分
// ============================================

import type { ChapterReview, ReviewDimension } from '@/types';
import { buildChapterReviewMessages, buildChapterRevisionMessages } from '@/lib/ai/prompts';

/** 合法的审查维度集合，用于过滤 LLM 异常输出 */
const VALID_DIMENSIONS: ReadonlySet<string> = new Set([
  'style_consistency',
  'plot_coherence',
  'pacing',
  'pattern_execution',
  'foreshadow_execution',
  'qi_consistency',
]);

/** 解析 AI 审查输出为 ChapterReview[] */
export function parseReviewOutput(raw: string): ChapterReview[] {
  try {
    // 尝试从 markdown 中提取 JSON 块
    const jsonMatch = raw.match(/\[[\s\S]*\]/);
    if (!jsonMatch) return [];

    const parsed = JSON.parse(jsonMatch[0]);
    if (!Array.isArray(parsed)) return [];

    return parsed
      .filter((item: Record<string, unknown>) =>
        item.dimension && typeof item.dimension === 'string' && VALID_DIMENSIONS.has(item.dimension) && typeof item.score === 'number')
      .map((item: Record<string, unknown>, index: number) => ({
        id: `review-${index}`,
        dimension: item.dimension as ReviewDimension,
        score: Math.min(10, Math.max(1, Number(item.score))),
        issues: Array.isArray(item.issues) ? item.issues.map(String) : [],
        suggestions: Array.isArray(item.suggestions) ? item.suggestions.map(String) : [],
        reviewedAt: new Date().toISOString(),
      }));
  } catch {
    return [];
  }
}

/**
 * 获取章节审查 API 的请求体
 */
export function getReviewRequestBody(
  chapterContent: string,
  styleGuide: string,
  chapterTask: string,
  apiKey: string,
  model: string,
  baseURL: string,
  daoContext?: string,
) {
  const { systemPrompt, userMessage } = buildChapterReviewMessages(chapterContent, styleGuide, chapterTask, daoContext);
  return { systemPrompt, userMessage, apiKey, model, baseURL, daoContext };
}

/**
 * 获取章节修正 API 的请求体
 */
export function getRevisionRequestBody(
  chapterContent: string,
  reviews: ChapterReview[],
  humanFeedback: string | null,
  styleGuide: string,
  chapterTask: string,
  apiKey: string,
  model: string,
  baseURL: string,
  daoContext?: string,
) {
  const reviewsText = reviews
    .map((r) => `- ${r.dimension}: ${r.score}/10\n  问题: ${r.issues.join('; ') || '无'}\n  建议: ${r.suggestions.join('; ') || '无'}`)
    .join('\n');
  const { systemPrompt, userMessage } = buildChapterRevisionMessages(chapterContent, reviewsText, humanFeedback, styleGuide, chapterTask, daoContext);
  return { systemPrompt, userMessage, apiKey, model, baseURL, daoContext };
}

/**
 * 判断审查是否通过
 * @param reviews 审查结果
 * @param threshold 及格线（默认 7）
 */
export function isReviewPassed(reviews: ChapterReview[], threshold = 7): boolean {
  if (reviews.length === 0) return false;
  return reviews.every((r) => r.score >= threshold);
}
</file>

<file path="src/lib/generation/context-assembler.ts">
// ============================================
// 上下文组装器 — 为每层生成组装输入
// v2: 支持新六层 DNA（道/气/骨/肉/势能）
// ============================================

import type { GenerationLayer, SourceNovel, WritingProject, NovelDNA, NovelDao, NovelQi } from '@/types';
import type { GenerationRulesDNA } from '@/lib/source-processing/pipeline-types';

// ---- 层级上下文截断常量（近似 token 预算） ----

/** Layer 1 道/气上下文最大字符数（大纲层只需核心信息） */
const DAO_CONTEXT_MAX_CHARS_L1 = 300;
/** Layer 2 节奏处方截断字符数 */
const RHYTHM_MAX_CHARS_L2 = 300;
/** Layer < 3 风格引擎摘要截断字符数 */
const STYLE_SUMMARY_MAX_CHARS_EARLY = 500;
/** 前一章末尾衔接字符数（硬核衔接轨） */
const PREV_CHAPTER_TAIL_CHARS = 1200;
/** 前情提要每章取头/尾字符数 */
const RECENT_CHAPTER_PREVIEW_CHARS = 200;
/** 前情提要回看章数 */
const RECENT_CHAPTER_LOOKBACK = 5;

export interface LayerContext {
  /** 文风档案（role='style' 的源小说拼接） */
  styleGuide: string;
  /** 情节报告（role='plot' 的源小说拼接） */
  plotGuide: string;
  /** 当前层以上的层级内容 */
  hierarchyContext: string;
  /** 当前层的具体任务（仅 Layer 5） */
  chapterTask: string;
  /** 已生成的前文状态 */
  previousState: string;
  // ── v2 新增字段 ──
  /** 道/气上下文（从新 DNA 提取） */
  daoContext: string;
  /** 节奏处方（从势能分析和消融测试提取） */
  rhythmPrescription: string;
  /** 风格引擎摘要（精简版文风指导） */
  styleEngineSummary: string;
}

/**
 * 从情节报告中提取大情节框架部分
 */
function extractMajorPlot(plotReport: string): string {
  const match = plotReport.match(/## 一、大情节框架[\s\S]*?(?=## 二、|$)/);
  return match ? match[0].trim() : plotReport;
}

/**
 * 从情节报告中提取小情节模式库
 */
function extractMinorPlots(plotReport: string): string {
  const match = plotReport.match(/## 二、小情节模式库[\s\S]*?(?=## 三、|$)/);
  return match ? match[0].trim() : '';
}

/**
 * 从情节报告中提取节奏和伏笔规则
 */
function extractPacingAndForeshadow(plotReport: string): string {
  const pacing = plotReport.match(/## 四、节奏规律[\s\S]*?(?=$)/);
  const foreshadow = plotReport.match(/## 三、伏笔手法[\s\S]*?(?=## 四、|$)/);
  return [foreshadow?.[0]?.trim(), pacing?.[0]?.trim()].filter(Boolean).join('\n\n');
}

/**
 * 从多个新 DNA（GenerationRulesDNA）构建道/气上下文
 */
function buildDaoContextFromRules(dnas: GenerationRulesDNA[]): string {
  return dnas.map((dna, i) => {
    const parts: string[] = [];
    if (dnas.length > 1) parts.push(`#### 源小说 ${i + 1}`);
    if (dna.qualitativeNotes.coreAppeal) {
      parts.push(`### 核心吸引力\n- ${dna.qualitativeNotes.coreAppeal}`);
    }
    parts.push(`### 节奏参数\n- 冲突间隔：${dna.conflictInterval} 字\n- 高潮间隔：${dna.peakInterval} 字\n- 冷却长度：${dna.cooldownLength} 字\n- 刺激循环：${dna.stimulationCycle.join(' → ')}`);
    parts.push(`### 句式参数\n- 平均句长：${dna.sentenceLength.avg} 字\n- 高潮句长：${dna.sentenceLength.climax} 字\n- 平静句长：${dna.sentenceLength.calm} 字\n- 对话占比：${Math.round(dna.dialogueRatio * 100)}%`);
    if (dna.settingDriftTolerance > 0) {
      parts.push(`### 一致性\n- 设定漂移容忍度：${Math.round(dna.settingDriftTolerance * 100)}%\n- 文风一致率：${Math.round(dna.styleConsistencyRate * 100)}%`);
    }
    if (dna.qualitativeNotes.styleSignature) {
      parts.push(`### 风格签名\n- ${dna.qualitativeNotes.styleSignature}`);
    }
    return parts.join('\n\n');
  }).join('\n\n---\n\n');
}

/**
 * 从多个新 DNA（GenerationRulesDNA）构建节奏处方
 */
function buildRhythmPrescriptionFromRules(dnas: GenerationRulesDNA[]): string {
  return dnas.map((dna, i) => {
    const parts: string[] = [];
    parts.push(dnas.length > 1 ? `## 节奏处方 — 源小说 ${i + 1}\n` : '## 节奏处方（从源小说量化提取）\n');
    parts.push(`- 冲突间隔：${dna.conflictInterval} 字`);
    parts.push(`- 高潮间隔：${dna.peakInterval} 字`);
    parts.push(`- 冷却长度：${dna.cooldownLength} 字`);
    parts.push(`- 刺激循环模式：${dna.stimulationCycle.join(' → ')}`);
    if (Object.keys(dna.stimulationDensity).length > 0) {
      parts.push(`- 刺激类型分布：${Object.entries(dna.stimulationDensity).map(([k, v]) => `${k} ${Math.round(v * 100)}%`).join('、')}`);
    }
    if (dna.informationRelease.avgSetupToHint > 0) {
      parts.push(`- 伏笔到提示：${dna.informationRelease.avgSetupToHint} 章`);
      parts.push(`- 提示到揭秘：${dna.informationRelease.avgHintToReveal} 章`);
    }
    if (dna.taboos.length > 0) {
      parts.push('\n### 禁忌清单');
      for (const t of dna.taboos) parts.push(`- ❌ ${t}`);
    }
    if (dna.qualitativeNotes.riskNotes.length > 0) {
      parts.push('\n### 仿写风险');
      for (const r of dna.qualitativeNotes.riskNotes) parts.push(`- ⚠️ ${r}`);
    }
    return parts.join('\n');
  }).join('\n\n---\n\n');
}

/**
 * 从多个新 DNA 构建风格引擎摘要
 */
function buildStyleSummaryFromRules(dnas: GenerationRulesDNA[]): string {
  return dnas.map((dna) => {
    const parts: string[] = [];
    if (dna.qualitativeNotes.styleSignature) {
      parts.push(dna.qualitativeNotes.styleSignature);
    }
    parts.push(`句长 ${dna.sentenceLength.avg} 字，对话 ${Math.round(dna.dialogueRatio * 100)}%，描写 ${Math.round(dna.descriptionRatio * 100)}%`);
    if (dna.characterRules.length > 0) {
      parts.push('\n### 角色规则');
      for (const c of dna.characterRules.slice(0, 5)) {
        parts.push(`- ${c.name}：${c.stimulusResponse.map(sr => `${sr.stimulus}→${sr.response}`).join('；')}`);
      }
    }
    return parts.join('\n');
  }).join('\n\n---\n\n');
}

/**
 * 从新 DNA（旧版）构建道/气上下文
 */
function buildDaoContext(allNovels: SourceNovel[]): string {
  const dnaV2s = allNovels
    .map((n) => n.novelDnaV2)
    .filter((d): d is NovelDNA => d !== null);

  if (dnaV2s.length === 0) return '';

  const parts: string[] = [];

  for (const dna of dnaV2s) {
    const dao = dna.dao;
    const qi = dna.qi;

    parts.push(`### 核心吸引力（道）\n- 主情绪场：${dao.primaryEmotionalField}\n- 读者为何停留：${dao.whyReadersStay}\n- AI 置信度：${Math.round(dao.confidence * 100)}%${dao.userConfirmed ? ' ✅ 已确认' : ' ⚠️ 待确认'}`);

    parts.push(`### 阅读状态维持（气）\n- 维持方法：${qi.maintenanceMethods.join('；')}\n- 呼吸周期：${qi.breathingCycleDescription}\n- 破坏因素：${qi.disruptors.join('；')}`);

    if (qi.rhythmProfile) {
      const rp = qi.rhythmProfile;
      parts.push(`### 节奏画像\n- 推进 ${Math.round(rp.propulsionRatio * 100)}% | 蓄力 ${Math.round(rp.buildupRatio * 100)}% | 释放 ${Math.round(rp.releaseRatio * 100)}%\n- 呼吸 ${Math.round(rp.breathRatio * 100)}% | 存在 ${Math.round(rp.existenceRatio * 100)}% | 校准 ${Math.round(rp.calibrationRatio * 100)}%`);
    }
  }

  return parts.join('\n\n');
}

/**
 * 从势能分析和消融测试构建节奏处方
 */
function buildRhythmPrescription(allNovels: SourceNovel[]): string {
  const dnaV2s = allNovels
    .map((n) => n.novelDnaV2)
    .filter((d): d is NovelDNA => d !== null);

  if (dnaV2s.length === 0) return '';

  const parts: string[] = [];
  parts.push('## 节奏处方（从源小说提取）\n');

  for (const dna of dnaV2s) {
    // 势能模式
    const patterns = dna.engines.tensionPatterns;
    if (patterns.length > 0) {
      const avgDuration = Math.round(patterns.reduce((s, p) => s + p.duration, 0) / patterns.length);
      parts.push(`- 平均势能积累周期：约 ${avgDuration} 个场景\n`);
      parts.push(`- 势能模式类型：${[...new Set(patterns.map((p) => p.climaxType))].join('、')}\n`);

      const highPayoff = patterns.filter((p) => p.payoffMultiplier === 'high' || p.payoffMultiplier === 'extreme');
      if (highPayoff.length > 0) {
        parts.push(`- ⚡ 高回报释放点：${highPayoff.length} 个（需充分积累后才释放）\n`);
      }
    }

    // 结构比例
    const structure = dna.structure;
    parts.push(`- 核心骨架段落：${structure.bones.length} 个 → **不可省略**\n`);
    parts.push(`- 增强体验段落：${structure.muscles.length} 个 → 可调整但会影响体验\n`);

    if (structure.fillerTypeA.length > 0) {
      parts.push(`- 🌬️ 体验填充段落：${structure.fillerTypeA.length} 个 → 维持"气"的关键，生成时需保留对应比例的呼吸段落\n`);
    }

    if (structure.fillerTypeB.length > 0) {
      parts.push(`- ⚠️ 机械填充段落：${structure.fillerTypeB.length} 个 → 模仿时替换为有意义的呼吸内容\n`);
    }

    // 失败模式
    const risks = dna.failureModes.risks;
    if (risks.length > 0) {
      parts.push('\n### 避坑指南\n');
      for (const risk of risks) {
        parts.push(`- **${risk.description}** → ${risk.mitigation}\n`);
      }
    }
  }

  return parts.join('');
}

/**
 * 组装层级上下文（v2：支持新 DNA）
 */
export function assembleLayerContext(
  layer: GenerationLayer,
  sourceNovels: SourceNovel[],
  project: WritingProject,
  chapterPlanId?: string,
): LayerContext {
  // 按角色分类源小说
  const styleSources = sourceNovels.filter((s) =>
    project.sourceRoles.some((r) => r.sourceNovelId === s.id && (r.role === 'style' || r.role === 'style_and_plot'))
  );
  const plotSources = sourceNovels.filter((s) =>
    project.sourceRoles.some((r) => r.sourceNovelId === s.id && (r.role === 'plot' || r.role === 'style_and_plot'))
  );

  // 拼接文风档案（优先用新 DNA 的风格引擎摘要）
  const styleGuide = styleSources
    .map((s) => s.styleProfile)
    .filter(Boolean)
    .join('\n\n---\n\n');

  // 拼接情节报告
  const plotGuide = plotSources
    .map((s) => s.plotReport)
    .filter(Boolean)
    .join('\n\n---\n\n');

  // 根据层级提取情节报告的不同部分
  const majorPlot = extractMajorPlot(plotGuide);
  const minorPlots = extractMinorPlots(plotGuide);
  const pacingRules = extractPacingAndForeshadow(plotGuide);

  // 道/气上下文（所有层共享，但 L1 用精简版）
  const allSources = [...styleSources, ...plotSources].filter(
    (s, i, arr) => arr.findIndex((x) => x.id === s.id) === i
  );

  // 优先使用新的 GenerationRulesDNA（收集所有源的 DNA）
  const newDnas = allSources
    .map(s => s.generationRulesDna as import('@/lib/source-processing/pipeline-types').GenerationRulesDNA | null)
    .filter((d): d is import('@/lib/source-processing/pipeline-types').GenerationRulesDNA => d !== null);

  const daoContext = newDnas.length > 0
    ? buildDaoContextFromRules(newDnas)
    : buildDaoContext(allSources);

  const rhythmPrescription = newDnas.length > 0
    ? buildRhythmPrescriptionFromRules(newDnas)
    : buildRhythmPrescription(allSources);

  // 风格引擎摘要
  const styleEngineSummary = newDnas.length > 0
    ? buildStyleSummaryFromRules(newDnas)
    : allSources
        .map((s) => s.novelDnaV2?.styleEngine.rawStyleProfile)
        .filter(Boolean)
        .join('\n\n---\n\n');

  // 层级上下文（当前层以上的内容）
  let hierarchyContext = '';
  let chapterTask = '';
  let previousState = '';

  if (layer >= 1 && project.outline) {
    hierarchyContext += `## 全书大纲\n\n${project.outline.content}\n\n`;
  }

  if (layer >= 2 && project.phases) {
    hierarchyContext += `## 阶段规划\n\n${project.phases.map((p) => `### ${p.title}\n${p.content}`).join('\n\n')}\n\n`;
  }

  if (layer >= 3 && project.volumes) {
    hierarchyContext += `## 分卷\n\n${project.volumes.map((v) => `### ${v.title}\n${v.content}`).join('\n\n')}\n\n`;
  }

  if (layer >= 4 && project.chapterSets) {
    hierarchyContext += `## 章节集合\n\n${project.chapterSets.map((s) => `### ${s.title}\n${s.content}`).join('\n\n')}\n\n`;
  }

  if (layer === 5 && project.chapterPlans && chapterPlanId) {
    const plan = project.chapterPlans.find((p) => p.id === chapterPlanId);
    if (plan) {
      chapterTask = `## 本章计划\n\n${plan.content}\n\n关键词: ${plan.plotKeywords.join(', ')}\n小情节模式: ${plan.patternStructure}`;
    }
  }

  // ═══ 前情提要修复：从"前5章前300字"改为"前一章末尾1200字 + 状态增量" ═══
  if (project.chapters.length > 0) {
    // 前一章的末尾 1200 字（硬核衔接轨）
    const lastChapter = project.chapters[project.chapters.length - 1];
    const lastChapterEnd = lastChapter.content.slice(-PREV_CHAPTER_TAIL_CHARS);
    previousState = `## 前一章末尾（无缝衔接）\n\n${lastChapterEnd}\n\n`;

    // 状态增量轨：前 5 章的关键事件摘要
    const recentChapters = project.chapters.slice(-RECENT_CHAPTER_LOOKBACK);
    if (recentChapters.length > 1) {
      previousState += `## 前情提要（最近${recentChapters.length}章关键事件）\n\n`;
      for (const ch of recentChapters.slice(0, -1)) { // 排除最后一章（已传末尾）
        const preview = ch.content.slice(0, RECENT_CHAPTER_PREVIEW_CHARS);
        const tail = ch.content.slice(-RECENT_CHAPTER_PREVIEW_CHARS);
        previousState += `- ${preview.replace(/\n/g, ' ')}...${tail.replace(/\n/g, ' ')}\n`;
      }
    }
  }

  // 按层级组装最终的上下文
  const layerContext = (() => {
    switch (layer) {
      case 1:
        return { styleGuide, plotGuide: majorPlot, hierarchyContext: '', chapterTask: '', previousState: '' };
      case 2:
        return { styleGuide, plotGuide: pacingRules, hierarchyContext, chapterTask: '', previousState: '' };
      case 3:
        return { styleGuide, plotGuide: minorPlots, hierarchyContext, chapterTask: '', previousState: '' };
      case 4:
        return { styleGuide, plotGuide: minorPlots, hierarchyContext, chapterTask: '', previousState: '' };
      case 5:
        return { styleGuide, plotGuide: minorPlots, hierarchyContext, chapterTask, previousState };
      default:
        return { styleGuide, plotGuide, hierarchyContext, chapterTask, previousState };
    }
  })();

  // L1 用精简版道/气上下文
  const daoForLayer = layer === 1
    ? daoContext.slice(0, DAO_CONTEXT_MAX_CHARS_L1)
    : daoContext;

  // L5 获得完整的节奏处方
  const rhythmForLayer = layer >= 4
    ? rhythmPrescription
    : (layer >= 2 ? rhythmPrescription.slice(0, RHYTHM_MAX_CHARS_L2) : '');

  return {
    ...layerContext,
    daoContext: daoForLayer,
    rhythmPrescription: rhythmForLayer,
    styleEngineSummary: layer >= 3 ? styleEngineSummary : styleEngineSummary.slice(0, STYLE_SUMMARY_MAX_CHARS_EARLY),
  };
}
</file>

<file path="src/lib/generation/layer-pipeline.ts">
// ============================================
// 五层管线编排器 — 先框架后填充
// 使用 stream-fetcher 统一流式调用
// ============================================

import type {
  BookOutline,
  ChapterPlan,
  ChapterSet,
  Phase,
  SourceNovel,
  Volume,
  WritingProject,
} from '@/types';
import { assembleLayerContext } from './context-assembler';
import { useSettingsStore } from '@/lib/store/settings';
import { createStreamFetcher } from '@/lib/stream-fetcher';

/** 获取 AI 设置 */
function getAISettings() {
  return useSettingsStore.getState().getAIConfig();
}

/** 通用流式调用（使用共享 stream-fetcher） */
async function streamGenerate(url: string, body: object): Promise<string> {
  const fetcher = createStreamFetcher();
  const { result, state } = await fetcher.fetch(url, body);
  if (!result) throw new Error(state.error || '生成请求失败');
  return result;
}

// ============================================
// Layer 1: 全书大纲（单次调用，无子项）
// ============================================

export async function generateOutline(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  userConcept: string,
  onStream?: (text: string) => void,
): Promise<BookOutline> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(1, sourceNovels, project);

  const body = {
    styleGuide: ctx.styleGuide,
    plotGuide: ctx.plotGuide,
    userConcept,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const content = await streamGenerate('/api/generation/outline', body);
  onStream?.(content);

  const outline: BookOutline = {
    content,
    generatedAt: new Date().toISOString(),
    isLocked: true,
    majorPlotFrameworkRef: sourceNovels.find((s) =>
      project.sourceRoles.some((r) => r.sourceNovelId === s.id && (r.role === 'plot' || r.role === 'style_and_plot'))
    )?.id ?? '',
  };

  return outline;
}

// ============================================
// Layer 2: 阶段规划（先框架后填充）
// ============================================

export async function generatePhaseFramework(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  onStream?: (text: string) => void,
): Promise<string> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(2, sourceNovels, project);

  const body = {
    outline: project.outline?.content ?? '',
    plotGuide: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const result = await streamGenerate('/api/generation/phase-framework', body);
  onStream?.(result);
  return result;
}

export async function generatePhaseDetail(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  phaseIndex: number,
  phaseFramework: string,
  onStream?: (text: string) => void,
): Promise<Phase> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(2, sourceNovels, project);

  const body = {
    outline: project.outline?.content ?? '',
    phaseFramework,
    phaseIndex,
    plotGuide: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const content = await streamGenerate('/api/generation/phase-detail', body);
  onStream?.(content);

  return {
    id: `phase-${phaseIndex}`,
    index: phaseIndex,
    title: `阶段${phaseIndex + 1}`,
    content,
    volumeCount: 0,
    generatedAt: new Date().toISOString(),
  };
}

// ============================================
// Layer 3: 分卷（先框架后填充）
// ============================================

export async function generateVolumeFramework(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  phaseId: string,
  onStream?: (text: string) => void,
): Promise<string> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(3, sourceNovels, project);
  const phase = project.phases?.find((p) => p.id === phaseId);

  const body = {
    outline: project.outline?.content ?? '',
    phaseContent: phase?.content ?? '',
    minorPlotPatterns: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const result = await streamGenerate('/api/generation/volume-framework', body);
  onStream?.(result);
  return result;
}

export async function generateVolumeDetail(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  volumeIndex: number,
  volumeFramework: string,
  phaseId: string,
  onStream?: (text: string) => void,
): Promise<Volume> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(3, sourceNovels, project);
  const phase = project.phases?.find((p) => p.id === phaseId);

  const body = {
    outline: project.outline?.content ?? '',
    phaseContent: phase?.content ?? '',
    volumeFramework,
    volumeIndex,
    minorPlotPatterns: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const content = await streamGenerate('/api/generation/volume-detail', body);
  onStream?.(content);

  return {
    id: `volume-${phaseId}-${volumeIndex}`,
    phaseId,
    index: volumeIndex,
    title: `卷${volumeIndex + 1}`,
    content,
    chapterSetCount: 0,
    generatedAt: new Date().toISOString(),
  };
}

// ============================================
// Layer 4: 章节集合（先框架后填充）
// ============================================

export async function generateChapterSetFramework(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  volumeId: string,
  onStream?: (text: string) => void,
): Promise<string> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(4, sourceNovels, project);
  const volume = project.volumes?.find((v) => v.id === volumeId);

  const body = {
    volumeContent: volume?.content ?? '',
    minorPlotPatterns: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const result = await streamGenerate('/api/generation/chapter-set-framework', body);
  onStream?.(result);
  return result;
}

export async function generateChapterSetDetail(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  setIndex: number,
  setFramework: string,
  volumeId: string,
  onStream?: (text: string) => void,
): Promise<ChapterSet> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(4, sourceNovels, project);
  const volume = project.volumes?.find((v) => v.id === volumeId);

  const body = {
    volumeContent: volume?.content ?? '',
    setFramework,
    setIndex,
    minorPlotPatterns: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const content = await streamGenerate('/api/generation/chapter-set-detail', body);
  onStream?.(content);

  return {
    id: `set-${volumeId}-${setIndex}`,
    volumeId,
    index: setIndex,
    title: `集合${setIndex + 1}`,
    content,
    chapterCount: 0,
    generatedAt: new Date().toISOString(),
  };
}

// ============================================
// Layer 5: 每章计划（先框架后填充）
// ============================================

export async function generateChapterPlanFramework(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  chapterSetId: string,
  onStream?: (text: string) => void,
): Promise<string> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(5, sourceNovels, project);
  const set = project.chapterSets?.find((s) => s.id === chapterSetId);

  const body = {
    setContent: set?.content ?? '',
    minorPlotPatterns: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const result = await streamGenerate('/api/generation/chapter-plan-framework', body);
  onStream?.(result);
  return result;
}

export async function generateChapterPlanDetail(
  project: WritingProject,
  sourceNovels: SourceNovel[],
  chapterIndex: number,
  planFramework: string,
  chapterSetId: string,
  onStream?: (text: string) => void,
): Promise<ChapterPlan> {
  const ai = getAISettings();
  const ctx = assembleLayerContext(5, sourceNovels, project);
  const set = project.chapterSets?.find((s) => s.id === chapterSetId);

  const body = {
    setContent: set?.content ?? '',
    planFramework,
    chapterIndex,
    minorPlotPatterns: ctx.plotGuide,
    daoContext: ctx.daoContext,
    rhythmPrescription: ctx.rhythmPrescription,
    apiKey: ai.apiKey,
    model: ai.model,
    baseURL: ai.baseURL,
  };

  const content = await streamGenerate('/api/generation/chapter-plan-detail', body);
  onStream?.(content);

  return {
    id: `plan-${chapterSetId}-${chapterIndex}`,
    chapterSetId,
    index: chapterIndex,
    title: `第${chapterIndex + 1}章`,
    content,
    plotKeywords: [],
    patternStructure: '',
    generatedAt: new Date().toISOString(),
  };
}
</file>

<file path="src/lib/source-processing/dao-pipeline.ts">
// ============================================
// 道/气 分析管线 — DAG 并行编排器
// 在现有 8 步管线完成后运行，提取道/气/骨/肉/势能
// ============================================

import {
  getExperienceAnnotationRequestBody,
  parseExperienceAnnotations,
  aggregateExperienceCurves,
  computeExperienceBatches,
} from './experience-annotator';
import {
  getAblationTestingRequestBody,
  parseAblationResults,
  computeAblationBatches,
  summarizeAblationResults,
} from './ablation-tester';
import {
  getTensionTrackingRequestBody,
  parseTensionAnalysis,
} from './tension-tracker';
import { READER_PERSONAS } from '@/lib/ai/prompts';
import { createStreamFetcher, type AIConfig } from '@/lib/stream-fetcher';
import type {
  SemanticSlice,
  ExperienceAnnotation,
  ExperienceCurve,
  AblationResult,
  TensionAnalysis,
  NovelDNA,
  NovelDao,
  NovelQi,
  NovelStructure,
  NovelEngines,
  FailureModes,
  StyleEngine,
  TechniqueSampleLibrary,
  SourceNovel,
  EventGraph,
} from '@/types';

// ---- 回调接口 ----

export interface DaoPipelineCallbacks {
  onStepStart: (step: string, message: string) => void;
  onStepComplete: (step: string) => void;
  onStepError: (step: string, error: string) => void;
  onProgress: (progress: number) => void;
}

// ---- 主编排函数 ----

export async function runDaoPipeline(
  novel: SourceNovel,
  aiConfig: AIConfig,
  callbacks: DaoPipelineCallbacks,
  externalFetcher?: ReturnType<typeof createStreamFetcher>,
): Promise<{
  experienceCurve: ExperienceCurve[];
  ablationResults: AblationResult[];
  tensionAnalysis: TensionAnalysis | null;
  novelDnaV2: NovelDNA | null;
  techniqueSamples: TechniqueSampleLibrary | null;
}> {
  const fetcher = externalFetcher ?? createStreamFetcher();
  const slices = novel.slices;
  const styleProfile = novel.styleProfile ?? '';
  const plotReport = novel.plotReport ?? '';
  const characterDynamics = novel.characterDynamics ?? '';
  const readerExperience = novel.readerExperience ?? '';
  const narrativeConstraints = novel.narrativeConstraints ?? '';

  if (!slices || slices.length === 0) {
    throw new Error('缺少切片数据，请先完成基础分析管线');
  }

  // ═══════════════════════════════════════════
  // Phase 1: 体验流标注 (多个 reader persona 并行)
  // ═══════════════════════════════════════════

  callbacks.onStepStart('experience', '体验流标注 — AI 以多读者身份阅读...');
  callbacks.onProgress(10);

  const personaEntries = Object.entries(READER_PERSONAS);
  const allAnnotations: ExperienceAnnotation[] = [];

  // 所有 persona 并行标注
  const personaResults = await Promise.allSettled(
    personaEntries.map(async ([name, description]) => {
      const batches = computeExperienceBatches(slices, aiConfig.maxContextTokens);
      let personaAnnotations: ExperienceAnnotation[] = [];

      for (const group of batches.sliceGroups) {
        const body = getExperienceAnnotationRequestBody(
          group, name, description,
          aiConfig.apiKey, aiConfig.model, aiConfig.baseURL,
        );
        const res = await fetcher.fetch('/api/source/process/experience-annotation', body);
        if (res.result) {
          const parsed = parseExperienceAnnotations(res.result, group, name);
          personaAnnotations = personaAnnotations.concat(parsed);
        }
      }
      return { persona: name, annotations: personaAnnotations };
    }),
  );

  for (const result of personaResults) {
    if (result.status === 'fulfilled') {
      allAnnotations.push(...result.value.annotations);
    }
  }

  if (allAnnotations.length === 0) {
    throw new Error('所有读者 persona 标注均失败，请检查 API 配置或重试');
  }

  const experienceCurve = aggregateExperienceCurves(allAnnotations, slices);
  callbacks.onStepComplete('experience');
  callbacks.onProgress(30);

  // ═══════════════════════════════════════════
  // Phase 2: 消融测试 + 势能追踪 (并行)
  // ═══════════════════════════════════════════

  callbacks.onStepStart('ablation+tension', '消融测试 + 势能追踪 — 并行分析...');

  const ablationPromise = (async (): Promise<AblationResult[]> => {
    const ablationBatches = computeAblationBatches(slices, aiConfig.maxContextTokens);
    let results: AblationResult[] = [];
    for (const group of ablationBatches.sliceGroups) {
      const body = getAblationTestingRequestBody(
        group, aiConfig.apiKey, aiConfig.model, aiConfig.baseURL,
      );
      const res = await fetcher.fetch('/api/source/process/ablation-testing', body);
      if (res.result) {
        results = results.concat(parseAblationResults(res.result, group));
      }
    }
    return results;
  })();

  const tensionPromise = (async (): Promise<TensionAnalysis | null> => {
    const body = getTensionTrackingRequestBody(
      slices, plotReport, experienceCurve,
      aiConfig.apiKey, aiConfig.model, aiConfig.baseURL,
    );
    const res = await fetcher.fetch('/api/source/process/tension-tracking', body);
    if (res.result) {
      return parseTensionAnalysis(res.result, slices);
    }
    return null;
  })();

  const [ablationResults, tensionAnalysis] = await Promise.all([
    ablationPromise,
    tensionPromise,
  ]);

  callbacks.onStepComplete('ablation+tension');
  callbacks.onProgress(60);

  const ablationSummary = summarizeAblationResults(ablationResults);

  // ═══════════════════════════════════════════
  // Phase 3: 构建新六层 DNA
  // ═══════════════════════════════════════════

  callbacks.onStepStart('dna-v2', '构建六层 DNA 结构...');

  const novelDnaV2: NovelDNA = buildNovelDNA({
    slices,
    styleProfile,
    plotReport,
    characterDynamics,
    readerExperience,
    narrativeConstraints,
    experienceCurve,
    ablationResults,
    ablationSummary,
    tensionAnalysis,
    novelId: novel.id,
    model: aiConfig.model,
  });

  callbacks.onStepComplete('dna-v2');
  callbacks.onProgress(80);

  // ═══════════════════════════════════════════
  // Phase 4: 技术样本库
  // ═══════════════════════════════════════════

  callbacks.onStepStart('technique-samples', '构建技术样本库...');

  const techniqueSamples = buildTechniqueSampleLibrary(slices, ablationResults, experienceCurve);

  callbacks.onStepComplete('technique-samples');
  callbacks.onProgress(100);

  return { experienceCurve, ablationResults, tensionAnalysis, novelDnaV2, techniqueSamples };
}

// ---- DNA 构建器 ----

function buildNovelDNA(inputs: {
  slices: SemanticSlice[];
  styleProfile: string;
  plotReport: string;
  characterDynamics: string;
  readerExperience: string;
  narrativeConstraints: string;
  experienceCurve: ExperienceCurve[];
  ablationResults: AblationResult[];
  ablationSummary: ReturnType<typeof summarizeAblationResults>;
  tensionAnalysis: TensionAnalysis | null;
  novelId: string;
  model: string;
}): NovelDNA {
  const {
    slices, styleProfile, characterDynamics, readerExperience,
    experienceCurve, ablationResults, ablationSummary, tensionAnalysis,
    novelId, model,
  } = inputs;

  // 道：从体验曲线推断主情绪场
  const sliceByIndex = new Map(slices.map((s) => [s.index, s] as const));
  const highPointSlices = experienceCurve.filter((c) => c.highPoints);
  const emotionalTones = highPointSlices.map((hp) => {
    const slice = sliceByIndex.get(hp.sliceIndex);
    return slice?.emotionalTone ?? '';
  }).filter(Boolean);

  const primaryEmotionalField = inferEmotionalField(emotionalTones, experienceCurve);

  // 气：维持方法
  const maintenanceMethods = inferMaintenanceMethods(ablationResults, slices, experienceCurve);

  // 结构
  const bones = ablationResults
    .filter((r) => r.category === 'bone')
    .map((r) => ({ description: r.lostIfRemoved, evidence: [sliceByIndex.get(r.sliceIndex)?.title ?? ''] }));
  const muscles = ablationResults
    .filter((r) => r.category === 'muscle')
    .map((r) => ({ description: r.lostIfRemoved, evidence: [sliceByIndex.get(r.sliceIndex)?.title ?? ''] }));
  const fillerTypeA = ablationResults
    .filter((r) => r.category === 'filler_a')
    .map((r) => ({ description: r.lostIfRemoved, purpose: r.reasoning }));
  const fillerTypeB = ablationResults
    .filter((r) => r.category === 'filler_b')
    .map((r) => ({ description: r.lostIfRemoved }));

  // 引擎
  const characterDecisionModels = [characterDynamics.slice(0, 500)].filter(Boolean);

  // 风险
  const total = ablationSummary.totalCount || 1;
  const hasFillerB = ablationSummary.fillerBCount > total * 0.1;
  const hasLongDuration = tensionAnalysis?.patterns.some((p) => p.duration > 20);

  const dao: NovelDao = {
    primaryEmotionalField,
    alternativeReadings: [],
    whyReadersStay: `读者持续阅读的主要原因与「${primaryEmotionalField}」相关`,
    confidence: highPointSlices.length > 3 ? 0.7 : 0.4,
    userConfirmed: false,
  };

  const qi: NovelQi = {
    maintenanceMethods,
    breathingCycleDescription: tensionAnalysis?.breathingCycle ?? '未知',
    rhythmProfile: tensionAnalysis?.rhythmProfile ?? null,
    disruptors: inferDisruptors(tensionAnalysis, ablationResults),
  };

  const structure: NovelStructure = {
    bones: bones.slice(0, 10),
    muscles: muscles.slice(0, 15),
    fillerTypeA: fillerTypeA.slice(0, 10),
    fillerTypeB: fillerTypeB.slice(0, 5),
  };

  const engines: NovelEngines = {
    tensionPatterns: tensionAnalysis?.patterns ?? [],
    characterDecisionModels,
    informationControl: readerExperience.slice(0, 300),
  };

  const failureModes: FailureModes = {
    risks: [
      ...(hasFillerB ? [{ description: '含机械填充段落，直接模仿会显得注水', severity: 'medium' as const, mitigation: '保留填充比例但替换为有意义的呼吸段落' }] : []),
      ...(hasLongDuration ? [{ description: '势能积累周期过长，不适合快节奏平台', severity: 'high' as const, mitigation: '缩短积累周期或拆分为多个子高潮' }] : []),
      { description: '主情绪场为 AI 推断，尚未经人工确认', severity: 'medium' as const, mitigation: '分析完成后请人工审阅 DNA 报告' },
    ],
    dependencies: ['长篇小说篇幅', '连载更新节奏'],
  };

  const styleEngine: StyleEngine = {
    prosePatterns: [],
    dialogueStyle: '',
    descriptionStyle: '',
    chapterOpenings: [],
    chapterEndings: [],
    rawStyleProfile: styleProfile,
  };

  return {
    dao,
    qi,
    structure,
    engines,
    failureModes,
    styleEngine,
    meta: {
      generatedAt: new Date().toISOString(),
      sourceNovelId: novelId,
      modelUsed: model,
      totalSlices: slices.length,
    },
  };
}

// ---- 辅助推断函数 ----

function inferEmotionalField(tones: string[], curve: ExperienceCurve[]): string {
  const highImmersion = curve.filter((c) => c.avgImmersion >= 7);
  const avgAnticipation = highImmersion.reduce((s, c) => s + c.avgAnticipation, 0) / (highImmersion.length || 1);

  if (avgAnticipation > 7) return '持续的期待感';
  if (tones.includes('紧张') && tones.length > 5) return '持续的紧张感';
  if (tones.includes('温馨') && tones.length > 5) return '持续的陪伴感/温暖感';
  if (avgAnticipation < 4 && highImmersion.length > 0) return '持续的沉浸感/舒适感';

  return '待确认（请查看体验曲线）';
}

// ═══════════════════════════════════════════
// 从事件图谱构建 DNA（200万字长篇分析新路径）
// ═══════════════════════════════════════════

export function buildNovelDNAFromEventGraph(eventGraph: EventGraph, styleProfile: string, novelId: string, model: string): NovelDNA {
  const events = eventGraph.events;

  // 道：从事件情绪分布推断
  const emotionCounts = new Map<string, number>();
  for (const e of events) {
    if (e.emotion) emotionCounts.set(e.emotion, (emotionCounts.get(e.emotion) || 0) + 1);
  }
  const topEmotion = [...emotionCounts.entries()].sort(([, a], [, b]) => b - a)[0]?.[0] ?? '待确认';

  // 从事件类型分布推断
  const typeCounts = new Map<string, number>();
  for (const e of events) typeCounts.set(e.type, (typeCounts.get(e.type) || 0) + 1);
  const typeEntries = [...typeCounts.entries()].sort(([, a], [, b]) => b - a);
  const combatRatio = (typeCounts.get('战斗/冲突') || 0) / events.length;
  const dailyRatio = (typeCounts.get('日常/过渡') || 0) / events.length;

  let primaryEmotionalField = topEmotion;
  if (dailyRatio > 0.3) primaryEmotionalField = `持续的陪伴感（${topEmotion}底色）`;
  else if (combatRatio > 0.25) primaryEmotionalField = `持续的紧张感（${topEmotion}底色）`;
  else primaryEmotionalField = `持续的${topEmotion}感`;

  const dao: NovelDao = {
    primaryEmotionalField,
    alternativeReadings: [],
    whyReadersStay: `读者持续阅读的核心驱动力与「${primaryEmotionalField}」相关。事件图谱中${events.length}个事件的情绪分布支持此推断。`,
    confidence: events.length > 1000 ? 0.7 : 0.5,
    userConfirmed: false,
  };

  // 气：从事件类型比例和因果链推断
  const maintenanceMethods: string[] = [];
  if (dailyRatio > 0.2) maintenanceMethods.push(`大量日常/过渡事件（${Math.round(dailyRatio * 100)}%）维持阅读状态`);
  if (combatRatio > 0.2) maintenanceMethods.push(`高频战斗/冲突事件（${Math.round(combatRatio * 100)}%）维持紧张感`);

  const avgTensionChange = events.reduce((s, e) => s + e.tensionChange, 0) / (events.length || 1);
  if (avgTensionChange > 1) maintenanceMethods.push('整体上升的紧张趋势维持驱动感');
  else if (avgTensionChange < -1) maintenanceMethods.push('整体下降的紧张趋势维持安全感');
  else maintenanceMethods.push('均衡的张弛节奏');

  const qi: NovelQi = {
    maintenanceMethods,
    breathingCycleDescription: typeEntries.slice(0, 3).map(([t, c]) => `${t}(${c}次)`).join(' → '),
    rhythmProfile: {
      propulsionRatio: combatRatio,
      buildupRatio: (typeCounts.get('决策/选择') || 0) / events.length,
      releaseRatio: (typeCounts.get('突破/升级') || 0) / events.length,
      breathRatio: dailyRatio,
      existenceRatio: dailyRatio * 0.6,
      calibrationRatio: 0.05,
    },
    disruptors: dailyRatio < 0.1 ? ['缺少呼吸段落', '连续高潮'] : ['无明显破坏因素'],
  };

  // 结构：从因果链推断
  const highDegreeEvents = events
    .filter((e) => e.causes.length + e.effects.length >= 5)
    .sort((a, b) => (b.causes.length + b.effects.length) - (a.causes.length + a.effects.length));

  const structure: NovelStructure = {
    bones: highDegreeEvents.slice(0, 10).map((e) => ({
      description: e.description,
      evidence: [`第${e.chapter}章`, ...e.participants],
    })),
    muscles: events
      .filter((e) => Math.abs(e.tensionChange) >= 2 && e.causes.length + e.effects.length < 5)
      .slice(0, 15)
      .map((e) => ({ description: e.description, evidence: [e.type] })),
    fillerTypeA: events
      .filter((e) => e.type === '日常/过渡' && Math.abs(e.tensionChange) <= 1)
      .slice(0, 10)
      .map((e) => ({ description: e.description, purpose: '维持阅读节奏' })),
    fillerTypeB: [],
  };

  // 引擎
  const engines: NovelEngines = {
    tensionPatterns: [],
    characterDecisionModels: [],
    informationControl: typeEntries.map(([t, c]) => `${t}:${c}`).join(','),
  };

  // 风险
  const longForeshadowing = eventGraph.foreshadowingPairs.filter((p) => p.distance > 30);
  const risks = [
    ...(longForeshadowing.length > 0 ? [{
      description: `含${longForeshadowing.length}对超长程伏笔（>30章），直接模仿可能丢失回收`,
      severity: 'medium' as const,
      mitigation: '生成时维护伏笔清单，每10章检查回收进度',
    }] : []),
    ...(dailyRatio > 0.3 ? [{
      description: '日常比例过高，可能不适合快节奏平台',
      severity: 'medium' as const,
      mitigation: '可压缩日常比例至15-20%',
    }] : []),
  ];

  const failureModes: FailureModes = {
    risks,
    dependencies: ['长篇连载格式', `${eventGraph.totalChapters}章篇幅`],
  };

  const styleEngine: StyleEngine = {
    prosePatterns: [],
    dialogueStyle: '',
    descriptionStyle: '',
    chapterOpenings: [],
    chapterEndings: [],
    rawStyleProfile: styleProfile,
  };

  return {
    dao,
    qi,
    structure,
    engines,
    failureModes,
    styleEngine,
    meta: { generatedAt: new Date().toISOString(), sourceNovelId: novelId, modelUsed: model, totalSlices: events.length },
  };
}

/** 根据分析结果推断破坏阅读状态的因素 */
function inferDisruptors(tensionAnalysis: TensionAnalysis | null, ablationResults: AblationResult[]): string[] {
  const disruptors: string[] = [];
  if (tensionAnalysis) {
    const longAccumulations = tensionAnalysis.patterns.filter((p) => p.duration > 20);
    if (longAccumulations.length > 0) {
      disruptors.push(`${longAccumulations.length} 处势能积累周期过长（>20 切片），可能导致读者疲劳`);
    }
  }
  const fillerBCount = ablationResults.filter((r) => r.category === 'filler_b').length;
  if (fillerBCount > ablationResults.length * 0.1) {
    disruptors.push(`${fillerBCount} 段机械填充可能打断阅读节奏`);
  }
  if (disruptors.length === 0) {
    disruptors.push('未检测到明显破坏因素');
  }
  return disruptors;
}

function inferMaintenanceMethods(
  ablationResults: AblationResult[],
  slices: SemanticSlice[],
  _curve: ExperienceCurve[],
): string[] {
  const methods: string[] = [];
  const fillerA = ablationResults.filter((r) => r.category === 'filler_a');
  const muscle = ablationResults.filter((r) => r.category === 'muscle');

  if (fillerA.length > ablationResults.length * 0.15) {
    methods.push('大量体验填充段落维持阅读状态（日常/准备/过渡）');
  }
  if (muscle.length > ablationResults.length * 0.2) {
    methods.push('丰富的人物互动和世界细节增强沉浸感');
  }

  const tensionLevels = slices.map((s) => s.tensionLevel).filter((t): t is number => t !== null && t !== undefined);
  const highTensionCount = tensionLevels.filter((t) => t >= 7).length;
  const lowTensionCount = tensionLevels.filter((t) => t <= 3).length;

  if (highTensionCount > lowTensionCount * 2) {
    methods.push('高频紧张-释放循环维持爽感');
  } else if (lowTensionCount > highTensionCount * 1.5) {
    methods.push('低强度持续叙事维持陪伴感');
  } else {
    methods.push('均衡的张弛节奏');
  }

  return methods;
}

// ---- 技术样本库构建 ----

function buildTechniqueSampleLibrary(
  slices: SemanticSlice[],
  ablationResults: AblationResult[],
  experienceCurve: ExperienceCurve[],
): TechniqueSampleLibrary {
  const selectSamples = (filter: (s: SemanticSlice, i: number) => boolean, count: number) => {
    const matched = slices.filter((s, i) => filter(s, i));
    return matched.slice(0, count).map((s) => ({
      sliceId: s.id,
      sliceIndex: s.index,
      title: s.title,
      content: s.content,
      selectionReason: '自动筛选',
    }));
  };

  const boneIndices = new Set(ablationResults.filter((r) => r.category === 'bone').map((r) => r.sliceIndex));
  const highPointIndices = new Set(experienceCurve.filter((c) => c.highPoints).map((c) => c.sliceIndex));

  // 按 sliceIndex 建立消融结果索引，避免位置映射错误
  const ablationBySliceIndex = new Map<number, AblationResult>();
  for (const r of ablationResults) {
    ablationBySliceIndex.set(r.sliceIndex, r);
  }

  return {
    hooks: selectSamples((s) => s.narrativeFunction === 'setup' || s.index <= 3, 3),
    climaxes: selectSamples(
      (s) => s.tensionLevel >= 8 || s.narrativeFunction === 'climax', 3,
    ),
    buildups: selectSamples(
      (s) => s.tensionLevel >= 5 && s.tensionLevel < 8 && s.narrativeFunction === 'escalation', 3,
    ),
    transitions: selectSamples(
      (s) => s.narrativeFunction === 'cooldown' || s.narrativeFunction === 'setup', 3,
    ),
    relationshipProgressions: selectSamples(
      (s) => s.semanticTags.some((t) => ['关系', '情感', '互动'].includes(t)), 3,
    ),
    worldRevelations: selectSamples(
      (s) => s.semanticTags.some((t) => ['世界观', '设定', '揭示'].includes(t)), 3,
    ),
    breathPassages: selectSamples(
      (s) => ablationBySliceIndex.get(s.index)?.category === 'filler_a' || (s.tensionLevel <= 3 && !boneIndices.has(s.index)), 3,
    ),
    foreshadowings: selectSamples(
      (s) => s.semanticTags.some((t) => ['伏笔', '铺垫', '暗示'].includes(t)) || s.dependencies.length > 0, 3,
    ),
  };
}
</file>

<file path="src/lib/store/prompts.ts">
// ============================================
// 提示词管理 Store — 自定义覆盖 + 持久化到服务端
// ============================================

import { create } from 'zustand';

/** 提示词元信息 */
export interface PromptMeta {
  key: string;
  label: string;
  category: string;
  description: string;
}

/** 所有提示词的注册表 */
export const PROMPT_REGISTRY: PromptMeta[] = [
  // 素材库处理 — 基础设施层
  { key: 'entity-classification', label: '实体分类', category: '素材处理', description: 'AI对候选实体列表进行类型分类（character/location/organization/artifact/concept）' },
  // 素材库处理 — 分析层
  { key: 'source-slicing', label: '智能切片', category: '素材处理', description: '按叙事弧线对小说文本进行智能切片（可配置目标字数）' },
  { key: 'source-style', label: '文风提取', category: '素材处理', description: '9维度分析写作风格特征（基础六维+首尾技术+telling/showing+紧张语言标记）' },
  { key: 'source-style-supplement', label: '文风补充', category: '素材处理', description: '多批次处理时补充文风分析' },
  { key: 'source-plot', label: '叙事动力学', category: '素材处理', description: '8维叙事动力学分析（大情节+小模式+伏笔+节奏+谜团图谱+翻页驱动+刺激-情绪+高光技术）' },
  { key: 'source-plot-supplement', label: '叙事动力学补充', category: '素材处理', description: '多批次处理时补充叙事动力学分析' },
  { key: 'source-character-dynamics', label: '角色动力学', category: '素材处理', description: '角色叙事功能+关系拓扑+角色弧线+角色演化' },
  { key: 'source-character-dynamics-supplement', label: '角色动力学补充', category: '素材处理', description: '多批次处理时补充角色动力学分析' },
  { key: 'source-reader-experience', label: '读者体验模型', category: '素材处理', description: '心理缺失模型+高潮势能追踪+势能曲线+综合翻页驱动力' },
  { key: 'source-reader-experience-supplement', label: '读者体验补充', category: '素材处理', description: '多批次处理时补充读者体验分析' },
  { key: 'source-narrative-constraints', label: '叙事约束', category: '素材处理', description: '降温机制+结构异常+风险模式+叙事禁忌+交叉审查' },
  { key: 'source-narrative-constraints-supplement', label: '叙事约束补充', category: '素材处理', description: '多批次处理时补充叙事约束分析' },
  { key: 'source-dna-compression', label: 'DNA压缩', category: '素材处理', description: '双层压缩：DNA超压缩(500字)+Genome完整基因库(5-10K字)' },
  { key: 'source-samples', label: '样本选取', category: '素材处理', description: '精选 3-5 个代表性切片（含高潮蓄能/情绪转换/异常结构）' },
  // 进阶分析 — 道/气 + 事件图谱
  { key: 'experience-annotation', label: '体验流标注', category: '进阶分析', description: '3个读者persona对切片打分（沉浸感/情绪/期待/节奏），聚合体验曲线' },
  { key: 'ablation-testing', label: '消融测试', category: '进阶分析', description: '四分类评估每个切片（骨架/肌肉/体验填充/机械填充），识别核心叙事元素' },
  { key: 'tension-tracking', label: '势能追踪', category: '进阶分析', description: '追踪高潮的积累→释放周期，计算势能模式和节奏画像' },
  { key: 'event-extraction', label: '事件提取', category: '统一管线', description: '逐小切片提取结构化事件，标注因果链和伏笔（Step 2）' },
  { key: 'event-alignment', label: '事件对齐', category: '统一管线', description: '代码预处理候选实体对 + AI 对齐跨切片实体，构建全书记忆图谱（Step 3）' },
  { key: 'deep-analysis', label: '深度分析', category: '统一管线', description: '逐大切片逆向工程 5 维度量化机制（文风/叙事/角色/刺激点/约束）（Step 5）' },
  { key: 'summary', label: '汇总报告', category: '统一管线', description: '文风演化 + 刺激周期 + 事件功能标注 + 一致性报告 + 信息释放曲线（Step 6）' },
  { key: 'dna-generation', label: 'DNA 生成规则', category: '统一管线', description: 'AI 补充定性规则（风格签名/核心吸引力/仿写风险），代码填充量化参数（Step 7）' },
  // 层级生成
  { key: 'outline', label: '全书大纲', category: '层级生成', description: '基于大情节框架和创作想法生成 ~200 字大纲' },
  { key: 'phase-framework', label: '阶段框架', category: '层级生成', description: '规划 4-6 个阶段的标题和章节范围' },
  { key: 'phase-detail', label: '阶段详情', category: '层级生成', description: '单个阶段 ~500 字详细规划' },
  { key: 'volume-framework', label: '分卷框架', category: '层级生成', description: '每阶段 1-2 卷的框架规划' },
  { key: 'volume-detail', label: '分卷详情', category: '层级生成', description: '单卷 ~300 字详细规划' },
  { key: 'chapter-set-framework', label: '章节集框架', category: '层级生成', description: '每卷 3-5 个章节集合的框架' },
  { key: 'chapter-set-detail', label: '章节集详情', category: '层级生成', description: '单集合 ~200 字详细规划' },
  { key: 'chapter-plan-framework', label: '章节计划框架', category: '层级生成', description: '每章标题和模式匹配' },
  { key: 'chapter-plan-detail', label: '章节计划详情', category: '层级生成', description: '单章 ~100 字场景节拍和伏笔操作' },
  // 章节生成
  { key: 'chapter-writing', label: '章节生成', category: '章节写作', description: '根据计划生成章节正文' },
  { key: 'chapter-review', label: '章节审查', category: '章节写作', description: '5 维度自动审查（文风/情节/节奏/模式/伏笔）' },
  { key: 'chapter-revision', label: '章节修正', category: '章节写作', description: '根据审查结果和人工反馈修正章节' },
];

export interface PromptsState {
  /** 自定义提示词覆盖（key -> content） */
  customPrompts: Record<string, string>;
  /** 默认提示词（key -> content） */
  defaultPrompts: Record<string, string>;
  /** 是否已加载 */
  loaded: boolean;

  // Actions
  loadPrompts: () => Promise<void>;
  getPrompt: (key: string) => string;
  setCustomPrompt: (key: string, content: string) => void;
  resetPrompt: (key: string) => void;
  saveCustomPrompt: (key: string, content: string) => Promise<void>;
}

export const usePromptStore = create<PromptsState>()((set, get) => ({
  customPrompts: {},
  defaultPrompts: {},
  loaded: false,

  loadPrompts: async () => {
    try {
      const res = await fetch('/api/prompts/list');
      if (!res.ok) return;
      const data = await res.json();
      set({
        defaultPrompts: data.defaults ?? {},
        customPrompts: data.custom ?? {},
        loaded: true,
      });
    } catch (err) {
      console.error('加载提示词失败:', err);
    }
  },

  getPrompt: (key: string) => {
    const { customPrompts, defaultPrompts } = get();
    return customPrompts[key] ?? defaultPrompts[key] ?? '';
  },

  setCustomPrompt: (key, content) => {
    set((s) => ({
      customPrompts: { ...s.customPrompts, [key]: content },
    }));
  },

  resetPrompt: (key) => {
    set((s) => {
      const next = { ...s.customPrompts };
      delete next[key];
      return { customPrompts: next };
    });
  },

  saveCustomPrompt: async (key, content) => {
    get().setCustomPrompt(key, content);
    try {
      await fetch('/api/prompts/save', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ key, content }),
      });
    } catch (err) {
      console.error('保存提示词失败:', err);
    }
  },
}));
</file>

<file path=".gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local data
data/
novel/

# crash dumps
*.stackdump

# local claude settings (auto-commit hook etc.)
.claude/settings.local.json
repomix-output.xml
</file>

<file path="src/app/globals.css">
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-sans);
  --font-mono: var(--font-geist-mono);
  --font-heading: var(--font-sans);
  --color-sidebar-ring: var(--sidebar-ring);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar: var(--sidebar);
  --color-chart-5: var(--chart-5);
  --color-chart-4: var(--chart-4);
  --color-chart-3: var(--chart-3);
  --color-chart-2: var(--chart-2);
  --color-chart-1: var(--chart-1);
  --color-ring: var(--ring);
  --color-input: var(--input);
  --color-border: var(--border);
  --color-destructive: var(--destructive);
  --color-accent-foreground: var(--accent-foreground);
  --color-accent: var(--accent);
  --color-muted-foreground: var(--muted-foreground);
  --color-muted: var(--muted);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-secondary: var(--secondary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary: var(--primary);
  --color-popover-foreground: var(--popover-foreground);
  --color-popover: var(--popover);
  --color-card-foreground: var(--card-foreground);
  --color-card: var(--card);
  --radius-sm: calc(var(--radius) * 0.6);
  --radius-md: calc(var(--radius) * 0.8);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) * 1.4);
  --radius-2xl: calc(var(--radius) * 1.8);
  --radius-3xl: calc(var(--radius) * 2.2);
  --radius-4xl: calc(var(--radius) * 2.6);
}

:root {
  /* 米白纸张底色 */
  --background: oklch(0.955 0.008 85);
  --foreground: oklch(0.22 0.02 60);

  /* 卡片 — 比背景稍白 */
  --card: oklch(0.975 0.005 85);
  --card-foreground: oklch(0.22 0.02 60);

  /* 弹出层 */
  --popover: oklch(0.975 0.005 85);
  --popover-foreground: oklch(0.22 0.02 60);

  /* 主色 — 深墨紫 */
  --primary: oklch(0.42 0.10 290);
  --primary-foreground: oklch(0.98 0.005 85);

  /* 次要 — 浅米色 */
  --secondary: oklch(0.915 0.010 85);
  --secondary-foreground: oklch(0.25 0.02 60);

  /* 柔和 */
  --muted: oklch(0.910 0.010 85);
  --muted-foreground: oklch(0.50 0.02 70);

  /* 强调 — 浅紫 */
  --accent: oklch(0.895 0.025 290);
  --accent-foreground: oklch(0.30 0.07 290);

  /* 警告 */
  --destructive: oklch(0.577 0.245 27.325);

  /* 边框/输入 */
  --border: oklch(0.885 0.010 85);
  --input: oklch(0.885 0.010 85);
  --ring: oklch(0.42 0.10 290);

  /* 图表 */
  --chart-1: oklch(0.55 0.10 290);
  --chart-2: oklch(0.65 0.08 85);
  --chart-3: oklch(0.45 0.06 60);
  --chart-4: oklch(0.35 0.10 290);
  --chart-5: oklch(0.30 0.05 60);

  --radius: 0.625rem;

  /* 侧边栏 — 比主背景稍深 */
  --sidebar: oklch(0.925 0.010 85);
  --sidebar-foreground: oklch(0.22 0.02 60);
  --sidebar-primary: oklch(0.42 0.10 290);
  --sidebar-primary-foreground: oklch(0.98 0.005 85);
  --sidebar-accent: oklch(0.895 0.020 290);
  --sidebar-accent-foreground: oklch(0.25 0.05 290);
  --sidebar-border: oklch(0.875 0.010 85);
  --sidebar-ring: oklch(0.42 0.10 290);
}

@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
  html {
    @apply font-sans;
  }
}

/* 隐藏侧边栏滚动条 */
.scrollbar-none {
  -ms-overflow-style: none;
  scrollbar-width: none;
}
.scrollbar-none::-webkit-scrollbar {
  display: none;
}
</file>

<file path="src/components/chapter/ChapterGenerationView.tsx">
// ============================================
// 章节生成视图 — 生成 + 审查 + 修正
// ============================================

'use client';

import { useState, useCallback, useMemo } from 'react';
import { ArrowLeft, Play, Loader2 } from 'lucide-react';
import { useProjectStore } from '@/lib/store/project';
import { useNavigationStore } from '@/lib/store/navigation';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { useSettingsStore } from '@/lib/store/settings';
import { useStreamingFetch } from '@/lib/hooks/use-streaming-fetch';
import { assembleLayerContext } from '@/lib/generation/context-assembler';
import { parseReviewOutput, isReviewPassed } from '@/lib/generation/chapter-reviewer';
import { StreamingText } from '@/components/StreamingText';
import { ReviewPanel } from './ReviewPanel';
import { FeedbackInput } from './FeedbackInput';
import type { GeneratedChapter, ChapterPlan } from '@/types';
import { nanoid } from 'nanoid';

export function ChapterGenerationView({ embedded = false }: { embedded?: boolean }) {
  const { projects, activeProjectId, addGeneratedChapter, updateGeneratedChapter } = useProjectStore();
  const setActiveView = useNavigationStore((s) => s.setActiveView);
  const { sourceNovels } = useSourceLibraryStore();
  const getAIConfig = useSettingsStore((s) => s.getAIConfig);

  // 用 useMemo 稳定派生值，避免 useCallback 依赖在每次渲染时变化
  const project = useMemo(() => projects.find((p) => p.id === activeProjectId), [projects, activeProjectId]);
  const projectSources = useMemo(
    () => sourceNovels.filter((s) => project?.sourceNovelIds.includes(s.id)),
    [sourceNovels, project],
  );
  const chapterPlans = useMemo(() => project?.chapterPlans ?? [], [project]);

  const [selectedPlanId, setSelectedPlanId] = useState<string | null>(null);
  const [currentChapter, setCurrentChapter] = useState<GeneratedChapter | null>(null);

  const generateStream = useStreamingFetch();
  const reviewStream = useStreamingFetch();
  const reviseStream = useStreamingFetch();

  const selectedPlan = useMemo(
    () => chapterPlans.find((p) => p.id === selectedPlanId) ?? null,
    [chapterPlans, selectedPlanId],
  );

  // 生成章节正文
  const handleGenerate = useCallback(async (plan: ChapterPlan) => {
    if (!project) return;

    const ai = getAIConfig();
    const ctx = assembleLayerContext(5, projectSources, project, plan.id);

    const result = await generateStream.startFetch('/api/chapter/generate', {
      styleGuide: ctx.styleGuide,
      hierarchyContext: ctx.hierarchyContext,
      chapterTask: ctx.chapterTask,
      previousState: ctx.previousState,
      daoContext: ctx.daoContext,
      rhythmPrescription: ctx.rhythmPrescription,
      apiKey: ai.apiKey,
      model: ai.model,
      baseURL: ai.baseURL,
    });

    if (result) {
      const chapter: GeneratedChapter = {
        id: nanoid(),
        chapterPlanId: plan.id,
        content: result,
        generatedAt: new Date().toISOString(),
        reviewStatus: 'pending_review',
        revisionCount: 0,
        reviews: [],
        humanFeedback: null,
      };
      setCurrentChapter(chapter);
      addGeneratedChapter(project.id, chapter);
    }
  }, [project, projectSources, getAIConfig, generateStream, addGeneratedChapter]);

  // 自动审查
  const handleReview = useCallback(async () => {
    if (!currentChapter || !project) return;
    const chapterId = currentChapter.id;

    const ai = getAIConfig();
    const ctx = assembleLayerContext(5, projectSources, project, currentChapter.chapterPlanId);

    const result = await reviewStream.startFetch('/api/chapter/review', {
      chapterContent: currentChapter.content,
      styleGuide: ctx.styleGuide,
      chapterTask: ctx.chapterTask,
      daoContext: ctx.daoContext,
      apiKey: ai.apiKey,
      model: ai.model,
      baseURL: ai.baseURL,
    });

    if (result) {
      const reviews = parseReviewOutput(result);
      const passed = isReviewPassed(reviews);
      const updated = {
        reviews,
        reviewStatus: passed ? 'approved' as const : 'needs_revision' as const,
      };
      setCurrentChapter((prev) => prev ? { ...prev, ...updated } : null);
      if (project) updateGeneratedChapter(project.id, chapterId, updated);
    }
  }, [currentChapter, project, projectSources, getAIConfig, reviewStream, updateGeneratedChapter]);

  // 修正
  const handleRevise = useCallback(async (feedback?: string) => {
    if (!currentChapter || !project || currentChapter.revisionCount >= 3) return;
    const chapterId = currentChapter.id;

    const ai = getAIConfig();
    const ctx = assembleLayerContext(5, projectSources, project, currentChapter.chapterPlanId);
    const reviewsText = currentChapter.reviews.map((r) =>
      `${r.dimension}: ${r.score}/10 - ${r.issues.join(', ')}`
    ).join('\n');

    const result = await reviseStream.startFetch('/api/chapter/revise', {
      chapterContent: currentChapter.content,
      reviews: reviewsText,
      humanFeedback: feedback ?? null,
      styleGuide: ctx.styleGuide,
      chapterTask: ctx.chapterTask,
      daoContext: ctx.daoContext,
      apiKey: ai.apiKey,
      model: ai.model,
      baseURL: ai.baseURL,
    });

    if (result) {
      const updated = {
        content: result,
        revisionCount: currentChapter.revisionCount + 1,
        reviewStatus: 'pending_review' as const,
        humanFeedback: feedback ?? null,
      };
      setCurrentChapter((prev) => prev ? { ...prev, ...updated } : null);
      updateGeneratedChapter(project.id, chapterId, updated);
    }
  }, [currentChapter, project, projectSources, getAIConfig, reviseStream, updateGeneratedChapter]);

  if (!project) {
    return (
      <div className="flex-1 flex items-center justify-center text-muted-foreground">
        未选择项目
      </div>
    );
  }

  if (chapterPlans.length === 0) {
    return (
      <div className="flex-1 flex flex-col items-center justify-center text-muted-foreground p-6">
        <p className="text-sm">尚未生成章节计划</p>
        {!embedded && (
          <button
            onClick={() => setActiveView('layer-generation')}
            className="mt-3 px-4 py-2 rounded text-sm bg-primary text-primary-foreground hover:bg-primary/90 transition-colors"
          >
            前往层级规划
          </button>
        )}
      </div>
    );
  }

  return (
    <div className="flex-1 flex overflow-hidden">
      {/* 左侧：章节列表 */}
      <div className="w-56 border-r border-border overflow-y-auto p-3">
        <div className="flex items-center gap-2 mb-3">
          {!embedded && (
            <button
              onClick={() => setActiveView('layer-generation')}
              className="p-1 rounded hover:bg-accent transition-colors"
            >
              <ArrowLeft className="w-3 h-3" />
            </button>
          )}
          <h3 className="text-xs font-medium text-muted-foreground uppercase tracking-wider">章节计划</h3>
        </div>

        {chapterPlans.map((plan) => {
          const chapter = project.chapters.find((c) => c.chapterPlanId === plan.id);
          const isSelected = selectedPlanId === plan.id;

          return (
            <button
              key={plan.id}
              onClick={() => { setSelectedPlanId(plan.id); setCurrentChapter(chapter ?? null); }}
              className={`
                w-full text-left px-2 py-1.5 rounded text-xs mb-0.5 transition-colors
                ${isSelected ? 'bg-accent text-foreground font-medium' : 'text-muted-foreground hover:bg-accent/50'}
              `}
            >
              <span>{plan.title}</span>
              {chapter && (
                <span className={`ml-1 ${chapter.reviewStatus === 'approved' ? 'text-green-500' : 'text-muted-foreground'}`}>
                  {chapter.reviewStatus === 'approved' ? '✓' : chapter.revisionCount > 0 ? `改${chapter.revisionCount}` : '○'}
                </span>
              )}
            </button>
          );
        })}
      </div>

      {/* 右侧：章节内容 + 审查 */}
      <div className="flex-1 overflow-y-auto p-6 space-y-4">
        {selectedPlan && (
          <>
            <div className="flex items-center justify-between">
              <h2 className="text-lg font-bold text-foreground">{selectedPlan.title}</h2>
              {!currentChapter && (
                <button
                  onClick={() => handleGenerate(selectedPlan)}
                  disabled={generateStream.isStreaming}
                  className="flex items-center gap-2 px-4 py-2 rounded-lg bg-primary text-primary-foreground text-sm hover:bg-primary/90 disabled:opacity-50 transition-colors"
                >
                  {generateStream.isStreaming ? <Loader2 className="w-4 h-4 animate-spin" /> : <Play className="w-4 h-4" />}
                  生成章节
                </button>
              )}
            </div>

            {/* 计划内容 */}
            <div className="p-3 rounded bg-muted/50 text-xs text-muted-foreground">
              {selectedPlan.content}
            </div>

            {/* 生成结果 */}
            {(generateStream.streamContent || currentChapter) && (
              <div className="rounded-lg border border-border p-4">
                <h3 className="text-sm font-medium text-foreground mb-2">章节正文</h3>
                {generateStream.isStreaming ? (
                  <StreamingText content={generateStream.streamContent} isStreaming={true} />
                ) : (
                  <div className="text-sm text-foreground whitespace-pre-wrap leading-relaxed">
                    {currentChapter?.content}
                  </div>
                )}
              </div>
            )}

            {/* 审查按钮 */}
            {currentChapter && currentChapter.reviewStatus === 'pending_review' && !reviewStream.isStreaming && (
              <button
                onClick={handleReview}
                className="px-4 py-2 rounded-lg bg-accent text-foreground text-sm hover:bg-accent/80 transition-colors"
              >
                开始审查
              </button>
            )}

            {/* 审查中 */}
            {reviewStream.isStreaming && (
              <div className="flex items-center gap-2 text-sm text-muted-foreground">
                <Loader2 className="w-4 h-4 animate-spin" />
                审查中...
              </div>
            )}

            {/* 审查结果 */}
            {currentChapter?.reviews && currentChapter.reviews.length > 0 && (
              <ReviewPanel
                reviews={currentChapter.reviews}
                onRevise={() => handleRevise()}
                canRevise={currentChapter.revisionCount < 3}
                revisionCount={currentChapter.revisionCount}
              />
            )}

            {/* 修正中 */}
            {reviseStream.isStreaming && (
              <div className="rounded-lg border border-border p-4">
                <h3 className="text-sm font-medium text-foreground mb-2">修正中...</h3>
                <StreamingText content={reviseStream.streamContent} isStreaming={true} />
              </div>
            )}

            {/* 人工反馈 */}
            {currentChapter && currentChapter.reviewStatus === 'needs_revision' && currentChapter.revisionCount < 3 && (
              <FeedbackInput
                onSubmit={(feedback) => handleRevise(feedback)}
                disabled={reviseStream.isStreaming}
              />
            )}
          </>
        )}

        {!selectedPlan && (
          <div className="flex items-center justify-center h-64 text-muted-foreground text-sm">
            选择左侧章节计划开始生成
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/lib/ai/prompts/index.ts">
// ============================================
// 提示词统一导出
// ============================================

// 素材库处理 — 函数
export { buildSlicingMessages } from './source-slicing';
export { buildEntityClassificationMessages } from './entity-classification';
export { buildStyleExtractionMessages, buildStyleSupplementMessages } from './style-extraction';
export { buildPlotExtractionMessages, buildPlotSupplementMessages } from './plot-extraction';
export { buildCharacterDynamicsExtractionMessages, buildCharacterDynamicsSupplementMessages } from './character-dynamics-extraction';
export { buildReaderExperienceExtractionMessages, buildReaderExperienceSupplementMessages } from './reader-experience-extraction';
export { buildNarrativeConstraintsExtractionMessages, buildNarrativeConstraintsSupplementMessages } from './narrative-constraints-extraction';
export { buildDnaCompressionMessages } from './dna-compression';
export { buildSampleSelectionMessages } from './sample-selection';

// 素材库处理 — 默认提示词
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_SLICING } from './source-slicing';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_ENTITY_CLASSIFICATION } from './entity-classification';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_STYLE, DEFAULT_SUPPLEMENT_PROMPT as DEFAULT_STYLE_SUPPLEMENT } from './style-extraction';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_PLOT, DEFAULT_SUPPLEMENT_PROMPT as DEFAULT_PLOT_SUPPLEMENT } from './plot-extraction';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHARACTER_DYNAMICS, DEFAULT_SUPPLEMENT_PROMPT as DEFAULT_CHARACTER_DYNAMICS_SUPPLEMENT } from './character-dynamics-extraction';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_READER_EXPERIENCE, DEFAULT_SUPPLEMENT_PROMPT as DEFAULT_READER_EXPERIENCE_SUPPLEMENT } from './reader-experience-extraction';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_NARRATIVE_CONSTRAINTS, DEFAULT_SUPPLEMENT_PROMPT as DEFAULT_NARRATIVE_CONSTRAINTS_SUPPLEMENT } from './narrative-constraints-extraction';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_DNA_COMPRESSION } from './dna-compression';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_SAMPLES } from './sample-selection';

// Layer 1: 全书大纲
export { buildOutlineGenerationMessages } from './outline-generation';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_OUTLINE } from './outline-generation';

// Layer 2: 阶段规划
export { buildPhaseFrameworkMessages } from './phase-framework';
export { buildPhaseDetailMessages } from './phase-detail';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_PHASE_FRAMEWORK } from './phase-framework';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_PHASE_DETAIL } from './phase-detail';

// Layer 3: 分卷
export { buildVolumeFrameworkMessages } from './volume-framework';
export { buildVolumeDetailMessages } from './volume-detail';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_VOLUME_FRAMEWORK } from './volume-framework';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_VOLUME_DETAIL } from './volume-detail';

// Layer 4: 章节集合
export { buildChapterSetFrameworkMessages } from './chapter-set-framework';
export { buildChapterSetDetailMessages } from './chapter-set-detail';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_SET_FRAMEWORK } from './chapter-set-framework';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_SET_DETAIL } from './chapter-set-detail';

// Layer 5: 每章计划
export { buildChapterPlanFrameworkMessages } from './chapter-plan-framework';
export { buildChapterPlanDetailMessages } from './chapter-plan-detail';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_PLAN_FRAMEWORK } from './chapter-plan-framework';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_PLAN_DETAIL } from './chapter-plan-detail';

// 章节生成 + 审查
export { buildChapterWritingMessages } from './chapter-writing';
export { buildChapterReviewMessages } from './chapter-review';
export { buildChapterRevisionMessages } from './chapter-revision';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_WRITING } from './chapter-writing';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_REVIEW } from './chapter-review';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_CHAPTER_REVISION } from './chapter-revision';

// 道/气 新模块
export { buildExperienceAnnotationMessages, READER_PERSONAS } from './experience-annotation';
export { buildAblationTestingMessages } from './ablation-testing';
export { buildTensionTrackingMessages } from './tension-tracking';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_EXPERIENCE_ANNOTATION } from './experience-annotation';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_ABLATION_TESTING } from './ablation-testing';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_TENSION_TRACKING } from './tension-tracking';

// 事件图谱
export { buildEventExtractionMessages, buildEventAlignmentMessages } from './event-extraction';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_EVENT_EXTRACTION } from './event-extraction';
export { DEFAULT_ALIGNMENT_PROMPT as DEFAULT_EVENT_ALIGNMENT } from './event-extraction';

// 统一管线 — 公共约束
export { ENGINEERING_ANALYSIS_CONSTRAINTS, withEngineeringConstraints } from './pipeline-common';

// 统一管线 — Step 5/6/7
export { buildDeepAnalysisMessages } from './deep-analysis';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_DEEP_ANALYSIS } from './deep-analysis';
export { buildSummaryMessages } from './summary';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_SUMMARY } from './summary';
export { buildDnaQualitativeMessages } from './dna-generation';
export { DEFAULT_SYSTEM_PROMPT as DEFAULT_DNA_GENERATION } from './dna-generation';
</file>

<file path="src/lib/file-parser.ts">
// ============================================
// TXT 文件解析
// ============================================

import { nanoid } from 'nanoid';
import type { ParsedNovel, ImportConfig, CleaningConfig } from '@/types';
import { cleanNovelText } from './text-cleaner';
import { chunkText, DEFAULT_MAX_CHUNK_SIZE } from './chunker';

// ---- 默认配置（保留导出用于 store 迁移兼容） ----

const STANDARD_CLEANING_CONFIG: CleaningConfig = {
  preset: 'standard',
  enabledSteps: [],
};

export const DEFAULT_CLEANING_CONFIG: CleaningConfig = STANDARD_CLEANING_CONFIG;

export const DEFAULT_IMPORT_CONFIG: ImportConfig = {
  cleaning: STANDARD_CLEANING_CONFIG,
  maxChunkSize: DEFAULT_MAX_CHUNK_SIZE,
};

// ---- 文件解析 ----

/**
 * 解析上传的 TXT 文件（固定使用 standard 清洗预设）
 */
export function parseTxtFile(file: File): Promise<ParsedNovel> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = (e) => {
      const rawText = e.target?.result as string;
      if (!rawText) {
        reject(new Error('文件读取失败'));
        return;
      }

      const cleaned = cleanNovelText(rawText, STANDARD_CLEANING_CONFIG);
      const title = file.name.replace(/\.txt$/i, '');
      const id = nanoid();
      const chunks = chunkText(id, cleaned, DEFAULT_MAX_CHUNK_SIZE);

      resolve({
        id,
        title,
        totalChars: cleaned.length,
        fullText: cleaned,
        chunks,
        rawText,
        importConfig: null,
      });
    };
    reader.onerror = () => reject(new Error('文件读取失败'));
    reader.readAsText(file, 'utf-8');
  });
}

/** 格式化字数显示 */
export function formatCharCount(count: number): string {
  if (count >= 10000) {
    return `${(count / 10000).toFixed(1)}万字`;
  }
  return `${count}字`;
}
</file>

<file path="src/lib/sync/idb-helpers.ts">
// ============================================
// IndexedDB 辅助 — 工作缓存（非主存储）
// ============================================

const DB_NAME = 'ainovr-sync';
const DB_VERSION = 4;
const NOVEL_STORE = 'novels';
const CACHE_STORE = 'cache';

function openDB(): Promise<IDBDatabase> {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);
    req.onupgradeneeded = () => {
      const db = req.result;
      // 清理旧版 handles 存储
      if (db.objectStoreNames.contains('handles')) {
        db.deleteObjectStore('handles');
      }
      // 保留 novels store（兼容旧数据）
      if (!db.objectStoreNames.contains(NOVEL_STORE)) {
        const store = db.createObjectStore(NOVEL_STORE, { keyPath: 'id' });
        store.createIndex('title', 'title');
      }
      // 新增 cache store（临时工作数据）
      if (!db.objectStoreNames.contains(CACHE_STORE)) {
        db.createObjectStore(CACHE_STORE, { keyPath: 'key' });
      }
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}

// ---- 旧版小说持久化（保留兼容） ----

/** 保存一本小说到 IndexedDB */
export async function saveNovel(novel: unknown): Promise<void> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(NOVEL_STORE, 'readwrite');
    tx.objectStore(NOVEL_STORE).put(novel);
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}

/** 读取所有已保存的小说 */
export async function loadAllNovels(): Promise<unknown[]> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(NOVEL_STORE, 'readonly');
    const req = tx.objectStore(NOVEL_STORE).getAll();
    req.onsuccess = () => resolve(req.result ?? []);
    req.onerror = () => reject(req.error);
  });
}

/** 删除一本小说 */
export async function removeNovel(id: string): Promise<void> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(NOVEL_STORE, 'readwrite');
    tx.objectStore(NOVEL_STORE).delete(id);
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}

/** 清空所有小说 */
export async function clearAllNovels(): Promise<void> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(NOVEL_STORE, 'readwrite');
    tx.objectStore(NOVEL_STORE).clear();
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}

// ---- 通用缓存 ----

/** 写入缓存 */
export async function setCache(key: string, value: unknown): Promise<void> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(CACHE_STORE, 'readwrite');
    tx.objectStore(CACHE_STORE).put({ key, value });
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}

/** 读取缓存 */
export async function getCache<T = unknown>(key: string): Promise<T | null> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(CACHE_STORE, 'readonly');
    const req = tx.objectStore(CACHE_STORE).get(key);
    req.onsuccess = () => resolve(req.result?.value ?? null);
    req.onerror = () => reject(req.error);
  });
}

/** 删除缓存 */
export async function deleteCache(key: string): Promise<void> {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(CACHE_STORE, 'readwrite');
    tx.objectStore(CACHE_STORE).delete(key);
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}
</file>

<file path="src/components/project/WritingProjectView.tsx">
// ============================================
// 写作项目视图 — 项目管理 + 内嵌层级规划/章节生成
// ============================================

'use client';

import { useState, useEffect } from 'react';
import { Plus, Trash2, FolderOpen, ArrowLeft } from 'lucide-react';
import { toast } from 'sonner';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { useProjectStore } from '@/lib/store/project';
import { SourceRoleSelector } from './SourceRoleSelector';
import { LayerGenerationView } from '@/components/generation/LayerGenerationView';
import { ChapterGenerationView } from '@/components/chapter/ChapterGenerationView';
import type { SourceRole, WritingProject } from '@/types';
import { nanoid } from 'nanoid';

export function WritingProjectView() {
  const { sourceNovels } = useSourceLibraryStore();
  const { projects, loadProjects, addProject, removeProject, activeProjectId, setActiveProjectId } = useProjectStore();

  const [title, setTitle] = useState('');
  const [selectedSourceIds, setSelectedSourceIds] = useState<string[]>([]);
  const [roles, setRoles] = useState<Record<string, SourceRole['role']>>({});
  const [activeTab, setActiveTab] = useState<'settings' | 'layers' | 'chapters'>('settings');

  useEffect(() => {
    loadProjects();
  }, [loadProjects]);

  const readyNovels = sourceNovels.filter((n) => n.status === 'ready');
  const activeProject = projects.find((p) => p.id === activeProjectId);

  // ── 项目已打开：内嵌标签页模式 ──

  if (activeProject) {
    return (
      <div className="flex-1 flex flex-col overflow-hidden">
        {/* 项目头部 */}
        <div className="flex items-center gap-3 px-6 py-4 border-b border-border">
          <button
            onClick={() => setActiveProjectId(null)}
            className="p-1.5 rounded hover:bg-accent transition-colors"
          >
            <ArrowLeft className="w-4 h-4" />
          </button>
          <h1 className="text-xl font-bold text-foreground">{activeProject.title}</h1>
          <span className="text-xs text-muted-foreground">
            层级 {activeProject.currentLayer}/5 · {activeProject.chapters?.length ?? 0} 章
          </span>
        </div>

        {/* 标签页 */}
        <div className="flex gap-1 px-6 border-b border-border">
          <button
            onClick={() => setActiveTab('settings')}
            className={`px-4 py-2.5 text-sm font-medium transition-colors relative
              ${activeTab === 'settings' ? 'text-primary' : 'text-muted-foreground hover:text-foreground'}
            `}
          >
            项目设置
            {activeTab === 'settings' && <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-primary" />}
          </button>
          <button
            onClick={() => setActiveTab('layers')}
            className={`px-4 py-2.5 text-sm font-medium transition-colors relative
              ${activeTab === 'layers' ? 'text-primary' : 'text-muted-foreground hover:text-foreground'}
            `}
          >
            层级规划
            {activeTab === 'layers' && <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-primary" />}
          </button>
          <button
            onClick={() => setActiveTab('chapters')}
            className={`px-4 py-2.5 text-sm font-medium transition-colors relative
              ${activeTab === 'chapters' ? 'text-primary' : 'text-muted-foreground hover:text-foreground'}
            `}
          >
            章节生成
            {activeTab === 'chapters' && <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-primary" />}
          </button>
        </div>

        {/* 标签页内容 */}
        <div className="flex-1 min-h-0 flex flex-col overflow-hidden">
          {activeTab === 'settings' && (
            <ProjectSettings
              project={activeProject}
              onClose={() => setActiveProjectId(null)}
              onDelete={async (id) => {
                await fetch('/api/project/delete', {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify({ id }),
                });
                removeProject(id);
                toast.success('项目已删除');
              }}
            />
          )}
          {activeTab === 'layers' && (
            <LayerGenerationView embedded key={activeProjectId} />
          )}
          {activeTab === 'chapters' && (
            <ChapterGenerationView embedded key={activeProjectId} />
          )}
        </div>
      </div>
    );
  }

  // ── 项目未打开：列表 + 创建表单 ──

  const toggleSource = (id: string) => {
    setSelectedSourceIds((prev) =>
      prev.includes(id) ? prev.filter((s) => s !== id) : [...prev, id]
    );
  };

  const handleCreate = async () => {
    if (!title.trim()) {
      toast.error('请输入项目名称');
      return;
    }

    const id = nanoid();
    const sourceRoles: SourceRole[] = selectedSourceIds.map((sid) => ({
      sourceNovelId: sid,
      role: roles[sid] ?? 'style_and_plot',
    }));

    const res = await fetch('/api/project/create', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ id, title: title.trim(), sourceNovelIds: selectedSourceIds, sourceRoles }),
    });

    if (res.ok) {
      const { project } = await res.json();
      addProject(project);
      setTitle('');
      setSelectedSourceIds([]);
      setRoles({});
      toast.success(`项目「${title.trim()}」已创建`);
    } else {
      toast.error('创建项目失败');
    }
  };

  const handleOpenProject = (project: WritingProject) => {
    setActiveProjectId(project.id);
    setActiveTab('layers');
  };

  return (
    <div className="flex-1 flex flex-col overflow-hidden p-6">
      <h1 className="text-xl font-bold text-foreground mb-6">写作项目</h1>

      {/* 创建项目表单 */}
      <div className="mb-8 p-4 rounded-lg border border-border bg-card">
        <h2 className="text-sm font-medium text-foreground mb-3">新建项目</h2>

        <input
          type="text"
          value={title}
          onChange={(e) => setTitle(e.target.value)}
          placeholder="项目名称"
          className="w-full px-3 py-2 rounded-md border border-border bg-background text-sm mb-4"
        />

        {readyNovels.length === 0 ? (
          <p className="text-xs text-muted-foreground">暂无已处理的源小说，请先到素材库处理小说</p>
        ) : (
          <div className="space-y-2 mb-4">
            {readyNovels.map((novel) => (
              <div key={novel.id} className="flex items-center gap-2">
                <input
                  type="checkbox"
                  checked={selectedSourceIds.includes(novel.id)}
                  onChange={() => toggleSource(novel.id)}
                  className="rounded"
                />
                {selectedSourceIds.includes(novel.id) ? (
                  <SourceRoleSelector
                    sourceTitle={novel.title}
                    role={roles[novel.id] ?? 'style_and_plot'}
                    onChange={(r) => setRoles((prev) => ({ ...prev, [novel.id]: r }))}
                  />
                ) : (
                  <span className="text-sm text-muted-foreground">《{novel.title}》</span>
                )}
              </div>
            ))}
          </div>
        )}

        <button
          onClick={handleCreate}
          disabled={!title.trim()}
          className="flex items-center gap-2 px-4 py-2 rounded-lg bg-primary text-primary-foreground text-sm hover:bg-primary/90 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
        >
          <Plus className="w-4 h-4" />
          创建项目
        </button>
      </div>

      {/* 项目列表 */}
      <h2 className="text-sm font-medium text-foreground mb-3">已有项目</h2>
      <div className="flex-1 overflow-y-auto space-y-2">
        {projects.length === 0 ? (
          <p className="text-xs text-muted-foreground text-center py-8">暂无项目</p>
        ) : (
          projects.map((project) => (
            <div
              key={project.id}
              onClick={() => handleOpenProject(project)}
              className="flex items-center gap-3 p-3 rounded-lg border border-border hover:bg-accent/50 transition-colors cursor-pointer"
            >
              <FolderOpen className="w-4 h-4 text-primary shrink-0" />
              <div className="flex-1 min-w-0">
                <p className="text-sm font-medium text-foreground truncate">{project.title}</p>
                <p className="text-xs text-muted-foreground">
                  层级 {project.currentLayer}/5 · {project.chapters?.length ?? 0} 章
                </p>
              </div>
              <button
                onClick={async (e) => {
                  e.stopPropagation();
                  await fetch('/api/project/delete', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id: project.id }),
                  });
                  removeProject(project.id);
                  toast.success('项目已删除');
                }}
                className="p-1 rounded hover:bg-destructive/10 text-muted-foreground hover:text-destructive transition-colors"
              >
                <Trash2 className="w-4 h-4" />
              </button>
            </div>
          ))
        )}
      </div>
    </div>
  );
}

// ── 项目设置标签页 ──

function ProjectSettings({
  project,
  onClose,
  onDelete,
}: {
  project: WritingProject;
  onClose: () => void;
  onDelete: (id: string) => void;
}) {
  const sourceNovels = useSourceLibraryStore((s) => s.sourceNovels);
  const projectSources = sourceNovels.filter((s) => project.sourceNovelIds.includes(s.id));

  return (
    <div className="flex-1 overflow-y-auto p-6 space-y-6">
      {/* 基本信息 */}
      <div className="p-4 rounded-lg border border-border bg-card">
        <h3 className="text-sm font-medium text-foreground mb-3">项目信息</h3>
        <div className="space-y-2 text-sm">
          <div className="flex items-center gap-2">
            <span className="text-muted-foreground w-20">项目名称</span>
            <span className="text-foreground">{project.title}</span>
          </div>
          <div className="flex items-center gap-2">
            <span className="text-muted-foreground w-20">创建时间</span>
            <span className="text-foreground">{new Date(project.createdAt).toLocaleDateString('zh-CN')}</span>
          </div>
          <div className="flex items-center gap-2">
            <span className="text-muted-foreground w-20">当前层级</span>
            <span className="text-foreground">{project.currentLayer}/5</span>
          </div>
          <div className="flex items-center gap-2">
            <span className="text-muted-foreground w-20">已生成章节</span>
            <span className="text-foreground">{project.chapters?.length ?? 0} 章</span>
          </div>
        </div>
      </div>

      {/* 关联的源小说 */}
      <div className="p-4 rounded-lg border border-border bg-card">
        <h3 className="text-sm font-medium text-foreground mb-3">关联源小说</h3>
        {projectSources.length === 0 ? (
          <p className="text-xs text-muted-foreground">无关联源小说</p>
        ) : (
          <div className="space-y-2">
            {projectSources.map((novel) => {
              const role = project.sourceRoles.find((r) => r.sourceNovelId === novel.id);
              const roleLabel = role?.role === 'style' ? '仅文风' : role?.role === 'plot' ? '仅情节' : '文风+情节';
              return (
                <div key={novel.id} className="flex items-center gap-2 text-sm">
                  <span className="text-foreground">《{novel.title}》</span>
                  <span className="text-xs px-1.5 py-0.5 rounded bg-primary/10 text-primary">{roleLabel}</span>
                </div>
              );
            })}
          </div>
        )}
      </div>

      {/* 危险操作 */}
      <div className="p-4 rounded-lg border border-destructive/20 bg-destructive/5">
        <h3 className="text-sm font-medium text-destructive mb-3">危险操作</h3>
        <div className="flex gap-3">
          <button
            onClick={() => onDelete(project.id)}
            className="px-4 py-2 rounded-lg text-sm border border-destructive/30 text-destructive hover:bg-destructive/10 transition-colors"
          >
            删除项目
          </button>
          <button
            onClick={onClose}
            className="px-4 py-2 rounded-lg text-sm border border-border text-muted-foreground hover:bg-accent transition-colors"
          >
            关闭项目
          </button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="src/components/source/SourceNovelDetailView.tsx">
// ============================================
// 源小说详情页 — 处理 + 结果查看（统一 7 步管线）
// ============================================

'use client';

import { useState, useEffect } from 'react';
import { ArrowLeft, Play, Loader2, XCircle, AlertTriangle, RotateCcw, Dna } from 'lucide-react';
import { toast } from 'sonner';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { useSourceProcessingStore } from '@/lib/store/source-processing';
import { useSettingsStore } from '@/lib/store/settings';
import { useNavigationStore } from '@/lib/store/navigation';
import { MarkdownViewer } from './MarkdownViewer';
import { StreamingText } from '@/components/StreamingText';

const STEPS = [
  { key: 'small-slice', label: '小切片' },
  { key: 'event-extract', label: '事件提取' },
  { key: 'event-align', label: '图谱对齐' },
  { key: 'large-slice', label: '大切片' },
  { key: 'deep-analysis', label: '深度分析' },
  { key: 'summary', label: '汇总报告' },
  { key: 'dna', label: 'DNA 压缩' },
];

// 新管线结果标签
type AnalysisTab = 'generation-rules-dna' | 'summary-report' | 'event-graph' | 'slice-analyses' | 'legacy-dna' | 'legacy-style' | 'legacy-narrative' | 'legacy-character' | 'legacy-reader' | 'legacy-constraints';

const TABS_NEW: { key: AnalysisTab; label: string; icon?: string }[] = [
  { key: 'generation-rules-dna', label: '🧬 生成规则 DNA', icon: '🧬' },
  { key: 'summary-report', label: '📊 汇总报告' },
  { key: 'event-graph', label: '🔗 事件图谱' },
  { key: 'slice-analyses', label: '🔬 切片分析' },
];

const TABS_LEGACY: { key: AnalysisTab; label: string }[] = [
  { key: 'legacy-dna', label: '旧 DNA' },
  { key: 'legacy-style', label: '旧文风' },
  { key: 'legacy-narrative', label: '旧叙事' },
  { key: 'legacy-character', label: '旧角色' },
  { key: 'legacy-reader', label: '旧体验' },
  { key: 'legacy-constraints', label: '旧约束' },
];

/** 将 JSON 对象格式化为可读的缩进文本 */
function formatJson(obj: unknown): string {
  if (!obj) return '';
  if (typeof obj === 'string') return obj;
  try {
    return JSON.stringify(obj, null, 2);
  } catch {
    return String(obj);
  }
}

export function SourceNovelDetailView() {
  const { sourceNovels, activeSourceId, setActiveSourceId } = useSourceLibraryStore();
  const setActiveView = useNavigationStore((s) => s.setActiveView);
  const getAIConfig = useSettingsStore((s) => s.getAIConfig);
  const processingStore = useSourceProcessingStore();

  const novel = sourceNovels.find((n) => n.id === activeSourceId);
  const [activeTab, setActiveTab] = useState<AnalysisTab>('generation-rules-dna');
  const [rawText, setRawText] = useState<string | null>(null);

  // 判断当前小说是否正在处理
  const isThisProcessing = processingStore.processingNovelId === activeSourceId;
  const isAnyProcessing = processingStore.isRunningAll;

  // 加载完整数据
  useEffect(() => {
    if (!activeSourceId) return;
    fetch(`/api/library/get?id=${activeSourceId}`)
      .then((res) => res.json())
      .then((data) => {
        setRawText(data.rawText ?? null);
        const updates: Record<string, unknown> = {};
        // 旧管线数据（兼容）
        if (data.styleProfile) updates.styleProfile = data.styleProfile;
        if (data.plotReport) updates.plotReport = data.plotReport;
        if (data.characterDynamics) updates.characterDynamics = data.characterDynamics;
        if (data.readerExperience) updates.readerExperience = data.readerExperience;
        if (data.narrativeConstraints) updates.narrativeConstraints = data.narrativeConstraints;
        if (data.novelDna) updates.novelDna = data.novelDna;
        // 新管线数据
        if (data.generationRulesDna) updates.generationRulesDna = data.generationRulesDna;
        if (data.summaryReport) updates.unifiedSummaryReport = data.summaryReport;
        if (data.eventGraph) updates.eventGraph = data.eventGraph;
        if (data.status) updates.status = data.status;
        if (Object.keys(updates).length > 0) {
          useSourceLibraryStore.getState().updateSourceNovel(activeSourceId, updates);
        }
      })
      .catch(console.error);
  }, [activeSourceId]);

  if (!novel) {
    return (
      <div className="flex-1 flex items-center justify-center text-muted-foreground">
        未选择源小说
      </div>
    );
  }

  const handleBack = () => {
    setActiveSourceId(null);
    setActiveView('source-library');
  };

  const handleStartProcessing = () => {
    if (!rawText || isAnyProcessing) return;
    processingStore.startProcessing(novel.id, rawText, getAIConfig());
  };

  const handleCancel = () => {
    processingStore.cancelProcessing();
  };

  const handleReanalyze = async () => {
    if (!novel || isAnyProcessing) return;

    const confirmed = confirm('重新分析将覆盖现有分析结果，是否继续？');
    if (!confirmed) return;

    // 加载原始文本（如果尚未加载）
    let text = rawText;
    if (!text) {
      const res = await fetch(`/api/library/get?id=${novel.id}`);
      const data = await res.json();
      text = data.rawText ?? null;
    }

    if (!text) {
      toast.error('无法加载原始文本，请重新上传小说');
      return;
    }

    processingStore.startProcessing(novel.id, text, getAIConfig());
  };

  const isReady = novel.status === 'ready';
  const isRaw = novel.status === 'raw';

  // 检查是否有新管线数据
  const hasNewDna = !!novel.generationRulesDna;
  const hasSummaryReport = !!novel.unifiedSummaryReport;
  const hasEventGraph = !!novel.eventGraph;
  const hasNewResults = hasNewDna || hasSummaryReport || hasEventGraph;

  // 检查是否有旧管线数据
  const hasLegacy = !!(novel.styleProfile || novel.plotReport || novel.characterDynamics || novel.readerExperience || novel.narrativeConstraints || novel.novelDna);
  const hasAnyResult = hasNewResults || hasLegacy;

  // 获取当前步骤的流式内容
  const currentStep = processingStore.currentStep;
  const currentStreamContent = isThisProcessing && currentStep >= 0
    ? processingStore.streamContents[currentStep]
    : '';

  // 获取当前 tab 对应的内容
  const getTabContent = (tabKey: AnalysisTab): string | null => {
    switch (tabKey) {
      // 新管线
      case 'generation-rules-dna': return novel.generationRulesDna ? formatJson(novel.generationRulesDna) : null;
      case 'summary-report': return novel.unifiedSummaryReport ? formatJson(novel.unifiedSummaryReport) : null;
      case 'event-graph': return novel.eventGraph ? formatJson(novel.eventGraph) : null;
      case 'slice-analyses': return null; // sliceAnalyses 在文件中，不在 novel 对象上
      // 旧管线
      case 'legacy-dna': return novel.novelDna;
      case 'legacy-style': return novel.styleProfile;
      case 'legacy-narrative': return novel.plotReport;
      case 'legacy-character': return novel.characterDynamics;
      case 'legacy-reader': return novel.readerExperience;
      case 'legacy-constraints': return novel.narrativeConstraints;
    }
  };

  // 判断 tab 是否有内容
  const hasTabContent = (tabKey: AnalysisTab): boolean => {
    switch (tabKey) {
      case 'generation-rules-dna': return hasNewDna;
      case 'summary-report': return hasSummaryReport;
      case 'event-graph': return hasEventGraph;
      case 'slice-analyses': return false;
      case 'legacy-dna': return !!novel.novelDna;
      case 'legacy-style': return !!novel.styleProfile;
      case 'legacy-narrative': return !!novel.plotReport;
      case 'legacy-character': return !!novel.characterDynamics;
      case 'legacy-reader': return !!novel.readerExperience;
      case 'legacy-constraints': return !!novel.narrativeConstraints;
    }
  };

  return (
    <div className="flex-1 flex flex-col overflow-hidden p-6">
      {/* 头部 */}
      <div className="flex items-center justify-between mb-6">
        <div className="flex items-center gap-3">
          <button
            onClick={handleBack}
            className="p-1.5 rounded hover:bg-accent transition-colors"
          >
            <ArrowLeft className="w-4 h-4" />
          </button>
          <h1 className="text-xl font-bold text-foreground">《{novel.title}》</h1>
          <span className="text-xs text-muted-foreground">{novel.totalChars.toLocaleString()} 字</span>
          {(novel.novelDna || novel.generationRulesDna) && (
            <span className="flex items-center gap-1 px-2 py-0.5 rounded-full bg-primary/10 text-primary text-xs font-medium">
              <Dna className="w-3 h-3" />
              {hasNewDna ? 'DNA 已提取' : '旧 DNA'}
            </span>
          )}
        </div>

        {/* 操作按钮 */}
        {(isReady || hasAnyResult) && (
          <button
            onClick={handleReanalyze}
            disabled={isAnyProcessing}
            className="flex items-center gap-2 px-4 py-2 rounded-lg border border-border text-sm hover:bg-accent disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
          >
            <RotateCcw className="w-4 h-4" />
            重新分析
          </button>
        )}
        {!isReady && !hasAnyResult && (
          <div className="flex items-center gap-2">
            {isThisProcessing ? (
              <button
                onClick={handleCancel}
                className="flex items-center gap-2 px-4 py-2 rounded-lg bg-destructive/10 text-destructive text-sm hover:bg-destructive/20 transition-colors"
              >
                <XCircle className="w-4 h-4" />
                取消处理
              </button>
            ) : (
              <button
                onClick={handleStartProcessing}
                disabled={isAnyProcessing || !rawText}
                className="flex items-center gap-2 px-4 py-2 rounded-lg bg-primary text-primary-foreground text-sm hover:bg-primary/90 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
              >
                <Play className="w-4 h-4" />
                一键分析
              </button>
            )}
            {!isThisProcessing && processingStore.errorStep !== null && processingStore.completedSteps.length > 0 && (
              <button
                onClick={() => processingStore.resumeProcessing(novel.id)}
                disabled={isAnyProcessing}
                className="flex items-center gap-2 px-4 py-2 rounded-lg border border-primary/30 bg-primary/5 text-primary text-sm hover:bg-primary/10 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
              >
                <Play className="w-4 h-4" />
                恢复处理（已完成 {processingStore.completedSteps.length}/{STEPS.length} 步）
              </button>
            )}
          </div>
        )}
      </div>

      {/* 进度指示器（处理中显示） */}
      {isThisProcessing && currentStep >= 0 && (
        <div className="mb-6 p-4 rounded-lg border border-border bg-card">
          {/* 总进度条 */}
          <div className="mb-3">
            <div className="flex items-center justify-between mb-1.5">
              <span className="text-xs font-medium text-foreground">
                正在分析 · 步骤 {currentStep + 1}/{STEPS.length} · {STEPS[currentStep]?.label}
              </span>
              <span className="text-xs text-primary font-medium">
                {processingStore.progress}%
              </span>
            </div>
            <div className="w-full h-2 rounded-full bg-muted overflow-hidden">
              <div
                className="h-full rounded-full bg-primary transition-all duration-300 ease-out"
                style={{ width: `${processingStore.progress}%` }}
              />
            </div>
          </div>

          {/* 步骤指示 */}
          <div className="flex items-center gap-1 flex-wrap">
            {STEPS.map((step, i) => {
              const isCompleted = processingStore.completedSteps.includes(i);
              const isRunning = currentStep === i;

              return (
                <div key={step.key} className="flex items-center gap-1">
                  <div className="flex items-center gap-1.5">
                    <div
                      className={`
                        w-6 h-6 rounded-full flex items-center justify-center text-xs font-medium transition-colors
                        ${isCompleted ? 'bg-green-500 text-white' : ''}
                        ${isRunning ? 'bg-primary text-primary-foreground animate-pulse' : ''}
                        ${!isCompleted && !isRunning ? 'bg-muted text-muted-foreground' : ''}
                      `}
                    >
                      {isCompleted ? '✓' : isRunning ? <Loader2 className="w-3 h-3 animate-spin" /> : i + 1}
                    </div>
                    <span className={`text-xs ${isRunning ? 'text-foreground font-medium' : isCompleted ? 'text-green-600 dark:text-green-400' : 'text-muted-foreground'}`}>
                      {step.label}
                    </span>
                  </div>
                  {i < STEPS.length - 1 && (
                    <div className={`w-4 h-px ${isCompleted ? 'bg-green-500' : 'bg-border'}`} />
                  )}
                </div>
              );
            })}
          </div>

          {/* 当前步骤流式输出 */}
          {currentStreamContent && (
            <div className="mt-2 p-3 rounded-md bg-muted/50 max-h-60 overflow-y-auto">
              <StreamingText
                content={currentStreamContent}
                isStreaming={currentStep >= 0 && processingStore.isStreaming[currentStep]}
              />
            </div>
          )}
        </div>
      )}

      {/* 处理出错提示 */}
      {isThisProcessing && processingStore.errorStep !== null && processingStore.errorMessage && (
        <div className="mb-6 p-4 rounded-lg border border-destructive/30 bg-destructive/5">
          <div className="flex items-start gap-3">
            <AlertTriangle className="w-5 h-5 text-destructive shrink-0 mt-0.5" />
            <div className="flex-1">
              <h3 className="text-sm font-medium text-destructive mb-1">
                步骤 {processingStore.errorStep + 1}「{STEPS[processingStore.errorStep]?.label}」出错
              </h3>
              <p className="text-xs text-muted-foreground">{processingStore.errorMessage}</p>
              <button
                onClick={() => {
                  processingStore.resetProcessing();
                  handleStartProcessing();
                }}
                className="flex items-center gap-1.5 mt-3 px-3 py-1.5 rounded text-xs bg-primary text-primary-foreground hover:bg-primary/90 transition-colors"
              >
                <RotateCcw className="w-3 h-3" />
                重新处理
              </button>
            </div>
          </div>
        </div>
      )}

      {/* 结果标签页 */}
      {hasAnyResult && (
        <div className="flex-1 flex flex-col overflow-hidden">
          {/* 标签切换 */}
          <div className="flex gap-1 mb-4 border-b border-border pb-0 overflow-x-auto">
            {TABS_NEW.map((tab) => {
              const hasContent = hasTabContent(tab.key);
              return (
                <button
                  key={tab.key}
                  onClick={() => setActiveTab(tab.key)}
                  className={`
                    px-4 py-2 text-sm font-medium transition-colors relative whitespace-nowrap
                    ${activeTab === tab.key
                      ? 'text-primary'
                      : hasContent
                        ? 'text-muted-foreground hover:text-foreground'
                        : 'text-muted-foreground/50 hover:text-muted-foreground'
                    }
                  `}
                >
                  {tab.label}
                  {!hasContent && <span className="ml-1 text-xs opacity-40">—</span>}
                  {activeTab === tab.key && (
                    <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-primary" />
                  )}
                </button>
              );
            })}
            {hasLegacy && (
              <>
                <div className="w-px h-6 bg-border self-center mx-1" />
                {TABS_LEGACY.map((tab) => {
                  const hasContent = hasTabContent(tab.key);
                  if (!hasContent) return null;
                  return (
                    <button
                      key={tab.key}
                      onClick={() => setActiveTab(tab.key)}
                      className={`
                        px-3 py-2 text-xs transition-colors relative whitespace-nowrap opacity-60 hover:opacity-100
                        ${activeTab === tab.key ? 'text-primary opacity-100' : 'text-muted-foreground'}
                      `}
                    >
                      {tab.label}
                      {activeTab === tab.key && (
                        <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-primary" />
                      )}
                    </button>
                  );
                })}
              </>
            )}
          </div>

          {/* 内容区 */}
          <div className="flex-1 overflow-y-auto">
            {(() => {
              const content = getTabContent(activeTab);
              if (!content) {
                return (
                  <div className="flex items-center justify-center h-32 text-muted-foreground text-sm">
                    尚未生成此分析报告
                  </div>
                );
              }

              // 新管线 JSON 结构化展示
              if (activeTab === 'generation-rules-dna' || activeTab === 'summary-report' || activeTab === 'event-graph') {
                return (
                  <div className="p-4 rounded-lg border border-border bg-card">
                    <pre className="text-sm font-mono whitespace-pre-wrap break-words text-foreground leading-relaxed max-h-[calc(100vh-300px)] overflow-y-auto">
                      {content}
                    </pre>
                  </div>
                );
              }

              // 旧管线 Markdown 渲染
              return (
                <div className="p-4 rounded-lg border border-border bg-card">
                  <MarkdownViewer content={content} />
                </div>
              );
            })()}
          </div>
        </div>
      )}

      {/* 未处理且未在处理中的空状态 */}
      {isRaw && !isThisProcessing && !hasAnyResult && (
        <div className="flex-1 flex flex-col items-center justify-center text-muted-foreground">
          <p className="text-sm mb-1">尚未处理此小说</p>
          <p className="text-xs opacity-60">点击右上角「一键分析」开始 7 步逆向工程分析</p>
        </div>
      )}
    </div>
  );
}
</file>

<file path="src/lib/store/source-processing.ts">
// ============================================
// 源小说处理全局 Store — 后台处理，跨页面存活
// v2: 统一 7 步管线（替代旧 basic + dao 双管线）
// ============================================

import { create } from 'zustand';
import { runUnifiedPipeline } from '@/lib/source-processing/unified-pipeline';
import { TOTAL_PIPELINE_STEPS } from '@/lib/source-processing/pipeline-types';
import type { PipelineCheckpoint, UnifiedPipelineCallbacks } from '@/lib/source-processing/pipeline-types';
import { createStreamFetcher, type AIConfig } from '@/lib/stream-fetcher';
import { useSourceLibraryStore } from './source-library';
import { useSettingsStore } from './settings';

function createFilledArray<T>(length: number, value: T): T[] {
  return Array.from({ length }, () => value);
}

export interface SourceProcessingState {
  /** 当前正在处理的小说 ID */
  processingNovelId: string | null;

  /** 7 个步骤的流式内容 */
  streamContents: string[];
  /** 7 个步骤的流式状态 */
  isStreaming: boolean[];
  /** 7 个步骤的错误信息 */
  streamErrors: (string | null)[];

  /** 当前步骤（-1 = idle） */
  currentStep: number;
  /** 已完成的步骤 */
  completedSteps: number[];
  /** 出错的步骤 */
  errorStep: number | null;
  /** 错误信息 */
  errorMessage: string | null;

  /** 总进度 0-100 */
  progress: number;

  /** 是否正在运行一键处理 */
  isRunningAll: boolean;

  // ── 旧版道/气管线状态（保留兼容） ──
  isRunningDao: boolean;
  daoProgress: number;
  daoCurrentStep: string;
  daoError: string | null;

  // Actions
  startProcessing: (novelId: string, rawText: string, aiConfig: AIConfig) => void;
  resumeProcessing: (novelId: string) => void;
  startDaoAnalysis: (novelId: string) => void;
  cancelProcessing: () => void;
  resetProcessing: () => void;

  // 内部更新
  _updateStreamContent: (step: number, content: string) => void;
  _updateIsStreaming: (step: number, streaming: boolean) => void;
  _updateStreamError: (step: number, error: string | null) => void;
  _setProgress: (progress: number) => void;
}

// 模块级 fetcher 用于 abort
const streamFetcher = createStreamFetcher();

export const useSourceProcessingStore = create<SourceProcessingState>()((set, get) => ({
  processingNovelId: null,
  streamContents: createFilledArray(TOTAL_PIPELINE_STEPS, ''),
  isStreaming: createFilledArray(TOTAL_PIPELINE_STEPS, false),
  streamErrors: createFilledArray(TOTAL_PIPELINE_STEPS, null),
  currentStep: -1,
  completedSteps: [],
  errorStep: null,
  errorMessage: null,
  progress: 0,
  isRunningAll: false,
  isRunningDao: false,
  daoProgress: 0,
  daoCurrentStep: '',
  daoError: null,

  _updateStreamContent: (step, content) => {
    set((s) => {
      const arr = [...s.streamContents];
      arr[step] = content;
      return { streamContents: arr };
    });
  },

  _updateIsStreaming: (step, streaming) => {
    set((s) => {
      const arr = [...s.isStreaming];
      arr[step] = streaming;
      return { isStreaming: arr };
    });
  },

  _updateStreamError: (step, error) => {
    set((s) => {
      const arr = [...s.streamErrors];
      arr[step] = error;
      return { streamErrors: arr, errorMessage: error };
    });
  },

  _setProgress: (progress) => {
    set({ progress: Math.min(100, Math.max(0, Math.round(progress))) });
  },

  /** 从头开始处理 */
  startProcessing: (novelId, rawText, aiConfig) => {
    if (get().isRunningAll) return;
    runUnifiedPipelineInternal(novelId, rawText, aiConfig, null, set, get);
  },

  /** 从断点恢复处理（加载已保存的中间产物） */
  resumeProcessing: async (novelId) => {
    if (get().isRunningAll) return;

    const aiConfig = useSettingsStore.getState().getAIConfig();
    const novel = useSourceLibraryStore.getState().sourceNovels.find((n) => n.id === novelId);

    if (novel?.status === 'ready') return;

    // 从服务端加载已保存的中间产物
    const completedSteps: number[] = [];
    let smallSlices: PipelineCheckpoint['smallSlices'] = null;
    let sliceExtractions: PipelineCheckpoint['sliceExtractions'] = null;
    let eventGraph: PipelineCheckpoint['eventGraph'] = null;
    let largeSlices: PipelineCheckpoint['largeSlices'] = null;
    let sliceAnalyses: PipelineCheckpoint['sliceAnalyses'] = null;
    let summaryReport: PipelineCheckpoint['summaryReport'] = null;

    try {
      const res = await fetch(`/api/library/get?id=${novelId}`);
      if (res.ok) {
        const data = await res.json();

        if (data.smallSlices) { smallSlices = data.smallSlices; completedSteps.push(0); }
        if (data.sliceExtractions) { sliceExtractions = data.sliceExtractions; completedSteps.push(1); }
        if (data.eventGraph) { eventGraph = data.eventGraph; completedSteps.push(2); }
        if (data.largeSlices) { largeSlices = data.largeSlices; completedSteps.push(3); }
        if (data.sliceAnalyses) { sliceAnalyses = data.sliceAnalyses; completedSteps.push(4); }
        if (data.summaryReport) { summaryReport = data.summaryReport; completedSteps.push(5); }
        if (data.generationRulesDna) { completedSteps.push(6); }
      }
    } catch (err) {
      console.error('加载断点数据失败，将从头开始:', err);
    }

    const checkpoint: PipelineCheckpoint = {
      novelId,
      currentStep: completedSteps.length,
      completedSteps,
      smallSlices,
      largeSlices,
      sliceExtractions,
      eventGraph,
      sliceAnalyses,
      summaryReport,
    };

    runUnifiedPipelineInternal(novelId, null, aiConfig, checkpoint, set, get);
  },

  /** 道/气分析 — 统一管线已包含，此方法保留兼容但提示已集成 */
  startDaoAnalysis: (novelId: string) => {
    set({ daoError: '道/气分析已集成到统一管线，请使用"一键分析"' });
  },

  cancelProcessing: () => {
    streamFetcher.abort();
    const novelId = get().processingNovelId;
    if (novelId) {
      useSourceLibraryStore.getState().updateSourceNovel(novelId, { status: 'error' });
    }
    set({
      processingNovelId: null,
      isRunningAll: false,
      currentStep: -1,
      isStreaming: createFilledArray(TOTAL_PIPELINE_STEPS, false),
      progress: 0,
      errorMessage: null,
      isRunningDao: false,
      daoProgress: 0,
      daoCurrentStep: '',
      daoError: null,
    });
  },

  resetProcessing: () => {
    streamFetcher.abort();
    set({
      processingNovelId: null,
      streamContents: createFilledArray(TOTAL_PIPELINE_STEPS, ''),
      isStreaming: createFilledArray(TOTAL_PIPELINE_STEPS, false),
      streamErrors: createFilledArray(TOTAL_PIPELINE_STEPS, null),
      currentStep: -1,
      completedSteps: [],
      errorStep: null,
      errorMessage: null,
      isRunningAll: false,
      progress: 0,
      isRunningDao: false,
      daoProgress: 0,
      daoCurrentStep: '',
      daoError: null,
    });
  },
}));

// ---- 内部统一管线执行 ----

function runUnifiedPipelineInternal(
  novelId: string,
  rawText: string | null,
  aiConfig: AIConfig,
  checkpoint: PipelineCheckpoint | null,
  set: (partial: Partial<SourceProcessingState> | ((s: SourceProcessingState) => Partial<SourceProcessingState>)) => void,
  get: () => SourceProcessingState,
) {
  set({
    processingNovelId: novelId,
    streamContents: createFilledArray(TOTAL_PIPELINE_STEPS, ''),
    isStreaming: createFilledArray(TOTAL_PIPELINE_STEPS, false),
    streamErrors: createFilledArray(TOTAL_PIPELINE_STEPS, null),
    currentStep: -1,
    completedSteps: [],
    errorStep: null,
    errorMessage: null,
    progress: 0,
    isRunningAll: true,
  });

  const callbacks: UnifiedPipelineCallbacks = {
    onStepStart: (step, message) => {
      set({ currentStep: step });
      get()._updateStreamContent(step, message);
      get()._updateIsStreaming(step, true);
    },
    onStepComplete: (step) => {
      set((s) => {
        const completed = [...s.completedSteps];
        if (!completed.includes(step)) completed.push(step);
        return { completedSteps: completed };
      });
      get()._updateIsStreaming(step, false);
    },
    onStreamUpdate: (step, content) => {
      get()._updateStreamContent(step, content);
    },
    onStreamError: (step, error) => {
      get()._updateStreamError(step, error);
      get()._updateIsStreaming(step, false);
    },
    onProgress: (progress) => {
      get()._setProgress(progress);
    },
    onSaveStep: async (step, data) => {
      try {
        const res = await fetch('/api/library/save-step', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ id: novelId, step, data }),
        });
        if (!res.ok) {
          const err = await res.json().catch(() => ({ error: `HTTP ${res.status}` }));
          console.error(`保存步骤 ${step} 失败:`, err);
        }
        // 更新源小说状态
        const updates: Record<string, unknown> = {};
        if (step === 6 && data.dna) {
          updates.generationRulesDna = data.dna;
          updates.status = 'ready';
          updates.processedAt = new Date().toISOString();
        }
        if (step === 5 && data.summaryReport) {
          updates.unifiedSummaryReport = data.summaryReport;
        }
        if (Object.keys(updates).length > 0) {
          useSourceLibraryStore.getState().updateSourceNovel(novelId, updates);
        }
      } catch (err) {
        console.error(`保存步骤 ${step} 失败:`, err);
      }
    },
    onUpdateNovel: (updates) => {
      useSourceLibraryStore.getState().updateSourceNovel(novelId, updates);
    },
  };

  (async () => {
    try {
      if (!rawText) {
        // 恢复模式：需要加载原文
        const novel = useSourceLibraryStore.getState().sourceNovels.find(n => n.id === novelId);
        if (!novel) throw new Error('找不到小说数据');
        const res = await fetch(`/api/library/get?id=${novelId}`);
        const data = await res.json();
        rawText = data.rawText;
        if (!rawText) throw new Error('无法加载原文');
      }

      await runUnifiedPipeline(novelId, rawText, aiConfig, checkpoint, callbacks, streamFetcher);
      set({ isRunningAll: false, currentStep: -1, progress: 100, processingNovelId: null });
    } catch (err) {
      const msg = err instanceof Error ? err.message : '处理管线异常';
      console.error('处理管线异常:', err);
      const step = get().currentStep;
      set({
        errorStep: Math.max(step, 0),
        errorMessage: msg,
        isRunningAll: false,
        currentStep: -1,
        processingNovelId: null,
      });
      useSourceLibraryStore.getState().updateSourceNovel(novelId, { status: 'error' });
    }
  })();
}
</file>

<file path="src/components/source/SourceLibraryView.tsx">
// ============================================
// 素材库视图 — 小说管理 + 上传
// ============================================

'use client';

import { useState, useCallback, useEffect } from 'react';
import { Upload } from 'lucide-react';
import { toast } from 'sonner';
import { parseTxtFile } from '@/lib/file-parser';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { useNavigationStore } from '@/lib/store/navigation';
import { SourceNovelCard } from './SourceNovelCard';
import type { SourceNovel } from '@/types';

const MAX_FILE_SIZE = 20 * 1024 * 1024;

export function SourceLibraryView() {
  const { sourceNovels, loadSourceNovels, addSourceNovel, setActiveSourceId } = useSourceLibraryStore();
  const setActiveView = useNavigationStore((s) => s.setActiveView);
  const [isDragging, setIsDragging] = useState(false);
  const [isLoading, setIsLoading] = useState(false);

  useEffect(() => {
    loadSourceNovels();
  }, [loadSourceNovels]);

  const handleUpload = useCallback(async (files: FileList | File[]) => {
    setIsLoading(true);
    try {
      for (const file of Array.from(files)) {
        if (file.size > MAX_FILE_SIZE) {
          toast.error(`"${file.name}" 超过 20MB 限制`);
          continue;
        }
        if (!file.name.endsWith('.txt')) {
          toast.error(`"${file.name}" 不是 TXT 文件`);
          continue;
        }

        const parsed = await parseTxtFile(file);

        const sourceNovel: SourceNovel = {
          id: parsed.id,
          title: parsed.title,
          totalChars: parsed.totalChars,
          importConfig: parsed.importConfig,
          status: 'raw',
          createdAt: new Date().toISOString(),
          processedAt: null,
          memory: null,
          slices: null,
          styleProfile: null,
          plotReport: null,
          characterDynamics: null,
          readerExperience: null,
          narrativeConstraints: null,
          representativeSamples: null,
          evolutionModel: null,
          novelDna: null,
          novelGenome: null,
          novelDnaV2: null,
          experienceAnnotations: null,
          experienceCurve: null,
          ablationResults: null,
          tensionAnalysis: null,
          techniqueSamples: null,
          textChunks: null,
          eventGraph: null,
          generationRulesDna: null,
          unifiedSummaryReport: null,
        };

        const res = await fetch('/api/library/upload', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            id: sourceNovel.id,
            title: sourceNovel.title,
            rawText: parsed.fullText,
            totalChars: sourceNovel.totalChars,
            importConfig: sourceNovel.importConfig,
          }),
        });

        if (res.ok) {
          addSourceNovel(sourceNovel);
          toast.success(`《${sourceNovel.title}》已添加到素材库`);
        } else {
          toast.error(`上传《${sourceNovel.title}》失败`);
        }
      }
    } catch (err) {
      toast.error(`文件处理失败: ${err instanceof Error ? err.message : '未知错误'}`);
    } finally {
      setIsLoading(false);
    }
  }, [addSourceNovel]);

  const handleDelete = useCallback(async (id: string) => {
    try {
      await fetch('/api/library/delete', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id }),
      });
      useSourceLibraryStore.getState().removeSourceNovel(id);
      toast.success('已删除');
    } catch {
      toast.error('删除失败');
    }
  }, []);

  const handleRename = useCallback(async (id: string, newTitle: string) => {
    try {
      const res = await fetch('/api/library/rename', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id, title: newTitle }),
      });
      if (res.ok) {
        useSourceLibraryStore.getState().updateSourceNovel(id, { title: newTitle });
        toast.success('已重命名');
      } else {
        toast.error('重命名失败');
      }
    } catch {
      toast.error('重命名失败');
    }
  }, []);

  const handleOpenDetail = useCallback((id: string) => {
    setActiveSourceId(id);
    setActiveView('source-detail');
  }, [setActiveSourceId, setActiveView]);

  return (
    <div className="flex-1 flex flex-col overflow-hidden p-6">
      {/* 标题 + 标签切换 */}
      <div className="flex items-center justify-between mb-4">
        <h1 className="text-xl font-bold text-foreground">素材库</h1>

        {/* 上传按钮 */}
        <label
          onDrop={(e) => { e.preventDefault(); setIsDragging(false); handleUpload(e.dataTransfer.files); }}
          onDragOver={(e) => { e.preventDefault(); setIsDragging(true); }}
          onDragLeave={() => setIsDragging(false)}
          className={`
            flex items-center gap-2 px-4 py-2 rounded-lg cursor-pointer text-sm transition-colors
            ${isDragging ? 'bg-primary/10 text-primary border border-primary' : 'bg-primary/5 text-primary hover:bg-primary/10'}
            ${isLoading ? 'pointer-events-none opacity-50' : ''}
          `}
        >
          <input
            type="file"
            accept=".txt"
            multiple
            className="hidden"
            onChange={(e) => { if (e.target.files) handleUpload(e.target.files); e.target.value = ''; }}
          />
          <Upload className="w-4 h-4" />
          {isLoading ? '上传中...' : '添加小说'}
        </label>
      </div>

      {/* 小说列表 */}
      <div className="flex-1 overflow-y-auto">
        {sourceNovels.length === 0 ? (
          <div className="flex flex-col items-center justify-center h-64 text-muted-foreground">
            <Upload className="w-10 h-10 mb-3 opacity-30" />
            <p className="text-sm">拖拽或点击上传 .txt 小说文件</p>
            <p className="text-xs mt-1 opacity-60">添加到素材库后可进行分析</p>
          </div>
        ) : (
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
            {sourceNovels.map((novel) => (
              <SourceNovelCard
                key={novel.id}
                novel={novel}
                onClick={() => handleOpenDetail(novel.id)}
                onRename={handleRename}
                onDelete={handleDelete}
              />
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/components/SettingsDialog.tsx">
// ============================================
// API 设置弹窗
// ============================================

'use client';

import { useState } from 'react';
import { useSettingsStore } from '@/lib/store/settings';
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
} from '@/components/ui/dialog';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Button } from '@/components/ui/button';
import { Switch } from '@/components/ui/switch';
import { DEFAULT_MODEL, DEFAULT_BASE_URL } from '@/lib/constants';

interface SettingsDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function SettingsDialog({ open, onOpenChange }: SettingsDialogProps) {
  const storeApiKey = useSettingsStore((s) => s.apiKey);
  const storeModel = useSettingsStore((s) => s.model);
  const storeBaseURL = useSettingsStore((s) => s.baseURL);
  const storeMaxContextTokens = useSettingsStore((s) => s.maxContextTokens);
  const storeThinkingMode = useSettingsStore((s) => s.thinkingMode);
  const setAISettings = useSettingsStore((s) => s.setAISettings);

  // 本地编辑状态
  const [apiKey, setApiKey] = useState('');
  const [model, setModel] = useState('');
  const [baseURL, setBaseURL] = useState('');
  const [maxContextTokens, setMaxContextTokens] = useState(1000000);
  const [thinkingMode, setThinkingMode] = useState(true);

  // 弹窗打开时从 store 同步到本地编辑状态
  function handleOpenChange(isOpen: boolean) {
    if (isOpen) {
      setApiKey(storeApiKey);
      setModel(storeModel);
      setBaseURL(storeBaseURL);
      setMaxContextTokens(storeMaxContextTokens);
      setThinkingMode(storeThinkingMode);
    }
    onOpenChange(isOpen);
  }

  const handleSave = () => {
    setAISettings({
      apiKey: apiKey.trim(),
      model: model.trim() || DEFAULT_MODEL,
      baseURL: baseURL.trim() || DEFAULT_BASE_URL,
      maxContextTokens: maxContextTokens > 0 ? maxContextTokens : 1000000,
      thinkingMode,
    });
    handleOpenChange(false);
  };

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogContent className="max-w-md">
        <DialogHeader>
          <DialogTitle>API 设置</DialogTitle>
        </DialogHeader>

        <div className="space-y-4 py-2">
          {/* API Key */}
          <div className="space-y-2">
            <Label>API Key</Label>
            <Input
              type="password"
              placeholder="sk-..."
              value={apiKey}
              onChange={(e) => setApiKey(e.target.value)}
            />
            <p className="text-xs text-muted-foreground">密钥保存在本地浏览器，也可通过环境变量 NEXT_PUBLIC_DEEPSEEK_API_KEY 配置</p>
          </div>

          {/* Base URL */}
          <div className="space-y-2">
            <Label>Base URL</Label>
            <Input
              placeholder="https://api.deepseek.com"
              value={baseURL}
              onChange={(e) => setBaseURL(e.target.value)}
            />
            <p className="text-xs text-muted-foreground">OpenAI 兼容接口地址</p>
          </div>

          {/* Model */}
          <div className="space-y-2">
            <Label>模型</Label>
            <Input
              placeholder="deepseek-v4-flash"
              value={model}
              onChange={(e) => setModel(e.target.value)}
            />
          </div>

          {/* 思考模式 */}
          <div className="flex items-center justify-between">
            <div className="space-y-0.5">
              <Label>思考模式</Label>
              <p className="text-xs text-muted-foreground">开启后模型先内部推理再输出，提升分析质量但耗时更长</p>
            </div>
            <Switch
              checked={thinkingMode}
              onCheckedChange={setThinkingMode}
            />
          </div>

          {/* 最大上下文长度 */}
          <div className="space-y-2">
            <Label>最大上下文长度（Token）</Label>
            <Input
              type="number"
              min={1000}
              value={maxContextTokens}
              onChange={(e) => setMaxContextTokens(Number(e.target.value))}
            />
            <p className="text-xs text-muted-foreground">控制分析长篇小说时的分批大小，需为模型支持的最大上下文长度</p>
          </div>
        </div>

        <DialogFooter>
          <Button variant="outline" size="sm" onClick={() => handleOpenChange(false)}>
            取消
          </Button>
          <Button size="sm" onClick={handleSave}>
            保存
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
</file>

<file path="src/lib/ai/providers.ts">
// ============================================
// AI 模型调用 — 原生 fetch 实现，无 SDK 封装
// ============================================

import { ALLOWED_API_PROTOCOLS } from '@/lib/constants';

/** 验证 baseURL 协议，防止 SSRF */
function validateBaseURL(baseURL: string): string {
  let parsed: URL;
  try {
    parsed = new URL(baseURL);
  } catch {
    throw new Error(`无效的 Base URL: ${baseURL}`);
  }
  if (!(ALLOWED_API_PROTOCOLS as readonly string[]).includes(parsed.protocol)) {
    throw new Error(`不支持的协议: ${parsed.protocol}，仅允许 ${ALLOWED_API_PROTOCOLS.join(', ')}`);
  }
  // 禁止指向内网地址
  const hostname = parsed.hostname;
  if (
    hostname === 'localhost' ||
    hostname === '127.0.0.1' ||
    hostname === '0.0.0.0' ||
    hostname === '::1' ||
    hostname.startsWith('169.254.') ||
    hostname.startsWith('10.') ||
    /^172\.(1[6-9]|2\d|3[0-1])\./.test(hostname) ||
    hostname.startsWith('192.168.')
  ) {
    throw new Error(`不允许访问内网地址: ${hostname}`);
  }
  return baseURL;
}

interface ModelOptions {
  apiKey: string;
  model: string;
  baseURL: string;
  thinkingMode?: boolean;
}

interface ChatParams {
  messages: { role: 'user' | 'assistant' | 'system'; content: string }[];
  system?: string;
  maxTokens?: number;
}

/** 流式调用，返回 ReadableStream */
export function chatCompletionStream(options: ModelOptions, params: ChatParams): ReadableStream<Uint8Array> {
  validateBaseURL(options.baseURL);
  const messages = params.system
    ? [{ role: 'system' as const, content: params.system }, ...params.messages]
    : params.messages;

  const body: Record<string, unknown> = {
    model: options.model,
    messages,
    max_tokens: params.maxTokens ?? 4096,
    stream: true,
  };

  // DeepSeek 思考模式
  if (options.thinkingMode) {
    body.thinking = { type: 'enabled' };
    body.reasoning_effort = 'high';
  } else {
    body.thinking = { type: 'disabled' };
  }

  const url = `${options.baseURL.replace(/\/$/, '')}/chat/completions`;

  const encoder = new TextEncoder();
  const decoder = new TextDecoder();

  return new ReadableStream({
    async start(controller) {
      try {
        const response = await fetch(url, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${options.apiKey}`,
          },
          body: JSON.stringify(body),
        });

        if (!response.ok) {
          const errText = await response.text().catch(() => 'Unknown error');
          // 用文本编码错误（controller.error 不跨 HTTP 传播）
          controller.enqueue(encoder.encode(`[STREAM_ERROR] API 调用失败 (${response.status}): ${errText}`));
          controller.close();
          return;
        }

        const reader = response.body?.getReader();
        if (!reader) {
          controller.enqueue(encoder.encode('[STREAM_ERROR] 无法读取响应流'));
          controller.close();
          return;
        }

        while (true) {
          const { done, value } = await reader.read();
          if (done) break;

          const text = decoder.decode(value, { stream: true });
          // 解析 SSE 格式: data: {"choices":[{"delta":{"content":"..."}}]}
          const lines = text.split('\n');
          for (const line of lines) {
            if (!line.startsWith('data: ')) continue;
            const jsonStr = line.slice(6).trim();
            if (jsonStr === '[DONE]') continue;
            try {
              const parsed = JSON.parse(jsonStr);
              const delta = parsed.choices?.[0]?.delta;
              if (!delta) continue;

              // 正文内容
              const content = delta.content;
              if (content) {
                controller.enqueue(encoder.encode(content));
              }
            } catch {
              // 跳过解析失败的行
            }
          }
        }
        controller.close();
      } catch (err) {
        // 用文本编码错误（controller.error 不跨 HTTP 传播）
        const msg = err instanceof Error ? err.message : '未知错误';
        controller.enqueue(encoder.encode(`[STREAM_ERROR] ${msg}`));
        controller.close();
      }
    },
  });
}
</file>

<file path="src/app/page.tsx">
// ============================================
// 首页 — Sidebar + 条件渲染视图
// ============================================

'use client';

import { useEffect } from 'react';
import { useProjectStore } from '@/lib/store/project';
import { useNavigationStore } from '@/lib/store/navigation';
import { useSourceLibraryStore } from '@/lib/store/source-library';
import { Sidebar } from '@/components/Sidebar';
import { SourceLibraryView } from '@/components/source/SourceLibraryView';
import { SourceNovelDetailView } from '@/components/source/SourceNovelDetailView';

import { WritingProjectView } from '@/components/project/WritingProjectView';
import { LayerGenerationView } from '@/components/generation/LayerGenerationView';
import { ChapterGenerationView } from '@/components/chapter/ChapterGenerationView';
import { PromptManagementView } from '@/components/prompts/PromptManagementView';

export default function HomePage() {
  const activeView = useNavigationStore((s) => s.activeView);
  const loadSourceNovels = useSourceLibraryStore((s) => s.loadSourceNovels);
  const loadProjects = useProjectStore((s) => s.loadProjects);

  useEffect(() => {
    loadSourceNovels();
    loadProjects();
  }, [loadSourceNovels, loadProjects]);

  return (
    <div className="flex h-screen">
      <Sidebar />
      <main className="flex-1 flex flex-col overflow-hidden bg-background">
        {activeView === 'source-library' && <SourceLibraryView />}
        {activeView === 'source-detail' && <SourceNovelDetailView />}
        {activeView === 'writing-project' && <WritingProjectView />}
        {activeView === 'prompt-management' && <PromptManagementView />}
        {activeView === 'layer-generation' && <LayerGenerationView />}
        {activeView === 'chapter-generation' && <ChapterGenerationView />}
      </main>
    </div>
  );
}
</file>

<file path="src/components/Sidebar.tsx">
// ============================================
// 左侧边栏 — 简洁导航 + 处理进度指示
// ============================================

'use client';

import { useState } from 'react';
import {
  Settings,
  Library,
  PenTool,
  BookOpen,
  Loader2,
  FileCode,
} from 'lucide-react';
import { useNavigationStore } from '@/lib/store/navigation';
import { useSourceProcessingStore } from '@/lib/store/source-processing';
import type { ActiveView } from '@/types';
import { SettingsDialog } from '@/components/SettingsDialog';

export function Sidebar() {
  const activeView = useNavigationStore((s) => s.activeView);
  const setActiveView = useNavigationStore((s) => s.setActiveView);
  const processingNovelId = useSourceProcessingStore((s) => s.processingNovelId);
  const [settingsOpen, setSettingsOpen] = useState(false);

  const isProcessing = !!processingNovelId;

  const navItems: { key: ActiveView; label: string; icon: typeof Library }[] = [
    { key: 'source-library', label: '素材库', icon: Library },
    { key: 'writing-project', label: '写作项目', icon: PenTool },
    { key: 'prompt-management', label: '提示词管理', icon: FileCode },
  ];

  return (
    <>
      <aside className="w-[280px] h-screen flex flex-col bg-sidebar border-r border-sidebar-border shrink-0">
        {/* 品牌 */}
        <div className="px-5 py-4 border-b border-sidebar-border">
          <div className="flex items-center gap-2">
            <BookOpen className="w-5 h-5 text-primary" />
            <span className="text-lg font-bold text-foreground tracking-tight">Ainovr</span>
          </div>
        </div>

        {/* 导航 */}
        <nav className="px-3 py-4 space-y-0.5">
          {navItems.map(({ key, label, icon: Icon }) => {
            // 素材库高亮：包括 source-detail 视图
            const isActive = key === 'source-library'
              ? (activeView === 'source-library' || activeView === 'source-detail')
              : activeView === key;

            return (
              <button
                key={key}
                onClick={() => setActiveView(key)}
                className={`
                  w-full flex items-center gap-2.5 px-3 py-2 rounded-md text-sm transition-colors
                  ${isActive
                    ? 'bg-sidebar-accent text-sidebar-accent-foreground font-medium'
                    : 'text-foreground/70 hover:bg-sidebar-accent/60 hover:text-foreground'
                  }
                `}
              >
                <Icon className="w-4 h-4" />
                {label}
                {/* 处理进度指示 */}
                {key === 'source-library' && isProcessing && (
                  <Loader2 className="w-3.5 h-3.5 animate-spin text-primary ml-auto" />
                )}
              </button>
            );
          })}
        </nav>

        {/* 设置按钮 — 底部 */}
        <div className="px-3 py-3 border-t border-sidebar-border mt-auto">
          <button
            onClick={() => setSettingsOpen(true)}
            className="w-full flex items-center gap-2.5 px-3 py-2 rounded-md text-sm text-foreground/60 hover:text-foreground hover:bg-sidebar-accent/60 transition-colors"
          >
            <Settings className="w-4 h-4" />
            设置
          </button>
        </div>
      </aside>

      <SettingsDialog open={settingsOpen} onOpenChange={setSettingsOpen} />
    </>
  );
}
</file>

<file path="src/types/index.ts">
// ============================================
// AI 仿写小说系统 — 核心类型定义
// ============================================

// ---- 文本清洗相关 ----

/** 清洗步骤 ID（对应 text-cleaner.ts 的 10 个步骤） */
export type CleaningStepId =
  | 'encoding'       // Step 1: 编码清理
  | 'urls'           // Step 2: URL 行
  | 'promos'         // Step 3: 广告推广
  | 'authorNotes'    // Step 4: 作者碎碎念
  | 'watermarks'     // Step 5: 水印/来源
  | 'nav'            // Step 6: 导航行
  | 'separators'     // Step 7: 分隔线
  | 'toc'            // Step 8: 目录块
  | 'punctuation'    // Step 9: 标点规范化
  | 'blankLines';    // Step 10: 空行整理

/** 清洗预设方案 */
export type CleaningPreset = 'aggressive' | 'standard' | 'light' | 'none';

/** 清洗配置 */
export interface CleaningConfig {
  preset: CleaningPreset;
  /** 仅 preset='none' 时生效，手动选择启用的步骤 */
  enabledSteps: CleaningStepId[];
}

// ---- 分块相关（上传阶段，保留兼容） ----

/** 小说分块 */
export interface NovelChunk {
  id: string;          // nanoid
  novelId: string;     // 所属小说 ID
  index: number;       // 在小说中的顺序（从 0 开始）
  title: string;       // 章节标题（如"第一章 陨落的天才"或"第3部分"）
  content: string;     // 块文本内容
  charCount: number;   // content.length
}

/** 导入管道配置（保留用于兼容） */
export interface ImportConfig {
  cleaning: CleaningConfig;
  maxChunkSize: number;
}

/** 仿写篇幅选项（旧版兼容，Phase 7 后移除） */
export type WriteLength = 'fragment' | 'chapter' | 'short';

/** 已上传的参考小说（上传阶段类型，保留兼容） */
export interface ParsedNovel {
  id: string;
  title: string;
  totalChars: number;
  fullText: string;
  chunks: NovelChunk[];
  rawText: string | null;
  importConfig: ImportConfig | null;
}

// ========================================
// 素材库（Source Library）
// ========================================

// ---- Novel Memory System（Step -1 产出）----

/** 实体类型 */
export type EntityType = 'character' | 'location' | 'organization' | 'artifact' | 'concept';

/** 实体索引条目（Step -1A 产出） */
export interface EntityEntry {
  type: EntityType;
  aliases: string[];
  frequency: number;
  occurrences: string[];  // 出现的 slice IDs
  segments: string[];     // 出现的 segment IDs
}

/** 实体索引 */
export type EntityIndex = Record<string, EntityEntry>;

/** 叙事段（Step -1B 产出） */
export interface NarrativeSegment {
  id: string;               // e.g. "segment_1"
  label: string;            // e.g. "廷根探索"
  startSlice: number;
  endSlice: number;
  startChapter: string;
  endChapter: string;
  charCount: number;
  primaryDriver: string;
  primaryMysteries: string[];
  primaryCharacters: string[];
  worldState: string;
  emotionBaseline: string;
  transitionReason: string | null;
}

/** 图节点 */
export interface GraphNode {
  id: string;
  type: string;
  frequency: number;
  segments: string[];
}

/** 图边 */
export interface GraphEdge {
  source: string;
  target: string;
  type: string;               // causes / reveals / parallels / contradicts / depends_on / co_occurrence
  coOccurrenceCount?: number;
  segments?: string[];
  evidence?: string;
}

/** 记忆图谱 */
export interface MemoryGraph {
  nodes: GraphNode[];
  edges: GraphEdge[];
}

/** Novel Memory 整体（Step -1 全部产出） */
export interface NovelMemory {
  entityIndex: EntityIndex | null;
  segments: NarrativeSegment[] | null;
  characterGraph: MemoryGraph | null;
  mysteryGraph: MemoryGraph | null;
  eventGraph: MemoryGraph | null;
}

/** 源小说处理状态 */
export type SourceNovelStatus =
  | 'raw'                // 已上传，未处理
  | 'indexing'           // Step -1A: 程序索引中
  | 'segmenting'         // Step -1B: 叙事分段中
  | 'slicing'            // Step 0: 智能切片中
  | 'extracting'         // Step 1+2: 文风+叙事动力学提取中
  | 'character_dynamics' // Step 2.5: 角色动力学分析中
  | 'deep_analyzing'     // Step 3+4: 读者体验+叙事约束分析中
  | 'selecting'          // Step 5: 代表性切片选取中
  | 'evolution_modeling' // Step 6.5: 演化建模中
  | 'compressing'        // Step 7: DNA+Genome 压缩中
  | 'ready'              // 全部处理完成，可被项目引用
  | 'error';             // 处理出错

/** AI 智能切片（增强版，含 Memory Graph 引用） */
export interface SemanticSlice {
  id: string;
  index: number;
  title: string;           // e.g. "001_开场_废柴处境"
  content: string;
  charCount: number;
  semanticTags: string[];  // e.g. ["战斗", "修炼", "转折"]
  plotArc: string;         // 所属情节弧线
  emotionalTone: string;   // e.g. "紧张", "温馨"

  // 增强字段（从 Novel Memory 填充）
  segmentId: string;              // 属于哪个 NarrativeSegment
  volume: string | null;          // 属于哪一卷
  chapterRange: string;           // 章节范围 e.g. "第1章-第12章"
  characterRefs: string[];        // 出现的角色名（引用 entity_index）
  narrativeFunction: string;      // setup / escalation / climax / cooldown / resolution
  tensionLevel: number;           // 0-10
  dependencies: string[];         // 依赖的其他切片 IDs
}

/** 代表性切片样本（3-5 个） */
export interface RepresentativeSample {
  sliceId: string;
  sliceIndex: number;
  title: string;
  content: string;
  selectionReason: string; // 选取理由（如"典型战斗场景"）
}

// ========================================
// Phase 2: 道/气 DNA 系统 — 新类型定义
// ========================================

// ---- 体验流标注 ----

/** 体验流四维分数（每切片） */
export interface ExperienceAnnotation {
  sliceId: string;
  sliceIndex: number;
  immersion: number;            // 沉浸感 1-10
  emotionalIntensity: number;   // 情绪强度 1-10
  anticipation: number;         // 期待感 1-10
  perceivedPace: 'fast' | 'medium' | 'slow';
  confidence: number;           // AI 置信度 0-1
  readerPersona: string;        // 读者 persona 名称
  notes: string;                // 自由文本备注
}

/** 聚合后的体验曲线 */
export interface ExperienceCurve {
  sliceId: string;
  sliceIndex: number;
  avgImmersion: number;
  avgEmotionalIntensity: number;
  avgAnticipation: number;
  dominantPace: 'fast' | 'medium' | 'slow';
  scoreSpread: number;           // 多个 persona 的标准差（越大越主观）
  highPoints: boolean;           // 是否为体验高点（多 persona 共识 >7 分）
}

// ---- 消融测试 ----

/** 消融测试段落分类 */
export type AblationCategory =
  | 'bone'           // 骨架：删掉剧情崩
  | 'muscle'         // 肌肉：删掉体验下降但剧情不崩
  | 'filler_a'       // 体验填充：维持气
  | 'filler_b'       // 机械填充：纯凑字数
  | 'uncertain';     // 不确定（默认分类）

/** 消融测试结果 */
export interface AblationResult {
  sliceId: string;
  sliceIndex: number;
  category: AblationCategory;
  lostIfRemoved: string;        // "删掉会失去什么？"
  confidence: number;           // 0-1
  reasoning: string;            // 判定理由
}

// ---- 势能追踪 ----

/** 势能积累点 */
export interface TensionAnchor {
  sliceId: string;
  sliceIndex: number;
  chapter: string;
  type: 'start' | 'reinforcement' | 'release';
  description: string;          // 发生了什么
  tensionBefore: number;        // 1-10
  tensionAfter: number;
}

/** 完整势能模式 */
export interface TensionPattern {
  id: string;
  climaxType: 'emotional' | 'cognitive' | 'power' | 'identity' | 'relationship';
  startIndex: number;           // 积累起点 sliceIndex
  duration: number;             // 积累时长（切片段数）
  reinforcements: TensionAnchor[];  // 中途强化点
  release: TensionAnchor;           // 释放点
  payoffMultiplier: 'low' | 'medium' | 'high' | 'extreme';
  description: string;
}

/** 势能分析整体输出 */
export interface TensionAnalysis {
  patterns: TensionPattern[];          // 检测到的势能模式
  breathingCycle: string;              // 呼吸周期描述
  rhythmProfile: RhythmProfile;        // 节奏画像
}

/** 节奏画像 */
export interface RhythmProfile {
  propulsionRatio: number;    // 推进场景比例 0-1
  buildupRatio: number;       // 蓄力场景比例
  releaseRatio: number;       // 释放场景比例
  breathRatio: number;        // 呼吸场景比例
  existenceRatio: number;     // 存在场景比例
  calibrationRatio: number;   // 校准场景比例
}

// ---- 新 DNA 六层结构 ----

/** 道：核心吸引力 */
export interface NovelDao {
  primaryEmotionalField: string;     // 主情绪场
  alternativeReadings: string[];     // 备选解读
  whyReadersStay: string;            // 读者为什么愿意一直读
  confidence: number;                // AI 对该判断的置信度
  userConfirmed: boolean;            // 用户是否已确认
}

/** 气：维持阅读状态的手段 */
export interface NovelQi {
  maintenanceMethods: string[];      // 维持主情绪场的手法列表
  breathingCycleDescription: string; // 呼吸周期描述
  rhythmProfile: RhythmProfile | null;
  disruptors: string[];             // 会破坏气的因素
}

/** 骨/肉/填充：消融测试聚合 */
export interface NovelStructure {
  bones: Array<{ description: string; evidence: string[] }>;    // 必须存在
  muscles: Array<{ description: string; evidence: string[] }>;  // 增强体验
  fillerTypeA: Array<{ description: string; purpose: string }>; // 体验填充
  fillerTypeB: Array<{ description: string }>;                  // 机械填充
}

/** 生成引擎 */
export interface NovelEngines {
  tensionPatterns: TensionPattern[];   // 势能模式
  characterDecisionModels: string[];    // 角色决策模型
  informationControl: string;          // 信息控制方式
}

/** 迁移风险 */
export interface FailureModes {
  risks: Array<{
    description: string;
    severity: 'high' | 'medium' | 'low';
    mitigation: string;
  }>;
  dependencies: string[];              // 依赖条件（篇幅/读者群/时代）
}

/** 技术层（保留原 Step 1 的产出） */
export interface StyleEngine {
  prosePatterns: string[];
  dialogueStyle: string;
  descriptionStyle: string;
  chapterOpenings: string[];
  chapterEndings: string[];
  rawStyleProfile: string;            // 原始完整文风报告引用
}

/** 新六层 DNA */
export interface NovelDNA {
  dao: NovelDao;
  qi: NovelQi;
  structure: NovelStructure;
  engines: NovelEngines;
  failureModes: FailureModes;
  styleEngine: StyleEngine;
  /** 分析元数据 */
  meta: {
    generatedAt: string;
    sourceNovelId: string;
    modelUsed: string;
    totalSlices: number;
  };
}

// ========================================
// 事件图谱系统（200万字长篇分析核心）
// ========================================

/** 结构化小说事件 */
export interface NovelEvent {
  id: string;                    // E0001
  chapter: number;               // 所在章节
  chunkIndex: number;            // 所在 Chunk
  type: string;                  // 事件类型：拜师/战斗/揭露/死亡/交易/突破/...
  participants: string[];        // 参与角色
  location: string;              // 发生地点
  description: string;           // 一句话描述（≤50字）
  causes: string[];              // 前置事件 ID 列表
  effects: string[];             // 后置事件 ID 列表
  tensionChange: number;         // 紧张度变化 -5 ~ +5
  emotion: string;               // 主要情绪
  foreshadowingOf: string | null;   // 伏笔指向的事件 ID
  foreshadowingFrom: string | null; // 此事件回收了哪个伏笔
  confidence: number;            // AI 提取置信度 0-1
}

/** 事件图谱 */
export interface EventGraph {
  novelId: string;
  events: NovelEvent[];
  totalChapters: number;
  totalChunks: number;
  extractedAt: string;
  // 索引加速
  byChapter: Record<number, string[]>;    // chapter → event IDs
  byParticipant: Record<string, string[]>; // character → event IDs
  byType: Record<string, string[]>;        // type → event IDs
  foreshadowingPairs: Array<{ setup: string; payoff: string; distance: number }>;
}

/** 文本分块（轻量索引） */
export interface TextChunk {
  id: string;              // chunk-0001
  index: number;
  title: string;           // 章节标题或自动生成
  content: string;
  charCount: number;
  chapterStart: number;
  chapterEnd: number;
  entities: string[];      // 程序化提取的实体名
}

/** 扩展技术样本库（替代 3-5 个样本） */
export interface TechniqueSampleLibrary {
  hooks: RepresentativeSample[];           // 开头 hook 样本
  climaxes: RepresentativeSample[];        // 高潮样本
  buildups: RepresentativeSample[];        // 蓄力样本
  transitions: RepresentativeSample[];     // 转场样本
  relationshipProgressions: RepresentativeSample[]; // 关系推进样本
  worldRevelations: RepresentativeSample[];  // 世界观揭露样本
  breathPassages: RepresentativeSample[];    // 呼吸段落样本
  foreshadowings: RepresentativeSample[];    // 伏笔样本
}

/** 素材库中的源小说 */
export interface SourceNovel {
  id: string;
  title: string;
  totalChars: number;
  importConfig: ImportConfig | null;
  status: SourceNovelStatus;
  createdAt: string;
  processedAt: string | null;

  // Novel Memory（Step -1 产出）
  memory: NovelMemory | null;

  // 处理产出（null 表示尚未处理到该步）
  slices: SemanticSlice[] | null;                      // Step 0: 增强切片
  styleProfile: string | null;                         // Step 1: 文风画像
  plotReport: string | null;                           // Step 2: 叙事动力学
  characterDynamics: string | null;                    // Step 2.5: 角色动力学
  readerExperience: string | null;                     // Step 3: 读者体验
  narrativeConstraints: string | null;                 // Step 4: 叙事约束
  representativeSamples: RepresentativeSample[] | null; // Step 5: 样本
  evolutionModel: string | null;                       // Step 6.5: 演化模型 JSON
  novelDna: string | null;                             // Step 7: DNA 超压缩 YAML（旧版）
  novelGenome: string | null;                          // Step 7: Genome 完整基因库 YAML
  novelDnaV2: NovelDNA | null;                         // Step 7.5: 新六层 DNA
  experienceAnnotations: ExperienceAnnotation[] | null;  // 体验流标注
  experienceCurve: ExperienceCurve[] | null;           // 聚合体验曲线
  ablationResults: AblationResult[] | null;            // 消融测试结果
  tensionAnalysis: TensionAnalysis | null;             // 势能分析
  techniqueSamples: TechniqueSampleLibrary | null;     // 技术样本库（替代代表性样本）
  // 事件图谱（200万字长篇分析新架构）
  textChunks: TextChunk[] | null;                      // 文本分块索引
  eventGraph: EventGraph | null;                       // 事件图谱

  // 统一管线产出（新架构）
  generationRulesDna: Record<string, unknown> | null;   // Step 7: 纯量化生成规则 DNA
  unifiedSummaryReport: Record<string, unknown> | null;  // Step 6: 汇总报告
}

// ========================================
// 写作项目（Writing Project）
// ========================================

/** 源小说在项目中的角色 */
export interface SourceRole {
  sourceNovelId: string;
  role: 'style' | 'plot' | 'style_and_plot';
}

/** 管线层级枚举 */
export type GenerationLayer = 0 | 1 | 2 | 3 | 4 | 5;

/** 写作项目 */
export interface WritingProject {
  id: string;
  title: string;
  createdAt: string;
  updatedAt: string;

  // 源小说引用
  sourceNovelIds: string[];
  sourceRoles: SourceRole[];

  // 五层产出（null 表示尚未生成）
  outline: BookOutline | null;           // Layer 1
  phases: Phase[] | null;                // Layer 2
  volumes: Volume[] | null;              // Layer 3
  chapterSets: ChapterSet[] | null;      // Layer 4
  chapterPlans: ChapterPlan[] | null;    // Layer 5

  // 生成的章节正文
  chapters: GeneratedChapter[];

  // 当前已生成到哪一层（0 = 尚未开始）
  currentLayer: GenerationLayer;
}

// ========================================
// 五层层级（Five-Layer Hierarchy）
// ========================================

/** Layer 1: 全书大纲（~200字，不可变） */
export interface BookOutline {
  content: string;
  generatedAt: string;
  isLocked: boolean;
  majorPlotFrameworkRef: string; // 引用哪个源小说的大情节框架
}

/** Layer 2: 阶段规划（4-6 份，每份 ~500 字） */
export interface Phase {
  id: string;
  index: number;
  title: string;
  content: string;
  volumeCount: number;
  generatedAt: string;
}

/** Layer 3: 分卷（每阶段 1-2 卷，每卷 ~300 字） */
export interface Volume {
  id: string;
  phaseId: string;
  index: number;
  title: string;
  content: string;
  chapterSetCount: number;
  generatedAt: string;
}

/** Layer 4: 章节集合（每卷 3-5 个，每个 ~200 字） */
export interface ChapterSet {
  id: string;
  volumeId: string;
  index: number;
  title: string;
  content: string;
  chapterCount: number;
  generatedAt: string;
}

/** Layer 5: 每章计划（~100 字/章） */
export interface ChapterPlan {
  id: string;
  chapterSetId: string;
  index: number;
  title: string;
  content: string;
  plotKeywords: string[];
  patternStructure: string; // 引用的小情节模式名称
  generatedAt: string;
}

// ========================================
// 章节生成 + 审查
// ========================================

/** 审查维度 */
export type ReviewDimension =
  | 'style_consistency'    // 文风一致性
  | 'plot_coherence'       // 情节逻辑
  | 'pacing'               // 节奏
  | 'pattern_execution'    // 小情节模式执行度
  | 'foreshadow_execution' // 伏笔/铺垫执行度
  | 'qi_consistency';      // 气的一致性（阅读状态维持）

/** 审查状态 */
export type ReviewStatus =
  | 'pending_review'
  | 'reviewing'
  | 'passed'
  | 'needs_revision'
  | 'approved';

/** 单维度审查结果 */
export interface ChapterReview {
  id: string;
  dimension: ReviewDimension;
  score: number;           // 1-10
  issues: string[];
  suggestions: string[];
  reviewedAt: string;
}

/** 生成的章节 */
export interface GeneratedChapter {
  id: string;
  chapterPlanId: string;
  content: string;
  generatedAt: string;
  reviewStatus: ReviewStatus;
  revisionCount: number;   // 最大 3
  reviews: ChapterReview[];
  humanFeedback: string | null;
}

// ========================================
// 管线任务追踪
// ========================================

/** 管线任务类型 */
export type PipelineTaskType =
  // 素材库处理 — 基础设施层
  | 'program_indexing'
  | 'entity_classification'
  | 'narrative_segmentation'
  | 'memory_graph_build'
  // 素材库处理 — 分析层
  | 'smart_slicing'
  | 'style_extraction'
  | 'narrative_dynamics_extraction'
  | 'character_dynamics_extraction'
  | 'reader_experience_extraction'
  | 'narrative_constraints_extraction'
  | 'sample_selection'
  | 'evolution_modeling'
  | 'dna_compression'
  | 'genome_compression'
  // 道/气 新管线步骤
  | 'experience_annotation'
  | 'ablation_testing'
  | 'tension_tracking'
  | 'dna_v2_compression'
  | 'technique_sampling'
  // 事件图谱
  | 'text_chunking'
  | 'event_extraction'
  | 'event_graph_merge'
  | 'value_sampling'
  // 五层生成
  | 'outline_generation'
  | 'phase_framework'
  | 'phase_detail'
  | 'volume_framework'
  | 'volume_detail'
  | 'chapter_set_framework'
  | 'chapter_set_detail'
  | 'chapter_plan_framework'
  | 'chapter_plan_detail'
  // 章节生成
  | 'chapter_writing'
  | 'chapter_review'
  | 'chapter_revision';

/** 进行中的管线操作 */
export interface PipelineTask {
  id: string;
  type: PipelineTaskType;
  status: 'pending' | 'running' | 'completed' | 'failed';
  progress: number;        // 0-100
  startedAt: string;
  completedAt: string | null;
  error: string | null;
}

// ========================================
// 视图导航
// ========================================

/** 当前激活的视图 */
export type ActiveView =
  | 'source-library'       // 素材库管理（含文风/情节库标签页）
  | 'source-detail'        // 单本源小说详情（含处理+结果）
  | 'writing-project'      // 写作项目（含项目设置/层级规划/章节生成标签页）
  | 'prompt-management'    // 提示词管理
  | 'source-process'       // [废弃] 旧处理页，迁移完成后移除
  | 'layer-generation'     // [废弃] 旧层级页，迁移完成后移除
  | 'chapter-generation'   // [废弃] 旧章节页，迁移完成后移除
  | 'analyze'              // 旧版兼容
  | 'write';               // 旧版兼容

// ---- 旧版应用状态（Phase 7 后移除） ----

/** 旧版应用全局状态（保留至旧组件迁移完成） */
export interface AppState {
  activeView: ActiveView;
  novels: ParsedNovel[];
  analysisReport: string | null;
  isAnalyzing: boolean;
  writeResult: string | null;
  isWriting: boolean;
  apiKey: string;
  model: string;
  baseURL: string;
  maxContextTokens: number;

  setActiveView: (view: ActiveView) => void;
  addNovel: (novel: ParsedNovel) => void;
  removeNovel: (id: string) => void;
  clearNovels: () => void;
  setAnalysisReport: (report: string | null) => void;
  setIsAnalyzing: (v: boolean) => void;
  setWriteResult: (result: string | null) => void;
  setIsWriting: (v: boolean) => void;
  setAISettings: (settings: Partial<Pick<AppState, 'apiKey' | 'model' | 'baseURL' | 'maxContextTokens'>>) => void;
  loadNovelsFromIDB: () => Promise<void>;
  getEffectiveApiKey: () => string;
}
</file>

</files>
