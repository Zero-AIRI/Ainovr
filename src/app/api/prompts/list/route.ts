// ============================================
// GET /api/prompts/list — 返回所有提示词（默认 + 自定义）
// ============================================

import { NextResponse } from 'next/server';
import { promises as fs } from 'fs';
import path from 'path';

const DATA_DIR = path.join(process.cwd(), 'data');
const CUSTOM_PROMPTS_PATH = path.join(DATA_DIR, 'prompts.json');

// 默认提示词 —— 和 src/lib/ai/prompts/ 下的硬编码一致
const DEFAULTS: Record<string, string> = {
  'source-slicing': `你的任务是分析小说文本，按叙事弧线进行智能切片。\n\n要求：\n- 以情节完整性为最高优先级（不在高潮中间切断）\n- 以章节边界为自然切割点\n- 每个切片包含一个相对完整的叙事单元\n- 为每个切片标注：序号、标题、语义标签、情节弧线、情绪基调\n\n输出格式（Markdown）：\n\n## 切片 {序号}: {标题}\n- 语义标签: {标签1}, {标签2}\n- 情节弧线: {弧线名称}\n- 情绪基调: {基调}\n- 字数: {字数}\n\n{切片正文}\n\n---\n`,

  'source-style': `你的任务是深入分析小说文本，提取 6 个维度的写作风格特征。\n\n分析维度：\n1. 句式特征（长短句比例、排比/对仗使用）\n2. 修辞偏好（比喻、拟人、夸张等手法的使用频率和方式）\n3. 词汇特征（文言/白话、口语/书面语、特定词汇频率）\n4. 叙事视角（人称、全知/有限视角、语言调性）\n5. 对话风格（对话长度、节奏、个性化程度）\n6. 描写手法（环境描写、动作描写、心理描写的偏好和方式）\n\n输出为 Markdown 格式的文风档案（style_profile.md）。`,

  'source-style-supplement': `你之前已经分析了该作者的部分作品并生成了一份文风档案。\n现在你收到了更多文本。请结合新文本，对文风档案进行补充和修正。\n输出完整的更新后的文风档案（不是增量，而是完整版本）。`,

  'source-plot': `你的任务是分析小说文本，提取情节规律，分为大情节和小情节两个层级。\n\n## 提取内容\n\n### 一、大情节框架\n- 识别小说的整体结构类型（如退婚流、废柴逆袭、重生流等）\n- 提取大情节的核心节点和典型章节位置\n- 提取关键词标签\n\n### 二、小情节模式库（10-20个）\n- 识别反复出现的情节模式（如装逼打脸、奇遇、拍卖会等）\n- 对每个模式：命名、拆解结构、统计频率、标注典型章节、提取关键词\n\n### 三、伏笔手法\n- 提前量、回收方式、密度、关键伏笔示例\n\n### 四、节奏规律\n- 小爽点间隔、大高潮间隔、战斗/过渡/日常占比\n\n输出为 Markdown 格式的情节规律报告（plot_report.md）。`,

  'source-plot-supplement': `你之前已经分析了该作者的部分作品并生成了一份情节规律报告。\n现在你收到了更多文本。请结合新文本，对情节规律报告进行补充和修正。\n输出完整的更新后的情节规律报告。`,

  'source-samples': `你的任务是从全部切片中精选 3-5 个代表性切片。\n\n要求：\n- 每个对应一种典型的写作场景（战斗/日常/高潮/对话/描写）\n- 最能体现该作者的文风特征\n- 附上选取理由\n\n输出格式（Markdown）：\n\n## 样本 {序号}: {标题}\n- 场景类型: {战斗/日常/高潮/对话/描写}\n- 选取理由: {一句话说明为什么这个切片最能代表该场景的文风}\n\n{切片正文}\n\n---\n`,

  'outline': `你的任务是根据大情节框架和用户的创作想法，生成一份全书大纲。\n\n要求：\n- 约 200 字\n- 包含：大情节框架节点映射、5 个关键转折（不可变锚点）、主角弧线、结局方向\n- 严格遵循大情节框架的结构`,

  'phase-framework': `你的任务是根据全书大纲和节奏/伏笔规则，规划阶段框架。\n\n要求：\n- 输出 4-6 个阶段的标题和预估章节范围\n- 每个阶段对应大纲中的哪些大情节节点\n- 标注大高潮位置和伏笔埋设计划\n- 不要填充详细内容，仅输出框架\n\n输出格式：\n1. 阶段标题（第X-Y章）：对应大情节节点 / 预估字数\n2. ...`,

  'phase-detail': `你的任务是为指定阶段生成详细规划。\n\n要求：\n- 约 500 字\n- 包含：阶段主题、关键事件（3-5个）、伏笔计划、节奏安排（小爽点位置+大高潮位置）、缓冲空间\n- 严格遵循上级大纲的约束`,

  'volume-framework': `你的任务是根据阶段规划和小情节模式库，为指定阶段规划分卷框架。\n\n要求：\n- 每阶段 1-2 卷\n- 每卷约 20-30 章\n- 输出每卷的标题、核心冲突、预估章节范围\n- 为每卷匹配适合的小情节模式\n- 不要填充详细内容，仅输出框架`,

  'volume-detail': `你的任务是为指定卷生成详细规划。\n\n要求：\n- 约 300 字\n- 包含：卷主题、核心冲突、主要事件列表+匹配的小情节模式、人物出场安排\n- 弹性约束：可调整事件顺序和形式`,

  'chapter-set-framework': `你的任务是根据卷规划，为指定卷规划章节集合框架。\n\n要求：\n- 每卷 3-5 个集合\n- 每个集合 5-8 章\n- 输出每个集合的标题、情节推进方向、预估章节范围\n- 安排小情节模式的出场顺序\n- 不要填充详细内容，仅输出框架`,

  'chapter-set-detail': `你的任务是为指定章节集合生成详细规划。\n\n要求：\n- 约 200 字\n- 包含：集合主题、情节推进方向、小情节分配（哪章用哪个模式）、节奏安排（快/慢/中）、伏笔操作点、铺垫任务\n- 弹性约束：小情节模式的选择可以灵活替换`,

  'chapter-plan-framework': `你的任务是根据章节集合规划，为每章生成计划框架。\n\n要求：\n- 输出每章的标题、对应的小情节模式（如适用）、情节关键词\n- 不要填充详细内容，仅输出框架列表`,

  'chapter-plan-detail': `你的任务是为指定章节生成详细计划。\n\n要求：\n- 约 100 字\n- 包含：3-5 个场景节拍、小情节模式结构（触发→发展→高潮→收尾）、情节关键词、节奏指令、伏笔操作、铺垫任务\n- 最大自由度：集合只给方向和情节模式结构，具体场景自由发挥`,

  'chapter-writing': `你的任务是根据章节计划和上下文，生成章节正文。\n\n生成前上下文包含：\n1. 风格指导（文风档案 + 情节规律报告）\n2. 层级上下文（大纲/阶段/卷/集合）\n3. 本章任务（章节计划 + 小情节模式结构 + 关键词 + 节奏指令 + 伏笔操作）\n4. 前文状态（前情提要 + 伏笔清单 + 上文续写）\n\n请严格遵循文风档案的风格特征，按照章节计划的场景节拍展开。`,

  'chapter-review': `你的任务是对生成的章节正文进行 5 维度审查。\n\n审查维度：\n1. style_consistency（文风一致性）：是否与目标文风档案一致\n2. plot_coherence（情节逻辑）：与层级大纲是否一致，逻辑是否通顺\n3. pacing（节奏）：是否符合章节计划的节奏指令\n4. pattern_execution（小情节模式执行度）：是否正确执行了指定的小情节模式结构\n5. foreshadow_execution（伏笔/铺垫执行度）：是否执行了章节计划中的伏笔操作\n\n对每个维度：\n- 打分（1-10）\n- 列出问题（如有）\n- 给出修正建议（如有）\n\n输出格式（JSON）：\n[\n  {"dimension": "style_consistency", "score": 8, "issues": [], "suggestions": []},\n  ...\n]`,

  'chapter-revision': `你的任务是根据审查结果和（可能的）人工反馈，修正章节正文。\n\n要求：\n- 只修正审查中指出的问题，保持其余部分不变\n- 如果有人工反馈，优先处理人工反馈\n- 修正后的正文必须仍然遵循文风档案和章节计划\n- 输出完整的修正后章节正文（不是增量）`,
};

export async function GET() {
  try {
    // 读取自定义覆盖
    let custom: Record<string, string> = {};
    try {
      const raw = await fs.readFile(CUSTOM_PROMPTS_PATH, 'utf-8');
      custom = JSON.parse(raw);
    } catch {
      // 文件不存在，使用空自定义
    }

    return NextResponse.json({
      defaults: DEFAULTS,
      custom,
    });
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : '读取提示词失败';
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
